#!/usr/bin/env bash
# Moved: the PATH shim is now local/bin/service/codex-shim; install it with
# local/bin/service/keyrot-install.sh, which copies it to <cache_root>/owner-bin/codex.
exec "$(cd "$(dirname "${BASH_SOURCE[0]}")/../../../local/bin/service" && pwd -P)/codex-shim" "$@"
