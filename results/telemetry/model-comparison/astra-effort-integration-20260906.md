# Effort evidence integration after PR238, September 6, 2026

GitHub reports PR238 merged at **2026-09-06T04:44:46Z**, commit
`32a32edee16d3932525e4b1da9f84009e1fbb13b`. The assignment recorded the observed
merge at 04:44:53Z; the former is the API's `merged_at`, not a new event inferred
from a local branch. This issue branch integrates that actual GitHub source.
Issue252, not this lane, owns reconciliation of private primary main.

The [initial report](astra-effort-20260906.md) and
[dataset](astra-effort-20260906.json) retain their original bytes from `3d009f5`;
their import into primary `9102b74` was telemetry preservation, not installation
of the pending policy. Their open-PR observation is a dated snapshot, superseded
by the merge recorded here. Counts remain two review attempts, two threads, one
task, one client-context max and one xhigh, and zero server-verified project
runs. Six historical probes remain separate; no new sample or probe is added.

The [archived API-max/official-Ultra finding](astra-max-ultra-label-finding-20260906.md)
is copied byte-identically from primary `9102b74`, retaining its source links,
inference qualifications and recording time. It is not a fresh verification of
the deployed relay, backend computation, full-mode equivalence or causation.
Do not rename the max sample Ultra; keep UI mode, configured/client-recorded API
effort, server-reported effort and delegation behavior distinct.

The [original role-overstep correction](../owner-messages/qpbt-meta-20260905-230133/owner-meta-boundary-correction.json)
is likewise preserved byte-identically from that archival commit, not rewritten
as a new owner decision. Main owns plans, decisions and assignments; meta only
guides. Main remains max; workers use primary Astra with main selecting exactly
max or xhigh and fan-out disabled. The 8–11 useful-worker floor and eleven-worker
cap do not authorize filler or bypass concrete service limits. All already-posted
#26 items, including B7 and B8, await the human owner.

The README now restricts effort claims to the measured probes rather than
relabeling unverified historical sessions. The original report's other
primary-only source links remain tied to their recorded provenance; issue252's
history reconciliation must preserve those incoming records. No primary history,
raw session stream, comparison script, proof/review budget or runtime policy is
rewritten by this integration. The current continuation is configured xhigh for
documentation reconciliation; its server effort and terminal usage remain unknown
until independently recorded. Publication and exact-head CI follow normal gates;
independent review and any PR merge are outside this author session.
