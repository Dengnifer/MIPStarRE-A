## Findings

- [ ] F1 (blocker) `local/bin/review.sh:600` — Fresh-base carry-forward can still republish the oldest duplicate ledger and certify stale review evidence

## Review

The duplicated change-level F1 entries are resolved: `post_review` now updates the newest matching review, and the added test verifies that review ID 9 is updated instead of ID 5.

The carry-forward blocker remains. Candidate selection at `review.sh:577` correctly chooses the newest review per head, but retains only its SHA. Body extraction then scans the original chronological rows at line 600 and stops at the first body containing that SHA. Given an older `APPROVED` review followed by a newer `CHANGES_REQUESTED` review for the same head, carry-forward republishes the older approval and lines 632–637 can post a successful summary. The merge gate would therefore accept stale evidence.

Carry-forward must retain the selected review identity or body and publish that exact row. Add a regression with conflicting duplicate ledgers. Since the round cap is reached, F1 must now be fixed or explicitly deferred to a tracked issue through exact-head operator adjudication.

The targeted unit test could not run because the read-only sandbox provides no writable temporary directory. Static inspection and the in-memory selection reproduction confirmed the mismatch; the task reports exact-head CI as green.

VERDICT: CHANGES_REQUESTED