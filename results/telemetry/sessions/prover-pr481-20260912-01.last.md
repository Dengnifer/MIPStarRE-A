Both findings are already fixed in current HEAD `d72c8b16`, newer than the reviewed commit:
- F1: Duplicate proofs removed; callers reuse public subline APIs.
- F2: Blueprint explicitly maps coordinate `i` to Lean index `i−1`, including unit vectors and prefix restrictions.
Revalidated `UniformLinePoint.lean` and `git diff --check`; both passed. Full build and blueprint rendering passed in the prior fix session and were not rerun.
No new edits; working tree is clean. Existing documented proof obligations remain unchanged.
