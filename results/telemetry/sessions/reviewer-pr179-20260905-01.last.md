## Findings
- [ ] F1 (changes) `blueprint/src/chapter/ch13_qpbt_test.tex:18` — The newly linked, completed `ldGame` definition lacks the required statement-level `\leanok`.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/DirectLowDegree/Transport/Consistency/Defect.lean:217` — The public coordinatewise consistency implications lack subordinate blueprint entries stating their premises and restricted scope.
- [ ] F3 (changes) `MIPStarRE/QPBT/Combining/DirectLowDegree/Transport/Consistency/Compression.lean:322` — The two public mixed-defect compression identities lack a blueprint support entry distinguishing them from the still-missing global/global compression.
- [ ] F4 (changes) `MIPStarRE/QPBT/Combining/DirectLowDegree.lean:28` — “transports the three mature consistency conclusions to the direct game” overstates results that assume the LDT relations and operate on individual polynomial coordinates.
- [ ] F5 (changes) `MIPStarRE/QPBT/Combining/DirectLowDegree/Transport/Consistency.lean:7` — “stable public facade re-exports” is software terminology; replace it with “This module provides”.
- [ ] F6 (changes) `MIPStarRE/QPBT/Combining/DirectLowDegree/Transport/Consistency/State.lean:29` — “bridge”, “aliases”, “downstream consumers”, and “shared API” describe implementation structure rather than the mathematical identity.
- [ ] F7 (changes) `MIPStarRE/QPBT/Games/DistanceTheorems/Support.lean:598` — “pure-state bridge” and “conversion API” are banned software language; describe the inverse constructions and quadratic-form identity directly.
- [ ] F8 (changes) `MIPStarRE/QPBT/Combining/DirectLowDegree/Transport/Consistency/State.lean:314` — The new `mature...` public names and repeated “mature LDT” prose encode formalization status rather than mathematical content.
- [ ] F9 (changes) `MIPStarRE/QPBT/Algebra/Pauli.lean:98` — The two generic normalization identities are formalization-only auxiliaries, but their docstrings do not identify them as such.
- [ ] F10 (changes) `MIPStarRE/QPBT/Combining/DirectLowDegree/Transport/Consistency/Compression.lean:40` — “constructor-preserving” and the repeated “adapter” terminology use Lean/software vocabulary instead of stating the preserved answer forms and correlated-residue extension.
## Review
F1 is a status error, not a signature mismatch. `LdParams` has the blueprint’s positivity, admissibility, and divisibility assumptions, and `ldGame` packages the stated question and answer types, distribution, and predicate. Its former `μ_prob` hole is now closed at `LowDegreeGame.lean:345`, so the definition block should carry statement-level `\leanok`.
For F2, the source theorem at `ch13_qpbt_test.tex:148` constructs joint polynomial-tuple measurements from a successful strategy. By contrast, the results at `Defect.lean:217` and `:303` fix one coordinate and assume the corresponding `ConsRel`; the result at `:394` assumes a pair of individual polynomial measurements. Record these as explicitly formalization-only support nodes with their full premises. They must not be added to the source-labelled soundness block as evidence for its `\leanok`.
For F3, `Compression.lean:322` and `:360` prove only the point/global and global/point compression identities. `Measurements.lean:320` explicitly records that separate diagonal-block averaging does not provide global/global compression. A subordinate blueprint node should state exactly these two valid identities and preserve that limitation.
F4 should say, for example: “`Transport.Consistency` reexpresses the three single-polynomial LDT consistency relations in direct coordinates, with the first two taken at a fixed tuple coordinate.” This avoids implying that `exists_direct_ld_soundness`, whose construction of joint tuple measurements remains open, has been established.
For F6, replace the comment by a mathematical sentence such as: “The following exported identities identify pure-state expectation with the Euclidean quadratic form used in the consistency calculations.” For F7, say that the construction sends a pure state to its Euclidean coordinate vector and that the displayed theorem identifies its quadratic form with density-state evaluation.
For F8, use `LDT` or `seed-indexed LDT` in prose and prefixes such as `ldt...` or `seedIndexed...` in the new public names. “Mature” distinguishes implementation history, not two mathematical notions. For F9, begin with “This formalization-only normalization identity supports...” and retain the cited EPR and seed-fiber constructions. For F10, use “the answer equivalence preserves point answers” and “the correlated-residue extension of the strategy”.
No changed `\notready` tag is stale. The existing source soundness entry correctly has statement-level `\leanok` without proof-level `\leanok`; the new conversion results do not change that status.
Prior-ledger triage: the cited sum helper, unused import, duplicate state conversion, and duplicate scale calculation are absent from this head. The remaining generic-conversion concern belongs to the code-review lane. The prior blueprint-coverage warning remains relevant and is narrowed here to F2 and F3.
Summary: 3 blueprint/status issues and 7 prose issues. No existing blueprint-to-Lean quantifier or conclusion mismatch was found, but the missing support nodes must not be repaired by overstating the source soundness theorem.
VERDICT: CHANGES_REQUESTED
