## Findings

- [ ] F1 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:454` — Reserved rounding omits the binary-observable hypothesis and explicit averaging domains.
- [ ] F2 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:407` — The measurement-extension formula lacks the backslash defining its indicator.
- [ ] F3 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:468` — New auxiliary entries omit dependency links to results actually used.
- [ ] F4 (changes) `MIPStarRE/QPBT/Combining/Linearity/Extension.lean:93` — The density-weighted transport identity used in the blueprint has no corresponding linked statement.
- [ ] F5 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:352` — Replace Lean identifiers and constructor terminology with mathematical notation.
- [ ] F6 (changes) `MIPStarRE/QPBT/Combining/Linearity/SetupComparison.lean:8` — Replace software-role terminology in the new docstrings.

## Review

Request changes: **2 mathematical-equivalence issues, 2 synchronization issues, and 2 prose issues**. The equivalence issues affect the associated statement-level `\leanok` claims.

**F1.** The blueprint quantifies over “any fibers” satisfying the correlation premise, whereas `ReservedRounding.lean:76` separately requires every \(O_x^u\) to be a binary observable. Correlation alone is insufficient: \(O^u=2I\), \(\delta=0\) satisfies it but cannot have zero error to binary observables. Explicitly state the binary hypothesis, \(u\in\mathbb F_2^{2t}\), uniform averaging over \(u\), and the finitely supported nonnegative weighting \(\mu\) on \(x\) appearing in `ReservedRounding.lean:70`.

**F2.** `mathbf{1}_{a=a_0}` renders literal letters instead of the indicator implemented by `Extension.lean:115`. Replace it with `\mathbf{1}_{a=a_0}` so the displayed effects match the linked measurement construction.

**F3.** The rounding proof has no `\uses`, although `ReservedRounding.lean:95–112` invokes results linked under `lem:naimark-rounding`, `lem:boolean-representation-stability`, and `lem:blr-defect-identity`. Add these proof dependencies. Also record the reservation dependency explicitly referenced by the initial-transport statement at blueprint line 380.

**F4.** Blueprint lines 471–472 invoke preservation of density-weighted squared errors, but the error-transport entry states and links only vector identities. Add a subordinate statement for `LinearityPadding.extendObservable_stateDepDistSq`, preserving its generality for arbitrary operators \(A,B,\rho\), with the corresponding `\lean` and status marks. Link that support entry from the rounding proof.

**F5.** “PauliAnswer,” `\operatorname{Option}`, “Pauli-outcome constructor,” and “Option-indexed” expose implementation terminology at blueprint lines 352, 355, 364, and 387. Introduce the answer alphabet \(\mathcal A\), write \(\mathcal A\sqcup\{\bot\}\), and explain that the alphabet contains all Pauli outcomes. Describe the dilation by its ancillary basis. Replace “Mathlib’s extension” at line 369 with the mathematical assertion that the finite partial bijection extends to a permutation.

**F6.** “Arbitrary-strategy consumer” at `SetupComparison.lean:8`, repeated at line 69, should become “the projective reduction used in the soundness theorem for arbitrary strategies.” Replace “assumed transport package” at `Extension.lean:130` with “The construction proves the intertwining identities.” These changes follow the mathematical terminology requirement without changing the documented limitations.

Reviewed the complete authoritative patch and affected sources at `b167f8e1368060201f5bb8fbf7dca961230d10da`; the cited labels resolve. No builds were rerun or files modified. Full source absorption correctly remains open: the six-register comparisons and identification with the earlier binary families are still outstanding.

VERDICT: CHANGES_REQUESTED