## What changed

Proved all three targets in [Pauli.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-64-epr-binary-fourier/MIPStarRE/QPBT/Algebra/Pauli.lean):

- `eprState_norm`
- `tauObservable_eq_sum_pauliProj`
- `pauliProj_eq_avg_tauObservable`

Added private characteristic-two Fourier adapters and the required scope-restriction docstrings. Public signatures are unchanged.

Committed as `ec328e8` (`feat(QPBT/Algebra): prove binary Pauli Fourier identities`).

## Evidence

- `lake env lean MIPStarRE/QPBT/Algebra/Pauli.lean`: passed without warnings.
- `rg -n "sorry|axiom" MIPStarRE/QPBT/Algebra/Pauli.lean`: no matches.
- `lake env lean MIPStarRE/QPBT/Algebra/PauliTheorems.lean`: passed; only pre-existing `sorry` warnings.
- Axiom closure for all three targets: only `propext`, `Classical.choice`, and `Quot.sound`; no `sorryAx`.
- Lock-aware `lake build`: passed, 9,040 jobs.
- Git hooks and pre-commit integrity checks: passed.
- Worktree is clean and ahead by one commit.

## Statement integrity

`eprState_norm`: the paper assumes a finite linear space; Lean proves normalization for any nonempty finite carrier with decidable equality. The conclusion is the same unit-norm assertion. Verdict: faithful boundary hypotheses.

The two Fourier identities: the paper states the formulas over general prime characteristic; Lean assumes a finite field algebra over `ZMod 2` and uses `phaseSign`. The conclusions are the corresponding forward and inverse Fourier formulas. Verdict: extra assumptions, intentionally documented as a characteristic-two scope restriction.

## Open obligations

None in `Pauli.lean`.

## Handoff

Run exact-head local CI and an independent reviewer session. Review should focus first on the reindexing of `fourier_fact_vector` and the characteristic-two support calculation. No re-export change is needed.