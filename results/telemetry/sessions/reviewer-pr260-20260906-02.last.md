## Findings

- none

## Review

Reviewed the complete 1,383-line authoritative patch at `ecba3e73c4e572d53967c058db9688829ff8b80f`; it matches the checkout diff.

Prior F1 is resolved: `local/bin/codex-policy-shim.sh:24` validates both `-mMODEL` and `-m=MODEL`, rejects disallowed models, and removes accepted duplicates. The attached configuration repair preserves selected effort, unrelated configuration values, and literal prompt text.

The policy amendments retain cumulative budgets, independent review, and exact-head gates while documenting main’s decision authority and replenishment duties. Historical observations remain distinguished from current policy and verified provider behavior.

Validation passed: 95 read-only shim cases with execution intercepted, Bash and Python syntax, JSON consistency checks, and `git diff --check`. The worktree remains clean.

The full fixture-based regression suite was not rerun because it requires filesystem writes. Installed-shim behavior and provider execution were not exercised; CI success is supplied by the trusted review trigger.

VERDICT: APPROVED