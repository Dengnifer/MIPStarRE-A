## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Lines/SamePlacementDistance.lean:30` — Replace the nonexistent blueprint citation `thm:line-point-same-placement-distance`.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/Lines/SubLineUniform.lean:129` — Reuse the extracted resampling lemma in the diagonal proof instead of retaining duplicate proofs.
## Review
F1: The docstring of `exists_expLine_point_same_placement_distance_le` cites a label absent from the blueprint. Replace it with a valid auxiliary-node citation, or retain only the accurate paper citation: `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:936–941`, `eq:pasting-q1`. Creating another blueprint node is unnecessary if this remains internal infrastructure. Unknown labels require correction before approval.
F2: `avgOver_lineRepMap_resample_parameter` duplicates the local `hresample` statement and proof in [DiagonalResampling.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-515-proof-combined-line-measurement-consistency/MIPStarRE/QPBT/Combining/Lines/DiagonalResampling.lean:52). That module already imports `SubLineUniform`. Replace its local proof with the new shared lemma, so both axis and diagonal resampling use the same argument. This addresses the review’s required duplication check without changing mathematical statements.
The mathematical review found no blocker. `combined_line_measurement_consistency` preserves its hypotheses, quantifier order, measurement, sampling law, and conclusion. The source-facing existence theorem still constructs the point witness internally. The proof correctly retains the X-Z-X sandwich from paper lines 942–949, conditions the collision argument on nonzero X direction, and restores the discarded contribution before polynomial absorption. The existing documented additive interpretation of polynomial error remains unchanged.
Validation: `lake env lean MIPStarRE/QPBT/Combining/Lines.lean` passed. Axiom checks for the target, source-facing existence theorem, conditioned estimate, and collision bound returned only `propext`, `Classical.choice`, and `Quot.sound`. Changed Lean files contain no proof holes or prohibited bypasses; whitespace checks passed.
Residual limits: I did not repeat the full build or blueprint render; the supplied context reports successful CI for this head. The canonical `lean-conventions` skill was unavailable in the searched locations, so the review used the supplied and checked-in policies. The checkout remains unchanged.
VERDICT: CHANGES_REQUESTED
