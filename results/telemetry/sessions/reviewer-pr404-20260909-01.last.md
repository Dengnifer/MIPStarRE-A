## Findings

- none

## Review

The four new lemmas are mathematically sound auxiliary estimates for `lem:qld-xz-lines`, matching the argument in `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:902` and blueprint `ch15_qpbt_combining.tex:1010`.

`norm_sub_projective_mul_sq_le` correctly uses complementary projection contraction without commutation. `absorb_X_le` and `absorb_Z_le` preserve the witness’s actual error parameter and use the correct ordered consistency fields. Those fields are conclusions of `lem:qld-4-10`, with an existing construction theorem.

`norm_marginal_sub_sq_le` correctly derives the factor `4`. Its orthogonality, projectivity, completeness, and commutation hypotheses hold in the intended paper application; completeness is required only of `B`. No source-statement drift or additional proof obligation is introduced.

Both files passed `lake env lean` without warnings. Proof-hole scans and `git diff --check` passed. All four declarations depend only on `propext`, `Classical.choice`, and `Quot.sound`.

Validation limits: no full build was rerun; the supplied status reports successful CI for this head. The canonical `lean-conventions` skill was unavailable, so checked-in integrity and review rules were used. The checkout remains unchanged.

VERDICT: APPROVED