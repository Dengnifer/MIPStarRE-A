## Findings
- [ ] F1 (changes) `scripts/blueprint_citations.py:226` — Explicit blueprint citations with unknown `eq:`, `sec:`, or `item:` labels are silently omitted instead of reported as unresolved.
- [ ] F2 (changes) `MIPStarRE/LDT/SelfImprovement/Theorems/Results/HelperCompleteness/InputSdp.lean:39` — Both migrated input-consistency citations name `eq:first-moved-completeness`, which describes a different mathematical step.

## Review

**F1 — Detect unknown explicitly identified blueprint labels.**
`CANONICAL_CITATION_RE` recognizes explicit citations such as `Blueprint `eq:missing-equation``, but the subsequent `BLUEPRINT_LABEL_RE` filter accepts only definition/theorem-style prefixes. I reproduced `find_citation_uses` returning neither a use nor an unresolved citation for missing equation, section, and item labels; the corresponding missing lemma is correctly reported.

These excluded label families occur in this migration. If one is mistyped or its target disappears, the generated review map silently loses it and resolution can return success. This violates the new review contract that unknown labels remain visible. Accept every syntactically valid label explicitly introduced as a blueprint citation, while retaining conservative handling for ambiguous bare paper references. Add regression tests for these missing-label families.

**F2 — Cite the input-consistency argument, not the projector-transport estimate.**
The same incorrect replacement appears at `MIPStarRE/LDT/SelfImprovement/Theorems/Results/HelperCompleteness/InputSdp.lean:120`.

`input_consistency_match_mass_lower_bound` proves that the matching mass of the original measurement `G` is at least `1 - nu`; `input_match_mass_eq_sdp_overlap` reindexes that mass as the polynomial-indexed SDP overlap. Their paper source, `references/ldt-paper/self_improvement.tex:407`, gives precisely this chain, ending with the lower bound justified by `item:self-improvement-G-consistency`.

By contrast, blueprint `eq:first-moved-completeness` at `blueprint/src/chapter/ch07_self_improvement.tex:137` removes a point projector from the expression involving the SDP measurement `T`, with approximation error `√δ`. It contains neither the input measurement `G` nor the `1 - ν` conclusion. The relevant blueprint argument starts at `blueprint/src/chapter/ch07_self_improvement.tex:188`.

Replace both citations with a suitable stable anchor for that argument, or cite the enclosing `lem:self-improvement-helper` with an explicit description of the input-consistency step. Do not restore numeric blueprint citations.

**Validation and scope**
- Confirmed the full on-disk patch matches the specified merge-base-to-head diff; the supplied inline attachment was truncated.
- All 100 changed Lean files have identical non-comment content before and after this PR. No theorem signatures, proofs, imports, or proof holes change.
- The scanner resolves 252 distinct cited labels uniquely in the changed Lean files. This establishes existence, not mathematical correctness, as F2 demonstrates.
- All 42 targeted citation and workflow tests pass; shell syntax and `git diff --check` pass.
- The attached prior ledger is empty. No working-tree changes or GitHub publications were made.

Residual risk: Lean compilation and live review dispatch were not rerun; existing proof debt is unchanged, not independently certified by this documentation/tooling review.

VERDICT: CHANGES_REQUESTED
