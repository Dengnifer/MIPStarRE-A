## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Extraction/Consistency.lean:101` — Reuse the shared placed-measurement API instead of introducing a second implementation and its supporting proofs.
## Review
F1: The private `placedMeasurement`, effect identity, and projectivity proof duplicate `ProjectiveSetting.placedMeasurement`, `placedMeasurement_effect`, and `placedMeasurement_isProjective` in `Combining/Points/Placement.lean:256–282`. Both constructions have the same hypotheses, carriers, and effects. The extraction version adds another register decomposition and roughly 100 lines of supporting code; its `place_finset_sum` also duplicates `place_finsetSum`. Consolidate these into shared placement infrastructure and use it from both modules. This requires changes under the modularity criterion.
No mathematical blocker was identified:
- `exists_combinedPointsWitness` preserves the admissible winning-strategy assumptions and all three conclusions, including every directed opposite placement, of `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:689–709`, `lem:qld-4-10`. The paper-gap note explains the alternative Fourier/sandwich proof without assuming an additional construction.
- `sum_marginalPoly_pointMeas_approx_id` proves all four placement instances of `eq:qld-sg-cons` at paper lines 1611–1635. Its internal `2 * deltaG` estimate fits the existing documented error enlargement.
- The native scalar theorem preserves the universal constants, distributions, original carriers, and three consistency bounds through postprocessing. It explicitly stops before projectivity and polynomial-shape conclusions of `lem:qld-4-7`.
The added-code integrity scan found no proof holes or kernel bypasses; existing unfinished line and extraction results remain. `git diff --check` passed.
Review is pinned to `816e6cb8`. The checkout had advanced to `eba9cb90`, so I used the authoritative patch and pinned Git contents and did not rerun Lean against the different revision. Compilation evidence remains the supplied successful CI status. The `lean-conventions` skill was unavailable; I used the checked-in proof-integrity rules and project addenda.
VERDICT: CHANGES_REQUESTED
