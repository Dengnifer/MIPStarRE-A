## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Lines/Sampling.lean:35` — This unimported module duplicates the canonical zero-direction bounds in `Lines/DiscardedMass.lean` and the resampling argument in `Lines/SubLineUniform.lean`.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/Lines.lean:3` — `Lines.Construction` is imported twice; remove the duplicate import.
- [ ] F3 (changes) `audits/2026-09-12_issue512-saved-proof-recovery.md:62` — The audit incorrectly says only construction dependencies were recovered even though `Lines/Sampling` is outside the target import graph and duplicates existing declarations.

## Review
F1 must be fixed before merge. The merge base already contains the same three public bounds in `Lines/DiscardedMass.lean`, and the active construction reaches them through `Lines/Conditioning.lean`. No module imports `Lines/Sampling`. Its resampling identity also restates `avgOver_uniform_lineRepMap_resample_parameter` from `Lines/SubLineUniform.lean`, while its remaining mass lemma is unused. Delete this stale recovery module, or retain only genuinely distinct, used results while reusing the canonical APIs.

F2 is a direct import-hygiene error and should be removed.

F3 makes the active recovery audit inaccurate. Its construction/provenance and required-action sections should describe the exact head after reconciliation with `main`, including removal or explicit disposition of the stale sampling module.

The earlier mathematical blockers are resolved. The printed `lem:qld-4-13` from `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:1020-1034` remains `\notready`. The certified `lem:qld-4-13-established` explicitly states its existential point family, directly indexed carrier and law, completed-answer comparison, and `C m poly(epsilon, md/q)` error. The remaining `sorry` sites are identified as open or unfaithful obligations with tracking references.

Kernel checks show that `exists_extendedLinesWitness_established` and `combined_line_measurement_consistency` depend only on `propext`, `Classical.choice`, and `Quot.sound`; no `sorryAx` reaches either theorem. The new sampling file type-checks, and `git diff --check` passes.

This is beyond the four-round cap. Under `local/protocols/review.md` §12, each remaining finding must now be fixed or explicitly deferred to a tracked issue in an exact-head adjudication.

VERDICT: CHANGES_REQUESTED