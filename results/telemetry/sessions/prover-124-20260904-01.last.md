## What changed

Added [BooleanFourier.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-124-boolean-fourier-foundations/MIPStarRE/QPBT/Combining/Linearity/BooleanFourier.lean:1) with:

- Boolean characters, multiplicativity, and orthogonality.
- Operator Fourier inversion, Hermiticity, matrix Parseval, and square-sum identity.
- Binary measurement effects and the substantive identity `binaryObservableDistSq = stateDepDistSq / 2`.
- Raw and normalized correlation formulas.

Added the [normalization paper-gap note](/home/drx/MIPStarRE-qpbt/.worktrees/issue-124-boolean-fourier-foundations/docs/paper-gaps/qpbt_linearity-distance-normalization.tex:1), including the four-point counterexample and correction plan, plus bibliography/index entries.

Committed as `45cc5d9 feat(QPBT/Combining): add Boolean Fourier foundations`.

## Evidence

- Direct Lean check: passed.
- Explicit Lake target build: passed, `8668` jobs; no new-module linter warnings.
- Forbidden-token and `sorry|axiom` scan: empty.
- Kernel dependencies: only `propext`, `Classical.choice`, and `Quot.sound`.
- Protocol-wrapped full `lake build`: passed, `9046` jobs.
- Paper-gap checker and full PDF batch build: passed.
- Strict blueprint web rendering: passed.
- Declaration checker: all `1053` declarations resolved.
- Git hooks installed and pre-commit integrity gates passed.
- Worktree is clean.

## Statement integrity

Paper assumptions and conclusions are preserved by the supporting identities. Lean adds only finite-type and decidable-equality boundary instances. No paper-labelled theorem was changed, and `thm:linearity` remains unmarked as complete.

Verdict: exact supporting identities with faithful boundary hypotheses.

## Open obligations

No proof obligations remain in the new file.

The existing `exists_exactly_linear_observables` proof remains outside this packet. Issue `#129` must repair its false raw-distance bound using the new paper-gap note: normalized distance at most `δ`, equivalently raw distance at most `2 * δ`.

## Handoff

The operator should add:

```lean
import MIPStarRE.QPBT.Combining.Linearity.BooleanFourier
```

to `MIPStarRE/QPBT.lean`; this session intentionally did not edit that serialized re-export.

Exact-head CI and independent review require pushing/opening the PR. Neither was attempted because this task explicitly prohibited pushing and self-review.