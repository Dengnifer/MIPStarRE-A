## What changed

Committed `4344230` (`fix(review): correct Magic Square imports and locators`).

- Removed the unused Pauli import from `Basic.lean`.
- Added direct Pauli imports to the facade and `PerfectStrategy.lean`.
- Corrected all split-module blueprint ranges: game `207-222`, rigidity `228-250`, perfect strategy `257-267`.
- No declarations, proofs, blueprint files, or re-exports changed.

## Evidence

All four touched files and `MIPStarRE/QPBT.lean` type-check. The locked full build passed with 9,043 jobs; all 822 blueprint declarations resolved. Axiom checks for the three foundation results and `msGameSymm` contain no `sorryAx`.

The scoped debt scan reports exactly the two preserved holes.

## Statement integrity

Paper and Lean assumptions and conclusions are unchanged. Rigidity retains arbitrary strategies and all seven universal `O(sqrt ε)` bounds. Perfect strategy retains projective, EPR-consistent anticommuting measurements and the exact SPCC, state, and effect conclusions. Verdict: faithful boundary hypotheses.

## Open obligations

- `exists_ms_rigidity`: tracked by issue #77.
- `exists_ms_perfect_strategy_of_anticommuting`: tracked by issue #76.

Neither has a paper-gap note because these are direct proof obligations, not statement discrepancies.

## Handoff

The worktree is clean. Push `4344230` to PR #87 and rerun exact-head CI and independent review. No `MIPStarRE/QPBT.lean` re-export is needed.