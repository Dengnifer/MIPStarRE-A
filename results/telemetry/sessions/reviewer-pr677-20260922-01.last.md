## Findings
- [ ] F1 (changes) `docs/comparator.md:116` — The run did not compare the old private/compiler-generated blockers because PR 671 removed them from the closure or made them public.
- [ ] F2 (changes) `docs/comparator.md:121` — CI does not run the comparator drift check on every commit carrying this documentation record.
- [ ] F3 (changes) `scripts/comparator/README.md:146` — The generator README still says four-target comparator acceptance is pending.

## Review

For F1, the verified challenge is correctly described as equal to the current four-target closure. However, the trailing claim about “including” the earlier blockers is inaccurate. Between `a534c7f9` and `ecb97d1f`, the expected challenge drops the private `pauliWinPredicate` auxiliary and numerous matcher declarations; `ldSpaceSplit` and distribution helpers become public, while the completeness proof is rewritten specifically to avoid module-private matcher equations. State that the repair eliminated those blockers before the successful comparison.

For F2, `docs/*.md` only selects the `paper-gaps` area in `local/bin/ci.sh:660`. The build step containing `check_challenge_drift.py` runs only for Lean, comparator, or workflow changes at lines 673 and 877-879. Either narrow the claim to the exact-head completion requirement, or extend the gate and its tests if every commit carrying the record must rerun drift verification.

For F3, the operational README directly contradicts the new canonical status by saying real-landrun, nanoda, merged-main pinning, and all-four equality remain pending. Update that paragraph or defer mutable status entirely to `docs/comparator.md`.

The core external evidence is otherwise consistent: the linked run is successful at the cited challenge commit, whose source pins the merged library SHA, all four targets, the three permitted axioms, nanoda, and the real-landrun verification path. citeturn0view0turn3view0turn3view1turn3view3turn3view4

The static completion gate recognizes C5 as `DELEGATED`, all four names occur in the registered challenge, the pin is an ancestor, and no Lean or comparator files changed after that pin. The overall gate still fails unrelated C3/C4 criteria, consistent with the new non-completion disclaimer.

VERDICT: CHANGES_REQUESTED