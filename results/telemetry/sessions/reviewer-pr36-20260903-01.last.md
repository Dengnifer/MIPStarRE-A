## Findings
- [ ] F1 (blocker) `MIPStarRE/QPBT/Observables/WinImplications.lean:186` — `win_cons` and `win_cons_approx` compare different endpoint questions instead of the same self-loop question.
- [ ] F2 (blocker) `MIPStarRE/QPBT/Games/DistanceTheorems.lean:255` — `exists_projective_close_of_consistent` drops the source theorem's unit-state hypothesis.
- [ ] F3 (blocker) `MIPStarRE/QPBT/Observables/LineMeasurement.lean:214` — `ExpandedLineConclusions` replaces the source lemma's common `δ_Line` bound in item 2 with `ε`.
## Review
F1: In `win_cons`, the index is a pair sampled from `pauliQuestionDistribution P`; Alice receives `questions.1` while Bob receives `questions.2`. The cited item `enu:qld-cons` in `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:197-202` compares both players' measurements for the same question `(t,x)`. The blueprint repeats this at `blueprint/src/chapter/ch14_qpbt_observables.tex:555-559`.
The joint distribution contains non-loop edges, including line/point edges. Their valid answers use disjoint `PauliAnswer` constructors, so the stated defect can remain positive even for a perfect strategy, contradicting the zero-error conclusion. `win_cons_approx` repeats the same indexing at lines 298-308. Both declarations must use a single-question distribution, such as the self-loop-conditioned distribution or its rigorously equivalent marginal, and pass that same question to both players.
F2: `lem:ortho` says “let `|ψ⟩` be a state” at `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:136-143`. The paper defines a strategy state as a unit vector in `references/qpbt-paper/06_nonlocal_games_and_mipstar.tex:26-38`; the blueprint preserves normalization at `blueprint/src/chapter/ch12_qpbt_games.tex:61-68`. The Lean theorem accepts an arbitrary vector. Since both defects are unnormalized quadratic forms in `ψ`, normalization is load-bearing. Add `‖ψ‖ = 1` or use a bundled normalized-state type.
F3: `lem:qld-comm-line-cons` introduces one function `δ_Line` and uses it for all three conclusions, including item 2 at `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:523-549`. The blueprint does likewise at `blueprint/src/chapter/ch14_qpbt_observables.tex:1164-1220`. The proof obtains a sharper `O(ε)` intermediate estimate, but the source-facing conclusion remains `O(δ_Line(ε))`. Keep `expLine_point_cons` as the sharper helper, but change `ExpandedLineConclusions` to use `C * δ ε` and provide the domain or large-error argument needed to derive it.
The 51 introduced `sorry` sites are explicitly enumerated as Stage 4.2 skeleton obligations in `local/briefs/42-ch14-brief.md:159-190`; I therefore did not report them as untracked proof holes. They remain substantial proof debt. The supplied exact-head CI evidence is green, but compilation through these placeholders does not address the three statement-level blockers above.
VERDICT: CHANGES_REQUESTED
