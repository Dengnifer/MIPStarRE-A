## Findings
- [ ] F1 (changes) `local/protocols/issues-prs.md:210` — The project-local infrastructure budget remains owner-gated, contradicting the new permissions-only boundary.
- [ ] F2 (changes) `local/personas/main.md:29` — The requested literal reply contains every option instead of the single selected letter required by the governing invariant.

## Review

**F1:** Section 6 now assigns decisions whose only risk is project completion to main, but this earlier section still reserves `MIPSTARRE_INFRA_OVERRIDE` for the owner. The same stale routing remains in `local/personas/main.md:181-193`, which requires owner escalation at the workflow budget and owner approval for consecutive workflow episodes, and in `.githooks/pre-commit:67-68`. These are project scope and gate decisions, not permissions involving owner files, accounts, spending, or external action. Synchronize these active surfaces so the guard remains enforced while its disposition follows the new main-decision contract.

**F2:** `AGENTS.md:701`, `local/DESIGN.md:114`, and the evolution entry specify `DECISION B<n>: <letter>`. The main prompt instead calls `DECISION B<n>: A | B | C` the literal reply, and `local/protocols/issues-prs.md:244` repeats it. That string selects no option. Use the single-letter placeholder as the literal format and present allowed alternatives separately.

The remaining migration is consistent: live references route to #500, historical #26 citations are identified as provenance, and `git diff --check` passes. No Lean or executable implementation changed.

VERDICT: CHANGES_REQUESTED