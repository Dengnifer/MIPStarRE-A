## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT/Games/DistributionAux.lean:19` — Move the generic support-normalization lemma into the shared `Distribution.IsProbability` API to eliminate existing duplicate proofs.

## Review

`weight_sum_eq_one_of_subset` contains no QPBT-specific data. The identical `Finset.sum_subset` and `outsideSupport` argument already appears twice in `MIPStarRE/LDT/Basic/DistributionAvg.lean:269-286`. No equivalent local or Mathlib lemma exists beyond the primitive `Finset.sum_subset`, so this adapter is useful, but it belongs beside `Distribution.IsProbability.weight_sum_eq_one` in `MIPStarRE/LDT/Basic/Distribution.lean:121` and should be reused from both modules. This modularity and duplication issue must be fixed before merge.

The three normalization proofs are otherwise correct and well structured. Product mass factorizes, mixture mass extends correctly to the union support, and bind swaps finite sums before normalizing each supported fiber. The file type-checks without warnings, `git diff --check` is clean, and the changed file contains no `sorry`, axioms, bypasses, or placeholder tactics.

No public signature changed. These declarations are formalization-only normalization auxiliaries rather than paper-labelled or directly blueprint-linked results. Comparison with `lem:ld-sandwich` at `references/qpbt-paper/06_nonlocal_games_and_mipstar.tex:465-501`, `def:line-point-dist` at `references/qpbt-paper/08_classical_and_quantum_low_degree_tests.tex:276-287`, and typed conditionally linear distributions at `references/qpbt-paper/07_types.tex:84-93` found no added proof-debt assumptions or source-statement drift.

VERDICT: CHANGES_REQUESTED