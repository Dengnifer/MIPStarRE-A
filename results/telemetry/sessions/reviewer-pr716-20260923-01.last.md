## Findings

- [ ] F1 (changes) `docs/paper-gaps/qpbt-gap-register.md:30` — The new `documented-deviation` status is described as terminal but remains rejected by the completion contract and checker.

## Review

C3 in `local/protocols/completion.md:48` permits only `corrected` and `no-difference`; `scripts/completion_gate.py:386` enforces that set. Running `criterion_paper_gaps` against this checkout explicitly reports line 30’s `documented-deviation` as nonterminal. Consequently, this row will still block completion after the other gaps are resolved, contrary to the updated documentation.

Either implement the authorized documentary status consistently in the protocol, checker, tests, and amendment ledger, or retain `open` in the C3 column and record documentary closure separately.

The mathematical comparisons checked against the paper mirrors and Lean declarations are consistent, including the exceptional parameter range, proof constants, and separate projected-point marginals. Referenced blueprint labels resolve, and `git diff --check` passes. No Lean statements or formalization marks change. Lean and TeX builds were not rerun in this read-only review.

VERDICT: CHANGES_REQUESTED