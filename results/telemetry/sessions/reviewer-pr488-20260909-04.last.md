## Findings

- [ ] F4 (changes) `MIPStarRE/QPBT/Combining/Lines/AxisLineResampling.lean:45` — Extract the duplicated joint-resampling identity into one shared lemma used by the axis and diagonal proofs.

## Review

The new local `hresample` repeats the statement and proof already at `MIPStarRE/QPBT/Combining/Lines/DiagonalResampling.lean:52`. Both derive the same joint expectation identity from `uniformDistribution_map_lineRepMap_add_smul`, independently of the line constructor. Move this identity into their shared support module and specialize it in both proofs. Maintaining two copies of this argument violates the review’s duplication requirement; this must be addressed before approval.

All prior findings are addressed:

- F1: `Sandwich.lean` uses the existing operator estimates and shared placement API.
- F2: the Parseval docstring correctly specifies orthogonal columns.
- F3: the prefix summary correctly states that preceding blocks vanish and later blocks remain.

No mathematical or source-statement blocker was found. The collision chain derives the degree-to-field-cardinality bound used at `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:955`. The product estimate preserves independence, permits dependence on the complete second sample, and explicitly restricts the first direction to be nonzero. It remains auxiliary support for `lem:qld-xz-lines`; no paper-facing hypotheses, quantifiers, conclusions, or error parameters change. The placement and quadratic-form proof changes also preserve their statements.

Validation: ten focused Lean checks passed. Axiom checks for the collision chain and changed operator lemmas reported only `propext`, `Classical.choice`, and `Quot.sound`. The two existing combined-line construction holes at `Lines.lean:70` and `Lines.lean:99` remain unchanged; the unrestricted construction is still unfinished. No full build was rerun. The unavailable `lean-conventions` skill was supplemented by the checked-in proof-integrity rules and project addenda.

VERDICT: CHANGES_REQUESTED