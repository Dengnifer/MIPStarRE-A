## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT/Games/StrategyClasses.lean:763` — The `**Unfaithful:**` marker proposes weakening the source-labelled theorem instead of preserving its tracked attainment obligation.
- [ ] F2 (changes) `MIPStarRE/QPBT/Games/DistanceTheorems/ProjectiveRounding.lean:18` — The blueprint citation points to `lem:povm-to-obs`, not the orthonormalization lemma implemented by this module.
- [ ] F3 (changes) `MIPStarRE/QPBT/Games/DistanceTheorems/ProjectiveRounding.lean:79` — The new module duplicates substantial left-marginal and rank-zero infrastructure already present in an imported module.
- [ ] F4 (changes) `MIPStarRE/QPBT/Games/DistanceTheorems.lean:812` — The completed theorem emits an `unusedVariables` linter warning for `hψ`.

## Review

**F1:** `exists_symmetric_projective_strategy` correctly retains the attainment statement printed at `references/qpbt-paper/06_nonlocal_games_and_mipstar.tex:94-99`, while its `sorry` is explicitly tracked and its `\leanok` was removed. However, the new marker says that elimination may “replace the source statement by its approximate form.” That conflicts with both the faithful-formalization policy and the conclusion of `docs/paper-gaps/qpbt_symmetrization-attainment.tex`, which prescribes retaining the source-shaped theorem as a tracked obligation and using the given-strategy or approximate theorem separately. Remove that replacement route; the marker should say that the `sorry` is discharged only by an independent attainment theorem, unless a future documented statement correction formally reclassifies the paper assertion.

**F2:** `blueprint/src/chapter/ch14_qpbt_observables.tex:356-383` contains the end of the preceding finite-sum proof and `lem:povm-to-obs`. The orthonormalization lemma is at lines 390-410. The module reference must be corrected so its mathematical provenance is reviewable.

**F3:** Lines 79-186 reproduce `diagBlock`, `leftMarginalDensity`, the block-diagonal trace calculation, normalization, and marginal evaluation from `MIPStarRE/LDT/MakingMeasurementsProjective/LocalityPreservingRepair.lean:54-158`. Lines 189-203 also duplicate its `matrix_eq_zero_of_rank_eq_zero` at lines 309-323. This is especially unnecessary because `Orthonormalization.lean` already imports `LocalityPreservingRepair.lean`. Extract these facts into a shared API and reuse them; maintaining separate copies of normalization-sensitive matrix calculations creates avoidable divergence risk.

**F4:** A targeted check reports `Variable name hψ is not explicitly referenced`. The normalization hypothesis is mathematically required, so it should remain; name the nested binder `_hψ` and introduce a local `hψ` in the proof, or otherwise restructure the binder so the warning disappears. Do not suppress the linter.

The substantive statement audit found no source drift:

- `abs_value_sub_le_of_areClose` matches `lem:close-strategies-have-close-values` at `references/qpbt-paper/06_nonlocal_games_and_mipstar.tex:531-539`; the type equivalences encode the paper’s identification of local spaces, and the proof supplies the universal constant `4`.
- `exists_projective_close_of_consistent` matches `lem:ortho` at `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:136-145`; it retains the normalized state, `0 ≤ δ ≤ 1`, consistency premise, and `O(δ^(1/4))` conclusion.
- `exists_symmetric_projective_strategy_of_strategy` is correctly identified as the formalization-only, value-preserving construction proved by the paper’s argument.
- The declarations moved into `DistanceTheorems/Calculus.lean` preserve their public signatures.

All four changed Lean modules type-check. The new completed theorems depend only on standard axioms, with no `sorryAx`; the sole remaining `sorry` is the clearly documented attainment gap in `exists_symmetric_projective_strategy`. Exact-head local CI was already green; I did not repeat the full build.

VERDICT: CHANGES_REQUESTED