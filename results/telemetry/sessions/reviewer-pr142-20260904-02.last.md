## Findings
- none
## Review
No blocking or changes-level issues found.
Statement-integrity audit:
| Declaration | Source comparison | Verdict |
|---|---|---|
| `isCondLinear_iff_nonempty_clData` | Matches `lem:cl-kth`, `references/qpbt-paper/05_conditionally_linear_functions.tex:150-178`: level positivity, prefix marginals, register partition, supported linear pieces, sum formula, and final marginal are preserved. | Exact, with faithful Lean boundary instances |
| `IsCondLinearOn.concat` | Matches `lem:cl-concat`, lines 282-314: the inner family is required only on `Set.range L`, and the result has level `k + ell`. | Exact |
| `IsCondLinear.directSum` | Matches `lem:cl-func-prod`, lines 315-364: pairwise-disjoint covering registers encode the direct-sum decomposition, and the resulting level is the maximum component level. | Exact, with explicit `m ≥ 1` boundary |
| `clDistribution_directSum_eq_prod` | Matches `lem:cl-dist-prod`, lines 365-383. Equality is stated pointwise for every output, which faithfully characterizes the finite distribution after identifying the ambient space with its register components. | Exact |
| `graphDistribution` / `graphDistribution_isProbability` | Matches `references/qpbt-paper/07_types.tex:65-82`; nonempty `E` is the necessary normalization boundary omitted by the paper. | Faithful boundary hypothesis |
The zero fallback used while totalizing the concatenation family is confined to indices outside `Set.range L`; the evaluation proof establishes that the selected index is always in the range, so this is not an A3 fallback or proof shortcut. The decomposition and direct-sum proofs construct the required register data rather than importing bridge or witness assumptions.
Both edited Lean files type-check without warnings. The changed lines contain no proof holes, prohibited tactics, unsafe features, or new axioms. `#print axioms` for all five paper-facing theorems reports only `propext`, `Classical.choice`, and `Quot.sound`. `git diff --check` also passes. Exact-head local CI was already green; I did not duplicate the full build or blueprint render.
VERDICT: APPROVED
