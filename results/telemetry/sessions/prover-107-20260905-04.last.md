## What changed

Merged `github/main` into the branch in commit `f0ce7f3` (`2961bf2` + `06ba680`).

Resolved the six conflicts while retaining main’s consolidated QPBT APIs and this branch’s proofs. The target declarations are closed:

- `AdmissibleParams.one_le_m`
- `isCondLinear_pauliCL`
- `pauliBasisTest`’s `μ_prob`
- `introParamsTuple_isAdmissible`
- `le_two_pow_introParams_m`

Also retained the upstream `LowDegreeGame.μ_prob` proof and reconciled the ch12/ch13 blueprint links and citations. `MIPStarRE/QPBT.lean` was not edited; no re-export is needed.

## Evidence

- `lake env lean` passed for all changed Lean modules, including `PauliBasisTest.lean`, `CanonicalParams.lean`, `Completeness.lean`, `LowDegreeGame.lean`, `CondLinearTheorems.lean`, and `DirectSumSupport.lean`.
- `lake build MIPStarRE.QPBT` completed successfully: `8840/8840`.
- The only remaining `sorry`s are the five inherited obligations listed below; no new `sorry` or `axiom` was introduced.
- Blueprint LaTeX and synchronization checks passed. The axiom audit reported 35 unrelated baseline failures and no failures for the completed Pauli declarations.
- Git hooks passed, and `git status --short --untracked-files=all` is empty.

## Statement integrity

Paper assumptions / Lean assumptions: the existing admissibility, finite-field, positivity, divisibility, and decidability boundary conditions; no bridge, residual, package, or extra proof-obligation hypotheses were added.

Paper conclusion / Lean conclusion: the Pauli conditional-linearity, sampler/game probability, and canonical-parameter conclusions are unchanged and fully proved.

Verdict: exact, with faithful boundary hypotheses.

## Open obligations

- `pauliQuestionDistribution_eq_typedCL`: derive the sampler equality from the common-seed typed-family construction; tracked by issue #180 and the cited sampler passage.
- `pauliQuestionDistribution_symm`: sampler symmetry proof remains open.
- `pauliWinPredicate_symm`: verifier symmetry proof remains open.
- `exists_spcc_value_one`: completeness argument remains open (paper passage 1229–1421).
- `exists_deltaQld_introParams_bound`: later numerical soundness packet; documented in `qpbt_delta-bound-exponent-comparison`.

## Handoff

Review the six conflict resolutions first, especially `PauliBasisTest.lean`, `LowDegreeGame.lean`, and the consolidated ch12/ch13 blueprint entries. The merge is committed locally and has not been pushed.