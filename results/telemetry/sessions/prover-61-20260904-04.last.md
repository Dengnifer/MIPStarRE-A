## What changed

Regenerated `scripts/comparator/expected/Challenge.lean.expected`. The diff updates three source-line provenance comments after the shared normalization lemma insertion.

Committed as `a0edeec`:

`fix(ci): regenerate the comparator challenge after the normalization lemma move`

Not pushed.

## Evidence

- Comparator drift check passes at committed HEAD.
- Generated fixture type-checks successfully.
- Its warning is the documented, pre-existing comparator `mainFormal` `sorry`.
- `git diff --check` passes.
- Worktree is clean.
- Full build skipped because only generated comments changed; exact fixture elaboration and regeneration validation passed.

## Statement integrity

No Lean declarations or paper-labelled theorem statements changed.

## Open obligations

None.

## Handoff

The CI drift finding is resolved. Review should confirm the three updated provenance ranges; no re-export changes are needed.