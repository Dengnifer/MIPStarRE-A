<!-- scout: scout-67-20260904-01 2026-09-04 -->
## Mathlib scouting report — 2026-09-04

### Mathematical source

- Issue #67 is structural; it introduces no new mathematical assertion. The immutable baseline is commit `ff5d719d937e76e565994a6c3b2e43351709ca53`, file `MIPStarRE/QPBT/Combining/DirectLowDegree.lean`.
- The game originates at `references/qpbt-paper/08_classical_and_quantum_low_degree_tests.tex:31-391` and `blueprint/src/chapter/ch13_qpbt_test.tex:17-105`.
- The direct-index repair is documented at `blueprint/src/chapter/ch15_qpbt_combining.tex:1257-1293` and `docs/paper-gaps/qpbt_ld-dimension-divisibility.tex`.
- The final auxiliary soundness obligation supports the polynomial-pair argument at `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:1267-1288`.
- Precisely: the direct game samples its coordinate index from `Fin m`, avoiding the source game’s divisibility-dependent `χ` encoding, while retaining its point, axis-line, diagonal-line, and nine ordered type branches.

### Relevant Mathlib definitions

- None need modification or replacement. This issue must move existing elaborated declarations verbatim; introducing a new Mathlib-based implementation would violate its scope.

### Relevant Mathlib lemmas and theorems

- None are needed. The six open proofs are intentionally unchanged.

### Relevant MIPStarRE declarations

All entries below lie under `MIPStarRE.QPBT`.

- `Geometry.lean` owns baseline lines 39–266, comprising 30 source declarations: `DirectLdParams`, `DirectLdParams.model`, `DirectScalarQ`, `DirectLdParams.firstIndex`, `instNonemptyFinM_1`, `AdmissibleParams.extendedDirectLd`, `DirectLdSpace`, `instNonemptyDirectLdSpace`, `directPrefixProjection`, `DirectLineDesc`, `DirectLineDesc.kind`, `DirectLineDesc.index`, `DirectLineDesc.base`, `DirectLineDesc.direction`, `DirectLineDesc.base_fixed`, `DirectLineDesc.diagonal_prefix_zero`, `DirectLineDesc.pointSet`, `directALineDescOf`, `directDLineDescOf`, the three `direct*LinePointDist` definitions, their three probability theorems, and the five final distribution theorems.
- Geometry also generates the existing constructors, projections and recursors, plus `instDecidableEqDirectLdSpace`, `instFintypeDirectLdSpace`, and `instDecidableEqDirectLineDesc`.
- `Game.lean` owns baseline lines 268–455, comprising 22 source declarations: `DirectDegPoly`, `DirectEvaluatesTo`, `directEvalOpt`, `DirectLdQuestion`, `directLdMap`, `directLdQuestionDistribution`, `directLdQuestionDistribution_isProbability`, `DirectLdAnswer`, `DirectLdAnswerCode`, `directLdAnswerEquiv`, `instInhabitedDirectLdAnswer`, `instFintypeDirectLdAnswer`, `validDirectLdAnswer`, `directAlinePointCondition`, `directDlinePointCondition`, `directLdWinPredicate`, `directLdGame`, `directLdPointQuestionOf`, `directLdPointValuesOrZero`, `DirectPolyTuple`, `DirectPolyMeasTuple`, and `evalDirectPolyTupleAt`.
- Game also retains the generated answer constructors and `instDecidableEqDirectLdAnswer`.
- `Soundness.lean` owns baseline lines 457–497: only `MIPStarRE.QPBT.exists_direct_ld_soundness`, including its complete docstring and unchanged proof hole.
- The facade owns no declarations. It retains the original module docstring and imports only `MIPStarRE.QPBT.Combining.DirectLowDegree.Soundness`.

The declaration payload hashes from the baseline are:

- Geometry, lines 39–266: `ac043396b833f4174f92f4f4e2cb59724ca44539f6cdf073dee1a602d1b463e9`
- Game, lines 268–455: `e6f83aa81aa3d6dbe935929da584cef4a8f6015ed5087ac4bd6c414be40ca9b6`
- Soundness, lines 457–497: `21b031731ec09530cf0ebb8604d86fde99f7c8720da64f7ba3ba252ef250a5c7`
- Facade docstring, lines 4–27: `2659324c17aaca54a6955162958aadfce58d1f011e9bf98bdcdcaae65912eae8`

### Proof-debt baseline

- `directALinePointDist_point_index_marginal_uniform` — baseline line 229; Geometry target line 218.
- `directALinePointDist_mem_line` — baseline line 237; Geometry target line 226.
- `directDLinePointDist_point_index_marginal_uniform` — baseline line 248; Geometry target line 237.
- `directDLinePointDist_mem_line` — baseline line 255; Geometry target line 244.
- `directDLinePointDist_prefix_zero` — baseline line 266; Geometry target line 255.
- `exists_direct_ld_soundness` — baseline line 497; Soundness target line 65.

There are exactly six `sorry` sites, no `admit`, and no source-level `axiom`.

### Import and namespace risks

- The required graph is `LowDegreeGameTheorems + PauliBasisTest → Geometry → Game → Soundness → facade`.
- Each internal file must independently repeat `open scoped BigOperators`, `namespace MIPStarRE.QPBT`, the three existing `open` commands, `noncomputable section`, and both closing `end` commands. These parser/elaboration settings are not supplied safely by merely importing the preceding leaf.
- Do not add a namespace derived from the directory name. `namespace MIPStarRE.QPBT.Combining.DirectLowDegree` would rename every declaration.
- Preserve Geometry’s original import order. In particular, the anonymous instance name `MIPStarRE.QPBT.instNonemptyFinM_1` is sensitive to the imported declaration environment.
- Neither test import reaches back into `Combining`, so the linear leaf chain creates no import cycle.
- Immediate consumers are only `MIPStarRE/QPBT.lean:41` and `MIPStarRE/QPBT/Combining/Witnesses.lean:2`. Both should continue importing the facade unchanged.
- `MIPStarRE/QPBT.lean` needs no change. Its existing facade import transitively exposes all 53 source declarations. Keeping that explicit import also avoids making the public API depend accidentally on `Witnesses.lean`.

### Suggested approach

Perform only the three exact payload moves and replace the facade imports. Payload identity plus the unchanged namespace and equivalent import environment preserves explicit signatures, generated constructors, projections, recursors, and instance names.

Post-split validation should be:

1. Recompute the four hashes above against the extracted leaf payloads and facade docstring.
2. Confirm `.ilean` declaration counts are Geometry `30`, Game `22`, Soundness `1`, facade `0`, and compare their declaration keys with the inventory above.
3. Run `lake env lean` on Geometry, Game, Soundness, the facade, and `Combining/Witnesses.lean` in dependency order.
4. Run `rg -n '\b(sorry|admit|axiom)\b'` over the four-file surface and require exactly the six locations listed above.
5. Run `git diff --check`.
6. Require no diff from baseline in `MIPStarRE/QPBT.lean`, `Witnesses.lean`, `Apply.lean`, `blueprint/`, or `docs/paper-gaps/`.
7. After committing and opening the PR, run `local/bin/ci.sh <PR>`; its exact-head build step performs the required `lake build` under the machine-wide lock.

### Gaps to fill

- No new gap belongs to issue #67. The five geometric distribution proofs and the direct soundness theorem remain the same six tracked obligations.
- No blueprint or paper-gap update is warranted because neither the statements nor their proof status change.

### Searched

Read the live issue and found zero existing comments; inspected the baseline blob and declaration ranges with `git show`/`git grep`; searched all Lean imports and downstream uses; inspected `Witnesses.lean`, `Claims.lean`, `Lines.lean`, `Apply.lean`, and `MIPStarRE/QPBT.lean`; read the cited paper, blueprint, and divisibility note; checked `docs/api_surface.md`, `docs/formalization-patterns.md`, and `audits/` (no prior DirectLowDegree/QPBT split report exists); and queried generated instance names and `.ilean` declaration inventories. No full build was run by this scouting session.