## Findings

- [ ] F2 (changes) `MIPStarRE/QPBT/Test/MagicSquareTheorems/Rigidity/GroundSlice.lean:215` — Consolidate the duplicate public `heteroKron_mul` and `heteroKron_one_one` lemmas into a shared tensor-placement API.

## Review

F2 requires changes. `heteroKron_mul` exactly duplicates the public theorem in `PerfectStrategy/Observables.lean:330`; the adjacent `heteroKron_one_one` duplicates its counterpart at line 369. Keeping parallel public declarations under different namespaces violates the shared-helper policy in `docs/mathematical_language.md:117-134` and creates two APIs whose fixes or generalizations may diverge. Move these lemmas to a common lower-level module imported by both developments and remove the local copies.

The prior-round blocker is resolved. The new ground-slice leakage estimates and `ms_effect_transfer_A/B` plus `ms_anticommutator_transfer_A/B` now relate the actual dilated projectors to the inflated original effects. Thus `opFamilyDistSq_naimarkInflation` is no longer the unsupported endpoint identified previously.

No paper-facing declaration changed. The unchanged `exists_ms_rigidity` statement remains aligned with `references/qpbt-paper/08_classical_and_quantum_low_degree_tests.tex:612-652` and `blueprint/src/chapter/ch13_qpbt_test.tex:224-253`; the new hypotheses occur only in explicitly named internal transfer lemmas. The construction also respects the Naimark caveat in `references/ldt-paper/orthonormalization.tex:82-101` by rederiving state-dependent bounds from preserved consistency masses.

All four new Lean modules and the umbrella module type-check. No new `sorry`, `axiom`, bypass, placeholder tactic, or debug command was introduced. `exists_ms_rigidity` retains its pre-existing `sorry` at `MagicSquareTheorems.lean:132`; this PR supplies infrastructure but does not close that known theorem frontier.

VERDICT: CHANGES_REQUESTED