## Findings

- [ ] F1 (changes) `docs/paper-gaps/qpbt_ld-dimension-divisibility.tex:460` — Rewrite the note’s earlier status sections, which still claim the now-proved seed-fibre transport, synchronicity control, and compression are open.

## Review

F1 requires changes before merge. The revised conclusion at lines 460–499 says the direct-index route is complete for `k = 1`, including parsing, correlated-register dilation, and recovery after compression. However, the same living note still says:

- Lines 26–41: the repair is only a proposed reformulation with open synchronicity and dilation–compression work.
- Lines 100–105: the direct theorem is merely a candidate and transport remains open.
- Lines 293–316: none of the listed transport obligations has been carried out.
- Lines 353–359 and 370–372: synchronicity control and preservation under compression remain unproved.

This violates `docs/paper-gaps/policy.tex:203-216`, which requires revisions to present one coherent account of the current mathematical status. Those sections should distinguish the completed correlated-register argument for `k = 1` from the still-open arbitrary-`k` combining reduction and downstream rereading.

The Lean changes themselves passed review. Compared with `lem:ld-soundness` in `references/qpbt-paper/08_classical_and_quantum_low_degree_tests.tex:413-440`, `exists_ld_soundness_of_k_eq_one` preserves the constants, projectivity and value hypotheses, three consistency conclusions, and `deltaLd` error; the explicit `L.k = 1` restriction is displayed as a formalization-only specialization rather than advertised as the unrestricted paper theorem. No bridge or proof-debt hypothesis was introduced.

I type-checked all three changed proof files, checked the new theorem axiom closures, and found only the pre-existing arbitrary-`k` `sorry` in `exists_direct_ld_soundness`, which is clearly documented as tracked work. The new theorems depend only on standard Lean axioms (`propext`, `Classical.choice`, and `Quot.sound`).

VERDICT: CHANGES_REQUESTED