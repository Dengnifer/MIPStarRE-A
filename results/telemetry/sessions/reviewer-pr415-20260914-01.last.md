## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Games/DistanceTheorems/Support.lean:253` — Consolidation silently changes the existing theorem’s fully qualified public name.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/Lines/OptionPostprocessDistance.lean:45` — The proof duplicates the existing placement-zero lemma and bypasses the public postprocessing API.
## Review
**F1.** At the merge base, `stateQForm_conjTranspose_mul_mul` was declared under `MIPStarRE.QPBT` in `Combining/Points/Consistency.lean`. Its replacement is inside `MIPStarRE.QPBT.DistanceCalculus`, changing the fully qualified name. Existing uses still compile because they open `DistanceCalculus`, masking the API regression. Preserve `MIPStarRE.QPBT.stateQForm_conjTranspose_mul_mul`, either as the shared declaration or through a compatibility alias. This must be fixed before merge.
**F2.** `ProjectiveSetting.place_zero` already provides the exact result at `Observables/LineMeasurement/LinePointOverlap.lean:64` and is available through the current import graph. Lines 45–48 repeat its proof. The final simplification also unfolds `Measurement.postprocess` and `Submeasurement.postprocess` despite the public `Measurement.postprocess_effect` theorem at `Quantum/Measurement.lean:136`. Rework this proof against those APIs instead of duplicating implementation details.
The new option-completion result is mathematically sound and clearly identified as formalization-only. Paper `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:931-940` uses ordinary field-valued outcomes, while blueprint `ch14_qpbt_observables.tex:1746-1750` introduces the completed `⊥` outcome. The Lean equality faithfully bridges that encoding and does not claim to be a paper theorem.
The full Lean-diff scan found one added `sorry`, at `Combining/Lines.lean:73`. It is explicitly documented as the `combined_line_measurement_consistency` proof obligation, with issues #18/#414, source lines, paper-gap note, and discharge plan, so it is a clearly marked known TODO rather than an untracked hole.
The previous duplicate-declaration problem is resolved in substance: only one declaration remains. F1 is the namespace regression introduced by that consolidation. The attached prior ledger contained no usable finding text. Exact-head local CI is reported green, and `git diff --check` is clean.
VERDICT: CHANGES_REQUESTED
