## Findings

- [ ] F1 (blocker) `blueprint/src/chapter/ch12_qpbt_games.tex:1384` — The linked Lean definition omits the typed conditionally linear family hypotheses required by the blueprint.
- [ ] F2 (blocker) `blueprint/src/chapter/ch13_qpbt_test.tex:87` — The low-degree `\leanok` is unsupported because the linked equality never establishes that `ldCL` is a typed conditionally linear family.
- [ ] F3 (changes) `blueprint/src/chapter/ch13_qpbt_test.tex:395` — The Pauli source lemma links to the same weaker sampling equality rather than a source-faithful open obligation.
- [ ] F4 (changes) `blueprint/src/chapter/ch13_qpbt_test.tex:64` — Both new level lemmas claim a concatenation dependency absent from their Lean proofs.
- [ ] F5 (changes) `MIPStarRE/QPBT/Games/DistributionAux.lean:203` — Eight new public distribution lemmas have no formalization-support nodes or dependency edges.
- [ ] F6 (changes) `blueprint/src/chapter/ch13_qpbt_test.tex:109` — The line-sampling proof dependencies omit the uniform push-forward, line-description, and representative-idempotence results actually used.
- [ ] F7 (changes) `MIPStarRE/QPBT/Games/CondLinear.lean:16` — The updated chapter-12 line citations are stale across several changed docstrings.
- [ ] F8 (changes) `MIPStarRE/QPBT/Games/CondLinear.lean:9` — The module docstrings describe formalization stages, supporting layers, and file ownership instead of mathematical roles.
- [ ] F9 (changes) `MIPStarRE/QPBT/Games/CondLinearTheorems.lean:26` — Three new docstrings use “stage” where the source terminology is “level” or “linear contribution.”

## Review

Changes are required. F1 and F2 are the most serious findings because they concern mathematical equivalence and an invalid completion status.

F1: The blueprint at line 1386 assumes that both families consist of conditionally linear functions. By contrast, `typedCLDistribution` at `MIPStarRE/QPBT/Games/TypedCondLinear.lean:34` accepts arbitrary map families and has neither a level nor an `IsTypedCondLinearFamily` hypothesis. The preceding comment acknowledges this strict generalization, but omitting `\leanok` does not make a mathematically mismatched `\lean` link valid. Either link a source-facing definition with the genuine source hypotheses or record the unconstrained construction separately as formalization support.

F2: `ldQuestionDistribution_eq_typedCL` at `MIPStarRE/QPBT/Test/LowDegreeGameTheorems.lean:327` proves only equality with that unconstrained construction. Its signature contains no typed-family assertion, and no linked result establishes a common conditional-linearity level for the point, axis-line, and diagonal-line maps. Consequently, the statement and proof block at blueprint lines 85–94 cannot carry `\leanok` as the source classification theorem.

F3: `pauliQuestionDistribution_eq_typedCL` at `MIPStarRE/QPBT/Test/Completeness.lean:28` likewise states only the sampling equality. Leaving the node unmarked is appropriate for an open result, but the `\lean` target must still be statement-equivalent. Either state this as a narrower formalization-only sampling-equality node or provide a source-faithful obligation that also records conditional linearity of the family.

F4: Blueprint lines 64, 69, 76, and 81 list `lem:cl-concat` and describe both proofs as applications of concatenation. The Lean proofs at `LowDegreeGame.lean:393` and `:434` instead construct `ldALineTerm` and `ldDLineTerm` directly and verify their support and evaluation. Remove the concatenation edges and describe the explicit two- and three-level register decompositions used by those proofs.

F5: `Distribution.ext_of_support_of_weight`, `Distribution.map_map`, `uniformDistribution_weight_apply`, `uniformDistribution_map_of_card_fiber`, `uniformDistribution_map_equiv`, `uniformDistribution_map_fst`, `uniformDistribution_map_snd`, and `bind_uniformDistribution_map` are new public formalization-only results at lines 203–350. None has a blueprint node. Record their mathematical statements as subordinate support nodes and connect the source-facing proofs that invoke them.

F6: The proofs linked from `lem:alnf` and `lem:dlnf` invoke the new distribution calculus and `lineRepMap_apply_self`; the diagonal proof also invokes the stored prefix-zero property of `LineDesc`. Their current `\uses` lists only the representative definition and incidence lemma. Add the corresponding support dependencies, including `def:line-point-dist`; the typed-distribution proof at line 93 similarly needs the bind/map support dependencies once recorded.

F7: The current chapter locations are `def:cl-func` at 1168–1178, `lem:cl-concat` at 1218–1231, direct-sum results at 1238–1266, support nodes at 1271–1327, graph results at 1348–1372, and the typed distribution at 1383–1387. The changed citations in `CondLinear.lean`, `CondLinearTheorems.lean`, `DirectSumSupport.lean`, `DistributionAux.lean`, and `LowDegreeGameTheorems.lean` still point roughly 115 lines earlier and should use those current ranges.

F8: Replace “needed by later formalization stages” with “used in the subsequent direct-sum results.” In `DirectSumSupport.lean:9`, replace “supporting layer” and “the module that owns the behaviour” with a mathematical description such as “These lemmas establish the equal-level direct-sum case used in the direct-sum and product-distribution results.”

F9: Replace “empty first stage” and “empty stages” with “zero level” or “zero levels,” and replace “supported linear stage” at line 105 with “first supported linear contribution.”

The earlier placement changes, line-representative support nodes, direct `dlnf` dependencies, and obsolete Lines citations are addressed. The typed-distribution repair remains only partial, while the new level nodes expose the dependency mismatch in F4.

Summary: 3 equivalence/status mismatches, 3 dependency/traceability mismatches, and 3 prose/documentation issues.

VERDICT: CHANGES_REQUESTED