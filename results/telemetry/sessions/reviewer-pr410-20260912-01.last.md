## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Points/Consistency.lean:99` — Remove the redundant square-summability lemmas and reuse the existing POVM bound.
- [ ] F2 (changes) `docs/paper-gaps/qpbt_combined-points-field-valued.tex:226` — Correct the assertion that the current blueprint presents the binary-refinement proof.
## Review
**F1:** `sum_effect_conjTranspose_mul_self_le_one_of_projective` reproves the conclusion of `measurement_sum_adjoint_mul_le_one` in `MIPStarRE/QPBT/Games/DistanceTheorems.lean:336`, adding an unnecessary projectivity hypothesis. The new specialized proof in `Points/Sandwich.lean:172` likewise follows by applying that existing theorem to `S.placedMeasurement`. The wrapper in `Points/Closeness.lean:87` introduces another redundant declaration. Repository searches found no consumers of these additions beyond their internal delegation; existing proofs already use the general POVM theorem. Remove these three additions. This requires changes under the duplication rule.
**F2:** The note says the blueprint presents the source’s binary-refinement proof. However, `blueprint/src/chapter/ch15_qpbt_combining.tex:851`, under `lem:qld-4-10`, explicitly states: “The formal proof works with field-valued outcomes directly.” Lines 852–869 describe the sandwich, orthonormalization, and `K ε^(1/8)` estimate. The binary-refinement construction belongs to `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:731`, with quantum linearity applied at line 832. Update the note to distinguish the original paper’s proof from the replacement already presented in both blueprint and Lean. This is an inaccurate source comparison in the changed paper-gap note.
Prior findings were reassessed individually:
- Import: resolved by `Points.lean:2`; the marginal module reaches the default import graph.
- Permutation identification: resolved by `Points/Placement.lean:42`, which distinguishes `eprCrossSwap` from `U_σ U_θ`.
- Missing replacement-proof documentation: addressed by the new note’s calculations; its remaining comparison error is F2 above.
- Operator-product duplication: resolved; `Points/Sandwich.lean:164` uses `DistanceCalculus.applyOperatorToState_mul`.
- Parseval documentation: resolved; `Points/Commutation.lean:58` correctly specifies orthogonal columns.
- Square-summability duplication: delegation at `Points/Closeness.lean:92` addresses the local repeated proof, but the redundant declarations remain as F1 above.
The mathematical audit found no new statement-fidelity blocker. `exists_combinedPointsWitness` preserves the universal error function, existing expanded spaces, both product orders, and directed opposite comparisons of `lem:qld-4-10`. The marginal lemmas derive explicit `4 * δQ` bounds from the previously constructed joint measurements, matching the calculations at paper lines 902–930 (`eq:qld-qxz-close-to-point` and its Z counterpart). Their witness argument supplies established measurement data; it introduces no missing construction assumption.
Validation: single-file Lean checks passed for `WitnessMarginals`, `Points`, `Consistency`, `Sandwich`, and `Closeness`. The reviewed point modules contain no `sorry`, `admit`, axioms, or prohibited proof bypasses. Full build and TeX compilation were not rerun; the supplied context reports successful CI for the reviewed SHA. The worktree remains unchanged.
VERDICT: CHANGES_REQUESTED
