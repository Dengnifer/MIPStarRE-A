## Findings
- [ ] F1 (blocker) `local/bin/pr_merge.py:27` — Removing gate 6 leaves the pre-lock auto-fix count race able to create commits beyond `MIPSTARRE_FIX_CAP`
- [ ] F2 (blocker) `local/bin/pr_merge.py:194` — `--adjudicated` accepts an arbitrary prefix and head substring without enforcing the §12 disposition contract
- [ ] F3 (blocker) `local/bin/pr_merge.py:184` — `--adjudicated` can waive a missing or failed `local-review/summary`, not only an adverse verdict
- [ ] F4 (changes) `local/bin/autofix.sh:774` — Reaching the cap in the last phase can bypass `cap_reached`, leaving the bot head without its required forced review
- [ ] F5 (changes) `local/bin/autofix.sh:562` — Cap handoff is not head-safe or terminal because the lock is released before push/review and reruns repeat the ritual without telemetry
- [ ] F6 (changes) `local/personas/main.md:104` — “Only owner-gated control” conflicts with the standing two-episode approval and checkpoint rules
- [ ] F7 (changes) `local/personas/main.md:133` — The advertised no-briefing fallback is unreachable while `main-session.sh` still requires absent `HANDOFF.md`
- [ ] F8 (changes) `results/telemetry/stages.jsonl:19` — Authoritative stage state still says PR #5 is blocked on cap authorization after this amendment records retirement
- [ ] F9 (changes) `local/personas/main.md:110` — `needs-owner` is documented but not provisioned or verified, so the mandated escalation can fail
- [ ] F10 (changes) `.githooks/pre-commit:25` — The claimed episode/PR 400-line budget is enforced only per commit and excludes workflow paths
- [ ] F11 (changes) `local/protocols/EVOLUTION.md:334` — The six-lane/three-refuter owner-audit claim is not reconstructible from committed telemetry
- [ ] F12 (changes) `.github/prompts/auto-fix-ci-prompt.md:42` — Trusted auto-fix prompts still authorize push/MCP and `[claude-*]` commits, bypassing the retained prefix guard
- [ ] F13 (changes) `local/personas/reviewer.md:71` — The reviewer persona still presents bot prefixes as the convention for all fixes, contradicting plain worker-repair subjects
- [ ] F14 (advisory) `scripts/tests/test_github_workflow.py:413` — Regression coverage checks only merge-count reporting, not auto-fix cap races or terminal-review lifecycle

## Review

Changes are required. This head changes workflow scripts, protocols, personas, and tests only; no Lean declaration, blueprint theorem, paper-gap note, `sorry`, or axiom changed, so proof-integrity and paper-statement checks are not implicated.

**F1.** `autofix.sh` computes `LOCAL_TIP`, the merge base, and `FIX_ITERATIONS` at lines 402–408 before acquiring the branch lock at lines 541–550. Two runs can both observe four fixes; the first creates the fifth and fails to push, while the queued second run sees the unchanged GitHub head and uses the stale count to create a sixth. The amendment explicitly leaves issue #9 open, so removing the independent merge check is premature. Recompute the count under the lock and revalidate the head, or retain a merge-time backstop.

**F2.** `check_review` only checks `startswith("ADJUDICATION")` and whether `head=<sha>` occurs as a substring (`pr_merge.py:189-200`). A comment containing only those tokens passes, although `local/protocols/review.md:382-398` requires a post-cap round, every finding ticked, and a one-line `fixed`, `deferred to issue`, or `moot` disposition. Parse and validate that structure, including the exact head and tracked issue references.

**F3.** The clean path requires both a clean verdict and `local-review/summary == success` (`pr_merge.py:175-180`), but the adjudicated path at lines 184–203 skips the summary requirement entirely. A marker review plus an adjudication comment can therefore merge with a missing or failed review status. `issues-prs.md:93-104` says `--adjudicated` waives only the adverse verdict. Preserve the status and marker checks and waive only the documented condition.

**F4.** The cap is checked only on entry to `run_phase` (`autofix.sh:695-697`), while the counter reaches the cap after the commit (`:773-774`). If the final selected phase changes count 4 to 5 and CI succeeds, the post-fix path exits; a later invocation returns at `:536-538` because there is nothing left to fix. `review.sh:429-434` then skips the bot-prefixed head, so no forced review is produced. Trigger the cap ritual immediately when the increment reaches the limit and test this path.

**F5.** `cap_reached` uses a PR-only marker and rewrites the same comment (`autofix.sh:562-578`), then releases the fix lock before pushing and reviewing (`:581-604`). Another invocation can move the branch during that handoff, and push/review failures are warnings rather than verified terminal state. The routine also does not append the telemetry event required by `local/protocols/autofix.md:180-184`. Use a head-bound terminal record, a serialized handoff with an exact-head recheck, and durable telemetry.

**F6.** `main.md:99-100` still requires owner approval for two consecutive workflow-only episodes, while `:104-107` says the only owner-gated control is `MIPSTARRE_INFRA_OVERRIDE` and makes every other remedy operator-controlled. The same edit removes the prior pause-and-report checkpoint. Clarify whether episode approval and checkpoints are exceptions, document precedence, and identify the owner decision rather than leaving “ONLY” ambiguous.

**F7.** `main.md:130-135` says an absent briefing should fall back to `goal.md`, events, and stages, but `local/bin/main-session.sh:25-26` exits before starting whenever `HANDOFF.md` is absent. There is no `HANDOFF.md` in this checkout. Wire the launcher to the fallback (including resume), or remove the advertised behavior.

**F8.** `main.md` now elevates `results/telemetry/stages.jsonl` to authoritative state, yet its latest entry (`:19`) says PR #5 is blocked because no cap authorization exists. `events.md:177-189` and `EVOLUTION.md:326-339` record the opposite disposition. Append a current stage milestone or define a deterministic precedence/reduction rule so a fresh main session cannot remain blocked on retired work.

**F9.** The new taxonomy row does not create the GitHub label. `gh_common.issue_create` rejects labels absent from the live repository (`gh_common.py:313-328`), and this PR provides no provisioning or verification evidence. Provision `needs-owner` through the documented live-label process or specify an existing-label fallback before making it the required escalation path.

**F10.** The hook’s `numstat` pathspec covers only `local/`, `.githooks/`, and one test file (`.githooks/pre-commit:25-26`), and it measures only the staged commit. `main.md:88-92` and `EVOLUTION.md:274-279` define a per-episode limit, while review only truncates prompt input; it does not enforce a 400-line PR total. Either add cumulative episode accounting or state and test a genuinely per-commit policy. This PR itself is below the limit, but the advertised guard is not.

**F11.** `EVOLUTION.md:334-335` claims an owner audit with “six read-only lanes” and “three adversarial refuters,” but the committed `events.md`, `sessions.jsonl`, and `stages.jsonl` contain no lane/refuter records. That conflicts with the quantitative, reconstructible research-data requirement in `local/protocols/meta.md:79-88`. Add session/artifact citations or remove the unsupported counts.

**F12.** `autofix.sh:713-715` executes the trusted prompt files from `main`, whose task prompts still say “Commit and push,” request GitHub MCP calls, and prescribe `[claude-auto-fix]`/`[claude-review-fix]` subjects (`.github/prompts/auto-fix-*:42-57`). The wrapper appends a contrary instruction later, but there is no mechanical enforcement; a model-followed Claude-prefixed commit is skipped by `review.sh` and omitted from the retained counter. Update the trusted prompt/system pair or enforce the prohibition in code.

**F13.** The changed personas distinguish automated prefixes from plain worker repairs, but `local/personas/reviewer.md:69-74` still describes the two bot prefixes as the required fix convention. A manually dispatched reviewer/repair session can therefore produce the very subject the new policy says should be reserved for `autofix.sh`. Align the remaining reviewer personas or explicitly document the role boundary.

**F14.** The new test correctly pins `MIPSTARRE_FIX_CAP=5` and verifies that six prefixed commits are reported rather than rejected. It does not invoke `autofix.sh`, exercise the pre-lock race, or cover the post-phase cap/forced-review path. This is advisory test debt, not a claim that the focused suite fails.

The focused workflow suite passes (`9` tests), and `git diff --check` plus the shell syntax checks pass. The supplied exact-head local-CI artifact reports success; full discovery was not independently clean in this read-only environment because of the existing `/tmp` write failure in `scripts/tests/test_generate_badges.py:309`. The incident telemetry and ambient-cap regression were repaired, but the unresolved findings above remain.

VERDICT: CHANGES_REQUESTED