Addressed both unresolved findings:

- F1: Corrected the Parseval docstring to specify orthogonal columns.
- F2: Replaced the duplicated proof with `sum_effect_conjTranspose_mul_self_le_one_of_projective` applied to the placed measurement.

Both files type-check without warnings. Full `lake build` passed (9,175 jobs); existing warnings remain in unchanged files. No theorem statements changed or proof holes introduced.

Changes remain uncommitted for `autofix.sh`.