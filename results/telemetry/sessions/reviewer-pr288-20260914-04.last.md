## Findings

- [ ] F1 (changes) `audits/2026-09-06_block-specialization.md:1` — The new audit omits the mandatory YAML metadata and standard audit sections.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/BlockSpecialization.lean:108` — The two public specialization results are not explicitly documented as formalization-only auxiliaries.
- [ ] F3 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:2092` — The extracted proof step is not identified as a subordinate formalization-support node.

## Review

F1 must be fixed before merge. `audits/2026-04-29_audit-document-format.md` requires every new audit to begin with YAML front matter, including `title`, `date`, and `purpose`, and recommends issue/PR metadata. It also prescribes `Scope`, `Source of Truth`, `Findings`, `Required Action`, `Validation`, and `Review Use` sections. This new audit has neither the metadata nor that structure.

F2 and F3 concern source provenance. The cited paper passage, `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:1349-1355`, says “To bound the second term” and performs the specialization argument inside the proof of `lem:qld-4-7`; it does not state a named lemma. Under `docs/CONTRIBUTING.md` §5 and `docs/blueprint_style_guide.md`, public declarations extracted from such a proof step must be explicitly described as formalization-only auxiliaries, and any blueprint node must visibly identify itself as subordinate formalization support.

The first three Lean docstrings do this correctly, but `exists_block_specialization_exceptional_coefficient` and `block_specialization_weighted_avg_le` do not. Likewise, the blueprint’s non-rendered `%` comment is insufficient: the node title or first rendered sentence should say that it is formalization support and not a named paper statement.

Mathematically, the Lean statements match the extracted argument. The dependence hypothesis faithfully expresses that \(p\) is not a polynomial in the parameter block alone; one nonzero coefficient controls a common exceptional set; and the weighted conclusion is exactly \(2D M/|K|\), with no answer-alphabet factor or hidden degree restriction.

`lake env lean MIPStarRE/QPBT/Combining/BlockSpecialization.lean` passed. The proof-integrity scan was empty, all five axiom closures contain only `propext`, `Classical.choice`, and `Quot.sound`, and blueprint synchronization passed. I did not rerun the full build because this review was dispatched after exact-head local CI succeeded.

VERDICT: CHANGES_REQUESTED