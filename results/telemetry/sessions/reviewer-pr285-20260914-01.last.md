## Findings
- [ ] F1 (changes) `local/bin/useful_queue.py:43` — The sandbox path is unreachable after #505 retired every queue entrypoint, and all added regressions are skipped.

## Review
As of September 14, 2026, `Supervisor.__init__` unconditionally raises at line 211 and `main()` returns 4 at line 450. The current protocol and operator guide direct work to `dispatch.sh`; `useful_queue.py` is retained only as historical evidence.

Consequently, none of the new schema, fingerprint, or execution behavior can run. Moreover, every added test is inside `QueueTests`, which has a class-level `@unittest.skip` at `scripts/tests/test_useful_queue.py:30`; green CI therefore validates only that the queue remains retired.

This stale feature should be dropped, or any still-current requirement should be implemented in the active dispatch/reservation path with non-skipped tests and a current protocol amendment. Syntax and diff checks passed; the working tree remained unchanged.

VERDICT: CHANGES_REQUESTED