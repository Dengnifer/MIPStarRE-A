## Findings

- [ ] F3 (changes) `MIPStarRE/QPBT/Combining/Points/Sandwich.lean:44` — Remove the duplicate `applyOperatorToState_mul'` and reuse `DistanceCalculus.applyOperatorToState_mul`.

## Review

F3 requires changes under the repository’s duplication rules. `applyOperatorToState_mul'` repeats the public theorem in `MIPStarRE/QPBT/Games/DistanceTheorems/Support.lean:64` with identical hypotheses and conclusion. The existing theorem is already available through `Commutation`; I verified that it directly proves the new signature. Delete the duplicate and update its callers, keeping one shared operator-application API.

Both prior findings are resolved: `ConsistencyPositivity` now uses the shared placement calculus, and `SubLinePrefix` correctly states that preceding blocks vanish while later blocks remain untruncated.

The mathematical comparison found no additional blocker:

- `exists_combinedPointsWitness` preserves `lem:qld-4-10` in `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:689–709`: original local spaces, both ordered-product conclusions, all directed opposite placements, and a universal polynomial error.
- The restriction estimates retain the source’s `2m` and `4m²` factors at paper lines 1049–1061.
- The weighted collision bound explicitly excludes zero directions. `exists_subLineWitness` remains a documented auxiliary for directly indexed lines; transport to the source distribution remains outstanding.

`Points.lean` and `Lines.lean` type-check; the latter reports only its two existing, documented `sorry`s. Axiom checks for the joint-point construction, sub-line construction, restricted consistency bound, and weighted collision bound show only `propext`, `Classical.choice`, and `Quot.sound`. The checkout matches the authoritative patch and remains unchanged.

I did not rerun the full build. The separate `lean-conventions` skill was unavailable; review used the supplied and checked-in conventions.

VERDICT: CHANGES_REQUESTED