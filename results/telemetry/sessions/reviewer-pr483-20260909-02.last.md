## Findings

- [ ] F3 (changes) `MIPStarRE/QPBT/Combining/Lines/DiagonalResampling.lean:52` — Extract the shared `hresample` lemma instead of duplicating the axis resampling proof.

## Review

F3: The local `hresample` statement and proof duplicate `AxisLineResampling.lean:45`, differing only in variable names. Both quantify an arbitrary direction and arbitrary real-valued function; neither uses its line kind. Extract this lemma into a common imported module and invoke it from both proofs. This requires changes under the duplication criterion.

Both prior findings are resolved: `SubLinePrefix.lean:11` correctly describes which blocks vanish, and `RestrictedAverage.lean` reuses `SandwichProduct.avgOver_distribution_prod`.

The completed mixture and restriction results preserve their public statements and the factors `2m` and `4m²` from `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:1049–1061`, corresponding to blueprint `lem:restricted-line-mixture-bounds`. No new load-bearing hypotheses were introduced. `exists_subLineWitness` remains explicitly a directly indexed auxiliary construction.

Validation: `FiberCollision.lean` and `Lines.lean` type-check. Axiom checks for the fiber collision theorem and five completed results report only `propext`, `Classical.choice`, and `Quot.sound`. The full on-disk patch was checked despite attachment truncation; no full build was rerun.

Residual limitations remain explicit: collision control requires nonzero directions, product-law/pasting transport remains separate, and the two existing joint-line construction proofs remain unfinished.

VERDICT: CHANGES_REQUESTED