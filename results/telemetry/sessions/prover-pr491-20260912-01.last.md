Addressed F1 by reusing the shared `ProjectiveSetting` placement API in both reviewed files, removing 200 lines net.
Both single-file Lean checks, the source-statement header audit, and `git diff --check` passed. Public hypotheses and conclusions remain unchanged; no new proof holes were introduced. The five existing extraction obligations remain.
The full `lake build` could not start because shared-lock waits timed out. Changes are left uncommitted for the lifecycle script; full-build validation remains outstanding.
