## Findings
- [ ] F1 (changes) `local/bin/merge_loss_guard.py:309` — A directory-rename conflict resolution preserving incoming content at Git’s suggested destination is still incorrectly rejected as a deletion.
## Review
Reviewed head `5ac4d72cf3153d437d18b4d568829833e3a2f2bd` against the authoritative patch and local workflow contracts. The working tree remains unchanged.
**F1 remains reproducible in both audit modes.** In a disposable repository:
1. The base contains `old/a`; the branch renames `old/` to `new/`.
2. Incoming adds `old/added`.
3. Git reports a file-location conflict, places the incoming blob at `new/added`, and records only `new/added` in the conflict metadata.
4. Staging that suggested destination preserves the incoming content, but both the pending guard and `--commit HEAD` exit 1 with `DELETED old/added`.
The exact-path conflict exemption does not associate the incoming source path with the relocated destination. Consequently, the deletion check rejects a legitimate resolution despite the conflict-resolution contract in `local/protocols/issues-prs.md:58`. Account for Git’s relocation when classifying missing incoming paths, and add regression coverage for pending, committed, and hook-enforced checks without allowing genuinely dropped additions.
**Prior-ledger triage:**
- Multiple-base silent loss: resolved by reconstructed-merge classification; the criss-cross regressions pass.
- Deleting a recorded conflict: resolved for same-path conflicts; its regression passes.
- Both incident-history rewrite findings: resolved; the authoritative patch adds a dated follow-up without replacing existing event text.
- Installer bypass wording: resolved at `scripts/install_git_hooks.sh:96`.
- Directory-rename resolution: unresolved, retained as F1.
**Validation:** All 13 tests in `scripts.tests.test_merge_loss_guard` pass. The additional directory-rename reproduction fails as described. Some combined read outputs were truncated; the relevant finding was checked directly in the checkout and reproduced independently. No full CI rerun or publication was performed.
VERDICT: CHANGES_REQUESTED
