## Findings

- [ ] F2 (changes) `MIPStarRE/QPBT/Extraction/Consistency.lean:101` — Replace the duplicated placement-measurement construction and projectivity proofs with the existing `ProjectiveSetting` API.

## Review

F2 requires changes before approval. The new private `placedMeasurement`, `placedMeasurement_effect`, and `placedMeasurement_isProjective` duplicate the corresponding declarations in `MIPStarRE/QPBT/Combining/Points/Placement.lean:256`. The accompanying `placementEquiv` machinery creates a second implementation of the same four placements, and `place_finset_sum` duplicates `ProjectiveSetting.place_finsetSum`. These existing APIs are already available through this file’s imports. Replace the private constructions with them and remove the redundant support proofs. I verified that the existing measurement and projectivity APIs type-check with the extraction file’s imports.

Prior F1 is resolved: `point_point_rejection_le` now uses the shared placement API.

The mathematical checks found no new proof holes or added proof-debt hypotheses. The completed self-consistency estimate preserves the defect under `some` and explicitly includes the conditioning loss. The combining degree bounds, extraction identity estimate, and swap unitarity proofs preserve their public statements. The directly indexed sub-line construction remains explicitly distinguished from the source-carrier theorem.

Validation: `PointSelfConsistencyCompleted.lean`, `Lines.lean`, `PassingValue.lean`, and `Extraction/Consistency.lean` type-check. Only documented, pre-existing TODOs produced `sorry` warnings. Axiom checks for nine key results reported only `propext`, `Classical.choice`, and `Quot.sound`. Shell and Python syntax checks passed.

The truncated attachment was supplemented with the authoritative on-disk patch and checkout. Full builds and workflow integration tests were not rerun; their coverage relies on the supplied green CI status. The referenced `lean-conventions` skill was unavailable in the exposed skill directories, so the checked-in review and integrity rules were used.

VERDICT: CHANGES_REQUESTED