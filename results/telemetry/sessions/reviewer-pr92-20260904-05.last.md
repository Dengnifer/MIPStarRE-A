## Findings
- [ ] F1 (changes) `local/protocols/EVOLUTION.md:468` — The final tree lacks the documented `MERGE_HEAD` budget logic, so inherited main changes remain budgeted and the new tests fail.
- [ ] F2 (changes) `scripts/tests/test_dispatch.py:140` — No qualifying-main rejection case covers oversized PR-owned or merge-time edits, so a zero-budget exemption could pass both tests.
## Review
The authoritative base-to-head diff does not modify `.githooks/pre-commit`. Commit `a45abd1` removed the intermediate merge-aware implementation, and merge commit `6059734` did not restore it. The hook still always compares the staged index with `HEAD`, contradicting the new evolution entry.
`python3 -m unittest scripts.tests.test_dispatch` ran five tests and failed two: the inherited-main case returned 1 after counting 401 lines, while the side-branch case lacked the expected merge classification. This directly contradicts the supplied green CI result.
The earlier ledger’s merge-classification findings were superseded when that implementation disappeared; the latest negative-test coverage finding remains unresolved as F2. `git diff --check` passed. No full Lean build was warranted for this workflow-only patch while its focused tests fail.
VERDICT: CHANGES_REQUESTED
