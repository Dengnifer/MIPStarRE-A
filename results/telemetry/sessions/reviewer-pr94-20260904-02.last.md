## Findings

- [ ] F1 (blocker) `MIPStarRE/QPBT/Algebra/Pauli.lean:82` — `forall_apply_eq_iff` exactly re-declares Lean core’s `funext_iff.symm`.
- [ ] F2 (changes) `MIPStarRE/QPBT/Algebra/Pauli.lean:344` — The new scope marker cites a paper-gap note that does not document the Fourier expansion or inversion specialization.

## Review

F1 must be fixed before merge. `#check funext_iff.symm` returns exactly `(∀ x, f x = g x) ↔ f = g`, matching `forall_apply_eq_iff`. The proof-integrity rules classify private duplicates of standard core/Mathlib API as blockers. Delete the helper and use `funext_iff.symm` or `← funext_iff` at lines 368 and 411–412.

F2 requires changes. The paper states the unrestricted prime-characteristic formulas at `references/qpbt-paper/04_preliminaries.tex:1157-1160` (`eq:pauli-obs-proj` and `eq:pauli-inversion-0`), with positive exponent in the expansion and negative exponent in the inverse. The blueprint repeats them at `blueprint/src/chapter/ch11_qpbt_algebra.tex:674-687`.

The Lean declarations are mathematically valid characteristic-two specializations because negation is trivial in `ZMod 2`. However, `docs/paper-gaps/qpbt_characteristic-two-pauli-scope.tex:30-73` discusses product, power, unitarity, commutation, and cancellation only; it never states or compares these Fourier formulas. Extend that note, or cite a note that does. The declaration prose should also say “binary specialization of” rather than “This is,” and its stale blueprint ranges `638-652` should be corrected to `674-688`.

Statement integrity otherwise checks out: no bridge or proof-obligation hypotheses were added; `Fintype`, `DecidableEq`, and the binary-field algebra instance are legitimate domain data. Allowing an empty finite index type is a valid strengthening of the blueprint’s `n ≥ 1` case. `eprState_norm` faithfully proves normalization from `def:EPR`.

`lake env lean MIPStarRE/QPBT/Algebra/Pauli.lean` and `git diff --check` pass. All three completed declarations have only `propext`, `Classical.choice`, and `Quot.sound` in their axiom closure, with no `sorryAx`; the changed file contains no remaining proof holes or prohibited tactics.

VERDICT: CHANGES_REQUESTED