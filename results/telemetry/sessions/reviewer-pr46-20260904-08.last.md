## Findings

- [ ] F1 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:35` — The marginal definition also asserts projectivity; move that derived fact and its theorem link into a lemma node.
- [ ] F2 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:49` — The dot-product-projector definition also asserts projectivity and completeness; these require a separate lemma node.
- [ ] F3 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:62` — The pulled-apart-effect definition also asserts that the effects form a projective measurement; split the assertion into a lemma node.
- [ ] F4 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:79` — Chapter 16 uses one-based basis indices while the linked Lean declarations and the blueprint convention are zero-based.
- [ ] F5 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:15` — The new full-field decoder, encoding predicate, and four decoder identities stated here lack blueprint traceability.
- [ ] F6 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:121` — The two error-scale definitions are not represented by definition nodes; `deltaConstructPaulis` is linked inside a lemma and `deltaExtract` is unlinked.
- [ ] F7 (changes) `MIPStarRE/QPBT/Extraction/Consistency.lean:107` — `nonencodingMarginalMass`, the quantity displayed and bounded in the blueprint, has no definition entry or Lean link.
- [ ] F8 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:197` — The non-encoding support lemma has no `\uses{...}` declaration.
- [ ] F9 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:200` — `\notready` is stale because `nonencodingMarginalMass_le` now has a matching, settled signature.
- [ ] F10 (changes) `MIPStarRE/QPBT/Extraction/Unitary.lean:115` — `deltaExtract_le_deltaQld` claims the robustness remark as its counterpart, but that remark has no traceability link.
- [ ] F11 (changes) `MIPStarRE/QPBT/Algebra/Decoding.lean:73` — Three decoder docstrings cite incorrect or stale blueprint and paper line ranges.
- [ ] F12 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:15` — “raw” is repeatedly used as mathematical terminology for the pre-enlargement error.
- [ ] F13 (changes) `MIPStarRE/QPBT/Algebra/Decoding.lean:6` — Decoder documentation repeatedly describes implementation layers and Lean representations instead of the mathematics.
- [ ] F14 (changes) `MIPStarRE/QPBT/Algebra/Decoding.lean:83` — “Package” and “subtype wrapper” are explicitly discouraged process terminology.
- [ ] F15 (changes) `MIPStarRE/QPBT/Extraction/Defs.lean:6` — Register documentation repeatedly uses private implementation vocabulary such as “bookkeeping,” “landed,” “Lean-only support,” and “API.”
- [ ] F16 (changes) `MIPStarRE/QPBT/Extraction/Consistency.lean:105` — “proposition-valued proof obligation” is type-system jargon rather than mathematical prose.
- [ ] F17 (changes) `MIPStarRE/QPBT/Extraction/Observables.lean:177` — The mathematical docstring uses programming syntax `j != j'`.

## Review

F1-F3 are classification errors rather than formula errors. The linked definitions and theorem signatures match the displayed mathematics, but projectivity and completeness are derived facts. Each definition should retain its defining formula, with the relevant `*_isProj` and `sum_*_eq_one` links moved to a separate lemma. Statement-level `\leanok` is appropriate there; proof blocks should remain untagged while the proofs are open.

F4 occurs again at lines 95, 100, 150, 268, and 317. Chapter 11 lines 255-256 establishes indices \(0,\ldots,t-1\), matching `Fin P.model.basisDim`, and explains their relation to the paper’s one-based labels. Chapter 16 should consistently use that zero-based convention or explicitly apply the relabeling.

For F5, add traceability for `decodeFq`, `IsEncoding`, `decodeFq_add`, `decodeFq_smul`, `decodeFq_lowDegreeEncoding`, and `decodeFq_dotProduct_indicatorVec`. Give the definitions their own entries and keep open internal identities in separate non-`\leanok` traceability entries rather than adding them to an existing source-labelled `\leanok` block.

For F6, the formula defining \(\delta_P\) should be a labelled definition carrying the `deltaConstructPaulis` link, instead of placing that definition link inside `lem:qld-construct-the-paulis`. Likewise, record `deltaExtract` separately for the explicit \(C(\delta_P^{1/4}+md/q)\) choice used at line 300.

F7-F9 concern the same support estimate but require distinct repairs. Introduce the mass itself as a definition linked to `nonencodingMarginalMass`; add dependencies including the marginal measurement, encoding predicate/decoder, expanded state, and construction-error definition; then remove `\notready`. Retain `\lean{MIPStarRE.QPBT.nonencodingMarginalMass_le}` without `\leanok`, since it is an open internal obligation.

For F10, add `\lean{MIPStarRE.QPBT.deltaExtract_le_deltaQld}` to the robustness remark or a separate implementation note, without `\leanok`.

For F11, line 73 should cite blueprint lines 404-424. Lines 105-106 should identify the decoder-linearity use near paper lines 1548-1550, not the observable regrouping at 1442-1450. Lines 129-130 should cite the decoder identity at `04_preliminaries.tex:917-922`; lines 1805-1822 are an application, not its source.

For F12, replace “raw global-witness error” and its variants at blueprint lines 15 and 121, `Defs.lean:239`, and `Consistency.lean:42,152` with “global polynomial-pair consistency error before enlargement.” At `Consistency.lean:152`, use “The displayed estimate retains `deltaG` separately from the additional terms.”

For F13-F14, remove “raw polynomial specialization,” “`Poly`-facing,” “Lean-only helper,” “function-level implementation,” “algebra layer,” “subtype projection,” and “`Poly` view,” including the parallel wording in `LowDegreeCodeTheorems.lean:7-9,43-48`. State the decoder formulas directly; replace line 143 with “Assume that \(g\) is an encoding.” Describe `encodingPoly` as regarding \(g_h\) as a bounded polynomial representative.

For F15, use direct descriptions such as “defines the register equivalences and operator placements,” “reindex \(AA'BB'\mid A''B''\) as \(AA'A''\mid BB'B''\),” and “place \(O\) on the selected player’s register and the identity elsewhere.” Define conjugation directly as \(N\mapsto VNV^\dagger\).

For F16, use “The asserted inequality remains to be proved from point consistency and the Schwartz–Zippel bound.” For F17, replace `j != j'` with \(j\ne j'\).

The earlier unitarity mismatch is resolved: `ExtractionWitness` now records both unitary equations. Apart from the indexing and node-classification issues above, the linked signatures match the displayed quantifiers, placements, sums, and error bounds, with no added bridge or residual hypotheses. This review records 10 category-A issues and 7 prose/reference issues; the category-A issues are the more serious blockers.

VERDICT: CHANGES_REQUESTED