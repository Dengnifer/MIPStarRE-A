## Findings

- none

## Review

The prior-round findings are resolved. The duplicate bibliography field was removed, the correlation hypothesis now includes the real part, and the dependency and documentation corrections are present.

Statement-integrity audit for `exists_exactly_linear_observables`:

- Paper assumptions: binary observables and a density operator satisfying the averaged correlation bound in `references/nv-paper/fullpaper.tex:1074-1080`.
- Lean assumptions: the same data in finite dimensions, with explicit positivity, trace-one, decidable equality, and nonnegative-error boundary conditions.
- Paper conclusion: an ancillary extension and exactly linear observables with the printed squared operator-distance bound `δ`.
- Lean conclusion: the same construction with squared operator-distance bound `2δ`, plus the equivalent binary-measurement-distance bound `δ`.
- Verdict: faithful finite-dimensional encoding with a documented numerical correction, not proof-debt statement drift. The factor-of-two discrepancy is derived and refuted by the four-point counterexample in `docs/paper-gaps/qpbt_linearity-distance-normalization.tex`; the argument remains valid under arbitrary ancillary extensions.

The Fourier, BLR, Naimark, and stability proofs form a coherent chain. In particular, the residual Naimark outcome is absorbed into the zero Fourier projector without changing its compression, and the resulting distance/defect identity yields exactly the corrected constants. No bridge hypotheses, trivial witnesses, proof holes, axioms, integrity bypasses, or unmarked TODOs were found.

`lake env lean MIPStarRE/QPBT/Combining/Linearity.lean` succeeds, and the checkout remained clean.

VERDICT: APPROVED