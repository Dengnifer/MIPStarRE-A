## Findings

- [ ] F1 (blocker) `MIPStarRE/QPBT/Combining/Apply.lean:17` — The corrected module header is contradicted by two directly indexed declarations still advertised as source-facing `lem:qld-4-13`.
- [ ] F2 (blocker) `MIPStarRE/QPBT/Extraction/Unitary.lean:78` — `exists_extractionWitness` remains unconditionally named despite its non-paper `GlobalPairWitness` input, and the cited gap note is now stale.

## Review

F1: The new header correctly classifies the combined-line declarations as directly indexed auxiliaries, but `exists_extendedLinesWitness_ofPointsWitness` and `exists_extendedLinesWitness` are still described at lines 30–62 as the “source-facing statement” and “source-facing extended-line construction.” Paper `lem:qld-4-13` in `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:1020-1033` instead quantifies lines in \(\mathbb F_q^{2m+2}\), averages over that source line-point distribution, and sums answers over \(a\in\mathbb F_q\). `ExtendedLinesWitness` uses `DirectLineDesc`, `directLinePointDist`, and `Option`-completed answers (`MIPStarRE/QPBT/Combining/Witnesses.lean:278-318`). The blueprint itself records these three discrepancies at `ch15_qpbt_combining.tex:1786-1802`. The declaration-level docstrings must consistently identify both declarations as directly indexed auxiliaries and must not call them source-facing before the transport obligation is proved.

F2: Paper `lem:qld-unitary`, `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:1669-1685`, constructs the unitaries and auxiliary state using the preceding global-measurement result; it does not assume a `GlobalPairWitness` in its public statement. The Lean theorem at lines 93–104 does assume `w : GlobalPairWitness S deltaG`. The new `**Unfaithful:**` marker correctly identifies that proof debt, but the mandatory quarantine naming rule still requires a name such as `exists_extractionWitness_ofGlobalPairWitness`. The revised blueprint also removes the prior explicit requirement for that rename and for a source-facing composition theorem. Rename the helper, update its support-node link, and retain a named source-facing target that obtains `w` through `exists_globalPairWitness`, with a tracked `sorry` if necessary.

The same change leaves `docs/paper-gaps/qpbt_extraction-transfer.tex:153-157` factually stale: it says the docstring “still describes that theorem as source-facing,” although this PR corrects that description. Rewrite the note to record the remaining rename and composition obligations as the current verdict.

The new statement-level `\leanok` for `lem:qld-4-10` is otherwise justified: `CombinedPointsWitness` matches the paper’s projective measurements, uniform \((x,z)\) averaging, ordered \(XZ\)/\(ZX\) conclusions, and symmetric placements. It does not claim proof closure because the proof block has no `\leanok`. The remaining changed prose and dependency metadata raised no additional code-review concerns. `git diff --check` passes; exact-head local CI was already green.

VERDICT: CHANGES_REQUESTED