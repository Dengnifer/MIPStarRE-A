Addressed both findings:

- F1: Removed duplicate `_current` proofs and private support definitions; callers now reuse public subline APIs.
- F2: Clarified the blueprint’s one-based labels versus Lean’s zero-based indices, including unit vectors and prefix restrictions.

Validation passed: `lake build` (9,207 jobs), `leanblueprint web`, and `git diff --check`. Affected results use only standard axioms; no proof holes were introduced. Existing unrelated warnings and documented proof obligations remain.

Changes are uncommitted in the two intended files, ready for the lifecycle script.