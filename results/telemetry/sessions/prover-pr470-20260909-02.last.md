Fixed F1: deleted `applyOperatorToState_mul'` and updated all callers to reuse `DistanceCalculus.applyOperatorToState_mul`.

Both edited files and `Points.lean` passed `lake env lean`; `git diff --check` passed. No paper statements changed or proof holes introduced. Full build not run.

Changes remain uncommitted for `autofix.sh`.