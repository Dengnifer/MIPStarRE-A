## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT/Test/QubitForm.lean:176` — Share the existing binary-coordinate algebra proofs instead of duplicating them in the soundness module.

## Review

Reviewed the complete authoritative patch at `fe26d4ab1a19419594619b3e4b984eae2252f64b`. One modularity issue requires changes; I found no new proof-correctness or source-statement defect.

**F1 — Duplicated algebra**
`shift_entry` and `phase_entry` repeat `tauObservable_X_apply` and `tauObservable_Z_apply` from `MIPStarRE/QPBT/Algebra/PauliTheorems.lean:615` and `MIPStarRE/QPBT/Algebra/PauliTheorems.lean:628`. Those existing lemmas already have the same arbitrary-index hypotheses. The surrounding label-equivalence, trace-pairing, observable-transport, and Fourier-inversion arguments also reproduce the implementation beginning at `MIPStarRE/QPBT/Algebra/PauliTheorems.lean:579`.

The cube-indexed register justifies generalizing the existing algebra interface, but maintaining a second derivation leaves future corrections and API changes to be applied independently in two modules. The audit acknowledges the private, `Fin L`-specialized implementation; that explains its current inaccessibility, not the need for duplicate proofs.

Generalize and expose the shared algebra implementation, or extract it into a common algebra module, and use it from both `exists_qubitIsometry` and the witness transport. Preserve `exists_qubitIsometry`’s public statement. This must be fixed before approval.

**Mathematical assessment**
- The coordinate construction agrees with `lem:pauli-binary`: the paper maps computational-basis labels through the fixed binary coordinates and uses self-duality and Fourier inversion (`references/qpbt-paper/04_preliminaries.tex:1202`).
- `PauliSoundnessWitness.toQubit` correctly post-composes both local maps. The supplied witness is input to exact transport identities, not an added hypothesis on the source soundness theorem. This matches the composition step in `cor:pauli-binary` at `references/qpbt-paper/08_classical_and_quantum_low_degree_tests.tex:1490`.
- Projector-reindexing orientation, auxiliary-factor placement, and EPR normalization are correct. Both operator comparisons retain the ideal reference state, field-valued answer sum, and squared Euclidean norms.
- `pauli_soundness_qubit`’s docstring, statement, and proof are byte-for-byte unchanged. Its universal constants, parameter bounds, quantifier order, and `deltaQld` conclusions remain intact.

**Validation and residual risk**
- `lake env lean MIPStarRE/QPBT/Test/QubitForm.lean` passes.
- Re-elaborating the edited source through `--stdin` and checking all 20 new public declarations reports only `propext`, `Classical.choice`, and `Quot.sound`; none depends on `sorryAx`.
- Integrity scans find only the documented, pre-existing `sorry` at `MIPStarRE/QPBT/Test/QubitForm.lean:516`. Whitespace and line-length checks pass.
- Soundness existence remains open; these transport results do not discharge it. I did not rerun a full project build in this read-only session. No files or GitHub records were modified.

VERDICT: CHANGES_REQUESTED