Addressed F1 in `blueprint/src/chapter/ch13_qpbt_test.tex:300`.
- Added `fact:triangle-for-simeq` to the proof’s `\uses` dependencies.
- Corrected the explanation: the triangle inequality combines the three point relations; the codeword-defect estimate and polynomial collision bound then recover polynomial consistency with additional error \(md/q\).
- No Lean statements, proofs, status tags, or proof-debt records changed.

Validation passed: `leanblueprint web`, single-file Lean check of `SeedIndexedSoundness.lean`, and `git diff --check`.

Changes remain uncommitted for the lifecycle script. Existing general-\(k\) obligations remain unchanged.