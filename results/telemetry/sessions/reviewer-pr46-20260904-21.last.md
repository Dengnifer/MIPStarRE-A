## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Extraction/Unitary.lean:2` — Importing `Test.Soundness` reverses the intended dependency and would create a cycle when `pauli_soundness` consumes extraction.
## Review
F1 must be fixed before merge. `Extraction.Unitary` uses only `deltaQld`, which is defined in [SoundnessDefs.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-0019-qpbt-extraction-skeleton/MIPStarRE/QPBT/Test/SoundnessDefs.lean:35). Importing [Soundness.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-0019-qpbt-extraction-skeleton/MIPStarRE/QPBT/Test/Soundness.lean:1) also imports the source-facing `pauli_soundness` theorem in the wrong direction. Stage 4.3 must eventually prove `pauli_soundness` using `exists_extractionWitness`, so the current edge would force an import cycle. Replace line 2 with:
```lean
import MIPStarRE.QPBT.Test.SoundnessDefs
```
The changed mathematical interfaces otherwise pass the statement-integrity review. In particular:
- The decoder correction faithfully isolates the false unrestricted step at paper `14_analysis_of_the_pauli_basis_test.tex:1483-1492`: `decodeFq_dotProduct_indicatorVec` requires `IsEncoding`, while `nonencodingMarginalMass_le` records the missing support estimate rather than adding it as a hypothesis.
- `tildeObs_twisted_commutation` retains the actual phase omitted by the paper at lines 1451-1456, with an appropriate `**Local fix:**` record.
- `ExtractionWitness` matches `lem:qld-unitary` at paper lines 1669-1685 and 1827-1859: concrete swap unitaries, normalized auxiliary state, squared state distance, and both players’ Pauli-family comparisons. It contains no bridge or transfer premise.
- The explicit error bounds and quantifier order agree with the revised Chapter 16 blueprint; the additional nonnegativity and `epsilon ≤ 1` conditions are legitimate error-domain boundary conditions.
All 26 new `sorry` declarations are clearly marked as proof obligations assigned to issue `#47`, with the intended discharge described. I found no untracked `sorry`, new axiom, or kernel bypass.
I type-checked all five new Lean modules and the `MIPStarRE/QPBT.lean` aggregate. They succeed with only the expected tracked `sorry` warnings; `git diff --check` also succeeds. The provided local CI status is green.
VERDICT: CHANGES_REQUESTED
