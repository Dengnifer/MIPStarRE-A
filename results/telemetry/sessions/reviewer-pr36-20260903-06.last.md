## Findings
- [ ] F1 (blocker) `MIPStarRE/QPBT/Observables/LineMeasurement.lean:268` — `expLine_point_cons` weakens the paper’s \(O(\varepsilon)\) item-2 estimate to \(O(\sqrt{\varepsilon})\).
- [ ] F2 (changes) `MIPStarRE/QPBT/Observables/Anticommuting.lean:61` — no theorem establishes that `commProb` is the complement of `anticommProb`.
- [ ] F3 (changes) `MIPStarRE/QPBT/Observables/ExpandedDefs.lean:137` — `expObs` lacks the Hermitian and involution properties asserted for the expanded observable.
- [ ] F4 (changes) `MIPStarRE/QPBT/Observables/ExpandedDefs.lean:157` — `tauPointProj` lacks the projector or projective-family property asserted by the source.
- [ ] F5 (changes) `MIPStarRE/QPBT/Observables/ExpandedDefs.lean:214` — `expPointTrace` is only a POVM and lacks the source’s projectivity assertion.
- [ ] F6 (changes) `MIPStarRE/QPBT/Observables/LineMeasurement.lean:70` — `tauLineProj` lacks the projective-measurement property used by the source proof.
## Review
F1 is source-statement drift. The proof of `lem:qld-comm-line-cons` obtains the item `eq:qld-comm-line-pt-cons` with error \(\varepsilon\) at `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:560-596`, explicitly concluding this at lines 595-596. The blueprint also records the stronger rate at `blueprint/src/chapter/ch14_qpbt_observables.tex:1233-1238`. Since `deltaLine ε = sqrt ε`, the current theorem is strictly weaker for \(0<\varepsilon<1\). `expLine_point_cons` must expose `C * ε`; the existential wrapper may separately retain the common square-root error.
F2 leaves part of tagged `fact:omega-anticomm-prob` unformalized. The blueprint states at lines 176-182 that commuting tuples have the complementary probability. `commProb_ge_half` proves only a bound, not `commProb P = 1 - anticommProb P`. Add the partition theorem and include it in the blueprint linkage.
F3-F6 concern algebraic structure that raw `Op` and `Measurement` types do not encode. In particular, `Measurement` only provides a POVM; projectivity is the separate predicate `Measurement.IsProjective`.
The paper calls `expObs` an observable at lines 374-377, and the blueprint asserts eigenvalues \(\pm1\) at lines 840-852. Add Hermiticity and square-to-one companion statements. Similarly, paper lines 379-389 and blueprint lines 875-895 call `tauPointProj` a projector family, while paper lines 412-420 and blueprint lines 904-912 call each trace-coarse-grained effect a projection. Those properties need named Lean declarations.
Finally, paper lines 552-558 and blueprint lines 1143-1172 use `tauLineProj` as the outcome projectors of a projective measurement. Its current raw operator definition does not provide that contract. Bundle the family as a measurement or state its completeness and `IsProj` properties explicitly.
The issue brief explicitly tracks the skeleton’s support lemmas and requires the separate wrong-form-mass bounds, so I did not treat those authorized `sorry` sites as untracked proof debt. Exact-head local CI is reported green; `git diff --check` and the proof-bypass scan were clean.
VERDICT: CHANGES_REQUESTED
