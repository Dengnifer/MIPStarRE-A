## Findings
- [ ] F1 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:854` — The changed `\uses` graph lists transitive paper ingredients instead of the Lean declarations actually invoked, while thirteen new public auxiliaries lack subordinate blueprint nodes.
- [ ] F2 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:1185` — The `\notready` marker is stale because `exists_combinedLinesWitness` has a source-faithful signature; only its proof remains open.
- [ ] F3 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:859` — Replace the visible paper-gap file path with a bibliography citation and add its missing `@techreport` entry.
- [ ] F4 (changes) `MIPStarRE/QPBT/Combining/Points.lean:7` — The module still describes the completed combined-point construction as a “construction obligation.”
- [ ] F5 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:1263` — The phrase “are formalized; … remains unformalized” is a progress report rather than mathematical prose.
## Review
The three new blueprint statements for marginal distance, marginal defect, and line-point-sampled self-consistency match their Lean signatures on quantifiers, supplied joint measurements, placements, sampling distributions, completed outcomes, and error bounds. The combined-point statement also remains faithful to paper Lemma `qld-4-10`. I found no mathematical-equivalence mismatch in these entries.
For F1, the direct proof dependencies are not represented accurately. For example, the marginal-distance theorem calls the same-placement and completed-marginal lemmas, while its `\uses` instead lists their transitive source ingredients. The defect proof calls `Measurement.postprocess_comp`, although `lem:sandwich-postprocess-compose` links a different declaration. The self-consistency proof directly calls the two new sampling auxiliaries rather than `lem:alnf`, `lem:dlnf`, and `fact:agreement`.
The same problem occurs in the revised `qld-4-10` proof metadata: its Lean proof directly invokes the sandwich-defect, ordered-distance, orthonormalization, placement-transfer, and chain-bound theorems, none of which are represented by the current `\uses` list. Add subordinate formalization-support nodes for the thirteen new helper declarations and any directly referenced combined-point helpers needed by the graph, then make each proof-level `\uses` list direct rather than transitive.
For F2, `exists_combinedLinesWitness` states the source construction without an additional hypothesis: its `deltaQ` and point family are existential output carrying Lemma `qld-4-10`, not bridge inputs. It therefore supports statement-level `\leanok`. Its axiom closure still contains `sorryAx` through `combined_line_measurement_consistency`, so its proof must remain without proof-level `\leanok`; the separate obligation node should remain.
For F3, replace the exact phrase `\path{docs/paper-gaps/qpbt_combined-points-direct.tex}` with `\cite{gap:qpbt_combined-points-direct}` and add the corresponding bibliography entry.
For F4, use: “This module constructs the joint X/Z point measurements and defines their scalar linear coarse-graining.”
For F5, use mathematical references instead, for example: “The concrete paired-line POVM is Definition~…; Lemma~… gives its degree support, and Lemma~… states the required consistency estimate.”
The earlier EPR-permutation error and the missing entries for the marginal estimates and sampled self-consistency are resolved. The marked completed proofs have axiom closures containing only `propext`, `Classical.choice`, and `Quot.sound`.
Summary: 0 equivalence mismatches, 1 stale `\notready`, 1 dependency/traceability issue, and 3 prose issues.
VERDICT: CHANGES_REQUESTED
