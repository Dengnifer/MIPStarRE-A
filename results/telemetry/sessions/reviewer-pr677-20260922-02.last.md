## Findings
- none

## Review
All prior findings are resolved:

- F1: `docs/comparator.md:116-121` limits acceptance to the current closure and explicitly says former blockers were not compared under their old names.
- F2: `docs/comparator.md:123-126` correctly requires same-commit CI drift evidence and notes that the record alone does not select the build.
- F3: `scripts/comparator/README.md:146-149` removes the stale pending status and delegates mutable verification status to the canonical document.

The completion gate reports C5 as `DELEGATED`, naming all four headline theorems and accepting `ecb97d1f66ee` as an ancestor pin. The challenge inputs and expected tree are unchanged since that pin. This PR touches `scripts/comparator/README.md`, so the comparator build and drift guard were selected by the exact-head green CI. Unrelated C3/C4 failures remain, consistent with the document’s explicit statement that comparator acceptance does not establish track completion.

The external record is internally consistent: the linked action succeeded at comparator commit `360402f...`; its configuration names all four targets, enables nanoda, and permits only the three standard axioms. The lake files pin `ecb97d1...`, while `verify.sh` requires real landrun and invokes `lake env comparator comparator.json`. citeturn0view0turn1view0turn2view0turn2view1turn2view2

`git diff --check` passes. No Lean or blueprint declarations are changed.

VERDICT: APPROVED