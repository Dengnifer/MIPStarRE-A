## Findings

- [ ] F1 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:1649` — The proof’s `\uses` omits the prescribed-answer transfer and error-domain monotonicity results directly invoked by the Lean proof.
- [ ] F2 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:1625` — “raw” is implementation-shaped terminology; use “prescribed-answer bounds/effects” throughout this new node.

## Review

F1 is a category-A dependency mismatch. The Lean proof of `exists_arbitrary_strategy_raw_isometry_bounds` directly invokes `raw_pauli_operator_distanceA_le_completed`, `raw_pauli_operator_distanceB_le_completed`, and `deltaQld_mono_epsilon` at `RawOperatorTransfer.lean:714`, `:723`, and `:735`. None is represented by the proof’s `\uses`. Add subordinate formalization-support nodes for these proved results and list their labels so the dependency graph reflects the actual proof.

For F2, `docs/mathematical_language.md` explicitly identifies “raw” as process-shaped terminology. Rename the title to “Formalization support for prescribed-answer bounds” and replace “completed-to-raw estimates” at line 1658 with “estimates comparing the completed and prescribed-answer effects.”

The new auxiliary statement otherwise matches its Lean signature in quantifier order, hypotheses, error domain, and all three conclusions. Both new proof-level `\leanok` claims are valid, and the top-level `thm:pauli` proof now correctly depends on the new auxiliary theorem. The prior theorem-status, top-level dependency, and Lean-jargon findings are resolved.

Summary: 0 equivalence mismatches, 0 invalid `\leanok`, 1 `\uses` mismatch, 1 prose issue.

VERDICT: CHANGES_REQUESTED