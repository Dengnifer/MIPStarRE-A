## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT.lean:43` — The new Boolean Fourier module is absent from the QPBT aggregate, so root imports cannot access its public declarations.
- [ ] F2 (changes) `docs/paper-gaps/qpbt_linearity-distance-normalization.tex:103` — The source comparison silently removes a second `ρ` from the cited distance definition.
- [ ] F3 (changes) `MIPStarRE/QPBT/Combining/Linearity/BooleanFourier.lean:293` — The theorem correcting source equation (3) lacks the required `**Local fix:**` provenance and paper-gap citation.
- [ ] F4 (changes) `docs/paper-gaps/qpbt_linearity-distance-normalization.tex:170` — The formal comparison does not identify the theorem whose raw `≤ δ` conclusion it declares false.

## Review

F1: `MIPStarRE/QPBT.lean` re-exports `MIPStarRE.QPBT.Combining.Linearity` but not `MIPStarRE.QPBT.Combining.Linearity.BooleanFourier`. A probe after `import MIPStarRE` reports `MIPStarRE.QPBT.booleanCharacter` as unknown. Add the nested module to the aggregate import list so the new public API participates in the established re-export structure.

F2: [fullpaper.tex](/home/drx/MIPStarRE-qpbt/.worktrees/issue-124-boolean-fourier-foundations/references/nv-paper/fullpaper.tex:871) defines `Tr_ρ(X) = Tr(ρX)`, but lines 873–875 print
`D_ρ(S,T)^2 = Tr_ρ((S-T)†(S-T)ρ)`, with another trailing `ρ`; line 894 repeats it. Literally this is `Tr(ρD†Dρ)`, not `Tr_ρ(D†D)` as the note claims, and the two differ for mixed states. The note should explicitly classify the trailing factor as an independent source typo and justify the intended single-state convention before diagnosing the factor-of-two error at lines 910–911. The scalar counterexample with `ρ = 1` remains valid.

F3: Source lines 910–911 print measurement distance as both one half of the raw expression and equal to the raw distance. `binaryObservableDistSq_eq_stateDepDistSq_div_two` formalizes the corrected relation. Under `AGENTS.md`, such a correction needs a `**Local fix:**` section naming the erroneous source equality and citing `docs/paper-gaps/qpbt_linearity-distance-normalization.tex`.

F4: The comparison concerns blueprint label `thm:linearity` and Lean theorem `MIPStarRE.QPBT.exists_exactly_linear_observables`, whose conclusion is at [Linearity.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-124-boolean-fourier-foundations/MIPStarRE/QPBT/Combining/Linearity.lean:84). Citing only `stateDepDistSq` establishes the meaning of “raw” but does not identify the formal theorem carrying the false `≤ δ` bound. Name both the blueprint label and Lean theorem in the note.

The Boolean-character identities, Fourier inversion, order-preserving matrix Parseval identity, and distance algebra are otherwise correct. The four-point counterexample and its extension to ancillary representations are mathematically sound. No new `sorry`, axiom, bypass, or proof-evasion pattern appears; axiom inspection of the principal new theorems reports only `propext`, `Classical.choice`, and `Quot.sound`. The pre-existing `sorry` in `exists_exactly_linear_observables` remains marked `\notready`, with repair assigned to issue #129.

`lake env lean MIPStarRE/QPBT/Combining/Linearity/BooleanFourier.lean` and `git diff --check` both succeeded without warnings.

VERDICT: CHANGES_REQUESTED