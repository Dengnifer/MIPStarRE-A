Addressed F1–F4; changes are uncommitted.

- Exported `WitnessMarginals` through `Points`.
- Corrected `eprCrossSwap` documentation to describe a distinct auxiliary permutation.
- Added the [replacement-proof note](/home/drx/MIPStarRE-qpbt/.worktrees/issue-406-witness-marginal-distance/docs/paper-gaps/qpbt_combined-points-field-valued.tex), documenting Parseval, rounding, dimension-independent error, and unchanged theorem assumptions/conclusions.
- Removed the duplicate lemma and reused `DistanceCalculus.applyOperatorToState_mul`.

Validation passed: full `lake build` (9,175 jobs), public-import and axiom checks, LaTeX compilation, note-style check, and `git diff --check`. No new proof holes; unrelated existing `sorry` warnings remain.

Mathematical verdict: the replacement proves `lem:qld-4-10` without additional ancillas. It bypasses, but does not discharge, the original linearity route’s common-extension obligation.