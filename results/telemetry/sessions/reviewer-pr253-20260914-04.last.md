## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Extraction/RegisterTransport.lean:186` — Public `extractionAuxReference` is described by the blueprint but lacks its own definition node and `\lean` tag.
- [ ] F2 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:672` — The blueprint restricts `placePlayer_eq_placeSide` to measurement operators, while Lean quantifies over every operator.
- [ ] F3 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:654` — The proof dependencies omit the operator-family distance and EPR results used by the linked Lean proofs.
- [ ] F4 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:671` — The statement dependencies name the EPR-first permutation and swap unitaries, although the linked action theorems use block reassociation and no swap operator.
- [ ] F5 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:676` — The proof lists the unused EPR-first definition but omits the transport-support lemma that it explicitly invokes.

## Review
**F1:** The blueprint statement at line 650 describes the reference auxiliary vector and links its norm theorem, but the defining declaration at Lean line 186 is absent from the blueprint. Since this is a public, downstream-facing object rather than an internal proof helper, add a subordinate definition node with `\lean{MIPStarRE.QPBT.ProjectiveSetting.extractionAuxReference}`, `\leanok`, and its dependencies.

**F2:** Lean lines 233–236 prove the placement identity for arbitrary
`operator : Op (setting.LocalSpace side)`. The blueprint instead says “A measurement operator of one player,” which is strictly narrower. Replace this with “An arbitrary operator on one player’s original register.” Until then, the statement-level `\leanok` is not mathematically exact.

**F3:** `opFamilyDistSq_reindexState` unfolds `opFamilyDistSq` at Lean line 109, corresponding to `def:povm-distance`. The normalization proofs also invoke `eprState_norm` at lines 181 and 193. Add `def:povm-distance` and `def:EPR` to the proof-level `\uses`.

**F4:** `def:extraction-epr-first-order` denotes the permutation
\(AA'A''\mid BB'B''\to A''AA'\mid B''BB'\). By contrast, Lean lines 342–346 and 359–363 use `sixRegExtractionEquiv`, which only reassociates the six registers into the two blocks \(AA'A''\mid BB'B''\). Make “after reordering” explicitly mean block reassociation and point to a node representing that equivalence. Remove `def:v-swap-unitary`, which is merely an application context and is not needed to state any linked result.

**F5:** The proof itself says that its last statements use Lemma `lem:extraction-register-transport-support`, and Lean lines 347 and 365 use the corresponding transport identities. Replace the current proof dependency with `\uses{lem:extraction-register-transport-support}`; `def:extraction-epr-first-order` is not used there.

The proof-level `\leanok` claims otherwise match complete proof bodies, no changed `\notready` is stale, and I found no category-B banned-language issue. Prior ledger F1 is resolved: this file no longer redeclares `ProjectiveSetting.psiHat_norm`.

Summary: 1 equivalence mismatch, 1 missing blueprint entry, 3 `\uses` inaccuracies, 0 prose issues.

VERDICT: CHANGES_REQUESTED