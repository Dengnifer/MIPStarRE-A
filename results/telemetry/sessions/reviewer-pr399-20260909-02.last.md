## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Lines/SubLineBind.lean:37` — Move the generic distribution laws into the shared probability layer and consolidate duplicate averaging proofs.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/Lines/RestrictedMixture.lean:148` — Reuse the existing seed-marginal theorem instead of maintaining a second proof.

## Review

F1: The new `bind_*`, `prod_*`, `restrict_*`, and averaging laws concern arbitrary finite distributions, but are distributed across modules importing sub-line geometry and witnesses. Lower-level game and test modules cannot reuse these laws without backward dependencies or import cycles. Move this generic API beside `Games/DistributionAux.lean`, retaining the geometric specializations under `Combining/Lines`. Consolidate duplicates during that move: `RestrictedAverage.avgOver_prod` has exactly the statement already proved by `SandwichProduct.avgOver_distribution_prod` in `MIPStarRE/QPBT/Games/Sandwich/Support.lean:29`. This requires changes before approval.

F2: `uniformDistribution_map_ldSeed` repeats the statement of `map_uniformDistribution_seed` in `MIPStarRE/QPBT/Test/LowDegreeGameTheorems.lean:83`. That module is already imported through `Combining.Defs`; the obstacle is only the original theorem’s private visibility. Expose one canonical theorem, update its callers, and remove the duplicate proof. The reference to issue #204 acknowledges the duplication but supplies no technical reason to retain it.

I found no mathematical blocker in the reviewed proofs. The statement audit against `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex` and blueprint Chapters 15–16 found:

- `exists_combinedPointsWitness` preserves the universal error function, both ordered products, and all opposite placements of `lem:qld-4-10` (paper lines 689–709). The paper-gap note explains the alternative Fourier and sandwich construction.
- The restricted-distribution estimates preserve the factors `2m` and `4m²` from paper lines 1049–1061.
- `marginalPoly_sub_pointMeas_approx_zero` preserves the polynomial-outcome summation and all four placements of `eq:qld-sg-cons2` (paper lines 1611–1662), retaining the existing explicit error envelope.
- The sub-line and supplied-strategy results remain explicitly scoped to the direct carrier; they do not claim completion of the unresolved source-carrier construction.

Focused Lean checks passed for `Extraction/Consistency.lean`, `Combining/Points.lean`, `Combining/Lines.lean`, and `ExtendedLineGame/PassingValue.lean`. Nine principal declarations’ axiom closures contain only `propext`, `Classical.choice`, and `Quot.sound`. No added proof holes or forbidden bypasses were found; remaining warnings concern unchanged, documented obligations.

The attachment was truncated; I reviewed the Lean and paper-gap changes from disk. I did not repeat the full build. The external `lean-conventions` reference was unavailable locally; the checked-in review and integrity policies were applied. The worktree remains unchanged.

VERDICT: CHANGES_REQUESTED