## Findings
- [ ] F1 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:1098` — The explicitly open internal consistency obligation carries `\leanok`.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/Lines.lean:327` — The completed directly indexed subline existence theorem lacks a matching auxiliary blueprint entry.
- [ ] F3 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:1886` — The concrete measurement, exact X marginal, and deficit estimate lack linked auxiliary entries and proof dependencies.
- [ ] F4 (changes) `MIPStarRE/QPBT/Combining/Points/PlacementSupport.lean:6` — Replace “re-exports” in the reader-facing module description.

## Review

**F1.** `combined_line_measurement_consistency` remains an explicitly tracked internal obligation with `sorry` at `MIPStarRE/QPBT/Combining/Lines.lean:70`; the targeted completion check confirms its `sorryAx` dependency. Removing the previous `\notready` was correct because the statement is settled. Under A.2’s special rule for open internal obligations, retain its separate `\lean{...}` link without `\leanok`; do not restore `\notready` or change the theorem’s hypotheses.

**F2.** The only blueprint link for `exists_subLineWitness` is at `ch15_qpbt_combining.tex:1368`, in the seed-indexed source statement. The newly completed Lean theorem instead constructs the directly indexed distribution, as its docstring explicitly explains. Add a separate auxiliary existence theorem stating its probability, marginal, incidence, compatibility, and axis-closure properties, with matching statement and proof tags. Keep the source statement pending.

**F3.** The auxiliary Claim 17-2 proof describes three completed results without linking them: `combinedLineMeasurement` at `Lines/CombinedMeasurement.lean:27`, `combinedLineMeasurement_sum_Z` at `Lines/Marginal.lean:24`, and `exists_concreteXPointOverlap_deficit_le` at `Lines/ConcreteXDeficit.lean:178`. Record these in subordinate definition/lemma entries and connect the auxiliary claim’s proof through proof-level `\uses`. Its current statement-level dependencies do not record these intermediate results.

**F4.** Replace “This module re-exports the canonical placement implementation” with, for example, “This module provides the register-placement identities and the invariance under exchanging the EPR pairs.”

The prior prose findings concerning source-claim certification, the gap note, the point-construction proof, prefix truncation, Parseval, uniform fibers, Z-only scope, and the placement description are resolved. The restricted-distribution status finding is also resolved; the previous internal-obligation readiness finding is only partly resolved, as F1 explains. Code-only ledger entries remain outside this review.

Summary: **3 status/traceability issues and 1 prose issue**. Targeted Lean declaration and completion checks succeeded for the completed results. The source-distribution and complex-overlap correspondence remain open and are now distinguished from the auxiliary estimates. No files were changed; no full project or blueprint build was run.

VERDICT: CHANGES_REQUESTED