Addressed both unresolved findings:
- **F1:** Empty secondary-home overrides now use the same default as `dispatch.sh` for model resolution and resume discovery.
- **F2:** Registry reads take a shared file lock, skip blank rows, diagnose malformed/non-object rows, and preserve rejection of conflicting affinity evidence.
- Added regression tests for empty overrides, damaged registries, rollout fallback, conflicts, and concurrent writes.
Validation: all 28 dispatcher tests passed; shell syntax, `git diff --check`, and `lake env lean MIPStarRE/Quantum.lean` passed.
Changes remain uncommitted for `autofix.sh`; no GitHub or telemetry records were modified. No real agents/provider calls were launched; runtime cutover remains operator-owned.
