## Findings
- [ ] F1 (blocker) `MIPStarRE/QPBT/Combining/Apply.lean:132` — A known-false unrestricted supplied-point theorem remains an “established” auxiliary with an unmarked `sorry`.
- [ ] F2 (changes) `MIPStarRE/QPBT/Games/GroundCompression.lean:42` — The new ground-compression API duplicates the existing exported `Measurement.compressAt` implementation and is unused.
- [ ] F3 (changes) `MIPStarRE/Quantum/ControlledUnitary.lean:1` — The unused helper creates a reverse `Quantum` to `QPBT` dependency and duplicates the existing swap-unitary proof.

## Review
**F1:** The changed paper-gap note explicitly concludes at `docs/paper-gaps/qpbt_combined-lines-error-term.tex:267-297` that `exists_extendedLinesWitness_established_ofPointsWitness` is false: it accepts an arbitrary `CombinedPointsWitness S δQ`, but its line error omits `δQ`. This conflicts with paper `lem:qld-4-10`, which constructs points with `δQ(ε) = poly(ε)` at `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:689-709`, and `claim:17-1`, which retains a `δQ^(1/2)` loss at lines 1140-1165. The declaration nevertheless calls itself “established,” has a bare `sorry` at line 140, and lacks the mandatory `**Unfaithful:**` marker and issue #511 elimination plan. Because the statement is false and preserves no proof content, marking it alone is insufficient: delete it or realign its domain to the polynomially controlled point witness.

**F2:** `groundCompressMeasurement` repeats `Measurement.compressAt` from `MIPStarRE/QPBT/Games/MeasurementCompression.lean:25`, including postprocessing, Naimark recovery, and padded-state consistency results at lines 53, 76, and 92. No other file references the new API, while `AnyStrategySoundness.lean` already uses the canonical implementation. Consolidate the extra corollaries over `compressAt` if needed and remove the parallel construction.

**F3:** `ControlledUnitary.lean` imports QPBT support into the reusable `Quantum` layer, and none of its public declarations are consumed or re-exported. Meanwhile `MIPStarRE/QPBT/Extraction/Observables.lean:386-426` independently performs the same projective-control diagonal reduction and inverse argument for `swapUnitary`. Integrate the generic lemma into extraction and place it in a QPBT-owned module, or remove it. A genuinely reusable Quantum theorem must not depend on the higher QPBT layer.

The option-valued line-point marginal transport itself is mathematically consistent with the completion step at paper lines 917-941: `some` adds a zero `none` effect, and the line-point point marginals are transported to the uniform product law without changing the supplied bound. The review context reports successful exact-head local CI; I did not independently rerun Lean in the read-only reviewer session.

VERDICT: CHANGES_REQUESTED