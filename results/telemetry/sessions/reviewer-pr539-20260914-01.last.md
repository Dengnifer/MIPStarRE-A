## Findings
- [ ] F1 (blocker) `MIPStarRE/QPBT/Extraction/Consistency.lean:919` — The source-linked theorem closes only a conditional result requiring a supplied `GlobalPairWitness`.
- [ ] F2 (changes) `MIPStarRE/QPBT/Extraction/NonencodingSupport.lean:186` — The docstring denies the explicit, load-bearing `GlobalPairWitness` assumption.
- [ ] F3 (changes) `audits/2026-09-12_issue-519-point-consistency-prime.md:1` — The new audit omits the required format and retains superseded, factually false dependency status.
- [ ] F4 (changes) `results/telemetry/events.md:7633` — An orphaned PR532 heading incorrectly precedes and mislabels the PR531 incident.

## Review
**F1:** Paper `lem:qld-construct-the-paulis` at `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:1463-1475` states the two consistency conclusions for the measurements obtained from `lem:qld-4-7`. The blueprint likewise says the witness is supplied by that lemma at `blueprint/src/chapter/ch16_qpbt_extraction.tex:214`, then directly links `tildeM_consistent_pointMeas'` to the source-labelled theorem at lines 220-225.

Lean instead universally assumes `w : GlobalPairWitness S deltaG`. This is load-bearing proof data: its point-consistency fields are used by the new support chain, while `exists_globalPairWitness` remains unfinished at `MIPStarRE/QPBT/Combining/Apply.lean:190-195`. Completing this conditional theorem therefore does not complete the cited source result. The docstring itself acknowledges that witness construction remains separate, but it lacks the required `**Unfaithful:**` marker and the source blueprint node still advertises it directly.

This must be fixed before merge. Preserve the proof as a visibly conditional theorem such as `tildeM_consistent_pointMeas'_ofGlobalPairWitness`, document the unresolved constructor, and link it only from a separate conditional blueprint node. The source-facing declaration must obtain the witness internally or remain an explicit tracked proof obligation. This is the same treatment already used for `exists_extractionWitness_ofGlobalPairWitness`.

**F2:** `global_marginal_encoding_consistency` says it “does not assume … the existence of a global witness,” immediately before accepting an actual `GlobalPairWitness`, which is stronger than an existence proposition. Its fields are used at lines 228-249. Rewrite this as a supplied-witness auxiliary and state clearly that it does not construct the witness.

**F3:** `audits/2026-04-29_audit-document-format.md` requires YAML metadata and the standard audit sections. This new audit has neither. It also says at lines 36-44 and 110-114 that the target uses `nonencodingMarginalMass_le` and has `sorryAx`, while lines 155-184 and the final code establish the opposite. Rewrite it as a coherent final-state audit rather than retaining a chronological section that is later declared superseded.

**F4:** The inserted heading at line 7633 has no body and is immediately followed by the PR531 heading; the actual PR532 entry begins at line 7655. Remove the orphaned duplicate so the incident ledger remains unambiguous.

The mathematical proof chain itself checks out: convolution preservation, register transport, the Schwartz-Zippel collision estimate, support-mass comparison, and final constant absorption have the expected directions. Fresh axiom queries for the target and principal new support theorems report only `propext`, `Classical.choice`, and `Quot.sound`. `git diff --check` and the 100-character Lean line check pass. The four remaining direct `sorry` sites in `Consistency.lean` are pre-existing and explicitly documented as proof obligations.

VERDICT: CHANGES_REQUESTED