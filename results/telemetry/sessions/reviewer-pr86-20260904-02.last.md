## Findings

- none

## Review

Prior F1 is resolved: `weight_sum_eq_one_of_subset` now belongs to the shared `Distribution.IsProbability` API and replaces duplicated support-normalization arguments across LDT and QPBT.

The product, convex-mixture, and dependent-bind proofs are correct finite-sum calculations grounded in Mathlib. No public theorem statement changed, no source-facing declaration gained assumptions, and the cited paper/blueprint constructions remain faithfully represented. Comparator source anchors were updated consistently.

All three changed Lean files type-check without warnings. `git diff --check` and the changed-file proof-integrity scan pass with no new `sorry`, axioms, bypasses, or placeholder tactics. Exact-head local CI is reported green; I did not repeat the full build in this read-only review.

VERDICT: APPROVED