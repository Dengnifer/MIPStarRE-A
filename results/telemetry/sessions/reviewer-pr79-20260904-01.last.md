## Findings

- [ ] F1 (blocker) `local/bin/review.sh:453` — Refusing every post-round-two review strands a corrected new head that `pr_merge.py --adjudicated` cannot accept without an exact-head review
- [ ] F2 (changes) `local/bin/review.sh:440` — The round count occurs before the review lock and differs from the existing distinct-head calculation, allowing queued reviews to exceed the cap
- [ ] F3 (changes) `local/bin/review.sh:448` — Rename detection can hide a non-workflow source path and incorrectly classify a mathematics-changing PR as workflow-only
- [ ] F4 (changes) `local/bin/review.sh:453` — The cap precedes the bot-commit skip and the force exception also bypasses the cap for non-bot heads
- [ ] F5 (changes) `local/bin/review.sh:457` — The complete round-cap gate and the new main-persona instruction are each duplicated verbatim
- [ ] F6 (changes) `local/personas/main.md:102` — The new queue rule conflicts with existing four-round and mandatory issue-disposition contracts
- [ ] F7 (changes) `local/bin/review.sh:434` — The new guard has no regression coverage for its counting, classification, locking, bot, or adjudication paths

## Review

F1 is a broken cross-file path. After reviews on heads A and B, fixing a round-two finding creates head C. The new gate counts two historical reviews and refuses review on C, directing the operator to adjudicate. However, `pr_merge.py:163-168` rejects C before examining an adjudication comment because C has no marker-bound review. The operator must either discard the fix or bypass the cap with `--force-review`. The adjudication path must support the terminal exact head, or the cap must permit a terminal exact-head review.

For F2, the canonical calculation at lines 551-567 runs after acquiring the lock and deduplicates marker reviews by head SHA. The new calculation instead runs before the lock, counts raw matching bodies, and degrades read failures to zero. Two invocations that both observe one prior round can therefore queue and both dispatch. Reuse one fail-closed, distinct-head history calculation inside the lock.

For F3, `git diff --name-only` reports only the destination of a detected rename. Moving a Lean or policy file into an allowed directory can consequently hide its source path. `local/protocols/ci.md:93-97` already mandates `--no-renames` for path gates for exactly this reason.

For F4, an unforced bot head with two earlier reviews blocks at the new gate instead of reaching the required exit-0 bot skip. Conversely, `--force-review` exempts any human-authored head despite being documented as the terminal bot-fix exception. Determine bot status before applying the cap and scope the exception accordingly.

F5 covers duplicated executable lines 457-478 and duplicated persona lines 110-117.

For F6, the same persona still says “at most FOUR” and “Every deferred finding becomes a tracked issue” at lines 52-55. `review.md:388-404` likewise assigns four rounds to every PR and requires every adjudicated finding to be fixed or tracked, while the new instruction says mechanism findings are not turned into issues. The gate ladder, exit-code documentation, and generated “at most 4” reviewer contract also remain unchanged. These normative instructions need one consistent workflow-versus-mathematics rule.

For F7, the existing fake-GitHub harness is suitable for pinning these branches, but this patch adds no tests. `bash -n local/bin/review.sh` and `git diff --check` passed. The static review tests passed; eight temp-directory-dependent tests could not run in the read-only sandbox because no writable temporary directory was available. CI was reported green, but no existing test exercises this new gate.

VERDICT: CHANGES_REQUESTED