## Findings
- [ ] F1 (changes) `scripts/blueprint_citations.py:221` — Explicit-citation scanning misclassifies blueprint filenames as unknown labels, producing false unresolved evidence on this PR.
## Review
**F1 — Distinguish labels from file references.** The new `CANONICAL_CITATION_RE` accepts any matching backtick token after “blueprint,” including `ch13_qpbt_test.tex`. The scanner then reports that filename as an unknown label. This reproduces against the current checkout:
```text
python3 -B scripts/blueprint_citations.py resolve \
  --path MIPStarRE/QPBT/Test/PauliBasisTest.lean --format plain
ch13_qpbt_test.tex    UNRESOLVED
(exit status 1)
```
For example, `MIPStarRE/QPBT/Test/PauliBasisTest.lean:62` contains a chapter filename accompanying the genuine label `def:admissible`; the filename is not an unresolved LaTeX label. Scanning the changed files produces twelve such filename occurrences. Because the review integration preserves unresolved rows preferentially, these false positives also consume the reserved failure-evidence budget.
Restrict explicit unknown-label detection to label-shaped tokens, excluding chapter filenames and legacy file locators while retaining detection of unknown `eq:`, `sec:`, and `item:` labels. Add a regression test using the existing filename-plus-label citation syntax. **This requires changes before approval.**
**Prior-finding triage**
- The extraction helper is explicitly conditional, named `exists_extractionWitness_ofGlobalPairWitness`, and retains its `**Unfaithful:**` marker and discharge plan. The earlier misadvertising finding is addressed.
- The decoder symmetry-step, given-strategy helper, low-degree parameter, and both `InputSdp` citations are corrected. The latter now identify the completeness argument of `lem:self-improvement-helper`, consistent with the reindexing and matching-mass bound in `references/ldt-paper/self_improvement.tex:407`.
- Exact-span rewrite precedence, refusal of proximity-only guesses, normalization limited to rewritten comments, the two cited overlong lines, and the specific legacy locator in `Linearity/Defs.lean` are addressed.
- Map-first attachment ordering, sanitized fallback input, and preferential retention of unresolved/duplicate rows are implemented.
- Bare-label detection and explicit unknown equation/section/item detection have regression coverage. However, the earlier scanner false-positive concern remains unresolved through the explicit-citation path described in F1.
**Validation and scope**
- Confirmed the complete on-disk authoritative patch equals the merge-base-to-head diff; the truncated attachment was not the sole review input.
- All **57 focused tests passed** across citation resolution, script-surface coverage, and review/workflow tests.
- `bash -n local/bin/review.sh` and `git diff --check` passed.
- The 100 changed Lean files contain documentation-only changes; no new theorem assumptions, proof terms, or `sorry` sites were introduced. Existing proof debt is not certified as discharged by this review.
- No Lean build or live review dispatch was run. The remaining demonstrated risk is incorrect generated citation evidence despite passing tests.
VERDICT: CHANGES_REQUESTED
