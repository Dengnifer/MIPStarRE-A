## Findings
- [ ] F1 (blocker) `blueprint/src/chapter/ch12_qpbt_games.tex:117` — Restore the source-labelled attainment statement; the given-strategy helper cannot replace it under `\leanok`.
- [ ] F2 (changes) `docs/paper-gaps/qpbt_combined-lines-error-term.tex:274` — The note claims the corrected error and Lean link landed, but Chapter 15 still retains the printed error and original theorem.
- [ ] F3 (changes) `docs/paper-gaps/qpbt-gap-register.md:20` — Refresh the linearity entries: the corrected `2δ` theorem and its measurement-distance companion are already proved.
- [ ] F4 (changes) `docs/paper-gaps/qpbt_symmetrization-attainment.tex:195` — The given-strategy symmetrization theorem is already proved; only the source-shaped attainment theorem remains open.

## Review
F1 violates the repository's faithful-formalization policy. The paper statement at `references/qpbt-paper/06_nonlocal_games_and_mipstar.tex:94` assumes only `val*(G) = 1 - ε` and concludes existence of an attaining symmetric projective strategy. The revised blueprint instead assumes a supplied strategy and advertises `exists_symmetric_projective_strategy_of_strategy` under the source label. That is a valid auxiliary result, but it has different hypotheses. The existing Lean file deliberately retains both declarations. Restore the source-labelled node and keep the given-strategy theorem as a separate formalization-support entry.

F2 is a cross-document contradiction. `blueprint/src/chapter/ch15_qpbt_combining.tex:1140` still states `poly(m²ε, md/q)`, links `exists_extendedLinesWitness`, and records `m * poly(ε, md/q)` only as the established proof route. The changed note, register, and Chapter 16 now claim that the corrected statement and `exists_extendedLinesWitness_established` link have landed. Those records must describe the actual blueprint state consistently.

For F3, `MIPStarRE/QPBT/Combining/Linearity.lean:89` already proves the corrected operator bound `≤ 2 * δ`, while the companion theorem at line 128 proves measurement distance `≤ δ`. Chapter 15 links both with `\leanok`. The register and quotation note incorrectly report the old factor-one statement and an open proof; only the common-extension construction remains open.

For F4, `MIPStarRE/QPBT/Games/StrategyClasses.lean:778` contains a complete proof of `exists_symmetric_projective_strategy_of_strategy`. The `sorry` belongs to the separate source-shaped attainment declaration at line 767. The note and register should distinguish these statuses.

The patch is mechanically clean under `git diff --check`, and the supplied exact-head local CI status is green. The requested changes are semantic consistency and statement-integrity corrections that those checks do not detect.

VERDICT: CHANGES_REQUESTED