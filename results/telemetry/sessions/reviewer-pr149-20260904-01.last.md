## Findings
- [ ] F1 (blocker) `MIPStarRE/QPBT/Test/MagicSquareTheorems/Rigidity/Dilation.lean:832` — `opFamilyDistSq_naimarkInflation` never relates the actual dilated projectors to inflated original effects, so the claimed rigidity-bound pullback is missing
## Review
F1 blocks the intended reduction from projective Magic Square rigidity to arbitrary POVMs. `msDilatedStrategy` uses `naimarkDilatedEffect`, whereas `opFamilyDistSq_naimarkInflation` only compares operator families already written as `naimarkInflation`. The compression identity
`E† P̂_a E = M_a` does not imply `P̂_a Eψ = E M_a ψ`; the discrepancy measures the non-projectivity of `M_a`.
This distinction is explicitly required by the source hierarchy:
- `references/ldt-paper/orthonormalization.tex:82-101` warns that Naimark dilation preserves outcome probabilities but need not preserve state-dependent `≈δ` estimates.
- `blueprint/src/chapter/ch04_projective.tex:255-270` gives the concrete `A_a = B_a = ½I` counterexample, where the original distance is zero but the dilated distance is one.
- `thm:ms-rigidity` in `references/qpbt-paper/08_classical_and_quantum_low_degree_tests.tex:624-646` and `blueprint/src/chapter/ch13_qpbt_test.tex:228-249` requires estimates for the original strategy’s conjugated effects and resulting anticommutators.
Consequently, applying projective rigidity to `msDilatedStrategy S` produces estimates for its Naimark projectors, while composing the rigidity isometries with `naimarkEmbedding` produces inflations of the original POVM effects. Nothing in this PR bounds the difference between those two families.
The fix should derive a Magic-Square-specific approximate intertwining estimate from the forward/reverse consistency masses, then use state closeness and contraction bounds to transfer all four effect estimates and both anticommutator estimates. No additional proof-obligation hypothesis should be added to `exists_ms_rigidity`.
The outcome-weight API, value-to-parity relations, projective completion, and exact Born/value preservation otherwise appear coherent. No new `sorry`, axiom, or prohibited proof mechanism was introduced. The unchanged `exists_ms_rigidity` `sorry` is catalogued in `local/briefs/42-residual-brief.md:186`. Exact-head CI was reported successful; static diff checks were clean.
The requested external `lean-conventions` skill was unavailable, so the review used the checked-in `AGENTS.md`, project conventions, anti-patterns, and review checklist.
VERDICT: CHANGES_REQUESTED
