## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Lines/SamePlacementDistance.lean:30` — The cited blueprint label `thm:line-point-same-placement-distance` does not exist.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/Lines/ConditionedConsistency.lean:58` — The new conditioned and restored consistency estimates lack linked blueprint support entries.
- [ ] F3 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:1013` — The proof dependency list omits the scalar conditioning bound and coefficient-polynomial agreement result actually used.
- [ ] F4 (changes) `MIPStarRE/QPBT/Combining/Lines/ConditionedConsistency.lean:17` — Mathematical docstrings contain implementation-history and API language.
## Review
**F1, category A.** The label cited by `exists_expLine_point_same_placement_distance_le` is absent from the blueprint. Add the intended auxiliary entry with the same-placement distance bound, its universal constant and hypotheses, and a matching `\lean{...}` link. This is an unresolved reference, not line-number drift.
**F2, category A.** `exists_combinedLine_conditioned_defect_le` and `exists_combinedLine_restored_defect_le` (`RestoredConsistency.lean:106`) are new public quantitative results. The blueprint describes their bounds at lines 1022–1041, but neither declaration has a linked statement. Add subordinate formalization-support entries recording their actual quantifiers, arbitrary point-error parameter, retained mass, and error bounds, with appropriate status and dependency tags. Keep these distinct from the source-labelled existence theorem.
**F3, category A.** The final scalar step explicitly invokes `exists_conditioned_polynomial_bound` at `Lines.lean:78`, whose existing blueprint node is `thm:qld-conditioned-polynomial-bound`. The collision calculation invokes `evalCoefficient_collision_card_le` at `UniformAffineCollision.lean:34`, represented by `lem:coefficient-polynomial-agreement`. Neither dependency appears in the new proof’s `\uses` list. Connect these existing nodes at the appropriate proof nodes and update the dependency list to reflect the implemented argument.
**F4, category B.** Replace “The proof from commit `f6a340c8` uses the current two-marginal pasting API” with “The proof applies heterogeneous pasting to the two point-to-line marginal comparisons.” Drop the analogous recovery-history sentences in `PastingPlacement.lean:16` and `RestoredConsistency.lean:17`. The naming norm in `docs/mathematical_language.md` requires mathematical descriptions rather than implementation history; the audit already records provenance.
There are **3 blueprint synchronization issues and 1 recurring prose issue**. I found no signature mismatch in the two newly marked results: the existence theorem obtains its point family internally, and the auxiliary preserves the required error-function quantifier order. The new proof-status tags are supported by the read-only Lean checks.
The on-disk patch was reviewed beyond the truncated attachment. All 133 tagged declarations in the changed chapter resolve. No full build or blueprint render was rerun; the worktree remains unchanged.
VERDICT: CHANGES_REQUESTED
