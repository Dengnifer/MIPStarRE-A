## Findings
- [ ] F1 (changes) `docs/paper-gaps/qpbt_combined-lines-error-term.tex:398` — The note still says both proofs are open and neither node is formalized, although this PR proves and marks the established auxiliary `\leanok`.
- [ ] F2 (changes) `audits/2026-09-12-issue512-saved-proof-recovery.md:1` — The new audit omits the mandatory YAML metadata and required audit structure, and its filename violates the `YYYY-MM-DD_` convention.
- [ ] F3 (changes) `MIPStarRE/QPBT/Combining/Claims.lean:40` — Two generic scalar helpers are promoted into the public API without docstrings or an appropriate scalar-utility module boundary.
- [ ] F4 (changes) `MIPStarRE/QPBT/Combining/Lines/Conditioning.lean:325` — The module ends with an empty section and a scoped `open DistanceCalculus in` that applies only to `end`.

## Review
F1 must be corrected to reflect the present state. `exists_extendedLinesWitness_established` is now proved at `Apply.lean:147`, and blueprint `lem:qld-4-13-established` carries statement and proof `\leanok` at `ch15_qpbt_combining.tex:2019,2050`. Thus “Both proofs are open” is false, as is the later assertion that neither blueprint node carries a formalization mark. The note should distinguish the still-open source-shaped `exists_extendedLinesWitness` from the completed directly indexed auxiliary. Paper-gap policy requires revisions to remain a coherent account of the current mathematical status.

F2 violates the active format in `audits/2026-04-29_audit-document-format.md`: new audits require YAML front matter, `YYYY-MM-DD_` filenames, and an explicit review-oriented structure. Add at least `title`, `date`, `purpose`, `issue`, and `pr`, reorganize or justify the narrower section structure, and update the paper-gap reference if the file is renamed.

F3 broadens `rpow_quarter_nonneg` and `sqrt_deficit_bound_le` from private implementation details into reusable declarations solely so `ExtendedLines/Estimates.lean` can import them. They now need mathematical docstrings. Since neither is specific to Claims 17-1 through 17-3, the cleaner boundary is `ErrorBounds.lean` or a focused scalar-estimates module, avoiding an unnecessary dependency on the claims module and its retained proof obligation.

F4 is extraction residue. The “Unequal-dimensional bipartite transport” section contains no declarations, while `open DistanceCalculus in` scopes only the closing `end`. Remove it or move the heading to `Lines/Construction.lean`, where the bipartition transport declarations actually live.

Mathematically, I found no source-statement blocker. Paper `lem:qld-xz-lines` at `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:882-894,942-961` constructs the same X-Z-X measurement, and the proof-only conditioning is explicitly restored to the original product law. Paper `lem:qld-4-13` at lines 1020-1034 remains separate and unproved. Blueprint `lem:qld-4-13-established` explicitly records the direct carrier/law, completed answers, and weaker \(C m\,\mathrm{poly}(\epsilon,md/q)\) error, so it does not misrepresent the source theorem.

`lake env lean MIPStarRE/QPBT/Combining/Apply.lean` passed. Its four `sorry` warnings and the retained `subline_remove_X_factor_direct` hole are all clearly documented as known obligations; no unmarked proof hole or unsafe proof mechanism was found.

VERDICT: CHANGES_REQUESTED