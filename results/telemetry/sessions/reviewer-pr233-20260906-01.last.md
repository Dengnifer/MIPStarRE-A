## Findings
- [ ] F1 (changes) `local/bin/account_router.py:86` — Normalize an empty secondary-home override consistently with dispatch.sh.
- [ ] F2 (changes) `local/bin/account_router.py:39` — Prevent unrelated malformed registry rows from disabling otherwise resolvable resumes.
## Review
Reviewed head `18ed0473b8dd6540502115eff481e62be89a8dbc` against the supplied patch and local routing contracts.
**F1:** With `MIPSTARRE_CODEX_HOME_SECOND=""`, Python resolves the secondary home to `Path(".")`, while `dispatch.sh:632` uses `${MIPSTARRE_CODEX_HOME_SECOND:-...}` and therefore executes against the default secondary home. Model resolution and resume discovery consequently inspect a different directory from execution. An in-memory reproduction confirmed that the router reads `config.toml` from the working directory. Without that file, secondary dispatch fails preflight; with one, it pins the wrong configuration’s model. Normalize the home once, or apply identical empty/unset fallback semantics in both implementations, and add an empty-override regression test.
**F2:** Resume discovery parses every registry line without blank-line handling, malformed-record handling, or dictionary validation. A blank or truncated unrelated row raises `JSONDecodeError`; a JSON `null` row raises an uncaught `AttributeError`. Each prevents reaching valid account records or the rollout fallback. I reproduced these failures with a valid secondary-account record following the problematic row. This differs from `telemetry.py`’s registry writer, which tolerates malformed historical records. Use compatible defensive parsing with diagnostics, preserve rejection of genuine conflicting affinity evidence, and coordinate registry reads with its writer’s lock to avoid observing incomplete concurrent appends.
**Validation:** All 24 dispatcher tests passed. Shell syntax checks and `git diff --check` passed; the working tree remains clean. Additional in-memory checks reproduced both findings. Initial bulk-read output was truncated; targeted reads covered the relevant implementations and cross-file contracts. No real agents or provider calls were launched. Runtime shim cutover remains an operator responsibility and was not inspected.
VERDICT: CHANGES_REQUESTED
