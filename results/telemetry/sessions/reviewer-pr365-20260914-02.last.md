## Findings
- [ ] F1 (blocker) `MIPStarRE/QPBT/Combining/Apply.lean:132` — `exists_extendedLinesWitness_established_ofPointsWitness` remains a known-false open theorem without the mandatory `**Unfaithful:**` marker and issue #511 elimination plan
- [ ] F2 (changes) `MIPStarRE/QPBT/Games/GroundCompression.lean:42` — `groundCompressMeasurement` duplicates the existing `Measurement.compressAt` API and its supporting lemmas
- [ ] F3 (changes) `MIPStarRE/Quantum/ControlledUnitary.lean:1` — the reusable `Quantum` layer now depends upward on QPBT-specific infrastructure and the module is neither consumed nor re-exported
## Review
**F1:** The updated paper-gap note explicitly identifies both unrestricted supplied-point auxiliaries as false: `docs/paper-gaps/qpbt_combined-lines-error-term.tex:202-217` assigns this declaration to issue #511, and lines 267-274 prove that its proposed error tends to zero while the defect remains bounded away from zero. The paper instead constructs the scalar point family in `lem:qld-4-12`, `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:993-1018`, and uses that specific family in `lem:qld-4-13`, lines 1020-1033. It does not quantify over every `δQ` and every `CombinedPointsWitness`.
The Lean theorem still makes exactly that stronger quantification, calls itself an “established auxiliary,” and ends in an unmarked `sorry` at line 140. It must be removed or realigned to the internally produced point witness. Any temporary retention must satisfy the repository’s unfaithful-dependency protocol by naming issue #511, stating that the current assertion is false, and giving a concrete restriction or deletion target.
**F2:** `groundCompressMeasurement` is definitionally the same principal-submatrix construction as `MIPStarRE.Quantum.Measurement.compressAt` in `Games/MeasurementCompression.lean:25`. Its postprocessing theorem duplicates `compressAt_postprocess`; its Naimark theorem duplicates `compressAt_dilatedMeasurement`; and its consistency theorem duplicates `consistencyDefect_padState_compressAt`. The new soundness proof already uses the established API, and no other Lean file imports this module. Remove the duplicate module or express only genuinely additional results through `compressAt`.
**F3:** `ControlledUnitary.lean` lives under the reusable `MIPStarRE.Quantum` hierarchy but imports `MIPStarRE.QPBT.Games.DistanceTheorems.Support`, opens `MIPStarRE.QPBT`, and depends on QPBT’s projectivity and distance-calculus declarations. This reverses the repository’s dependency layering and prevents normal re-export through `MIPStarRE/Quantum.lean`. Move the extraction-specific result under QPBT, or first move the genuinely generic projectivity API into the Quantum layer. The module currently has no external consumer.
The issue-364 target itself is mathematically well scoped. `exists_native_scalar_polynomial_measurements_of_supplied_witnesses` correctly takes the unique coordinate of the `k = 1` polynomial tuple, transports evaluation through the marginal, and retains the supplied-witness error. It is documented as Lean-only support rather than as the full `lem:qld-4-7`. Targeted Lean checks for this theorem, `GroundCompression.lean`, and `ControlledUnitary.lean` succeeded; the native scalar soundness chain has no `sorryAx` dependency.
VERDICT: CHANGES_REQUESTED
