## Findings

- [ ] F1 (blocker) `local/bin/merge_loss_guard.py:311` — Multiple best merge bases let a whole-tree reset silently discard a clean incoming modification
- [ ] F2 (changes) `local/bin/merge_loss_guard.py:298` — Deleting a recorded conflict path is rejected despite the documented conflict-resolution exemption
- [ ] F3 (changes) `results/telemetry/events.md:2698` — The PR rewrites an existing event despite the append-only telemetry contract
- [ ] F4 (advisory) `scripts/install_git_hooks.sh:96` — Installer output incorrectly implies that only the line-budget check survives `MIPSTARRE_SKIP_HOOKS`

## Review

F1: `branch_matches_base = all(...)` compares against each raw best base, not Git’s recursively synthesized merge base. In a disposable criss-cross history with two best bases, I let the incoming branch modify a file after both branches had merged those bases. Git’s normal merge carried the modification cleanly; after resetting the pending result to `HEAD`, the guard returned success. The reference-transaction path shares `inspect`, so it has the same false negative. The multiple-base regression test currently checks only that a valid merge passes; it needs a lossy-result case.

F2: For a normal modify/modify conflict resolved with `git rm`, the pending guard reports `DELETED`, and the reference-transaction hook also aborts a `--no-verify` commit. The deletion branch executes before the `path in trees.conflicts` exemption. This contradicts `issues-prs.md` and `EVOLUTION.md`, which state that recorded conflict paths remain resolution decisions.

F3: `local/protocols/meta.md:30` requires reports and session records to remain append-only. The original 11:28 incident entry is replaced with expanded prose. Restore the original entry and append the diagnosis and recovery as a later dated note.

F4: The installer says `MIPSTARRE_SKIP_HOOKS=1` bypasses tooling checks while naming only the line budget as an exception. The new guard deliberately ignores that bypass, so the operator-facing message should name both non-bypassable checks.

The authoritative patch is 893 lines and was not truncated. The focused suite passed all 10 tests; hook installation validation and shell syntax checks passed. The guard correctly rejected both historical lossy merges and accepted the exact PR head. No full build was rerun because this is workflow-only and the review was triggered by green exact-head local CI. The checkout remained clean at `9e184d8eca554f5c16a46fdd8338f7866a92015c`.

VERDICT: CHANGES_REQUESTED