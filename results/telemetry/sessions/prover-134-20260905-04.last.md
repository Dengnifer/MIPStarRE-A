## What changed

Committed merge `d8af73c03105795c2ca695def605626008601a87` (`chore(QPBT/Games): merge renamed sandwich API`).

- Adopted main’s `SandwichProduct` implementations in `Games/Sandwich.lean`, `Defs.lean`, `Quantitative.lean`, and `Support.lean`.
- Updated `Transport/Simultaneous.lean` to use `SandwichProduct.sandwichProduct_isMeasurement`.
- Resolved the chapter 12 and bibliography conflicts, retaining both branches’ relevant material.
- Preserved the previously proved direct polynomial, coordinate soundness, and `k = 1` simultaneity declarations.
- Did not modify `MIPStarRE/QPBT.lean`; no re-export was needed.

## Evidence

- All 26 changed Lean files passed `lake env lean`.
- `lake env lean MIPStarRE/QPBT/Combining/DirectLowDegree/Transport/Simultaneous.lean` passed post-commit.
- `lake build MIPStarRE.QPBT.Combining.DirectLowDegree` passed after 9030 jobs.
- `python3 scripts/check_blueprint_latex.py` passed.
- `leanblueprint web` completed successfully; only existing bibliography warnings were emitted.
- Git hooks installation/check and the commit-time audits passed.
- `Simultaneous.lean` contains no `sorry` or `axiom`.
- `git status --porcelain --untracked-files=all` is empty.

## Statement integrity

The sandwich theorem statements and assumptions are unchanged; this is a namespace/API merge. Verdict: exact.

The direct tuple construction remains the existing documented `k = 1` scope restriction. The general paper conclusion for arbitrary `k` is not claimed by this merge; the restriction and its obstruction are recorded in `docs/paper-gaps/qpbt_ld-simultaneous-sandwich.tex`.

## Open obligations

Inherited proof holes remain in merged files:

- `exists_pasting_error` in `Games/Sandwich.lean`; the product-form error predicate needs the correction documented in `docs/paper-gaps/qpbt_pasting-product-error.tex` / issue #196.
- `exists_ld_soundness` in `Test/LowDegreeGameTheorems.lean`; the general game-correspondence and auxiliary-parameter proof is tracked in `docs/paper-gaps/qpbt_ld-dimension-divisibility.tex` / issue #16.
- `isCondLinear_pauliCL` in `Test/PauliBasisTest.lean`; issue #180.
- The `μ_prob` field of `pauliBasisTest`; inherited Pauli distribution normalization obligation.
- `pauliQuestionDistribution_eq_typedCL`, `pauliQuestionDistribution_symm`, `pauliWinPredicate_symm`, and `exists_spcc_value_one` in `Test/Completeness.lean`; Pauli completeness obligations tracked by issue #180.
- `exists_symmetric_projective_strategy` in `Games/StrategyClasses.lean`; finite-dimensional attainment is documented in `docs/paper-gaps/qpbt_symmetrization-attainment.tex` / issue #98.

The arbitrary-`k` simultaneity theorem additionally needs the unformalized combining reduction (Theorem 4.43 in the NEEXP source); no false proof or new `sorry` was introduced.

## Handoff

Review the merge-parent selection for the four `SandwichProduct` files and the qualified reference in `Transport/Simultaneous.lean` first. The branch is committed and clean; nothing was pushed.