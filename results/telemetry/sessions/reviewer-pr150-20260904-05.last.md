## Findings

- [ ] F1 (blocker) `MIPStarRE/QPBT/Test/LowDegreeGameTheorems.lean:327` — `ldQuestionDistribution_eq_typedCL` proves equality to an unconstrained constructor, not that `ldCL` is a typed conditionally linear family.
- [ ] F2 (blocker) `blueprint/src/chapter/ch13_qpbt_test.tex:395` — The new Pauli typed-CL node links an equality-only theorem with an untracked `sorry`, rather than a source-faithful typed-family obligation.
- [ ] F3 (changes) `MIPStarRE/QPBT/Games/DistributionAux.lean:203` — Consolidate this push-forward API with `DistributionMarginals.lean`, which already provides distribution extensionality, map composition, and uniform marginal infrastructure.
- [ ] F4 (changes) `MIPStarRE/QPBT/Test/LowDegreeGameTheorems.lean:117` — The new `chiIndex` fiber-count proof duplicates the existing exact seed-fiber decomposition in `Transport/Correspondence.lean`.

## Review

F1 remains from the previous round. `references/qpbt-paper/07_types.tex:84-93` defines a typed CL distribution only for typed families of conditionally linear maps. The low-degree paper identifies `L_Point`, `L_ALine`, and `L_DLine` as levels 1, 2, and 3 respectively at `08_classical_and_quantum_low_degree_tests.tex:203-237`.

The new proofs establish the latter two levels, but there is still no theorem for `ldPointCL` and no common-level `IsTypedCondLinearFamily` result. Moreover, `typedCLDistribution` at `Games/TypedCondLinear.lean:34-42` accepts arbitrary maps. Consequently the equality at line 327 is true for non-CL maps as well and cannot discharge `lem:ld-question-typed-cl`, despite its `\leanok`. Add a public point-level proof, expose level monotonicity, and prove a common-level family theorem, such as level 3, alongside the distribution equality.

F2 is the same unresolved defect for the newly added Pauli node. The paper says the Pauli question law is typed CL at `08_classical_and_quantum_low_degree_tests.tex:964-966,1084-1120`. Its linked theorem, `Completeness.lean:28-33`, again states only equality to the unconstrained constructor and ends in `sorry`; the needed `isCondLinear_pauliCL` prerequisite at `PauliBasisTest.lean:259-261` is also unfinished. Omitting `\leanok` avoids claiming a completed proof, but the linked public statement is still weaker than the source and neither hole cites a tracking issue. Introduce a source-faithful, tracked family-plus-equality obligation.

F3 arose from the latest main merge. `DistributionMarginals.lean:27-77` already owns extensionality, map composition, and uniform push-forward results for the same `lem:alnf`/`lem:dlnf` consumers. The new `DistributionAux` implementation establishes stronger variants but leaves two parallel APIs and proof strategies. Keep the stronger general results, but derive the older specialized wrappers from them or otherwise consolidate ownership.

F4 similarly duplicates `LdParams.seedFiberCard_pos`, `seedFiberProduct_eq`, `seedFiberEquiv`, and `seedFiberEquiv_fst` from `Combining/DirectLowDegree/Transport/Correspondence.lean:217-276`. Move that exact seed decomposition below both consumers and use it to prove uniformity of `chiIndex`; do not maintain a second lengthy arithmetic proof.

Earlier module-placement, line-representative support-node, citation, and wording findings are addressed. The previous low-degree typed-family finding remains as F1, and its Pauli analogue remains as F2. General prose and `\uses` observations are not reissued in this code-review lane.

`Lines.lean`, `LowDegreeGame.lean`, `LowDegreeGameTheorems.lean`, `DistributionAux.lean`, `DirectSumSupport.lean`, and `CondLinearTheorems.lean` type-check. `git diff --check` is clean. The only warning in an edited Lean file is the clearly documented `exists_ld_soundness` TODO tracked by issue #16.

VERDICT: CHANGES_REQUESTED