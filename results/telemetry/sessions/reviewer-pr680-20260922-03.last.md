## Findings

- [ ] F1 (changes) `docs/DEVIATIONS.md:263` — The completed c4 status still contradicts the summary at lines 530–532, which says the general-prime eigenvectors and projectors are absent.
- [ ] F7 (changes) `MIPStarRE/QPBT/Algebra/PrimePauliBasis.lean:55` — `addChar_sum` duplicates the identical helper in the directly imported `PauliTheorems.lean:303`.

## Review

**F1 remains partially unresolved.** The c4 table and completion-exemption record are corrected, but the later paragraph still describes `primePauliVec` and `primePauliProj` as absent. Update that paragraph to agree with the new declarations and the `no-difference` verdict. This is a documentation inconsistency, not an outstanding mathematical gap.

**F7 requires changes.** Both files contain the same general statement and induction proof of `addChar_sum`. Reuse one shared helper or apply Mathlib’s `map_prod` through `ψ.toMonoidHom`. I verified that the latter proves this statement using `toAdd_prod` and `toAdd_ofAdd`. Maintaining a second copy conflicts with the requested duplication criterion.

The other prior findings are resolved:

- **F2:** Shared orthogonality is extracted at `MIPStarRE/LDT/Preliminaries/FiniteFields.lean:222` and reused by both Pauli implementations.
- **F3:** `docs/paper-gaps/qpbt_characteristic-two-pauli-scope.tex:138` states the mathematical no-discrepancy verdict directly.
- **F4:** `blueprint/src/chapter/ch11_qpbt_algebra.tex:778` now cites full-space Fourier orthogonality.
- **F5:** The projector declarations have blueprint links at `blueprint/src/chapter/ch11_qpbt_algebra.tex:629`.
- **F6:** `MIPStarRE/QPBT/Algebra/PrimePauliBasis.lean:7` describes mathematical content.

The statement-integrity audit found no mathematical blocker. The definitions, eigenvalue identity, and Fourier expansions preserve the negative eigenvector phase, positive expansion phase, and negative inverse phase in `references/qpbt-paper/04_preliminaries.tex:1107`, `:1113`, and `:1157`. The finite-field typeclasses and nonempty coordinate domain faithfully encode the source and blueprint assumptions. No bridge hypotheses or weakened conclusions were introduced. The extracted orthogonality agrees with `prop:fourier-fact-vector` in `references/ldt-paper/preliminaries.tex:65`.

Validation passed for `FiniteFields.lean`, `Pauli.lean`, `PrimePauliBasis.lean`, and the `QPBT.lean` umbrella. The new module also passed with Mathlib’s standard linters enabled. All eleven new public theorems depend only on `propext`, `Classical.choice`, and `Quot.sound`; the proof-hole scan found nothing.

The checkout remains unchanged. Full builds and blueprint rendering were not rerun; this session was triggered by green CI for the reviewed head. The canonical `lean-conventions` skill was unavailable, so the supplied rules and checked-in policies were used.

VERDICT: CHANGES_REQUESTED