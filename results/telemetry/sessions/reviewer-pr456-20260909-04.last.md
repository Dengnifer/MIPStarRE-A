## Findings

- [ ] F1 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:967` — Replace the undefined `\lnf` command here and at line 1391 with the blueprint’s existing `\mathrm{Ln}` notation.

## Review

Reviewed the authoritative patch for `85a5d78b7c9ed98fd70e025cda93cb10531a176e`.

F1 concerns compilation: neither the shared blueprint macros nor its print/web macros define `\lnf`. Both new theorem statements therefore introduce an undefined LaTeX command. Use `\mathrm{Ln}`, as in `def:line-representative`.

During review, the checkout advanced to `2d5ae7f16ef461ad78b0737373d0fe816403d5d2`, which contains exactly these two replacements. F1 is fixed in that later commit; this verdict remains bound to the requested earlier SHA.

All seven prior findings are addressed:

| Prior finding | Resolution |
|---|---|
| Code F1 | `SubLinePrefix.lean:11` correctly describes preceding blocks vanishing. |
| Code F2 | `SubLineUniform.lean:128` supplies the shared resampling lemma used by both components. |
| Code F3 | `ConsistencyPositivity.lean:48` reuses `Quantum.reindex_nonneg`. |
| Prose F1 | Blueprint `thm:direct-sub-line-distribution` states the directly indexed result separately. |
| Prose F2 | Restricted-distribution entries are updated; the given-measurement consistency estimate has its own auxiliary entry. |
| Prose F3 | Blueprint `cor:line-point-parameter-resampling` covers all three component identities. |
| Prose F4 | `RestrictedMixture.lean:142` describes the scalar-coordinate marginal mathematically. |

No mathematical or Lean proof defect found. The resampling identities preserve the joint law, including zero directions, and introduce no proof-obligation hypotheses. They agree with the auxiliary blueprint statements and the distribution in `references/qpbt-paper/08_classical_and_quantum_low_degree_tests.tex:274–287`, `def:line-point-dist`.

`restricted_lines_consistency_bound` retains the universal factor `4` from the paper’s restriction argument at `14_analysis_of_the_pauli_basis_test.tex:1049–1061`. Its auxiliary blueprint statement explicitly assumes the given measurements. `exists_subLineWitness` matches its new directly indexed auxiliary entry; the source-labelled `lem:qld-sublines` remains unfinished, with the transport gap explicit.

Six affected modules passed individual Lean checks. Axiom audits of twelve completed declarations found only `propext`, `Classical.choice`, and `Quot.sound`. The two existing combined-line construction `sorry`s remain unchanged; the audited results do not depend on them.

Full project and blueprint builds were not rerun. The requested `lean-conventions` reference was unavailable in the searched skill locations; repository review and integrity instructions were applied.

VERDICT: CHANGES_REQUESTED