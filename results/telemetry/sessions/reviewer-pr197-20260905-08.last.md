## Findings

- [ ] F1 (blocker) `local/bin/checked-push.sh:33` — Empty porcelain status accepts sparse or skip-worktree checkouts, allowing validation of an incomplete tree before publishing the full commit

## Review

The prior tag findings are resolved: both validated and emergency-bypass pushes disable `push.followTags` and pass `--no-follow-tags`, with regressions covering both paths.

F1 still breaks the checkout-binding invariant. [Git documents](https://git-scm.com/docs/git-sparse-checkout.html) that sparse checkout omits tracked files while status remains clean; this repository enables that mode in `local/bin/review.sh:1062`. The hook then skips an absent changed Lean file at `.githooks/pre-push:80`, while tree-walking audits likewise cannot inspect omitted paths. Consequently, `require_validation_checkout` can approve an incomplete filesystem and publish unchecked commit bytes.

Reject sparse, skip-worktree, and assume-unchanged entries before and after preflight, or validate from a fully materialized immutable checkout. Add a regression where a changed payload is excluded and verify transport never starts. At the round cap, this must be fixed or explicitly deferred through the §12 tracked-issue adjudication.

Static shell parsing, Python AST parsing, and `git diff --check` passed. The push-based unit suite was not rerun because this review contract prohibits `git push`; exact-head local CI is reported green.

VERDICT: CHANGES_REQUESTED