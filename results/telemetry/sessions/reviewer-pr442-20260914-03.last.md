## Findings
- [ ] F7 (changes) `MIPStarRE/QPBT/Combining/Lines/PointSelfConsistency.lean:31` — Reuse the existing `avgOver_mix` theorem instead of duplicating it under a private implementation name.
- [ ] F8 (changes) `MIPStarRE/QPBT/Combining/Lines/OptionPostprocessDistance.lean:45` — Reuse or relocate the public `ProjectiveSetting.place_zero` lemma instead of reproving it locally.
- [ ] F9 (changes) `MIPStarRE/QPBT/Combining/Points/Consistency.lean:99` — Delete the unused projective-only square-summability theorem because the existing POVM theorem is strictly stronger.
- [ ] F10 (changes) `MIPStarRE/QPBT/Combining/Points/Closeness.lean:62` — Replace the unused placed square-summability theorem with the existing POVM theorem applied to `S.placedMeasurement`.
- [ ] F11 (changes) `MIPStarRE/QPBT/Combining/Points/Sandwich.lean:172` — Delete the unused point-specific square-summability theorem and use the existing placed-measurement API.
- [ ] F12 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:857` — Update the cited combined-points paper-gap note, which still claims the blueprint uses the binary-refinement proof.

## Review
The six prior findings are addressed: the Parseval description now states column orthogonality, the distance and operator-action proofs reuse shared APIs, `eprCrossSwap` is distinguished from `U_\sigma U_\theta`, and the blueprint now documents both the direct combined-point proof and the two line-point support theorems.

F7 duplicates `avgOver_mix` from `Combining/Lines/RestrictedAverage.lean:60-84` statement-for-statement. Import and reuse that theorem, or move the generic distribution identity into `LDT/Basic/DistributionAvg.lean`. This also permits replacing the broad `Combining.Points` dependency with focused imports.

F8 reproduces `ProjectiveSetting.place_zero` from `Observables/LineMeasurement/LinePointOverlap.lean:63-67`. Since this is general placement algebra, it should live in `Combining/Points/PlacementSupport.lean` and be reused by both modules.

F9-F11 are all subsumed by `measurement_sum_adjoint_mul_le_one` in `Games/DistanceTheorems.lean:367-377`, which applies to every POVM without projectivity. The three new declarations are unused. The placed forms compile directly by applying that theorem to `S.placedMeasurement p M` and simplifying with `ProjectiveSetting.placedMeasurement_effect`.

F12 is a mathematical-documentation inconsistency. The revised blueprint explicitly gives the direct field-valued sandwich proof at lines 857-912, while `docs/paper-gaps/qpbt_combined-points-direct.tex:216-218` still says, “The blueprint retains the source's ... binary-refinement proof.” The paper-gap policy requires these notes to describe the current blueprint and formal proof accurately.

I compared the two new blueprint-linked statements with `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:693-697` and `:936-963`. Their sampling laws, placements, outcome alphabets, and error bounds match the extracted formalization-support claims; no proof-debt hypothesis or source-statement blocker was introduced.

Verification completed: all three new Lean files type-check, `git diff --check` passes, no forbidden proof-integrity token was added, and the new declarations' axiom closures contain only standard classical and quotient axioms.

VERDICT: CHANGES_REQUESTED