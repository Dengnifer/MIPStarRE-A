## Findings

- [ ] F1 (changes) `blueprint/src/chapter/ch13_qpbt_test.tex:117` — The proof omits two lemmas directly invoked by the Lean proof.
- [ ] F2 (changes) `blueprint/src/chapter/ch13_qpbt_test.tex:69` — The point-map proof omits the definition of the map it unfolds.
- [ ] F3 (changes) `blueprint/src/chapter/ch13_qpbt_test.tex:432` — The component-level statement cites the typed-family definition instead of conditional linearity.
- [ ] F4 (changes) `MIPStarRE/QPBT/Test/PauliBasisTest.lean:365` — The answer-alphabet docstring retains a stale blueprint range.
- [ ] F5 (changes) `blueprint/src/chapter/ch12_qpbt_games.tex:1373` — “as in the linked declaration” is formalization jargon in reader-facing prose.
- [ ] F6 (changes) `MIPStarRE/QPBT/Games/TypedCondLinear.lean:22` — “concrete source-facing uses supply” describes implementation organization rather than mathematics.
- [ ] F7 (changes) `MIPStarRE/QPBT/Test/LowDegreeGameTheorems.lean:252` — “completed distribution-identity component” encodes proof status in a mathematical docstring.

## Review

F1: `ldQuestionDistribution_eq_typedCL` invokes `bind_uniformDistribution_map` at `LowDegreeGameTheorems.lean:276` and `Distribution.map_map` at line 290. Add `lem:uniform-bind-map` and `lem:distribution-map-comp` to the proof’s `\uses`.

F2: The construction of `ldPointLinear` unfolds `ldPointCL` at `LowDegreeGame.lean:461` and line 464, and the theorem uses that construction at line 472. Add `def:ld-question-distribution`, matching the neighboring axis-line and diagonal-line proofs.

F3: The linked Lean signature at `PauliBasisTest.lean:267` concludes `IsCondLinearOn`, not `IsTypedCondLinearFamily`. Replace `def:typed-cl-functions` with `def:cl-func`; the typed-family definition belongs to the subsequent common-level lemma.

F4: The win-predicate definition now occupies blueprint lines 470–506, not 460–496. Update the citation accordingly.

F5: Replace “as in the linked declaration” with mathematical prose, for example: “Finiteness plays no part in this definition; hence the type set may be arbitrary.”

F6: Replace the quoted phrase with: “This definition permits an arbitrary type `T`; the low-degree and Pauli type sets are finite.”

F7: Drop the status word: “This is the distribution identity in `lem:ld-question-typed-cl`; the common-level family assertion is stated separately.”

All eight prior-ledger findings have been addressed. In particular, the earlier omission at blueprint line 117 was fixed by adding `def:ld-question-distribution`; F1 above is distinct because two directly invoked helper lemmas remain absent. The Pauli proof frontier is now split into source-shaped declarations and correctly carries no `\leanok`. The completed low-degree statements and their status marks match their Lean signatures.

Summary: 0 mathematical-equivalence mismatches, 3 `\uses` inaccuracies, 1 stale citation, and 3 prose issues.

VERDICT: CHANGES_REQUESTED