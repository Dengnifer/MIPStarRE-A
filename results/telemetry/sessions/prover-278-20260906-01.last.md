## What changed

Proved the adapter in [RoundingTransport.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-278-rounding-transport/MIPStarRE/QPBT/Games/DistanceTheorems/RoundingTransport.lean): simultaneous projective roundings, universal question-dependent maps, and both orientations by explicit factor exchange.

Exact bound: `δ + sqrt(220 * δ^(1/4)) + 2 * sqrt(δ + η)`, with no cardinality factor.

## Evidence

Single-file compilation passed without warnings. No `sorry|axiom` matches. All six public declarations have only `propext`, `Classical.choice`, and `Quot.sound` in their axiom closure.

Full build and exact-head CI were not run.

## Statement integrity

Paper: normalized state, complete consistent POVMs, same-space projective rounding. Lean derives both roundings and transport using explicit probability normalization and comparison bounds.

Verdict: exact derived auxiliary; no existing public signature changed. Full [audit](/home/drx/MIPStarRE-qpbt/.worktrees/issue-278-rounding-transport/audits/2026-09-06_rounding_transport.md).

## Open obligations

None in the new module. The global-witness construction remains separate and open.

## Handoff

Files remain **uncommitted**: staging failed because Git’s `index.lock` is on a read-only filesystem. Existing hook verification passed. Main must commit, publish through the checked workflow, and arrange exact-head CI and independent review.

Elapsed: **1,103 seconds** of 2,400. Available-token count is not exposed. B8 remains unchanged.