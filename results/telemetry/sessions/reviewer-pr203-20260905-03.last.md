## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT/Games/Sandwich/Defs.lean:44` — Remove the unused `SandwichInternal` compatibility layer; it creates pass-through public names that never existed on the base branch
- [ ] F2 (changes) `MIPStarRE/QPBT/Games/Sandwich/Support.lean:631` — Consolidate the new generic measurement helpers with existing lower-layer copies instead of adding duplicate proofs
- [ ] F3 (changes) `MIPStarRE/QPBT/Games/Sandwich/Defs.lean:1` — Restore direct import layering; the definitions leaf imports the full theorem aggregate while downstream files rely on its transitive imports

## Review

F1: `SandwichInternal` appears only at its declaration sites here, at `Support.lean:902-982`, and at `Quantitative.lean:788-796`. The merge base had no such public namespace; even `sandwichProductCore` was private. These aliases therefore preserve no released compatibility contract. They violate the explicit rule in `docs/mathematical_language.md` against empty pass-through names without a paper-facing or blueprint-facing purpose. Delete the namespace and aliases.

F2: Several public, chapter-local helpers reproduce existing proofs:

- `measurement_sum_adjoint_mul_le_one` duplicates `DistanceTheorems.lean:364`.
- `measurement_pair_fiber_sum_adjoint_mul_le_one` duplicates `DistanceTheorems/TensorSupport.lean:162`.
- `postprocess_effect_mul_effect` duplicates `DistanceTheorems/TensorSupport.lean:233`.
- `postprocess_effect_of_injective` duplicates `PerfectStrategy/Measurements.lean:99`.

The repository’s shared-helper policy requires these to be promoted or generalized in a common lower module and reused at all call sites. Blueprint-facing names may remain as documented thin wrappers, but duplicate proof bodies must be removed.

F3: `Sandwich/Defs.lean` uses neither the distance-theorem API nor `IsPolyErr₂`, yet imports both aggregates. `Support.lean` then obtains the distance calculus transitively through `Defs.lean`, while the root module obtains `ErrorFunctions` transitively even though only it uses `IsPolyErr₂`. The split should give `Defs.lean` a definition-level import, give `Support.lean` a direct calculus import, and import `ErrorFunctions` directly in `Sandwich.lean`.

The source audit otherwise passes. `consistencyDefect_sandwich_le` matches `lem:ld-sandwich` at `references/qpbt-paper/06_nonlocal_games_and_mipstar.tex:466-494` and the blueprint at `ch12_qpbt_games.tex:469-496`. Its additional probability, normalization, finiteness, nonemptiness, decidability, and nonnegative-error hypotheses are faithful boundary conditions; no proof-debt input was added. The conclusion faithfully makes the hidden constant explicit as the universal choice `C₀ = 8`. The corrected dependent index pairing agrees with the original Fact 4.34 at `references/neexp-paper/05_quantum_preliminaries.tex:965-968`.

The new pasting paper-gap note correctly compares `lem:pasting`, the product convention at `references/qpbt-paper/04_preliminaries.tex:26-29`, and `IsPolyErr₂`. Its fixed-positive-δ counterexample is valid. The existing `exists_pasting_error` `sorry` is clearly marked, tracked by issue #196, and not newly introduced.

Targeted Lean checks for `Sandwich/Support.lean`, `Sandwich/Quantitative.lean`, and `Sandwich.lean` succeeded. The new sandwich theorem’s axiom closure contains only `propext`, `Classical.choice`, and `Quot.sound`; there is no `sorryAx`.

VERDICT: CHANGES_REQUESTED