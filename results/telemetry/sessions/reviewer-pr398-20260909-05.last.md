## Findings
- [ ] F1 (blocker) `blueprint/src/chapter/ch14_qpbt_observables.tex:1755` — The `\leanok` lemma claims a linear `ε` bound for item 2, but every linked public Lean signature exposes only the common `√ε` bound.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/Points/Closeness.lean:38` — The five public final-distance and error lemmas used by the combined-point proof lack a blueprint support entry and corresponding `\uses` edge.
- [ ] F3 (changes) `MIPStarRE/QPBT/Combining/Points.lean:7` — “states the construction obligation” is stale now that `exists_combinedPointsWitness` is proved.
- [ ] F4 (changes) `MIPStarRE/QPBT/Combining/Lines.lean:39` — “This Lean-only conditional form” uses Lean-specific prose instead of the prescribed “formalization-only auxiliary.”
- [ ] F5 (changes) `blueprint/src/chapter/ch14_qpbt_observables.tex:1851` — “The formalization uses” and “the Lean proof” describe implementation rather than the mathematical argument.

## Review

**F1:** `deltaLine ε` is definitionally `√ε` at `LineMeasurement.lean:71`, and `expLine_point_cons` concludes only a bound by `C * deltaLine ε` at line 201. `ExpandedLineConclusions` and `exists_deltaLine` merely package that signature. The linear estimate remains local to the proof body, so it cannot justify the stronger sentence inside the blueprint lemma. Move that sentence into the proof and describe it as an intermediate linear estimate, or expose and link a public theorem with the stronger signature. Until then, both `\leanok` tags overstate the formalized result.

**F2:** The missing declarations are `opFamilyDistSq_uniform_le_four`, both product square-summability lemmas, `ProjectiveSetting.chain_bounds`, and `error_terms_le_rpow_eighth`. They are invoked directly at `Points.lean:129`, `:141`, and `:190-211`, but the proof dependency list at `ch15_qpbt_combining.tex:1036` has no node representing them. Add a separate formalization-support entry with `\leanok`, then add its label to that `\uses`.

**F3:** Replace the opening with “This module constructs the projective joint X/Z point measurements and defines their scalar linear coarse-graining.”

**F4:** Replace the phrase with “This formalization-only auxiliary fixes the point-error function and its polynomial bound before choosing the line-error function.”

**F5:** Use mathematical prose such as “An equivalent argument reverses the order of the estimates” and “Thus this argument does not require the symmetric-equivalent transfer lemma…”

The combined-point source statement, combined-line POVM entries, and coefficient-representative outcome convention otherwise agree with their Lean declarations. The `\notready` status on `lem:qld-xz-lines` remains appropriate.

Summary: **1 mathematical-equivalence mismatch, 1 blueprint traceability/dependency gap, and 3 prose issues.**

VERDICT: CHANGES_REQUESTED