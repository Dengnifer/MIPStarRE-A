## Findings
- none
## Review
The prior six findings are resolved. The diagonal adapter now uses reversed coordinates and the direction’s leading index, zero-direction answers retain their constant value, shared distribution/incidence lemmas are reused, transport modules are re-exported, Born weights use `outcomeWeight`, and the line-representative citation is accurate.
The new declarations introduce no source-labelled statement drift or proof-debt assumptions. The coordinate reversal agrees with `references/ldt-paper/test_definition.tex:49-65`, while canonical incidence agrees with `blueprint/src/chapter/ch11_qpbt_algebra.tex:497-517`. The nonzero-pivot exception is explicitly documented and no exact mature diagonal sampling-law claim is made; its probability loss remains work for the later pass-conversion layer.
`Transport/Strategy.lean` and `DistributionMarginals.lean` type-check successfully. Changed files contain no `sorry`, axioms, prohibited proof bypasses, placeholders, or debug commands; formatting and diff checks pass. The external `lean-conventions` skill was unavailable, so the checked-in integrity and review policies were used.
VERDICT: APPROVED
