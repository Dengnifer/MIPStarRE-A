## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Points.lean:1` — Import the marginal theorem module into the public API and default build graph.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/Points/Placement.lean:289` — `eprCrossSwap` is not the blueprint permutation `U_σ U_θ`.
- [ ] F3 (changes) `MIPStarRE/QPBT/Combining/Points/Commutation.lean:35` — The cited paper-gap note does not document the replacement proof attributed to it.
- [ ] F4 (changes) `MIPStarRE/QPBT/Combining/Points/Sandwich.lean:44` — Reuse the existing operator-product application lemma instead of duplicating it.
## Review
**F1: Public Imports**
Nothing imports `WitnessMarginals.lean`, leaving it and its absorption/contraction dependencies outside the root import graph. With the current root-only Lake configuration, ordinary `lake build` will not check these leaves. Importing `MIPStarRE.QPBT.Combining.Points` and checking either marginal theorem produces `Unknown constant`. Import `Points.WitnessMarginals` from the public module; its dependencies do not create an import cycle.
**F2: Incorrect Identification**
Blueprint `lem:symmetric-equivalents-transfer`, at `blueprint/src/chapter/ch14_qpbt_observables.tex:1413`, defines
`σ = (A B)(A' B')(A'' B'')` and `θ = (A B)(A' A'')`.
Their product is a four-cycle on the ancillas. By contrast, `eprCrossSwap` implements `(A' B'')(A'' B')`, an involution. These are different permutations.
The Lean invariance proof is valid for the implemented permutation, but the identification is false and is repeated in the module references and transfer-theorem docstrings. Describe this as a distinct auxiliary permutation supporting symmetric equivalents, without claiming it is the blueprint’s `U_σ U_θ`.
**F3: Replacement-Proof Documentation**
The new modules repeatedly attribute the field-valued Parseval, sandwich, and rounding argument to `docs/paper-gaps/qpbt_linearity-theorem-quotation.tex`. That note instead discusses the original linearity route and its unresolved common-extension construction, particularly at line 118 onward. It contains no account of this replacement argument.
The source’s binary-refinement construction is in `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:711`, following `lem:qld-4-10`. Update or add the mathematical note required by `docs/paper-gaps/policy.tex:34`, explaining the replacement argument, dimension-independent error, and why its proof avoids the original route’s ancilla obligation. Correct the new citations accordingly.
**F4: Duplicate Infrastructure**
`applyOperatorToState_mul'` has exactly the statement of `DistanceCalculus.applyOperatorToState_mul` in `MIPStarRE/QPBT/Games/DistanceTheorems/Support.lean:64`. The existing theorem is already available through this file’s imports; I verified it directly proves the new statement. Remove the duplicate and use the existing declaration in its consumers.
**Statement Integrity**
`exists_combinedPointsWitness` preserves `lem:qld-4-10`: admissible parameters and a winning projective strategy produce projective measurements on the original expanded local spaces, with both ordered-product comparisons and all directed opposite placements. The universal `K ε^(1/8)` realizes the existing polynomial-error convention without additional proof-debt assumptions.
The marginal estimates faithfully implement `eq:qld-qxz-close-to-point` and its Z counterpart at paper lines 917–935. They retain the supplied witness error as `4 * δQ` and assume no same-player X/Z commutation.
**Verification**
Single-file checks passed for `Points`, `WitnessMarginals`, `Commutation`, `Consistency`, and `Orthonormalization`. The construction and both marginal theorems depend only on `propext`, `Classical.choice`, and `Quot.sound`; no proof holes were found in the changed files. The truncated attachment was supplemented from disk. No full build was rerun, and the checkout remains unchanged.
VERDICT: CHANGES_REQUESTED
