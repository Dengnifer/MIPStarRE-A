## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Claims.lean:44` — Reuse the shared placement, product-average, completed-effect, and line-answer regrouping APIs instead of maintaining parallel private implementations.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/UniformLinePoint.lean:36` — Remove the duplicate bind and uniform-projection proofs and the no-op `_current` alias in favor of this PR’s shared support lemmas.
- [ ] F3 (changes) `docs/paper-gaps/qpbt_ld-dimension-divisibility.tex:440` — Identify `SubLineWitness.source_mixture` as using refined seed-bearing restrictions, not the paper’s source restricted line-point laws.
- [ ] F4 (changes) `docs/paper-gaps/qpbt_ld-dimension-divisibility.tex:460` — State accurately that the joint point witness is projective while the paired-line witness is only complete.

## Review

F1: `Claims.lean` duplicates `ProjectiveSetting.placedMeasurement`, `avgOver_prod`, the two `pointMeasExpOption_effect_*` lemmas, and essentially all of `regroup_line_answer_sum`. The existing declarations are in `Points/PlacementSupport.lean:258-313`, `Lines/RestrictedAverage.lean:89`, and `SubLineZDeficit.lean:46-130`; the last module is already imported. These parallel implementations create two APIs for the same tensor-placement and fiber-regrouping facts and must be consolidated.

F2: `Distribution.bind_const_current` duplicates `Distribution.bind_const` from `Lines/SubLineBind.lean:81`. The public `_current` theorem merely aliases `uniformDistribution_map_lineRepMap_add_smul`, while `uniformDistribution_map_projX_projZ_pauli_current` re-proves `Lines/SubLineTransport.lean:106` modulo the existing uniform-product identity. The import graph permits using those support modules without a cycle.

F3: The formal components use `restrictedALineDist` and `restrictedDLineDist`, the refined seed-bearing restrictions. Blueprint `def:ith-restricted-line-refined`, lines 1236-1260, explicitly distinguishes these from their source push-forwards and notes that the axis map forgets the residual seed. Calling them “restricted source line-point laws” incorrectly suggests that this unresolved transport has occurred.

F4: `CombinedPointsWitness.projective` establishes projectivity of `Q`, but `CombinedLinesWitness.T` at `Witnesses.lean:142-148` is only a complete `Measurement`. The blueprint correctly states “projective joint point measurements” and “complete paired-line measurements” at lines 1869-1874. The paper-gap note must use the same precise hypotheses.

The earlier source-fidelity blockers are addressed: the directly indexed real-part estimates now have separate auxiliary blueprint nodes, while source Claims 17-1 and 17-3 remain uncertified pending carrier transport and the complex estimate. The restricted seed-bearing laws are also separated from the source definitions.

The supplied exact-head CI is green. `git diff --check` passes, and the only changed-file `sorry` occurrences are the clearly documented existing obligations in `Claims.lean:522` and `Lines.lean:61,90`.

VERDICT: CHANGES_REQUESTED