## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Extraction/Consistency.lean:101` — Reuse the existing measurement-placement API instead of duplicating its construction and projectivity proof.
## Review
F1 remains unresolved; the two prior code-ledger entries describe the same issue. `placedMeasurement`, its effect lemma, and `placedMeasurement_isProjective` duplicate `ProjectiveSetting.placedMeasurement` and its associated results in `MIPStarRE/QPBT/Combining/Points/Placement.lean:256`. That API is already available through the imports and handles the same four placements and heterogeneous local spaces. Use it directly and remove the redundant coordinate construction and proofs. Maintaining both implementations creates unnecessary proof dependencies and maintenance work. This requires changes before ordinary approval.
The checked mathematical statements preserve the relevant contracts:
- `evalOpt_uniform_parameter_collision_le` explicitly restricts to nonzero directions and derives the stated degree/cardinality bound from polynomial root counting. It imposes no unnecessary degree-versus-field-size restriction.
- `exists_pasting_error` retains the hypotheses printed in `references/qpbt-paper/06_nonlocal_games_and_mipstar.tex:504`, label `lem:pasting`. The stronger forward-comparison theorem has a separate statement.
- `exists_subLineWitness` explicitly documents its directly indexed carrier and the outstanding transport to the source distribution; it does not claim that transport is complete.
Prior-ledger triage: prose F2 concerns the unchanged readiness marker; F3 concerns the existing coordinate-index presentation; F4 concerns the existing blueprint linkage to the witness-based restriction theorem. The changed Lean proof adds no hypothesis to that theorem. These remain matters for the prose lane/operator, not resolved findings here. F5–F11 are likewise within the expressly excluded prose category and are not duplicated in this code ledger.
Validation passed for `UniformAffineCollision.lean`, `Extraction/Consistency.lean`, and the QPBT aggregate import. Seven principal theorem axiom closures contained only `propext`, `Classical.choice`, and `Quot.sound`. Extraction emitted five existing `sorry` warnings, each explicitly tracked under issue #47; the patch adds no proof holes or prohibited proof primitives.
The attachment was truncated, so source comparisons used the authoritative on-disk patch. Validation used existing compiled imports, not a clean full rebuild. The documented extraction and source-carrier obligations remain residual proof debt.
The review cap has been reached. Under §12, the operator must repair F1 or defer it to a tracked issue through adjudication for this exact head.
VERDICT: CHANGES_REQUESTED
