## Findings
- none

## Review
Reviewed the complete authoritative patch for PR #238 at `954c1bc004aae12cfff60a8a27b8eac6259f5327`. No actionable correctness or local-contract regressions found.

- `local/bin/dispatch.sh:629` normalizes effort after account/model resolution, before the `mathfix` guard and CLI argument construction. Explicit non-default Astra efforts and non-Astra requests remain unchanged.
- `local/bin/dispatch.sh:729` records the normalized request and preserves it in telemetry-replay instructions.
- `local/bin/telemetry.py:361` keeps `requested_effort` optional, preserving compatibility with legacy callers and historical rows. Documentation distinguishes requested effort from provider-measured behavior.

Validation: all **28 dispatcher/account-router regression tests passed**. Shell syntax checks for both changed scripts and `git diff --check` passed. The checkout matches the authoritative patch and remains unchanged.

Residual limitation: tests use mocked Codex binaries; actual provider behavior was not independently probed. No Lean or blueprint changes require mathematical validation.

VERDICT: APPROVED