## Findings

- [ ] F1 (changes) `blueprint/src/chapter/ch11_qpbt_algebra.tex:775` — The proof cites subspace cancellation, but the Lean proofs use full-space vector Fourier orthogonality instead.
- [ ] F2 (changes) `MIPStarRE/QPBT/Algebra/PrimePauliBasis.lean:210` — Three new public projector theorems lack blueprint traceability.
- [ ] F3 (changes) `MIPStarRE/QPBT/Algebra/PrimePauliBasis.lean:7` — “completes the ... layer” describes formalization progress rather than the mathematical content.

## Review

The source-facing signatures are mathematically faithful. `Fact p.Prime`, the finite field with its `ZMod p` algebra, and the finite coordinate type encode the paper’s field and dimension domain; `Nonempty ι` matches \(n\geq1\). The negative eigenvector phase, positive forward coefficient, and negative inverse coefficient all agree with the source. No bridge or proof-obligation hypothesis appears. The new statement-level `\leanok` marks are therefore justified, and the proof-level mark has no direct proof hole or nonstandard axiom dependency.

**F1:** The blueprint proof lists `lem:cancellation` and describes that argument. The implementation instead derives `sum_ffChar_dotProduct` through `fourier_fact_vector` at line 97 and applies it at lines 407 and 499. Neither linked theorem invokes `ffChar_dotProduct_submodule_expect_eq_zero`. Change the dependency to `prop:fourier-fact-vector` and describe the entrywise calculation with full-space Fourier orthogonality, or refactor the proof to use the cited cancellation theorem.

**F2:** `primePauliProj_mul_primePauliProj`, `primePauliProj_conjTranspose`, and `posSemidef_primePauliProj` are new public results, but none has a `\lean{...}` entry. Link them under `def:generalized-pauli` or a subordinate formalization-support entry. If the first two are intended only as proof auxiliaries, making them private also resolves the traceability issue.

**F3:** Replace the progress-oriented opening with mathematical prose, for example: “Generalized Pauli observables in arbitrary prime characteristic have the common eigenvectors and rank-one projectors defined below. Their orthogonality, projective-measurement properties, and Fourier expansions are established here.”

Summary: 0 mathematical-equivalence mismatches, 1 inaccurate proof dependency/sketch, 1 missing-traceability issue covering 3 declarations, and 1 prose issue.

VERDICT: CHANGES_REQUESTED