## What changed
- Committed as `ff4c92d`; worktree is clean.
- Proved pointwise and averaged supported-reference mass bounds in `MIPStarRE/QPBT/Games/SupportMass.lean:41` and `MIPStarRE/QPBT/Games/SupportMass.lean:86`.
- Added public import integration in `MIPStarRE/QPBT.lean:26`.

## Evidence
- `lake env lean` passes for both edited Lean files.
- A scratch example successfully composes the pointwise bound with the existing codeword-collision comparison.
- `sorry|axiom` scan: no matches. Both axiom closures contain only `propext`, `Classical.choice`, and `Quot.sound`.
- Commit hooks pass. Full build skipped because other CI jobs held the machine-wide lock.

## Statement integrity
No paper-labelled theorem changed. The new bounds use complete POVMs on opposite tensor factors, explicit reference support, and the same state vector. They hold for normalized states—and, more generally, arbitrary vectors—without projectivity or symmetry assumptions.

## Open obligations
None in the new module. `nonencodingMarginalMass_le`, global-witness existence, and excluded construction work remain untouched.

## Handoff
The source comparison and intended application are documented in `audits/2026-09-06_supported-reference-mass.md:41`. Full-build validation and independent review remain before merge.