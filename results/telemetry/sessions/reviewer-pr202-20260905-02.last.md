## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Linearity/Defs.lean:104` — A legacy numeric blueprint locator remains in a Lean file touched by this migration.
- [ ] F2 (changes) `MIPStarRE/QPBT/Test/PauliBasisTest.lean:63` — `AdmissibleParams.toLdParams` is attributed to `thm:pauli`, but the replaced span is `def:ld-game`.
- [ ] F3 (changes) `scripts/blueprint_citations.py:207` — The scanner treats arbitrary backtick tokens containing `:` as blueprint labels, producing false unknowns.
- [ ] F4 (changes) `local/bin/review.sh:602` — Prefix byte truncation removes the unresolved and duplicate rows from the citation evidence.
- [ ] F5 (changes) `local/bin/review.sh:830` — The no-dispatch fallback places the citation map after the diff, contrary to the map-first budget contract.
- [ ] F6 (changes) `scripts/blueprint_citations.py:389` — The rewrite command collapses duplicate labels even when no legacy locator was replaced, mutating non-legacy text.

## Review

Changes requested. The migration is broadly effective, and the migration hunks do not introduce new Lean proof holes, axioms, or kernel bypasses. The existing conditional extraction result retains its explicit `**Unfaithful:**` marker. The remaining problems are citation correctness and review-tool behavior.

`isBinaryObservable_one_heteroKron` still stores `blueprint/src/chapter/ch13_qpbt_test.tex:224-253`. The new policy in `AGENTS.md` explicitly forbids numeric blueprint ranges in Lean docstrings. The current `thm:ms-rigidity` node is at `ch13_qpbt_test.tex:266-291`; the resolver can safely rewrite this line to `blueprint \`thm:ms-rigidity\``.

The `AdmissibleParams.toLdParams` docstring migrated `ch13_qpbt_test.tex:17-31` to `thm:pauli`. That span is the `def:ld-game` definition at `ch13_qpbt_test.tex:17-32`, while `thm:pauli` is at lines 461-478. The cited paper passage at `references/qpbt-paper/08_classical_and_quantum_low_degree_tests.tex:958-961` is instead `def:admissible`. This is a mathematically incorrect source anchor; use `def:ld-game` for the low-degree view and reserve `def:admissible` for the parameter package.

`find_citation_uses` runs `CODE_LABEL_RE` over every backtick token and treats any unmatched token containing a colon as an unknown blueprint label. The changed-file scan consequently reports path references such as `CONTRIBUTING.md:122-124`, paper-only labels such as `lem:efficient_basis`, paper equation labels, and the script's own fixture labels. This makes an otherwise valid resolver exit with status 1 and hides genuine unknown blueprint labels. Restrict detection to explicit blueprint citation contexts (while retaining bare-label checking where intended) and add regressions for paper/path references.

The resolver output is about 80 KB, but `_format_resolutions` emits every `cited_at` occurrence and `_resolve_command` appends unknown labels after known labels. `review.sh:602` then keeps only the first 30,000 bytes. The attached map therefore ends before the unresolved rows, despite `local/protocols/review.md:126-133` promising that unknown and duplicate labels remain visible. Deduplicate or compact origins and reserve space for failure rows, then test a capped map containing those rows.

The dispatcher path correctly attaches the map first, but `build_standalone` writes the diff at lines 830-832 and the map only at lines 833-836. A large diff can therefore consume the fallback prompt budget before the required evidence. Move the bounded map before the diff and add a fallback-order regression.

Finally, the duplicate-collapse substitutions at lines 389-416 run regardless of whether `replacements` is empty. For example, `rewrite_text("/-- See \`lem:alpha\` (\`lem:alpha\`). -/", {})` becomes `blueprint \`lem:alpha\``. That exceeds the documented “conservatively rewrite legacy locators” behavior and can alter already-migrated or paper-only prose under `--write`. Scope duplicate cleanup (and any formatting pass) to actual locator rewrites, or explicitly redefine the command as a broader canonicalizer and test that contract.

Round-1 findings concerning the unfaithful extraction marker, exact/proximity candidate selection, the Decoding and StrategyClasses citations, raw-map sanitization, and line lengths are addressed. Round-1’s scanner finding remains F3. Its map-order finding is only partly addressed: dispatcher ordering is fixed, but truncation and the standalone fallback remain as F4 and F5.

The supplied `local-ci/summary` is successful, and `git diff --check` plus `bash -n local/bin/review.sh` pass. I could not complete the Python tests independently because this read-only sandbox denies their temporary-directory writes. Residual rollout risk: `review.sh` deliberately loads helpers and prompts from trusted `main`, which does not yet contain this new helper in the current checkout; exercise the new path with a post-merge dry run (or an explicit trusted-ref override) using a large diff.

VERDICT: CHANGES_REQUESTED