## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Extraction/Consistency.lean:101` — Replace the duplicated measurement-placement construction and projectivity proofs with the existing `ProjectiveSetting` API.
## Review
F1 remains unresolved from the supplied ledger. The private `placedMeasurement`, effect lemma, and projectivity proof duplicate `ProjectiveSetting.placedMeasurement`, `placedMeasurement_effect`, and `placedMeasurement_isProjective` in [PlacementSupport.lean](/tmp/qpbt-point-self-consistency-completed-20260909/MIPStarRE/QPBT/Combining/Points/PlacementSupport.lean:257). Both constructions produce exactly `S.place placement (measurement.effect answer)`. Use the shared API and remove the redundant coordinate construction. Maintaining both implementations creates unnecessary proof maintenance.
The earlier duplication finding in `PointPointRejection.lean` is resolved: its proof now calls the shared placement API.
The completed self-consistency lemmas preserve the defect under injective `some` completion and explicitly retain the inverse retained-mass factor after conditioning. They supply an auxiliary estimate for `eq:pasting-2` in `references/qpbt-paper/06_nonlocal_games_and_mipstar.tex:511`; they do not claim the complete pasting theorem.
Validation: single-file Lean checks passed for `PointSelfConsistencyCompleted.lean` and `PointPointRejection.lean`. Neither contains direct proof holes. The five remaining `sorry` sites in `Extraction/Consistency.lean` explicitly identify proof obligations tracked by #47.
This was round-cap revalidation of the supplied findings and affected contracts, not another full audit of the 115,577-line patch. The inline attachment was truncated; reviewed hunks came from authoritative `diff.patch`. The installed `lean-conventions` skill was unavailable; I used the repository’s proof-integrity rules.
Under §12, the operator must fix F1 or defer it to a tracked issue through an adjudication bound to this head. The supplied ledger contains no such disposition.
VERDICT: CHANGES_REQUESTED
