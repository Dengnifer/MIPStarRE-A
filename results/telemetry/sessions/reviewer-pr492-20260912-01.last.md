## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Lines/ConsistencyPositivity.lean:171` — Finish the shared-placement migration by removing the now-unused duplicate positivity, commutation, and finite-sum proofs.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/Lines/PolynomialCollision.lean:39` — Reuse `evalCoefficient_collision_card_le` instead of maintaining an unused specialized root-count proof.
## Review
**F1:** The changed `place_mul_place_nonneg` correctly uses shared placement positivity, commutation, and `Commute.mul_nonneg`. However, `place_AA'_mul_place_BA''_nonneg`, `place_AB''_mul_place_BB'_nonneg`, both private commutation lemmas, and `place_finset_sum_local` remain without callers. The corresponding shared API is available through `Points/PlacementSupport.lean`. Remove these redundant proofs and replace the remaining `place_zero_local` use with shared `place_zero`. This consolidates the two prior placement findings, which are only partially addressed.
**F2:** `DegPoly L bound` is definitionally the coefficient-vector type accepted by `evalCoefficient_collision_card_le` in `MIPStarRE/QPBT/Algebra/Coefficients.lean:78`. I verified that `exact evalCoefficient_collision_card_le first second hne` proves the new theorem’s statement. `UniformAffineCollision.lean:35` already uses that shared theorem; the specialized theorem has no callers. Remove it or delegate to the shared result if its name must remain, then remove supporting code made unnecessary.
The other prior findings are resolved: `SubLinePrefix.lean:11` correctly describes preceding blocks vanishing, and `Points/Sandwich.lean` uses `DistanceCalculus.applyOperatorToState_mul`.
The mathematical comparison found no new statement drift. Paper `references/qpbt-paper/06_nonlocal_games_and_mipstar.tex:505`, `lem:pasting`, requires a collision estimate conditional on each fixed first-question pair. `nondegenerateLinePastingDist_collision_bound` proves precisely this predicate for its explicitly restricted distribution, with error `bound / #F_q`, matching the root-count step at `14_analysis_of_the_pauli_basis_test.tex:955`. The restriction remains internal; the unrestricted paper-facing theorem is unchanged. The discarded-mass estimate retains the missing contribution explicitly.
All 15 changed Lean files type-check. The only warning is the existing, documented `combined_line_measurement_consistency` TODO, tracked by issues #18 and #414. Axiom checks for the principal collision, mass, normalization, and changed positivity results show only `propext`, `Classical.choice`, and `Quot.sound`. No proof holes or bypasses were added.
Residual limitation: the full unconditioned consistency proof remains unfinished. I did not rerun the full build; the supplied context reports successful CI for this head. The external `lean-conventions` skill was unavailable, so I applied the checked-in integrity rules and review checklist. The working tree remains unchanged.
VERDICT: CHANGES_REQUESTED
