## Findings

- [ ] F1 (changes) `blueprint/src/chapter/ch13_qpbt_test.tex:945` — The envelope uses base-two `\log 2` under the section’s convention, but its Lean signature uses the natural logarithm; replace these factors throughout the new estimate with `\ln 2` and explicitly distinguish the two logarithms.

## Review

**One category-A mathematical equivalence mismatch; no new category-B prose findings.**

### F1 — The logarithm convention changes the asserted bound

`blueprint/src/chapter/ch13_qpbt_test.tex:890` explicitly states “All logarithms are base $2$.” Consequently, the denominator at line 945 is interpreted as \(t^n\), where \(n=\lceil s\rceil_+\). However, `MIPStarRE/LDT/Basic/RpowBounds.lean:24` proves the bound with denominator \((t\ln 2)^n\).

This is a substantive discrepancy, not merely typographical ambiguity. With \(s=5\), \(t=1\), and \(x=7\), the blueprint’s stated convention yields the false inequality
\[
7^5 2^{-7}=\frac{16807}{128}\le 5!=120.
\]
The linked declaration therefore does not justify the support lemma’s current statement-level `\leanok`.

The same discrepancy affects the constant at `blueprint/src/chapter/ch13_qpbt_test.tex:921`, the inline estimate and Taylor substitution at lines 925 and 927, and the support proof at line 951. The corresponding constant in `MIPStarRE/QPBT/Test/CanonicalParams.lean:181` uses `Real.log 2`.

**Correction:** distinguish “\(\log\) denotes the base-two logarithm; \(\ln\) denotes the natural logarithm,” and replace every new exponential-conversion factor `\log 2` with `\ln 2`. Keep the parameter expressions involving `\log R` unchanged. The updated paper-gap note already makes this distinction correctly.

### Prior-ledger triage

- The previous supremum-versus-explicit-constant discrepancy is addressed in form: the blueprint now uses the factorial estimate, \(x=m\), and \(a'=a((2c)^a+C)\). F1 records the remaining numerical mismatch.
- The missing monotonicity entry is resolved by `blueprint/src/chapter/ch16_qpbt_extraction.tex:562`; its hypotheses and conclusion match `deltaQld_mono`.
- The missing envelope link and reader-facing raw label are resolved at `blueprint/src/chapter/ch13_qpbt_test.tex:939`.
- The plain-ceiling discrepancy is resolved in both the inline estimate and support statement, and in the envelope docstring.
- The outdated specialization description and paper-gap account now describe the explicit estimate at \(x=m\).
- Prior findings concerning duplication, tactics, unused bindings, and shared placement belong to the code-review lane and are not adjudicated here.

### Scope and residual risk

The canonical theorem retains the paper’s assumptions and conclusion, with the documented nonnegative failure-parameter boundary condition and no added conditional inputs. The newly completed bounds have no direct proof-hole markers in the inspected files.

This was a read-only source review of the authoritative patch and relevant declarations, paper passages, and documentation. Some combined terminal output was truncated; targeted reads recovered the passages supporting these conclusions. Compilation and blueprint rendering were not rerun. The next revision needs a blueprint notation correction, not a change to the Lean statements.

VERDICT: CHANGES_REQUESTED