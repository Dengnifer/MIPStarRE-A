Addressed F1: moved `linePolynomialOfCoefficients_natDegree_le` unchanged into [Defs.lean](/tmp/qpbt-nondegenerate-pasting-mass-20260909/MIPStarRE/QPBT/Combining/Defs.lean:189), removed its declaration from `Answers.lean`, and replaced the duplicate `coefficientDegree` proof with the shared lemma.

Validation passed: full `lake build` (9,176 jobs), both single-file checks, and `git diff --check`. Neither edited file contains proof holes or emitted warnings. Existing warnings elsewhere remain.

Statement audit: `eq:combine-lines` bounds the combination of degree-`c` line polynomials by `c + 1`. Lean preserves those inputs and that conclusion over `CommSemiring`; no hypotheses changed. Verdict: source-faithful.

**Mathlib Audit**
The moved proof retains `Polynomial.natDegree_sum_le_of_forall_le` (`Algebra/Polynomial/BigOperators.lean`), `natDegree_C_mul_le` (`Degree/Lemmas.lean`), and `natDegree_pow_le` / `natDegree_X_le` (`Degree/Defs.lean`) to bound each summand and their sum.

Changes are uncommitted in the requested worktree, ready for `autofix.sh`.