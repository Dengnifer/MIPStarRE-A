## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT/Games/CondLinear.lean:115` — `condLinearConcat` still cites the paper’s direct-sum lemma instead of `lem:cl-concat`.

## Review

F1 must be fixed before merge. `condLinearConcat` encodes
\(T(x)=L(x^U)+R_{L(x^U)}(x^V)\), stated under `lem:cl-concat` in
`references/qpbt-paper/05_conditionally_linear_functions.tex:282-314`.
The current citation, lines 315–364, instead contains `lem:cl-func-prod` on
direct sums. Use `:282-314`, matching the corrected `IsCondLinearOn.concat`
docstring and `blueprint/src/chapter/ch12_qpbt_games.tex:1217-1224`.

The prior-round finding on `IsCondLinearOn.concat` itself is resolved. Its
paper assumptions and \(k+\ell\) conclusion now align with the source, with
only ordinary Lean boundary instances added. All other refreshed blueprint
ranges enclose their named definitions or lemmas.

The patch changes no declarations or proof terms and introduces no new
`sorry` or `axiom`; existing scaffold holes are unchanged and recorded in the
local briefs. `git diff --check` passes. I did not rerun Lean because the
changes are comments only and the supplied exact-head local CI is green.

VERDICT: CHANGES_REQUESTED