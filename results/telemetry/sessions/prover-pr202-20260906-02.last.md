Fixed F1 with a minimal two-file change.
- `scripts/blueprint_citations.py:29`: explicit citations now require colon-separated label syntax, excluding filenames and numeric file locators while retaining unknown `eq:`, `sec:`, and `item:` citations.
- `scripts/tests/test_blueprint_citations.py:103`: added filename and numeric-locator regression coverage, including successful CLI resolution.
Validation: all 28 citation tests pass; the reported `PauliBasisTest.lean` resolver command exits successfully; its single-file Lean check passes; `git diff --check` passes.
No Lean statements or blueprint links changed. Changes remain uncommitted for the lifecycle script. The operator can mark F1 resolved; full-project build and independent review remain outside this targeted validation.
