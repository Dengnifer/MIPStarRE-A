## What changed

Proved all five decoder laws in [Decoding.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-63-full-field-decoder-laws/MIPStarRE/QPBT/Algebra/Decoding.lean):

- `lowDegreeEncoding_mem_poly`
- `decodeFq_add`
- `decodeFq_smul`
- `decodeFq_lowDegreeEncoding`
- `decodeFq_dotProduct_indicatorVec`

Added the required `**Local fix:**` documentation. Public signatures match PR #46 exactly. Committed as `7909374 feat(QPBT/Algebra): prove full-field decoder laws`.

## Evidence

- `lake env lean MIPStarRE/QPBT/Algebra/Decoding.lean`: passed.
- `lake build MIPStarRE.QPBT.Algebra.Decoding`: passed.
- Serialized full `lake build`: passed.
- `rg -n "sorry|admit|axiom"` on the file: no output.
- Forbidden-token and line-length scans: clean.
- Pre-commit integrity hooks: passed.
- Worktree: clean, one commit ahead.

`lake exe checkdecls blueprint/lean_decls` could not run because `blueprint/lean_decls` is absent from this checkout.

## Statement integrity

Paper assumptions: admissible parameters; the source incorrectly states decoder/evaluation agreement for arbitrary bounded polynomials.

Lean assumptions: the same admissibility data; `P.hd` supplies the implicit degree boundary; only the evaluation theorem additionally requires `hg : IsEncoding g`.

Paper and Lean conclusions agree for degree membership, additivity, scalar linearity, and `Dec(g_h) = h`. The final conclusion agrees with the corrected Chapter 16 interface.

Verdict: exact for decoder linearity and inversion; faithful boundary hypothesis for degree membership; documented necessary restriction versus the false unrestricted source identity.

## Open obligations

The owned file has no remaining proof holes.

All five axiom audits currently include `sorryAx` solely because the type `PauliScalar P` depends on `exists_fixed_field_model`, still open in `FieldBasis.lean`. A type-only `True` probe has the same axiom closure, while #62’s `decodeAt_lowDegreeEnc` is free of `sorryAx`.

PR #46 remains open, so its gap note, extraction consumers, and re-export are absent here. Consequently those consumers could not be checked.

## Handoff

Rebase after PR #46 merges and resolve the expected add/add conflict by retaining these proofs. The operator-owned re-export needed is:

```lean
import MIPStarRE.QPBT.Algebra.Decoding
```

in `MIPStarRE/QPBT.lean`. Reviewer attention should start with that reconciliation and the upstream issue #65 axiom dependency.