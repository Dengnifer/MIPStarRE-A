## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Points/Closeness.lean:62` — Remove the two unused placed-effect square-sum lemmas; both duplicate the existing measurement bound.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/Lines/PointSelfConsistency.lean:31` — Reuse the existing mixture-averaging theorem instead of copying its proof.
## Review
**F1:** `ProjectiveSetting.sum_place_effect_conjTranspose_mul_self_le_one` and `sum_place_pointMeasExp_conjTranspose_mul_self` at `Points/Sandwich.lean:172` have no callers. Both follow directly from `measurement_sum_adjoint_mul_le_one` in `Games/DistanceTheorems.lean:336`, applied to `S.placedMeasurement p M`. That theorem also works without projectivity. I verified both replacements in Lean. Delete these unused additions; they preserve the duplication raised by the two prior code findings.
**F2:** `avgOver_mix_noFinite` duplicates the statement and proof of `MIPStarRE.QPBT.avgOver_mix` in `Combining/Lines/RestrictedAverage.lean:60`. The existing theorem already requires no `Finite` instance. Import and reuse it, or relocate the shared theorem if needed for layering. I verified the complete `avgOver_linePointDist_point` proof using this existing theorem. The finite-only `WinImplications.avgOver_mix` is not a direct replacement here.
The mathematical comparisons appear sound. In `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:900–940`, the argument consumes the measurements constructed by `lem:qld-4-10`, derives their coordinate-marginal comparisons, and obtains `eq:pasting-q1` under independent line-point sampling. The Lean declarations retain these objects, placements, and sampling, with explicit bound `8 * δQ + C * (ε + deltaLine ε)` and a universal constant preceding all parameters. `CombinedPointsWitness` is grounded by `exists_combinedPointsWitness`; it introduces no unresolved construction assumption here. The completed alphabets and hypotheses match the new blueprint entries. The edited `extendedQ_spec` retains its public statement.
Prior-finding disposition:
- Both code F1 entries are partially addressed by consolidating finite-sum and distance lemmas; the remaining duplication is F1 above.
- Permutation F2 is addressed by `Points/Placement.lean:19`, which distinguishes the ancillary involution from the blueprint’s four-cycle.
- Construction F3 is addressed by the revised proof and dependencies at `ch15_qpbt_combining.tex:852`.
- Marginal-coverage F4 is addressed by the entries at lines 1021 and 1079.
- Sampling-coverage F2 is addressed by the linked entry at line 1116.
Validation: the main defect file type-checks; the proposed reuse proofs pass. Axiom checks for the distance, defect, sampling self-consistency, point construction, and `extendedQ_spec` report only `propext`, `Classical.choice`, and `Quot.sound`. No new proof holes were found. The existing `sorry` at `Lines.lean:78` remains explicitly tracked under issues #18 and #414.
Full project and blueprint builds were not rerun; the supplied context reports successful CI for this SHA. The worktree remains unchanged.
VERDICT: CHANGES_REQUESTED
