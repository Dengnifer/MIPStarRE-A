Addressed both findings:
- F1: removed duplicate placement and operator-application proofs; all callers now reuse the existing APIs.
- F2: corrected prefix truncation: earlier blocks vanish, later blocks remain unchanged, matching paper `eq:cl-dlnf` at `08_classical_and_quantum_low_degree_tests.tex:237`.

Validation: all four edited files type-check; `lake build` passed (9179 jobs); `git diff --check` passed. Five affected results use only standard axioms. Theorem assumptions and conclusions are unchanged; no new proof debt.

**Mathlib Audit**
The reused `DistanceCalculus.applyOperatorToState_mul` relies on `Matrix.toLpLin_mul_same` in `Mathlib/Analysis/Normed/Lp/Matrix.lean`.

Existing repository warnings remain. Changes are uncommitted for the lifecycle script; F1/F2 resolution text is above for the operator.