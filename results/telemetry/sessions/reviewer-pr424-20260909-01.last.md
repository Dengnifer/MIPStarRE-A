## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Points/Placement.lean:289` — `eprCrossSwap` is incorrectly identified with the blueprint’s `U_σ U_θ`.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/Points/Commutation.lean:58` — The Parseval docstring states row orthogonality, while the theorem requires column orthogonality.
- [ ] F3 (changes) `MIPStarRE/QPBT/Combining/Points/Sandwich.lean:44` — `applyOperatorToState_mul'` duplicates an already imported theorem.
- [ ] F4 (changes) `MIPStarRE/QPBT/Combining/Points/Commutation.lean:35` — The cited paper-gap note does not explain the replacement construction attributed to it.

## Review

**F1.** In `blueprint/src/chapter/ch14_qpbt_observables.tex:1413–1420`, `lem:symmetric-equivalents-transfer` defines σ by exchanging corresponding Alice/Bob registers and θ by exchanging A/B and A′/A″. Consequently σθ sends B′ to A′, whereas `eprCrossSwap` sends B′ to A″. The former acts as a four-cycle on the ancillas; the latter is an involution. Correct this identification throughout the new placement docstrings and describe the implemented permutation independently. Its proved invariance remains valid; the attribution is mathematically incorrect.

**F2.** `sum_norm_sum_smul_sq_of_orthogonal` assumes orthogonality of the columns `j ↦ c j i`, indexed by `i`. The documented row condition does not imply the conclusion: take one row `(1,0)`, `N = 1`, and `v = (0,1)` in ℂ. That row satisfies the documented condition, but the claimed identity becomes `0 = 1`. Correct the docstring to describe column orthogonality. The Lean statement and proof are correct.

**F3.** `DistanceCalculus.applyOperatorToState_mul`, at `MIPStarRE/QPBT/Games/DistanceTheorems/Support.lean:64`, already has exactly these hypotheses and conclusion. It is available through the existing imports; I verified the replacement application in Lean. Remove the duplicate declaration and use the existing theorem at its call sites. This requires changes under the review’s duplication criterion.

**F4.** The new modules repeatedly attribute the direct Parseval/sandwich construction to `docs/paper-gaps/qpbt_linearity-theorem-quotation.tex`. Its relevant discussion at lines 118–130 instead describes a common ancillary extension using quantum linearity. It contains neither the field-valued Parseval transfer nor the sandwich defect identity. Add or update a mathematical note explaining the replacement of the binary-refinement route in `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:711–790`, and point these citations to it. Distinguish the completed combined-point construction from the separate common-extension obligation.

The statement audit for `exists_combinedPointsWitness` passes under the existing parameter and error conventions: it preserves the projective-strategy premise, original expanded local spaces, both ordered products, all directed opposite placements, and a universal polynomial error. Every intermediate bound used by the construction is derived internally.

All nine changed Lean files type-check. Axiom checks for the construction, commutation transfer, orthonormalization, and conditioning lemma report only `propext`, `Classical.choice`, and `Quot.sound`. No new proof holes were found. The seven existing `sorry` declarations in `Lines.lean` remain; a full project build was not rerun. The full on-disk patch was reviewed, and the checkout remains unchanged.

VERDICT: CHANGES_REQUESTED