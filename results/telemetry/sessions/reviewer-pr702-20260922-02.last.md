## Findings

- [ ] F1 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:407` — Restore the indicator command in the projective-measurement extension formula.

## Review

F1: In `lem:linearity-padding-measurement-extension`, `+mathbf{1}_{a=a_0}` lacks the backslash, so TeX renders mathematical letters instead of the indicator. This formula is linked to `linearity_padding_measurement_transport`, whose `extendMeasurement` definition explicitly adds the complementary projection only when `a = a0` (`Extension.lean:115`). Change it to `+\mathbf{1}_{a=a_0}` so the displayed construction matches the proved measurement.

Otherwise, the auxiliary statements preserve their advertised hypotheses, quantifier order, and conclusions. The reservation follows from admissibility alone; transport identities are proved; rounding retains the corrected bound `2 * avgOver μ δ`. I found no added proof-debt hypotheses or proof holes.

The complete absorption used in `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:832`, within the proof of `lem:qld-4-10`, remains open as documented. Six-register transport and identification with the earlier binary families remain obligations under #697.

All five new Lean modules, `Observables/Setup.lean`, and `QPBT.lean` type-check without diagnostics. The 13 principal results use only `propext`, `Classical.choice`, and `Quot.sound`. Full build and blueprint rendering were not rerun. The canonical `lean-conventions` skill was unavailable in the configured directories; I used the repository’s integrity and convention documents.

VERDICT: CHANGES_REQUESTED