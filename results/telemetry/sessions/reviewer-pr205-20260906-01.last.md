## Findings
- [ ] F1 (blocker) `MIPStarRE/QPBT/Games/Sandwich.lean:163` — `exists_pasting_error` adds a load-bearing register-exchanged comparison that the cited passages do not establish as an ambient hypothesis of `lem:pasting`.
- [ ] F2 (changes) `MIPStarRE/QPBT/Games/Sandwich/Defs.lean:106` — The new left/right measurement-placement constructors duplicate the already imported `DistanceCalculus.leftPlacedMeasurement` and `rightPlacedMeasurement`.

## Review

### F1 — The fourth comparison is not justified as a boundary hypothesis

`references/qpbt-paper/06_nonlocal_games_and_mipstar.tex:504` (`lem:pasting`) assumes the collision bound, the two marginal comparisons `eq:pasting-1`, and answer self-consistency `eq:pasting-2`. It does not explicitly assume the additional comparison now introduced at `Sandwich.lean:163`.

The justification in the docstring and `docs/paper-gaps/qpbt_pasting-product-error.tex:149` overstates the cited source:
- Lines 84–86 **define** a symmetric strategy; they do not impose symmetry on subsequent operator lemmas.
- Line 174 says, “We almost always restrict our attention to symmetric strategies.” This is not a universal standing assumption identifying this particular lemma as symmetric.
- The register move in the imported proof at `references/neexp-paper/05_quantum_preliminaries.tex:1150` identifies where symmetry would help; its use does not itself establish that symmetry is a hypothesis of the printed assertion.

The new hypothesis is mathematically substantive: `consistencyDefect_codeword_cross_le` consumes it to establish cross consistency of the second codeword family, and the assembly passes `h₄` to that lemma. The paper-gap note acknowledges that the corresponding one-sided quantitative argument is unavailable. Calling this comparison a “boundary hypothesis” therefore does not discharge the missing step.

**Required correction:** retain the source-facing statement over the documented corrected error contract without this additional comparison, with a tracked proof obligation if necessary. The completed restricted result can be recorded separately with its restriction explicit, but must not replace the unrestricted source-labelled theorem. Alternatively, supply a source passage establishing the claimed ambient symmetry assumption. Update the paper-gap note and the changed source-labelled blueprint entry at `blueprint/src/chapter/ch12_qpbt_games.tex:973` consistently.

**Statement-integrity audit for `exists_pasting_error`:**
- **Paper assumptions:** measurement/projectivity conditions, conditional collision bound, two marginal consistencies, and answer self-consistency.
- **Lean assumptions:** those conditions, faithful finite-dimensional/probability boundary conditions, **plus the reverse second-marginal consistency**.
- **Paper conclusion:** consistency with the evaluated sandwiched measurement, with polynomial error.
- **Lean conclusion:** the same operator comparison under the explicitly corrected additive polynomial contract.
- **Verdict:** the scalar correction is supported by the counterexample, but the additional comparison remains unsupported source-statement drift. This must be fixed before merge.

### F2 — Reuse the existing placement constructions

`MIPStarRE/QPBT/Games/DistanceTheorems/TensorSupport.lean:27` and `MIPStarRE/QPBT/Games/DistanceTheorems/TensorSupport.lean:42` already construct these same measurements, with the same typeclass assumptions and effects. They are available through the new `DistanceTheorems` import.

The PR now maintains two implementations of each construction. The new assembly itself mixes them: most steps use `Measurement.leftPlacement`/`rightPlacement`, while `consistencyDefect_placed_le_one` uses the existing constructors.

**Required correction:** reuse the existing definitions and attach the new effect, projectivity, and postprocessing lemmas to that implementation. If the new namespace is desirable, use thin aliases rather than duplicate positivity and completeness constructions.

### Validation and residual risk

The added pasting modules and changed commutator-calculus file contain no matches for `sorry`, `axiom`, `admit`, `native_decide`, or `unsafe` in the scan performed. The additive-error counterexample and the main collision/commutator proof decomposition are mathematically coherent; F1 concerns what theorem has been proved, not a demonstrated kernel-proof failure.

I did not rerun Lean or the full build; successful exact-head CI is supplied by the session context, not independently verified here. The attached diff was truncated, so I consulted the on-disk patch and checkout. The supplied prior ledger contains no findings to triage. The separate `lean-conventions` reference was not located under the available skill directory.

VERDICT: CHANGES_REQUESTED