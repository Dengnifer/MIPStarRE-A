## Findings

- [ ] F1 (changes) `.githooks/pre-push:7` — An already-current checked push fails because Git advertises no ref tuple.
- [ ] F2 (blocker) `local/bin/checked-push.sh:108` — Tuple binding silently disappears when the native hook is stale or not selected.

## Review

F1 reproduces consistently: the initial checked push returns 0, while repeating it unchanged returns 1 after the preflight succeeds. Git supplies no native pre-push input for an already-current ref, so line 7 rejects a valid no-op publication. This breaks retries in `github-sync.sh` and repeatable `pr_open.py` usage. Add an explicit no-op path and regression coverage.

F2 violates the fail-closed contract in `local/protocols/issues-prs.md`. The helper verifies that the target worktree’s hook file exists, but the final push relies on whichever hook `core.hooksPath` selects. Older branches can also contain the pre-change hook, which ignores `MIPSTARRE_EXPECTED_PUSH_TUPLE`. In a temporary repository with an unselected hook, deleting the remote ref after preflight still produced return code 0 and published the captured commit. Enforce the remote lease independently or force the exact trusted confirmation hook, with stale/missing-hook tests.

The earlier immutable-source and emergency-bypass findings are resolved by lines 65–68 and 99–108. The two later findings remain.

Validation: all 14 targeted unit tests passed; shell syntax, Python indentation, `git diff --check`, and worktree cleanliness passed. The two failing edge cases were independently reproduced.

VERDICT: CHANGES_REQUESTED