## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Points/MarginalContraction.lean:24` — Identify `norm_marginal_sub_sq_le` explicitly as a formalization-only auxiliary rather than as the paper’s calculation.

## Review

The paper’s argument in `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:902-927` first obtains `eq:qqm`, then bounds an averaged sum involving `∑ b, Q_{a,b} M_b^Z - M_a^X`. The Lean theorem instead gives a pointwise, abstract factor-four bound for `∑ b, Q b - D`, combining both contraction steps.

That abstraction is mathematically sound, but it is not a named or literal paper statement. Under `docs/CONTRIBUTING.md` §5.9 and `docs/mathematical_language.md`, its docstring must explicitly call it a “formalization-only auxiliary lemma” and describe which paper steps it combines. This must be fixed before merge.

The proof itself checks out:

- `hU` applies the orthogonal-projector sum estimate and the preceding absorption lemma.
- `hV` uses completeness and orthogonality of the projective measurement `B`.
- `hsum` correctly uses pointwise commutation, idempotence, and `B.sum_eq_one`.
- The final factor four follows from the squared triangle inequality.

The assumptions faithfully abstract facts available in the source application: `hQ` and `horth` come from the fixed fiber of the joint projective measurement, `hB` from the point measurement, and `hcomm` from opposite tensor placements. They are not proof-debt hypotheses.

The file type-checks without warnings. `#print axioms` reports only `propext`, `Classical.choice`, and `Quot.sound`; no `sorryAx` is present. The exact-head CI was already reported successful, so I did not repeat the full build.

VERDICT: CHANGES_REQUESTED