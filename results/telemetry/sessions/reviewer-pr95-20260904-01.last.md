## Findings
- none
## Review
The three proofs are mathematically correct and source-faithful.
`finrank_add_finrank_dotOrthogonal` and `dotOrthogonal_dotOrthogonal` exactly split the two conclusions of `lem:perp_perp` in `references/qpbt-paper/04_preliminaries.tex:263-281`. The paper explicitly says these hold over all fields. Lean’s `[Field K] [Fintype ι] [DecidableEq ι]` assumptions faithfully encode the finite coordinate setting; the declaration-local linter exceptions narrowly preserve the established public signatures.
`ker_canonicalProjPerp` matches `lem:L_perp_perp` at paper lines 394-415 and blueprint lines 160-175. The basis-free encoding is justified by the basis independence documented in `blueprint/src/chapter/ch11_qpbt_algebra.tex:149-158`.
The coordinate bilinear form is correctly shown nondegenerate by testing against `Pi.single i 1`; `dotProduct_comm` correctly reconciles Mathlib’s right-orthogonal convention with the project’s left-oriented `dotOrthogonal`. The kernel proof reduces to Mathlib’s projection-zero theorem and is not circular or definitionally vacuous.
The changed file passes Lean with warnings treated as errors, `git diff --check`, and the proof-hole scan. All three public theorems depend only on `propext`, `Classical.choice`, and `Quot.sound`. I did not repeat the full build because exact-head local CI was supplied as successful.
VERDICT: APPROVED
