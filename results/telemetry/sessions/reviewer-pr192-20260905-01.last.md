## Findings

- [ ] F1 (blocker) `MIPStarRE/QPBT/Test/MagicSquareTheorems.lean:189` — The advertised symmetric-consistent specialization omits the projectivity required by the cited definition.
- [ ] F2 (changes) `docs/paper-gaps/qpbt_ms-rigidity-symmetric-strategies.tex:412` — Symmetry gives one-way value at least, not exactly, `1-ε` under the corrected theorem’s hypotheses.
- [ ] F3 (changes) `docs/paper-gaps/qpbt_ms-rigidity-symmetric-strategies.tex:528` — The claimed averaged consistency defect skips necessary distribution, postprocessing, and swapped-state transports.
- [ ] F4 (changes) `MIPStarRE/QPBT/Test/MagicSquareTheorems/Rigidity/Consistency.lean:227` — This observable-distance proof duplicates the existing general `povm_to_obs` theorem.

## Review

F1 is source-statement drift. `references/qpbt-paper/06_nonlocal_games_and_mipstar.tex:162-174` defines a consistent strategy starting with “Let S be a projective strategy”; `blueprint/src/chapter/ch12_qpbt_games.tex:167-175` retains that prerequisite. In contrast, `SymmetricStrategy.IsConsistent` only records the state-action equalities, and the new theorem assumes no `S.toStrategy.IsProjective`. The paper-gap note repeats the omission at lines 361-376 and then uses `O² = I`, which requires projectivity; the POVM `M₀=M₁=I/2` shows the equality predicate alone does not imply that identity. Add projectivity to the specialization, or explicitly classify it as a stronger Lean-only corollary and provide an exact A′ wrapper.

For F2, if the two oriented values are `p_A,p_B`, the premise only gives `(p_A+p_B)/2 ≥ 1-ε`. Symmetry yields `p_A=p_B≥1-ε`, not equality. The same assertion occurs in `blueprint/src/chapter/ch13_qpbt_test.tex:305`; both occurrences need correction or an explicit reparameterization by the actual failure probability.

For F3, paper item 1 at `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:201` averages over the full question distribution, while item 7 at lines 219-227 uses the distribution conditioned on anticommutation. The Lean theorem cited also concerns observables under uniform `u`, and its opposite orientation is stated on `swappedState`. Establishing the claimed defect therefore additionally needs the anticommuting-probability conditioning bound, the binary effect/observable identity or a postprocessing contraction argument, and the tensor-swap reindexing. Two triangle inequalities alone do not supply these transitions.

For F4, `MIPStarRE/QPBT/Games/DistanceTheorems.lean:730` already proves that a unit-modulus weighted observable distance is bounded by the answer-cardinality times `opFamilyDistSq`. Specializing it to `ZMod 2`, the two signs, and the singleton distribution gives this theorem directly; the local `Fin.sum_univ_two`/Cauchy–Schwarz/`nlinarith` reimplementation should be replaced.

Statement-integrity audit: paper `thm:ms-rigidity` at `08_classical_and_quantum_low_degree_tests.tex:624-646` assumes only value `1-ε`; the corrected Lean theorem adds the two defect hypotheses and replaces `√ε` by `√ε+√δ`. This is a documented mathematical correction rather than hidden proof debt: the two-copy and symmetric flag counterexamples are valid, and the blueprint matches the corrected main declaration. The retained `sorry` at line 174 is explicitly identified as issue #105’s assembly obligation, and no proof-level completion mark is claimed.

The successful local CI result covers elaboration; no additional proof-integrity bypasses were found in the changed Lean files.

VERDICT: CHANGES_REQUESTED