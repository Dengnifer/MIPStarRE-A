## What changed

Proved all three targets in `Pauli.lean`: `eprState_norm`, `tauObservable_eq_sum_pauliProj`, and `pauliProj_eq_avg_tauObservable`.

Review fixes:

- Removed the redundant `forall_apply_eq_iff` helper and used core `funext_iff`.
- Extended the characteristic-two paper-gap note to document the forward Fourier expansion and inversion explicitly.
- Public theorem signatures remain unchanged.

Commits: `ec328e8` and review fix `12bc24c`.

## Evidence

- `lake env lean MIPStarRE/QPBT/Algebra/Pauli.lean`: passed cleanly.
- `rg -n "sorry|axiom" .../Pauli.lean`: no matches.
- `PauliTheorems.lean`: type-checks; only pre-existing `sorry` warnings.
- `lake build`: passed, 9,041 jobs.
- Axiom closure: only `propext`, `Classical.choice`, and `Quot.sound`; no `sorryAx`.
- Paper-gap style, reference resolution, and PDF build: passed.
- Pre-commit integrity checks: passed.
- Worktree is clean.

## Statement integrity

`eprState_norm`: paper assumes a finite linear space; Lean assumes a nonempty finite carrier with decidable equality and proves the same unit-norm conclusion. Verdict: faithful boundary hypotheses.

Fourier identities: paper allows general prime characteristic; Lean specializes to finite fields over `ZMod 2`, where the forward and inverse characters both equal `phaseSign`. Conclusions match the characteristic-two instances. Verdict: extra assumptions, explicitly documented scope restriction.

## Open obligations

None in `Pauli.lean`. The unrestricted prime-characteristic expansion remains a separate formalization task documented in the paper-gap note.

## Handoff

Push `12bc24c`, then rerun exact-head CI and independent review. Review should confirm the `funext_iff` replacement and the expanded scope note. No re-export change is needed.