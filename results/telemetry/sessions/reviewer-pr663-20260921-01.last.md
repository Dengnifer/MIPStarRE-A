## Findings

- [ ] F1 (changes) `scripts/comparator/expected/qpbt/Challenge/MIPStarRE/QPBT/Test/LowDegreeGameMeasurements.lean:23` — The four-target closure contains private `ldSpaceSplit`, so its module-qualified constant cannot match the library during comparison.
- [ ] F2 (changes) `scripts/comparator/assemble_challenge.py:294` — Split assembly rejects or crashes on absent header/footer files despite the documented `--write` development path promising to omit them.

## Review

F1 must be fixed before merge. The generated provenance identifies `_private.MIPStarRE.QPBT.Test.LowDegreeGameMeasurements.0.MIPStarRE.QPBT.ldSpaceSplit`, declared at `MIPStarRE/QPBT/Test/LowDegreeGameMeasurements.lean:41`. Re-declaring it privately inside `Challenge.MIPStarRE...` produces a different kernel name. This is exactly the incompatibility described in `docs/comparator.md:122-137` and contradicts its claim that required closure members were made public. It also extends beyond the documented pending completeness auxiliaries because this declaration is reached through `exists_ld_soundness`. Make `ldSpaceSplit` public without changing its body, regenerate the tree, and include it in comparator verification.

For F2, `check_challenge_drift.py:228-248` explicitly permits `--write` with an absent challenge part, and the configuration schema makes header/footer optional. The split branch then requires both values and unconditionally calls `read_text`; a missing configured footer reproduces as `FileNotFoundError`. The existing regression mocks only the monolithic `assemble_candidate` path. Split assembly must support the same omission contract, or the schema and documentation must explicitly reject it, with a split-specific regression test.

The prior findings are resolved. The LDT fixture matches its original SHA-256 (`cbe5642b...`) and is unchanged from the merge base. The documentation now accurately separates the pending four-target verification from the historical two-target fake-landrun result.

The four target statements otherwise match paper `lem:ld-soundness`, `lem:pauli-completeness`, `thm:pauli`, and `cor:pauli-binary`, including their constant bounds, quantifier order, three low-degree consistency conclusions, and raw prescribed-answer Pauli effects. No load-bearing bridge assumptions were introduced. The only `sorry` occurrences are the four explicitly documented challenge target stubs; the changed library declarations contain no proof holes or bypasses.

`git diff --check` passed. Exact-head CI was reported successful, but official all-four comparator equality, real landrun/nanoda verification, and the final verified revision pin remain pending as documented.

VERDICT: CHANGES_REQUESTED