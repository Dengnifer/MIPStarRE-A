## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Lines/PointSelfConsistency.lean:31` — Remove `avgOver_mix_noFinite` and reuse the existing unrestricted `MIPStarRE.QPBT.avgOver_mix`.
## Review
F1 requires changes under the modularity and duplication rules. The statement and proof already exist in `MIPStarRE/QPBT/Combining/Lines/RestrictedAverage.lean:60`, including the absence of a `Finite` assumption. Import that module, delete the private duplicate, and use `MIPStarRE.QPBT.avgOver_mix` at line 90. I verified that the resulting proof of `avgOver_linePointDist_point` type-checks.
No mathematical correctness or source-statement blocker was found. The expanded-line conclusions preserve the hypotheses, universal constants, placements, and common square-root error of `lem:qld-comm-line-cons` in `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:523–678`, using the blueprint’s existing completed-outcome convention. The marginal estimates follow the calculations at lines 902–941, retaining `δQ` explicitly; conditioning divides by the positive retained mass.
Validation: the authoritative full patch matches the checkout diff. Direct Lean checks passed for `LineMeasurement.lean` and `ConditionedPointLineMarginalDefect.lean`. Inspected axiom closures contain only `propext`, `Classical.choice`, and `Quot.sound`. No new proof holes or prohibited bypasses were found.
Residual limitation: the three inherited construction holes in `Combining/Lines.lean` remain outside these new proofs. Full project and blueprint builds were not rerun. The checkout is unchanged.
VERDICT: CHANGES_REQUESTED
