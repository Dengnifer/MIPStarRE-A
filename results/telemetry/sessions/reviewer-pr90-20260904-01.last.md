## Findings

- [ ] F1 (blocker) `MIPStarRE/QPBT/Algebra/SelfDualBasis.lean:34` — `binaryCoordinates_mul` retains an unmarked `sorry`, so the source-linked equality remains axiom-dependent.
- [ ] F2 (changes) `MIPStarRE/QPBT/Algebra/FieldBasis.lean:93` — `trace_alg_equiv_apply` re-proves Mathlib’s existing `Algebra.trace_eq_of_algEquiv`.
- [ ] F3 (changes) `MIPStarRE/QPBT/Algebra/FieldBasis.lean:62` — The new private definition `squareMulEquiv` lacks the required declaration docstring.
- [ ] F4 (changes) `MIPStarRE/QPBT/Algebra/FieldBasis.lean:294` — The central theorem `exists_self_dual_normal_basis_gal` lacks the required mathematical docstring.

## Review

F1. The `sorry` predates this patch, but it remains bare in a changed file. `binaryCoordinates_mul` is the first equality of `eq:eq-mult` from `references/qpbt-paper/04_preliminaries.tex:693-696` and `blueprint/src/chapter/ch11_qpbt_algebra.tex:313-335`; its docstring provides no TODO, tracking issue, or accepted-gap marker. Focused checking emits `declaration uses sorry`. Prove it or explicitly cite the accepted tracker and scoped exception.

F2. Mathlib’s `Algebra.trace_eq_of_algEquiv` at `Mathlib/RingTheory/Trace/Basic.lean:174` has exactly the required specialization and uses weaker assumptions. Delete `trace_alg_equiv_apply` and invoke the Mathlib result directly at current lines 125 and 246. The current helper is the project’s documented A5 duplication pattern.

F3-F4. `AGENTS.md:406` and `docs/CONTRIBUTING.md:406` require docstrings on every definition and significant theorem. The detached section comment does not document `squareMulEquiv`, while `exists_self_dual_normal_basis_gal` is the substantive construction supporting the public result. Add concise declaration docstrings; the key transition, unit, involution, and pairing lemmas should likewise be documented where their role is not immediate.

No mathematical or source-statement drift was found. `IsDualBasisPair`, `Basis.IsSelfDual`, and `Basis.IsNormal` match paper lines 494-502. The assumptions of `exists_selfDualNormalBasis` are faithful field-model boundary data, and its conclusion is the correct existential consequence for odd degree. The stronger polynomial-time algorithm from `lem:efficient_basis` is not claimed, and the implementation explicitly disclaims that complexity result.

Both changed files type-check, `git diff --check` passes, and the new group-algebra construction has no type, instance, or performance defect. I relied on the supplied exact-head local CI success for the full build. The external `lean-conventions` skill was unavailable, so the checked-in companion integrity rules were applied.

VERDICT: CHANGES_REQUESTED