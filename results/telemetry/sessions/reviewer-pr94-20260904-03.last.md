## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT/Algebra/Pauli.lean:337` — The Fourier theorem docstrings still identify binary specializations as the unrestricted blueprint lemma and cite obsolete lines.
- [ ] F2 (changes) `docs/paper-gaps/qpbt_characteristic-two-pauli-scope.tex:18` — The revised implementation-weight entry omits the honest range required by the paper-gap policy.

## Review

The prior duplicate-helper finding is resolved: `forall_apply_eq_iff` was removed and its uses now rely on core `funext_iff`. The prior mathematical scope finding is also substantively resolved: the note now states both Fourier formulas, explains why the inverse sign agrees with the forward sign in characteristic two, and records that the unrestricted prime-characteristic declarations remain future work.

F1 requires changes. `blueprint/src/chapter/ch11_qpbt_algebra.tex:638-652` now contains the end of the twisted-commutation proof and the beginning of `lem:cancellation`; `lem:pauli-observable-expansion` is actually at lines 674-688, with `eq:pauli-obs-proj` at 678-680 and `eq:pauli-inversion-0` at 682-687. Correct both theorem citations at lines 337 and 413, and the module range at line 17. The forward theorem should also say it is the “characteristic-two specialization of” the lemma instead of “This is” the unrestricted lemma, consistently with the scope note’s statement that these declarations are not its blueprint implementations.

F2 requires changes. `docs/paper-gaps/policy.tex:51-58` requires the first page to give a rough implementation weight using honest ranges, and `docs/paper-gaps/template.tex:31-36` specifically requests estimated declarations/interfaces and proof lines. “The source-general identities remain a separate formalization task” gives no range for that remaining work. Supply an honest range while distinguishing completed binary work from the outstanding general expansion and inversion.

The mathematical proofs otherwise check out. The paper’s general formulas are at `references/qpbt-paper/04_preliminaries.tex:1157-1160`; under `p = 2`, negation is trivial, so the Lean coefficients in both directions are correct. `Field`, `Fintype`, `DecidableEq`, and `Algebra (ZMod 2) K` are legitimate domain data, with no bridge hypotheses. Allowing an empty finite coordinate type is a valid extension of the paper’s `n ≥ 1` case. `eprState_norm` faithfully proves normalization of `def:EPR`.

`lake env lean MIPStarRE/QPBT/Algebra/Pauli.lean`, `lake build MIPStarRE.QPBT.Algebra.Pauli`, and `git diff --check` pass. The changed file has no proof holes or prohibited tactics, and all three completed public theorems have only `propext`, `Classical.choice`, and `Quot.sound` in their axiom closure.

VERDICT: CHANGES_REQUESTED