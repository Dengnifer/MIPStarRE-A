## Findings

- none

## Review

The round-one code findings are resolved: the symmetrization attainment gap is now explicitly marked, the projective-rounding citation is corrected, shared marginal/rank-zero lemmas are reused, and the unused normalization binder is named appropriately.

Statement-integrity checks passed:

- `exists_symmetric_projective_strategy` retains the exact statement of `lem:symmetric-strat` from `references/qpbt-paper/06_nonlocal_games_and_mipstar.tex:94-99`. Its remaining `sorry` is clearly documented as the known attainment gap, linked to issue #98 and the paper-gap note, and the theorem is not marked `\leanok`.
- `exists_symmetric_projective_strategy_of_strategy` faithfully formalizes the value-preserving construction actually proved at paper lines 101-130 and matches the separate blueprint support statement.
- `abs_value_sub_le_of_areClose` matches `references/qpbt-paper/06_nonlocal_games_and_mipstar.tex:531-539`: same transported state, either strategy projective, and a universal square-root bound.
- `exists_projective_close_of_consistent` and `projective_rounding_with_explicit_constant` match `lem:ortho` at `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:136-145`, with the explicit bound `220 · δ^(1/4)` correctly isolated as formalization support.
- The declarations moved into `DistanceTheorems/Calculus.lean` preserve their public statements.

Independent type-checking passed for `StrategyClasses.lean`, `DistanceTheorems.lean`, `Calculus.lean`, `ProjectiveRounding.lean`, and `LocalityPreservingRepair.lean`. Proof-integrity scans found no bypasses or unmarked holes. Axiom inspection of the three completed public results found only `propext`, `Classical.choice`, and `Quot.sound`, with no `sorryAx`. `lake exe checkdecls blueprint/lean_decls` resolved all 909 declarations, `git diff --check` passed, and the worktree remained clean. The exact-head local CI was also reported green.

The external `lean-conventions` skill was unavailable in this runtime; the checked-in `docs/PROOF_INTEGRITY.md`, `docs/anti_patterns.md`, and project conventions were applied instead.

Residual risk is limited to the explicitly tracked finite-dimensional attainment gap in `exists_symmetric_projective_strategy`; this PR neither hides that gap nor advertises the theorem as proved.

VERDICT: APPROVED