## Findings
- [ ] F1 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:261` — The proof sketch and `\uses` list describe transitive estimates rather than the composition performed by Lean.
- [ ] F2 (changes) `MIPStarRE/QPBT/Extraction/Construction.lean:43` — Related paper-gap and helper status records still state that this now-completed composition is open.
- [ ] F3 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:253` — “Lean proof” and “composition history” are implementation-oriented rather than mathematical prose.

## Review
The blueprint statement matches `exists_pulled_apart_consistency`: the universal constants precede the admissible parameters and strategy, the witness is existential rather than supplied, and the two point-consistency estimates and observable estimate use the same witness and common scale. The `Fin P.model.basisDim` index matches the blueprint’s \(0,\ldots,t-1\) convention. Axiom inspection reports only `propext`, `Classical.choice`, and `Quot.sound`, with no `sorryAx`, so both new `\leanok` markers are valid. No `\notready` issue was found.

**F1:** The Lean proof at `Construction.lean:68-101` invokes `exists_globalPairWitness` and the three declarations represented by `lem:qld-construct-the-paulis-given-global-pair`, then takes the maximum of their constants. The blueprint proof instead lists their lower-level, transitive dependencies and narrates the old direct derivation. Replace the proof `\uses` with the directly invoked nodes, principally `lem:qld-4-7` and `lem:qld-construct-the-paulis-given-global-pair`, and rewrite the proof sketch around witness selection and the common maximum. The detailed derivation can remain with the subordinate estimates it proves.

**F2:** The completion claim conflicts with current status records. `docs/paper-gaps/qpbt_extraction-transfer.tex:164-166` still says that `exists_globalPairWitness` is unfinished, excludes zero error, and that no declaration performs the composition. The elimination paragraphs in `SuppliedPointConsistency.lean:102-106`, `:221-225`, and `ObservableConsistency.lean:42-46` say the same work remains. Retain the conditional classification of those helpers, but update the records to state that `exists_pulled_apart_consistency` now performs their source-facing composition and distinguish any remaining downstream obligations.

**F3:** Replace “Lean proof obtains…” with mathematical prose such as: “The proof chooses the global polynomial-pair measurements supplied by Lemma~…, applies the three estimates of Example~…, and replaces their constants by their maximum.” Drop “composition history” or describe the corrected mathematical estimates directly.

Summary: 0 signature-equivalence mismatches, 1 proof-sketch/`\uses` mismatch, 1 stale status-record issue, and 1 prose issue.

VERDICT: CHANGES_REQUESTED