## Findings

- [ ] F1 (blocker) `MIPStarRE/QPBT/Combining/Claims.lean:535` — Claims 17-1 and 17-2 retain real-part bounds instead of the paper’s complex-modulus bounds.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/Points/PlacementSupport.lean:258` — Duplicate placement declarations prevent importing both placement modules; other support proofs are also duplicated.
- [ ] F3 (changes) `docs/paper-gaps/qpbt_combined-lines-error-term.tex:172` — The discharge description does not explain how its conditioned construction proves the current concrete-measurement obligation.

## Review

**F1.** `subline_replace_by_ordered_product` proves `|Re A - Re B| ≤ C√δQ`; `subline_remove_X_factor` retains the analogous restriction. The paper bounds the complex modulus of the correlation difference in `claim:17-1` at [14_analysis_of_the_pauli_basis_test.tex:1150](/home/drx/MIPStarRE-qpbt/.worktrees/issue-474-concrete-x-overlap-deficit/references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:1150) and `claim:17-2` at [line 1178](/home/drx/MIPStarRE-qpbt/.worktrees/issue-474-concrete-x-overlap-deficit/references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:1178). The ordered product of the X and Z projectors need not be Hermitian, and no reality lemma justifies dropping its imaginary part. The new Claim 17-2 note acknowledges this weaker scalar assertion, but that does not establish source fidelity. Restore the complex-modulus statements, or prove the required reality identities. Real-part results may remain separately identified auxiliary results.

**F2.** A Lean stdin file importing both `Points.Placement` and `Points.PlacementSupport` fails with:

```text
environment already contains 'MIPStarRE.QPBT.ProjectiveSetting.placedMeasurement'
```

Both new modules define the same public placement API. Remove the duplicate implementation, use a re-export, or extract shared declarations into one module. The same consolidation is needed in `Claims.lean`: `completed_pair_norm_sq_sum` duplicates `ProjectiveSetting.completedPair_norm_sq_sum_ZX` from `ZEvalDeficit.lean:166`; its completion lemmas duplicate `LinePointOverlap.lean:142–173`; and `avgOver_prod_current` duplicates `RestrictedAverage.lean:89`. These APIs are already available through the imports.

**F3.** The added note describes recovering four named obligations from a protected commit, including a “conditioned XZX” construction, without defining the conditioning event or stating its defect estimate. Meanwhile, `Lines.lean:53` now requires consistency of the explicit `combinedLineMeasurement` under the original product distribution. The source constructs that sandwich at paper lines 942–949 and proves its consistency at 951–961. Rewrite the discharge discussion around `combined_line_measurement_consistency`; if conditioning remains the intended proof method, explain its event, mass, error bound, and transfer back to this same measurement. This is needed for the self-contained mathematical comparison required by `docs/paper-gaps/policy.tex`.

The concrete X-deficit proof itself follows the marginal identity and restricted-average argument without an extra marginal or producer assumption. The repaired point-error quantifier order is also justified.

Validation: `ConcreteXDeficit.lean`, `Claims.lean`, and `Lines.lean` type-check. Six completed public results have axiom closures containing only `propext`, `Classical.choice`, and `Quot.sound`. The two remaining direct holes are explicitly tracked. The duplicate-placement import check fails as described above. I used the on-disk patch beyond the truncated attachment; no full build was rerun and the checkout remains unchanged.

VERDICT: CHANGES_REQUESTED