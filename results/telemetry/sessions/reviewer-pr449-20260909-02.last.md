## Findings
- [ ] F7 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:873` — Define the exact error hypotheses on Q needed for the coefficient `8δ_Q`.
- [ ] F8 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:851` — The replacement sandwich formula uses original rather than expanded point-measurement notation.
- [ ] F9 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:886` — The completed marginal theorem lacks a proof block and proof-level `\leanok`.
## Review
Two category-A mathematical synchronization issues and one proof-status omission remain. No separate category-B findings.
**F7.** “Supplied combined-point witness Q with error δ_Q” does not specify its quantitative hypotheses. The preceding lemma uses `≈δ_Q`, defined with an `O(δ_Q)` bound in `ch12_qpbt_games.tex:238`. However, `CombinedPointLineMarginalDistance.lean:30` assumes `CombinedPointsWitness S δQ`, whose three distance bounds are **exactly** `≤ δQ` (`MIPStarRE/QPBT/Combining/Witnesses.lean:108`, `:116`, `:126`). An unspecified constant multiplying δ_Q cannot justify the displayed coefficient 8. State projectivity and these exact averaged bounds explicitly, including opposite placements. Keep this as a separate auxiliary entry; no change to the paper theorem’s hypotheses is needed.
**F8.** The displayed product uses unhatted `M`, which denotes the strategy’s original measurements. Lean’s `sandwichPoint` instead multiplies three `pointMeasExp` effects (`MIPStarRE/QPBT/Combining/Points/Sandwich.lean:106`). Their established blueprint notation is `\hat M` (`ch14_qpbt_observables.tex:1245`). Restore the hats on all three factors; the distinction matters because the approximate-commutation estimate concerns the expanded measurements.
**F9.** The new lemma has only statement-level `\leanok`, although its linked theorem has a completed proof beginning at `CombinedPointLineMarginalDistance.lean:48`. After correcting F7, add a matching proof block with proof-level `\leanok` and the actual dependencies, including expanded-point consistency used through `SamePlacementDistance.lean:40`. Otherwise the blueprint reports only statement formalization.
Prior findings: F3 and F6 are corrected. F4’s requested proof-status and dependency updates are present. F5’s missing entry is now present, with the remaining issues above. Code-lane F1/F2 were not re-adjudicated.
The attachment was truncated; I reviewed the full on-disk patch and corresponding sources at the specified head. No files were modified, and builds were not rerun.
VERDICT: CHANGES_REQUESTED
