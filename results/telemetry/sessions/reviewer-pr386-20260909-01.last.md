## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Lines/RestrictedAverage.lean:89` — Reuse the existing product-average theorem instead of adding a duplicate proof.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/Lines/ConsistencyPositivity.lean:45` — Derive `reindexOp_nonneg` from the existing operator-order API.

## Review

**F1:** `avgOver_prod` duplicates `SandwichProduct.avgOver_distribution_prod` in `MIPStarRE/QPBT/Games/Sandwich/Support.lean:29`, with identical hypotheses and conclusion. The existing theorem is already available through this file’s imports. I verified that `SandwichProduct.avgOver_distribution_prod μ ν f` proves the new statement directly. Reuse it at the call sites instead of maintaining a second implementation.

**F2:** `reindexOp_nonneg` repeats `MIPStarRE.Quantum.reindex_nonneg` from `MIPStarRE/Quantum/FiniteMatrix/Order.lean:164`. Because `reindexOp` reverses the equivalence convention, its entire proof can be `reindex_nonneg e.symm hM`; this replacement type-checks with the existing imports. The adapter name can remain, but its proof should reuse the established result.

Both findings require changes under the review’s duplication policy; neither indicates an unsound proof.

**Statement Integrity**
The seven completed declarations preserve their public statements. The mixture identities and inflation factors `2m` and `4m²` match `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:1049-1061`, represented by blueprint `lem:restricted-line-mixture-bounds`. The consistency estimate keeps its universal constant outside the parameter quantifiers.

`exists_subLineWitness` constructs the explicitly documented direct-index auxiliary without additional assumptions. Its fixed auxiliary indices differ from the paper’s sampling procedure but satisfy the stated existential specification. Transport to the source’s seed-indexed `lem:qld-sublines` remains open, as documented; this PR does not discharge that gap.

**Verification**
Reviewed the complete 19-file patch, recovering the truncated attachment from the checkout and confirming it matches `diff.patch`. All 19 files type-check individually. Only the two unchanged joint-measurement TODOs produce `sorry` warnings. Axiom audits of all seven completed declarations and `subLineBranchRaw_map_joint` report only `propext`, `Classical.choice`, and `Quot.sound`.

`git diff --check` passes; the checkout remains unchanged. Full build was not rerun. The external `lean-conventions` skill was unavailable; repository integrity and convention documents were used.

VERDICT: CHANGES_REQUESTED