## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Test/LowDegreeGameTheorems.lean:43` — Move the generic finite-distribution lemmas into the existing distribution infrastructure and make them reusable.
- [ ] F2 (changes) `MIPStarRE/QPBT/Games/CondLinear.lean:452` — Place the direct-sum construction and its supporting proofs in the module dedicated to conditionally-linear direct sums.
## Review
F1 requires changes. Lines 43–224 introduce eight generic results about distribution extensionality, push-forward composition, uniform fibers, equivalences, product marginals, and bind/map interaction. None is specific to the low-degree game, yet all are private. The repository already has `MIPStarRE/QPBT/Games/DistributionAux.lean` for exactly this class of operations. Keeping these results private in `LowDegreeGameTheorems.lean` prevents reuse and invites duplicate proofs. They should be moved to the distribution layer with appropriately reusable names.
F2 requires changes. `CondLinearTerm.directSum` and its support/evaluation development occupy lines 291–644 of the foundational `CondLinear.lean`, while `CondLinearTheorems.lean` explicitly owns “the behavior of such maps and their shared-seed distributions under finite coordinate direct sums.” These declarations are only consumed by that theorem module. Move the direct-sum block and its supporting coordinate-restriction lemmas there so basic users of conditional linearity, concatenation, or graph distributions do not inherit the unrelated proof stack.
I compared the changed source-facing declarations with `references/qpbt-paper/05_conditionally_linear_functions.tex` (`lem:cl-kth`, `lem:cl-concat`, `lem:cl-func-prod`, and `lem:cl-dist-prod`), `references/qpbt-paper/08_classical_and_quantum_low_degree_tests.tex:128-174` and `:243-272`, and the corresponding blueprint entries. The Lean statements preserve the relevant hypotheses, quantifier structure, incidence properties, conditional-linearity levels, and distribution identities. In particular, the split formalizations of `lem:alnf` and `lem:dlnf` collectively record the uniform point/index marginals, incidence, and diagonal prefix condition without adding proof-debt assumptions.
No new `sorry`, axiom declaration, bypass, or conditional bridge was introduced. The existing `exists_ld_soundness` hole at line 668 is explicitly documented as tracked proof debt. The changed declarations’ axiom audit showed only standard classical/propositional axioms. The supplied exact-head local CI is green, and `git diff --check` is clean.
VERDICT: CHANGES_REQUESTED
