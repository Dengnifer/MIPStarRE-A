## Findings
- [ ] F1 (changes) `local/bin/codex-policy-shim.sh:25` — Alternate feature overrides bypass the shim’s fan-out normalization.
- [ ] F2 (changes) `local/bin/account_router.py:150` — Continuation validation accepts budgets that omit the predecessor’s newly completed working time.
- [ ] F3 (changes) `local/bin/dispatch.sh:735` — Ordinary resumes drop continuation provenance, allowing subsequent handoffs to reset the shared budget.
- [ ] F4 (changes) `local/bin/dispatch.sh:759` — The telemetry recovery command loses continuation metadata when replaying a failed append.

## Review

Reviewed the authoritative 1,466-line patch against head `2e6b1441f8662f4f6c716b5f5756b3cc5276950e`; it matches the supplied merge-base/head diff. The prior findings ledger is empty.

**F1 — Normalize all feature-override forms.** An argv-only reproduction shows that `--enable multi_agent exec -- "task"` passes through unchanged after the injected `features.multi_agent=false`. Whole-table requests such as `-c 'features={multi_agent=true}'` also survive. Filtering only the dotted configuration keys leaves contradictory overrides in the final invocation. Reject or normalize these forms, and test the resulting effective configuration rather than merely asserting that a disabling argument appears somewhere.

**F2 — Require cumulative time charges.** The validator separately compares the supplied total against the predecessor’s `wall_s` and its earlier budget snapshot; it never requires their sum. I reproduced acceptance of a snapshot containing 12,452 seconds followed by a completed 2,600-second session, with the next budget still reporting 12,452 seconds. The required lower bound is 15,052 seconds. Incrementing `attempts` does not compensate for the omitted time. Add a rejection regression for this undercharged handoff.

**F3 — Preserve provenance across resumes.** `CONTINUATION_JSON` is populated only for `--continue-from`, so an ordinary `--resume` of a continued thread produces a new session row without its continuation link. Later, `continuation()` inspects only that predecessor row. With both rows present in a mocked registry, I reproduced acceptance of a different budget file, a new anchor, and an attempt count reset to one. Inherit the provenance on resume or recover it through the thread’s registry history before validating another handoff.

**F4 — Make recovery preserve the validated snapshot.** Normal summarization receives `--continuation-json`, but the printed replay command does not. If the append fails before writing the row, following those instructions records the session without its predecessor, checkpoint, or shared-budget snapshot. Preserve the validated metadata durably and include it in recovery; rereading a subsequently modified budget cannot reconstruct the launch-time snapshot.

**Validation:** All four shell syntax checks, Python syntax parsing, `git diff --check`, and two existing read-only-compatible unit tests pass. In-memory routing checks and the reproductions described here execute without filesystem writes or agent launches. The full regression suite is not rerun because its fixtures require writes. Runtime installation and real CLI/provider behavior remain unverified. The worktree remains clean.

VERDICT: CHANGES_REQUESTED