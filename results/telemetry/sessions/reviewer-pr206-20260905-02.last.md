## Findings

- [ ] F1 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:1800` — The proof still claims the source-shaped auxiliary is established despite the preceding unresolved transport requirement.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/Apply.lean:101` — The cited divisibility note does not document the completed-answer-alphabet deviation or its discharge.
- [ ] F3 (changes) `MIPStarRE/QPBT/Extraction/Unitary.lean:93` — The conditional extraction theorem retains a source-shaped name that hides its `GlobalPairWitness` input.
- [ ] F4 (changes) `MIPStarRE/QPBT/Extraction/Unitary.lean:81` — The cited extraction gap record still says the now-corrected docstring presents this theorem as source-facing.
- [ ] F5 (changes) `MIPStarRE/QPBT/Combining/Apply.lean:97` — “Lean-only bound” is implementation jargon rather than mathematical prose.

## Review

F1: Lines 1799–1802 now correctly say that the directly indexed construction does not establish the displayed formulation until transport is proved. The proof immediately below nevertheless begins “This is what the proof … establishes” and says the remaining steps are unchanged. Revise it to say that only the numerical error calculation is available, conditional on the unresolved question-space, distribution, and answer-alphabet transports.

F2: `qpbt_ld-dimension-divisibility.tex` describes the directly indexed questions and line-point law, but not the replacement of \(\mathbb F_q\) by \(\mathbb F_q\cup\{\bot\}\). That deviation is described in `qpbt_combined-lines-error-term.tex:305-317`. Cite the two notes separately and record an explicit obligation converting the completed-answer comparison, including degenerate lines, to the source’s sum over field answers.

F3: `exists_extractionWitness` requires the non-paper input `w : GlobalPairWitness S deltaG`. Although its revised docstring and separate blueprint support node now quarantine it correctly, its public name still reads as the unconditional source existence theorem. Rename the existing helper to a conditional name such as `exists_extractionWitness_ofGlobalPairWitness` and update its blueprint and gap-note references.

F4: `qpbt_extraction-transfer.tex:153-157` still says that the docstring “describes that theorem as source-facing and as introducing no additional premise.” This PR removes precisely that defect. Update the record to mark the docstring correction complete while retaining the genuinely open composition and range-projection obligations.

F5: Replace “same Lean-only bound” with mathematical wording such as “same directly indexed estimate” or simply “same bound.”

The new statement-level `\leanok` for `lem:qld-4-10` is valid: its quantifiers, projective measurements, ordered \(XZ/ZX\) consistency estimates, and symmetric placements match `CombinedPointsWitness` and `exists_combinedPointsWitness`. No proof-level mark was added. The conditional extraction support node likewise matches its Lean signature, while the source lemma remains correctly unmarked. The decoder dependency addition is also accurate.

Count: 0 signature-equivalence mismatches, 0 invalid `\leanok` marks, 3 status/traceability issues, and 2 prose/naming issues.

VERDICT: CHANGES_REQUESTED