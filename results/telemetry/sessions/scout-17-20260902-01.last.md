<!-- scout: scout-17-20260902-01 2026-09-02 -->
## Mathlib scouting report — 2026-09-02
### Mathematical source
- Primary source: `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:95-131,160-190,367-460,462-679`.
- Blueprint: `blueprint/src/chapter/ch14_qpbt_observables.tex:395-470,750-1168`.
- The chapter fixes a successful projective Pauli-basis strategy, defines point observables
  \(W^r(u)=\sum_a(-1)^{\operatorname{tr}(ar)}M_a^{(\mathrm{Point},W),u}\), expands the state by two
  EPR registers, and constructs point/line measurements whose consistency relations are evaluated
  on the six-register state.
- The four symmetric register placements are
  \(AA'\), \(BA''\), \(BB'\), and \(AB''\). The source-facing symmetry-transfer lemma at
  `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:882-946` is marked `\notready`;
  the implementation should expose placements and state each required conclusion explicitly.
### Relevant Mathlib definitions
- `Matrix.kronecker` — matrix tensor product, `Mathlib/LinearAlgebra/Matrix/Kronecker.lean:274`.
- `Finsupp.equivFunOnFinite` — finite-function/Finsupp equivalence used for bounded coefficient tables,
  `Mathlib/Data/Finsupp/Fintype.lean:24`.
- `MvPolynomial.totalDegree` — total polynomial degree,
  `Mathlib/Algebra/MvPolynomial/Degrees.lean:433`.
- `Real.rpow` — real powers used by polynomial error functions,
  `Mathlib/Analysis/SpecialFunctions/Pow/Real.lean:35`.
No Mathlib definition matches `ProjectiveSetting`, Pauli line descriptions, six-register placements,
expanded Pauli measurements, or the project’s finite-support error predicates.
### Relevant Mathlib lemmas and theorems
- `Matrix.kronecker_add`, `Matrix.smul_kronecker`, `Matrix.kronecker_smul` — tensor-product linearity,
  `Mathlib/LinearAlgebra/Matrix/Kronecker.lean:306-314`.
- `Matrix.mul_kronecker_mul` — multiplication of Kronecker products,
  `Mathlib/LinearAlgebra/Matrix/Kronecker.lean:382`.
- `Matrix.reindex_apply` — entrywise reindexing along equivalences,
  `Mathlib/LinearAlgebra/Matrix/Defs.lean:509`.
- `MvPolynomial.mem_restrictDegree` — coordinatewise degree bound,
  `Mathlib/RingTheory/MvPolynomial/Basic.lean:184`.
- `MvPolynomial.schwartz_zippel_totalDegree` — total-degree zero probability bound useful for later
  line arguments, `Mathlib/Algebra/MvPolynomial/SchwartzZippel.lean:192`.
- `Real.rpow_nonneg`, `Real.rpow_add` — nonnegativity and exponent algebra,
  `Mathlib/Analysis/SpecialFunctions/Pow/Real.lean:163,207`.
- `PMF.map`, `PMF.map_apply` — general push-forward probability API,
  `Mathlib/Probability/ProbabilityMassFunction/Constructions.lean:45-54`; the project’s explicit-support
  `Distribution.map` is the better local surface here.
### Relevant MIPStarRE declarations
The current worktree is unchanged at `b5da371`. Issue #16 is not merged; its APIs were inspected
read-only from branch `issue-0016-qpbt-residual-skeleton`, whose commits are
`93bcd20`, `311efd9`, and `704c6b8`.
Current stage-4.1 APIs:
- `MIPStarRE.QPBT.Strategy` — heterogeneous local spaces `ιA`, `ιB`, normalized state, and separate
  measurement families `A` and `B`, `MIPStarRE/QPBT/Games/Defs.lean:88-98`.
- `MIPStarRE.QPBT.heteroKron` — rectangular tensor placement,
  `MIPStarRE/QPBT/Games/Defs.lean:108-109`.
- `MIPStarRE.QPBT.Strategy.value` — distribution average of Born correlations,
  `MIPStarRE/QPBT/Games/Defs.lean:137-140`.
- `MIPStarRE.QPBT.Measurement.IsProjective` — all effects satisfy `IsProj`,
  `MIPStarRE/QPBT/Games/Defs.lean:76-81`.
- `MIPStarRE.Quantum.Measurement.postprocess` and
  `MIPStarRE.Quantum.Measurement.postprocess_effect` — complete POVM postprocessing and fiberwise
  effect formula, `MIPStarRE/Quantum/Measurement.lean:127-140`.
- `MIPStarRE.Quantum.IsProj` — local alias for Mathlib’s self-adjoint projection predicate,
  `MIPStarRE/Quantum/FiniteMatrix/NormalizedTrace.lean:82-83`.
- `MIPStarRE.QPBT.phaseSign`, `tauObservable`, `pauliProj`, and `eprState` —
  generalized Pauli operators and EPR state, `MIPStarRE/QPBT/Algebra/Pauli.lean:43-117`.
- `MIPStarRE.QPBT.pauliQuestion` currently lives in `Test/Soundness.lean:108-109`; it must be moved to
  `Test/PauliBasisTest.lean` before ch14 imports it, otherwise the expanded-observable layer risks an
  import cycle.
Issue #16 APIs to consume after merge:
- `MIPStarRE.QPBT.consistencyDefect` — common-space off-diagonal overlap functional,
  `MIPStarRE/QPBT/Games/Consistency.lean:30-37`.
- `MIPStarRE.QPBT.IsConsistentWithin` — inequality form of the consistency relation,
  `Games/Consistency.lean:41-45`.
- `MIPStarRE.QPBT.opDistSq` — averaged squared state-dependent distance between single-operator
  families, `Games/Consistency.lean:49-53`.
- `MIPStarRE.QPBT.opDistSq_eq_opFamilyDistSq_unit` — singleton-family adapter,
  `Games/Consistency.lean:57-63`.
- `MIPStarRE.QPBT.IsPolyErr` and `IsPolyErr2` — exact landed names for one- and two-variable
  polynomial error predicates, `Games/ErrorFunctions.lean:22-31`. The name is `IsPolyErr2`, not
  the Unicode-spelled `IsPolyErr₂` used in the brief prose.
- `MIPStarRE.LDT.Distribution.mixture`, `prod`, and `restrict` — explicit-support distribution
  combinators, `Games/DistributionAux.lean:21-95`.
- `mixture_isProbability`, `prod_isProbability`, `restrict_isProbability` — existing proof obligations
  for those combinators, `Games/DistributionAux.lean:39-44,61-65,92-96`.
- `MIPStarRE.QPBT.Strategy.IsProjective` — heterogeneous strategy predicate,
  `Games/StrategyClasses.lean:19-21`.
- `MIPStarRE.QPBT.LineKind` and `LineDesc L` — landed line interface, parameterized by
  `L : LdParams`, `Observables/LineDefs.lean:35-60`.
- `LineDesc.direction`, `LineDesc.pointSet` — geometric line accessors,
  `Observables/LineDefs.lean:50-60`.
- `DegPoly L c`, `degPolyEval`, `DegPoly.padTo`, `EvaluatesTo`, `evalOpt` —
  coefficient-list and completed evaluation interfaces, `Observables/LineDefs.lean:64-93`.
- `aLinePointDist`, `dLinePointDist`, `linePointDist` — dimension-generic line-point distributions,
  `Observables/LineDefs.lean:110-141`.
- `PolyIndex`, `PolyTuple`, `PolyMeasTuple`, `evalPolyTupleAtOpt`, and `exists_ld_soundness` —
  low-degree measurement/provider interface, `Test/LowDegreeGameTheorems.lean:31-130`.
Important parameterization mismatch: the landed #16 line API is `LineDesc L`, `DegPoly L c`, and
`linePointDist L` for `LdParams L`; it is not keyed directly by bare `(q,m,d)`. Any ch14/ch15
dimension adaptation must use an explicit `LdParams` record. Do not redeclare bare-parameter aliases
merely to match the earlier sketch.
### Suggested approach
1. Rebase issue #17 onto the merged result of issue #16, preserving the current `b5da371` tree until
   then. Import the landed `Games.Consistency`, `Games.ErrorFunctions`, `Games.DistributionAux`,
   `Games.StrategyClasses`, `Observables.LineDefs`, and `Test.LowDegreeGameTheorems` modules.
2. Define `ProjectiveSetting` as a wrapper around
   `Strategy (pauliBasisTest P)` with the source success bound and projectivity proof. Preserve
   `ιA`, `ιB`, `A`, and `B`; the paper’s single `M` is notation, not a justification for collapsing
   heterogeneous player spaces.
3. Define question embeddings and player-specific postprocessed measurements using
   `Measurement.postprocess`. Fold malformed `PauliAnswer` constructors into a fixed valid answer when
   constructing complete typed measurements. Reserve `Option` for evaluation classes such as
   `evalOpt`; do not duplicate the residual-owned `pointAnswerOpt`.
4. Generalize the six-register carrier to
   `((ιA × Reg × Reg) × (ιB × Reg × Reg))` if heterogeneous spaces are retained. Define the four
   `Placement` constructors and side-aware placement operations using `heteroKron`, local
   `leftTensor`/`rightTensor`, and `Matrix.reindex`.
5. Define `pointObs`, `expObs`, `expPointOp`, `pointMeasExp`, `expPointTrace`, and line analogues
   from the landed `phaseSign`, `tauObservable`, `pauliProj`, `eprState`, `LineDesc`, and `DegPoly`
   interfaces. Pass explicitly placed families to `consistencyDefect`/`opFamilyDistSq`.
6. Use top-level `deltaAnticom` and `deltaLine` definitions; do not create strategy namespace
   duplicates. Keep source-facing symmetry transfer out of the completed interface because its
   blueprint node is `\notready`.
7. Mark any theorem whose proof transitively relies on the unfaithful `exists_ld_soundness` provider
   with the required `**Unfaithful:**` documentation until the issue-16
   `docs/paper-gaps/qpbt_ld-dimension-divisibility.tex` obligation is discharged.
### Gaps to fill
- No existing declaration provides `ProjectiveSetting`; the missing source-faithful bundle should
  preserve a heterogeneous `Strategy`, rather than assume a common local Hilbert space.
- No existing declaration provides the four six-register placements. The required new interface is
  a side-aware `place`/placement family over `AA'`, `BA''`, `BB'`, and `AB''`.
- No expanded point or line measurement exists. The concrete operators and their POVM/projectivity
  proof fields remain ch14 obligations.
- No Mathlib theorem directly proves the Pauli convolution, EPR self-consistency, or expanded
  measurement projectivity statements. The required named obligations include
  `expPointOp_eq_convolution`, `pointMeasExp_isProjective`, and the corresponding line claims.
- The landed `linePointDist` probability proofs and low-degree soundness provider remain open
  (`sorry`). Ch14 may consume them, but must not duplicate or silently strengthen them.
- The source-facing symmetry-transfer result is intentionally not ready; expose explicit placement
  conclusions without presenting a transfer theorem as complete.
- The current `pauliQuestion` location must be corrected before introducing ch14 imports.
### Searched
- GitHub issue lookup: `local/bin/gh_common.py issue-view 17` was attempted but blocked by the sandbox
  network (`api.github.com` unavailable). No local issue comment could be checked.
- Read `AGENTS.md`, `docs/api_surface.md`, `local/briefs/42-ch14-brief.md`, the ch14 blueprint, and
  the cited QPBT paper source at lines 47-679.
- Inspected current stage-4.1 declarations under `MIPStarRE/QPBT`, `MIPStarRE/Quantum`, and
  `MIPStarRE/LDT/Basic`.
- Inspected issue #16 without cherry-picking via
  `git show issue-0016-qpbt-residual-skeleton:<path>`.
- Searched Mathlib for Kronecker/reindexing, finite polynomial degree and coefficient APIs,
  finite-support maps, PMF maps, and real powers.
- Searched audits including
  `audits/2026-04-03_paper-formalization-audit-report.md`,
  `audits/2026-04-04_ch08-commutativity-gap-analysis.md`,
  `audits/2026-04-05_lean-formalization-problems.md`, and
  `audits/2026-04-04_stream-d-faithfulness-scouting.md`.
- Confirmed `HEAD` remains `b5da371de32dbaf487d140e389be1d8d317709a4`; no files were edited or committed.
