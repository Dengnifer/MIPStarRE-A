## What changed

Committed `67f77e4` (`feat(QPBT/Algebra): prove Pauli laws and cancellation`).

In [PauliTheorems.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-68-pauli-laws-cancellation/MIPStarRE/QPBT/Algebra/PauliTheorems.lean:98), proved all eleven requested declarations using private entrywise Pauli identities, adjoint formulas, characteristic-two bridges, and additive-character orthogonality with scalar rescaling.

The only remaining hole is the explicitly excluded `exists_qubitIsometry`.

## Evidence

- `PauliTheorems.lean`, `Test/QubitForm.lean`, and `Observables/ExpandedDefs.lean`: type-check successfully.
- `rg -n "sorry|axiom"` reports only line 565, inside `exists_qubitIsometry`.
- `#print axioms` for all eleven targets reports only `propext`, `Classical.choice`, and `Quot.sound`; no `sorryAx`.
- Lock-aware full build: `Build completed successfully (9040 jobs)`.
- Blueprint web render succeeded.
- Blueprint synchronization passed; four unlinked binary specializations produced the expected coverage warnings.
- All 822 generated blueprint declarations resolved.
- Blueprint axiom audit: 652 passed, 0 failed; no proof-level `\leanok` declaration depends on `sorryAx`.
- Pre-commit integrity hooks and `git diff --check` passed.
- Worktree is clean.

## Statement integrity

Paper assumptions: a finite field of prime characteristic, finite coordinate sets, `n ≥ 1` for prime twisted commutation, a nonzero eigenvector for the eigenvalue result, and `v ∉ V⊥` for cancellation.

Lean assumptions: the corresponding field, finite, decidability, prime, algebra, nonempty-coordinate, eigenvector, and submodule hypotheses. No assumptions were added or changed.

Paper conclusions: generalized Pauli multiplication, powers, unitarity, eigenvalue order, twisted commutation, and Fourier cancellation.

Lean conclusions: the same matrix identities and complex-valued uniform expectation identity using canonical `ffChar`.

Verdict: faithful boundary hypotheses. The four binary declarations remain documented characteristic-two specializations with their scope markers preserved.

## Open obligations

`exists_qubitIsometry` at [PauliTheorems.lean:555](/home/drx/MIPStarRE-qpbt/.worktrees/issue-68-pauli-laws-cancellation/MIPStarRE/QPBT/Algebra/PauliTheorems.lean:555) remains open as explicitly excluded from issue #68. It requires constructing the basis-coordinate tensor isometry for `lem:pauli-binary`; the source-index correction is recorded in blueprint remark `rem:pauli-binary-source` and issue #16. No new obligation was introduced.

## Handoff

Review the finite-product multiplication lift, the empty-index binary commutation branch, and the cancellation rescaling that avoids the trace-kernel error. No re-export change is needed because `MIPStarRE/QPBT.lean` already imports this module.

The operator should now run exact-head local CI and dispatch the required independent reviewer. The commit was not pushed.