Addressed F1: removed the duplicate lemma from `Commutation.lean`, imported the existing `applyOperatorToState_finset_sum`, and updated its sole caller.

Validation passed: single-file Lean check, full locked `lake build` (9,179 jobs), and `git diff --check`. No theorem statements changed or proof holes added; existing repository warnings remain.

Changes are uncommitted, ready for `autofix.sh`.