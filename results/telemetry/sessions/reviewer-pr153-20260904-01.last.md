## Findings

- [ ] F1 (changes) `blueprint/src/chapter/ch13_qpbt_test.tex:333` — The proved positivity proposition `AdmissibleParams.one_le_m` is incorrectly placed under a definition-level `\leanok`.
- [ ] F2 (changes) `blueprint/src/chapter/ch13_qpbt_test.tex:352` — The proved level theorem `isCondLinear_pauliCL` is embedded in a definition and has no proof-level completion marker.
- [ ] F3 (blocker) `blueprint/src/chapter/ch13_qpbt_test.tex:353` — `pauliBasisTest` does not match the question-distribution node carrying its `\leanok`.
- [ ] F4 (changes) `MIPStarRE/QPBT/Test/LowDegreeGame.lean:640` — The newly completed `ldGame` definition has no blueprint link or status marker.
- [ ] F5 (changes) `blueprint/src/chapter/ch13_qpbt_test.tex:69` — Both low-degree level proofs claim to apply `lem:cl-concat`, but their Lean proofs construct and verify the representations directly.
- [ ] F6 (changes) `MIPStarRE/QPBT/Games/DistributionAux.lean:203` — Eight new public distribution lemmas have no formalization-support nodes or blueprint dependency edges.
- [ ] F7 (changes) `MIPStarRE/QPBT/Test/SoundnessDefs.lean:47` — The newly completed public theorem `deltaQld_mono` has no blueprint traceability or proof-completion status.
- [ ] F8 (changes) `blueprint/src/chapter/ch13_qpbt_test.tex:565` — The proof-level `\leanok` describes a supremum argument and witness different from the formal proof.
- [ ] F9 (changes) `blueprint/src/chapter/ch12_qpbt_games.tex:1269` — “register-bookkeeping” and implementation-history prose should be replaced by the mathematical coordinate-restriction content.
- [ ] F10 (changes) `MIPStarRE/QPBT/Games/CondLinearTheorems/DirectSumSupport.lean:9` — Changed module documentation uses software-architecture language such as “supporting layer” and “module that owns”.
- [ ] F11 (changes) `MIPStarRE/QPBT/Games/CondLinear.lean:16` — Blueprint line ranges in several changed docstrings are stale and point to unrelated material.

## Review

F1: The blueprint derives \(m\ge1\) at line 347, while the corresponding proposition is the theorem at `PauliBasisTest.lean:50`. A mathematical fact must have its own lemma block with statement- and proof-level `\leanok`; the definition block should link only the admissible-parameter definitions.

F2: The level assertions in lines 371–375 correspond to `isCondLinear_pauliCL` at `PauliBasisTest.lean:574`. Move this theorem to a separate lemma with both completion markers and accurate dependencies.

F3: The blueprint node ends after defining the question distribution at line 398. By contrast, `pauliBasisTest` at `PauliBasisTest.lean:931` also fixes both answer alphabets and `pauliWinPredicate`. Its tag belongs on the complete-game or win-predicate node; the present definition-level `\leanok` is mathematically invalid for it.

F4: `ldGame` now has a complete probability proof, but no `\lean{MIPStarRE.QPBT.ldGame}` occurs in the blueprint. Link it from the node that states the complete game at `ch13_qpbt_test.tex:155`, or introduce a separate complete-game definition, and mark that definition `\leanok`.

F5: `isCondLinear_ldALineCL` (`LowDegreeGame.lean:393`) and `isCondLinear_ldDLineCL` (`LowDegreeGame.lean:434`) explicitly construct `ldALineTerm` and `ldDLineTerm`; neither invokes concatenation. Remove `lem:cl-concat` from the statement and proof `\uses` and describe the direct two- and three-stage constructions, or make the formal proofs actually use that lemma.

F6: Missing nodes cover `Distribution.ext_of_support_of_weight`, `Distribution.map_map`, `uniformDistribution_weight_apply`, `uniformDistribution_map_of_card_fiber`, `uniformDistribution_map_equiv`, both product marginals, and `bind_uniformDistribution_map` through line 344. These are public and several are directly invoked by the newly completed sampler proofs, so each needs a subordinate support statement and the consuming proofs need corresponding `\uses` edges.

F7: `deltaQld_mono` formalizes the constant-adjustment argument discussed around `ch16_qpbt_extraction.tex:175`. Add a formalization-support lemma there with matching hypotheses, conclusion, and both `\leanok` markers.

F8: The blueprint chooses
\(C=\sup_{R'\ge4}(\log R')^{a+b}(R')^{-b}\) and \(a'=a(2c)^a(1+C)\). The formal proof instead sets the explicit factorial bound at `CanonicalParams.lean:216`, invokes `rpow_mul_two_rpow_neg_le` at line 341, and chooses \(a'=a((2c)^a+C)\) at line 223. The proof paragraph must describe that argument. Line 556 should also say that \(2^j\mid2^k\) because \(j\le k\), rather than saying that the exponents \(j,k\) are powers of two.

F9: Replace “register-bookkeeping step” with, for example, “coordinate-restriction identity used in the proof.” At line 1321, replace the implementation-history explanation with the mathematical statement that the general case follows from monotonicity in the number of levels.

F10: State the mathematics directly, such as “This module proves closure of equal-level conditionally linear functions under direct sums.” The related phrases “needed by later formalization stages,” “development lives in,” and “Lean-only support” in `CondLinear.lean:9`, `:51`, `:59`, `:73`, and `:306` should likewise become mathematical descriptions using “formalization-only auxiliary” where needed.

F11: For example, `CondLinear.lean:16` cites lines 1053–1257, while the named nodes now occupy lines 1168–1369; `DirectSumSupport.lean:17` cites 1123–1212 instead of 1238–1326. The Pauli and canonical-parameter docstrings similarly retain pre-insertion ranges. Refresh all changed blueprint citations against the final merged chapter layout.

Summary: 1 direct equivalence mismatch, 5 status or traceability defects, 2 proof/dependency mismatches, and 3 prose/reference issues. No changed `\notready` marker was stale, and I found no new bridge-style hypothesis on the reviewed source-facing declarations.

VERDICT: CHANGES_REQUESTED