## Findings
- [ ] F1 (changes) `.githooks/pre-commit:28` — Treating every `MERGE_HEAD` as a zero-line change lets merge-time edits and non-main merges bypass the owner-only workflow budget.
- [ ] F2 (changes) `.githooks/pre-commit:28` — No behavioral regression test distinguishes permitted inherited base changes from oversized merge-time edits.

## Review
`MERGE_HEAD` reliably identifies an active merge, including in a linked worktree, but it does not establish that every staged byte came from reviewed `main`. Conflict resolution, `git merge --no-commit`, or merging another branch permits additional workflow edits before commit; line 30 then forces `INFRA_CHANGED=0`. That bypasses the sole owner-gated control documented in `local/personas/main.md`.

For a fresh-base merge, the hook can measure the staged result against the verified incoming base (`MERGE_HEAD`), preserving the PR-side diff while excluding inherited base changes. Other merge states should retain ordinary enforcement.

The existing test at `scripts/tests/test_dispatch.py:91` only checks that its pathname appears in the hook. Add executable cases for ordinary oversized rejection, an allowed fresh-base merge with a small PR-side diff, and rejection of oversized edits staged during a merge.

The repository-wide search confirmed this hook is the only `INFRA_CHANGED` enforcement point. The EVOLUTION entry has the required incident citation, and `sh -n .githooks/pre-commit` passed. The supplied context reports green exact-head CI; I could not rerun temp-directory-based tests in the read-only sandbox.

VERDICT: CHANGES_REQUESTED