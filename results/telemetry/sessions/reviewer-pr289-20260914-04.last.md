## Findings

- [ ] F1 (changes) `audits/2026-09-06_combined-polynomial-image.md:1` — The new audit omits the mandatory YAML front matter required by the repository audit format.
- [ ] F2 (changes) `audits/2026-09-06_combined-polynomial-image.md:88` — The audit incorrectly describes `combinePoly_mem_polyFunc` as admitted although it has a complete proof at this head.

## Review

**F1:** `docs/CONTRIBUTING.md` §5 requires new audits to follow `audits/2026-04-29_audit-document-format.md`, which states that every audit begins with YAML metadata. Add at least `title`, `date`, `purpose`, and the relevant `issue`/`pr` fields. This must be fixed before merge.

**F2:** [`MIPStarRE/QPBT/Combining/Defs.lean:150`](MIPStarRE/QPBT/Combining/Defs.lean:150) proves `combinePoly_mem_polyFunc` without `sorry`. Replace “the admitted `combinePoly_mem_polyFunc`” with a current description, such as saying that the new proof uses `combinePolyTuple_mem_polyFunc` instead of the existing direct theorem. A statement-integrity audit must report current proof status accurately.

The Lean mathematics otherwise checks out. The paper defines the combining map at `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:978-983`, identifies \(\mathcal G\) through linearity and separation at lines 1290 and 1326-1371, and reindexes it by polynomial pairs at lines 1374-1379. `IsSeparatedCombined` faithfully expresses this through coefficient vanishing and wrong-block variable exclusion, while the reconstruction and uniqueness theorems establish the required image characterization. The `1 ≤ d` hypothesis on the forward degree map and equivalence is a genuine boundary condition, already present as `AdmissibleParams.hd`, rather than proof debt.

`lake env lean MIPStarRE/QPBT/Combining/CombinedPolynomialImage.lean` completed without diagnostics. The changed Lean file contains no proof holes or forbidden proof mechanisms, and the key reconstruction, degree, uniqueness, and equivalence declarations have only standard axioms in their closures.

VERDICT: CHANGES_REQUESTED