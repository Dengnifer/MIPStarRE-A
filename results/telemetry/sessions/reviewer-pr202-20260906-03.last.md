## Findings
- [ ] F1 (changes) `scripts/blueprint_citations.py:221` — Explicit-citation scanning still misclassifies blueprint filenames as unknown labels, producing false unresolved evidence on this PR.
## Review
**Round-cap adjudication triage for head `8531017cfb8a3821b2bea3175342e1ebe998e7c7`.** I checked the attached prior ledger against the authoritative patch and current checkout rather than starting a fifth full review.
**F1 remains unresolved.** `CANONICAL_CITATION_RE` accepts a backticked filename after “blueprint”; the loop then reports every matching token absent from the label index as unknown. The existing citation at `MIPStarRE/QPBT/Test/PauliBasisTest.lean:63` therefore produces a false failure. Running:
```text
PYTHONDONTWRITEBYTECODE=1 python3 scripts/blueprint_citations.py resolve --path MIPStarRE/QPBT/Test/PauliBasisTest.lean --format plain
```
reports `ch13_qpbt_test.tex UNRESOLVED` and exits with status 1. Restrict explicit-label recognition to label-shaped tokens, excluding filenames and numeric file locators while retaining unknown `eq:`, `sec:`, and `item:` citations. Add a regression case for this existing filename citation. This requires changes before ordinary approval.
**Earlier ledger findings appear addressed:**
- The numeric locator in `Combining/Linearity/Defs.lean` is replaced by `thm:ms-rigidity`; `AdmissibleParams.toLdParams` now identifies `def:ld-game`.
- Bare-token scanning is narrowed, and explicit unknown equation, section, and item labels are recognized.
- Citation-map budgeting retains failure rows; the standalone fallback places the map before the diff.
- Rewriting returns unchanged text when no legacy locator is replaced.
- Both `HelperCompleteness/InputSdp.lean` citations now name `lem:self-improvement-helper`. Its completeness proof contains the input-overlap lower bound and reindexing, consistent with `references/ldt-paper/self_improvement.tex:407` and `blueprint/src/chapter/ch07_self_improvement.tex:187`, rather than the distinct projector-removal step.
**Disposition and residual risk:** The latest prior finding is reproduced, not newly raised. Under §12, the operator must record its fix or defer it to a tracked issue through exact-head adjudication; no such disposition was supplied. The inline attachment was truncated, so I consulted the on-disk authoritative patch for the relevant changes. This limited triage does not certify the entire patch or newly integrated merge hooks. No files were modified, and no review was published directly.
VERDICT: CHANGES_REQUESTED
