## Findings
- [ ] F1 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:947` — The retained-mass support nodes have inaccurate `\uses` metadata and are not connected to the formalized conditioning chain.

## Review
The mathematical statements match their Lean declarations: the retained-mass sum, first-line restriction, push-forward ordering, bounds, positivity, and normalization are represented accurately.

The dependency metadata does not match the proofs. `exists_combinedLine_conditioned_defect_le` directly uses retained-mass positivity and distribution normalization, while `combined_line_measurement_consistency` directly uses the retained-mass bounds. Their blueprint proof blocks at lines 1334 and 1371 omit the new labels, leaving every new support node without a downstream consumer. Conversely, the positivity statement lists the bounds theorem at line 947 even though that dependency belongs only in its proof at line 956. Add the corresponding proof-level edges and remove the misplaced statement-level edge, as required by the formalization-support `\uses` contract.

The labels resolve uniquely. `check_blueprint_latex.py`, `blueprint_lean_sync.py --ci`, and the parse-only axiom audit passed; exact-head CI was already green.

VERDICT: CHANGES_REQUESTED