## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Lines/SubLinePrefix.lean:11` — Correct the reversed mathematical description of which coordinate blocks vanish under prefix truncation.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/Lines/RestrictedAverage.lean:89` — Reuse `SandwichProduct.avgOver_distribution_prod` instead of independently reproving its identical statement.

## Review

F1: The overview says preceding blocks remain untruncated and later blocks vanish. The direction is reversed. Paper `references/qpbt-paper/08_classical_and_quantum_low_degree_tests.tex:233–237`, `eq:cl-dlnf`, defines the projection by zeroing the first `i−1` coordinates; blueprint `def:ld-question-distribution` agrees. The new lemmas `projX_directPrefixProjection_embZ` and `projZ_directPrefixProjection_embX` correctly show that earlier blocks vanish and later blocks remain unchanged. Correct the overview to match these mathematical statements.

F2: `avgOver_prod` has exactly the hypotheses and conclusion of `SandwichProduct.avgOver_distribution_prod` at `MIPStarRE/QPBT/Games/Sandwich/Support.lean:29`. That theorem predates this patch and is already available through the imports. I verified that it closes the new statement directly with `exact`. Remove the duplicate and use the existing API, avoiding two independently maintained proofs.

No new proof holes, kernel bypasses, or public theorem-statement drift were found. The combined-point construction preserves the universal error function and both ordered-product conclusions. The fiber collision estimate explicitly restricts to nonzero directions; the direct sub-line construction retains its documented auxiliary scope.

Four targeted Lean checks passed. `Lines.lean` emitted only its two unchanged, documented construction-gap warnings. Axiom checks for the point construction, sub-line construction, restricted consistency bound, and fiber estimate reported only standard Lean axioms. A full build was not rerun.

The authoritative on-disk patch supplied the truncated attachment’s missing content; the prior ledger contained no findings. The unavailable `lean-conventions` skill was supplemented by the checked-in integrity rules and project conventions. Both findings require correction before approval.

VERDICT: CHANGES_REQUESTED