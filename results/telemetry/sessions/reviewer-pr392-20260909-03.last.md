## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Points/Commutation.lean:58` — The Parseval helper’s docstring assumes row orthogonality, but its conclusion requires the column orthogonality encoded by `horth`.
## Review
F1 requires correction before approval. `sum_norm_sum_smul_sq_of_orthogonal` correctly assumes that the vectors `j ↦ c j i`, indexed by `i`, are orthogonal. Its docstring instead describes `i ↦ c j i`, indexed by `j`. These conditions differ for rectangular coefficient systems: one row `(1, 0)` has squared norm `1`, but with `v = (0, 1)` the weighted-sum squared norm is `0`, not `1`. Correct the docstring’s indices; retain the existing Lean statement and proof.
All four prior findings are addressed:
- `Placement.lean:288` distinguishes `eprCrossSwap` from the blueprint’s four-cycle.
- `qpbt_linearity-theorem-quotation.tex:173` now explains the replacement argument, including normalization, rounding, placements, and error bounds.
- `Consistency.lean:287` uses the imported square-summability estimate.
- `Sandwich.lean:164` uses `DistanceCalculus.applyOperatorToState_mul`; the duplicate declaration is gone.
**Statement Integrity**
For `exists_combinedPointsWitness`, I compared QPBT paper `14_analysis_of_the_pauli_basis_test.tex:689`, label `lem:qld-4-10`, with blueprint `ch15_qpbt_combining.tex:802` and the Lean statement:
- Paper assumptions: admissible parameters and the ambient projective strategy winning with probability at least `1 - ε`.
- Lean assumptions: the same through `AdmissibleParams` and `ProjectiveSetting`, without additional proof-obligation inputs.
- Both conclusions: projective field-pair measurements on the existing expanded spaces, self-consistency, and consistency with both ordered products across all directed opposite placements.
- Verdict: faithful encoding. The universal `K ε^(1/8)` bound supplies the required polynomial error.
The revised paper-gap note correctly distinguishes this completed construction from the still-open common-extension argument for the separate linearity route.
**Validation**
All seven changed Lean files passed individual `lake env lean` checks. The proof-hole/bypass scan and `git diff --check` were clean. The axiom audit for `exists_combinedPointsWitness` reports only `propext`, `Classical.choice`, and `Quot.sound`.
The authoritative patch matches the checkout diff; the worktree remains unchanged. Full-project and document builds were not rerun. The external `lean-conventions` reference was unavailable locally; repository review and integrity rules were inspected.
VERDICT: CHANGES_REQUESTED
