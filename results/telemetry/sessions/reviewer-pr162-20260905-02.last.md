## Findings
- [ ] F1 (blocker) `MIPStarRE/QPBT/Test/MagicSquareTheorems/Rigidity/Anticommutation.lean:344` — `msVarObsA_anticommute_opDistSq` and its Bob analogue are pre-isometry estimates on the Naimark-dilated state, not the cited post-isometry, ideal-state conclusions of `thm:ms-rigidity`.
## Review
F1 must be fixed before merge. In `references/qpbt-paper/08_classical_and_quantum_low_degree_tests.tex:624-646`, `thm:ms-rigidity` first constructs local isometries, defines `\tilde A = φ_A A φ_A†` and `\tilde B = φ_B B φ_B†`, and states the anticommutation estimates with respect to the ideal EPR-plus-auxiliary state. The blueprint repeats these objects and the same-state requirement at `blueprint/src/chapter/ch13_qpbt_test.tex:232-249`.
By contrast, these Lean lemmas concern `msVarObsA/B`, formed from the Naimark-dilated measurements, on `(msDilatedStrategy S).ψ`; they contain no rigidity witness, subsequent isometry, auxiliary state, or ideal state. They are valid formalization-only precursors, but their docstrings must not call them “Alice's/Bob's half” or say they give “that statement.” The related claim at `CellRelations.lean:189-191` that `msCellObsA` is the source’s `\tilde A` needs the same correction. Reserve source-facing wording for bounds on `msAnticommutatorDistanceA/B` after the witness construction.
All prior-ledger findings are otherwise resolved: the duplicate reflection predicate and alias are gone, `NormCloseOn` is now explicitly distinguished from the source’s squared distance, its generic operations are colocated with the definition, and the existing Kronecker projection lemma is reused.
The operator proofs and the `624 * sqrt ε` bookkeeping check out. All six changed Lean files type-check. The new code contains no `sorry`, axioms, or proof-bypass constructs. The remaining `exists_ms_rigidity` `sorry` is pre-existing and inventoried as the overarching target in `local/briefs/42-residual-brief.md:186`.
VERDICT: CHANGES_REQUESTED
