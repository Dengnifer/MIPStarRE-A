Addressed F1: removed `applyOperatorToState_mul'` and updated all uses to `DistanceCalculus.applyOperatorToState_mul`.
Both edited files type-check without warnings. Full `lake build` passed (9160 jobs); existing warnings remain elsewhere. Proof-integrity scans and `git diff --check` passed.
Paper-facing statements and blueprint links are unchanged. Changes remain uncommitted for `autofix.sh`.
