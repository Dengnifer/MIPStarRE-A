## Findings

- [ ] F1 (blocker) `blueprint/src/chapter/ch15_qpbt_combining.tex:895` — The corrected theorem yields the operator-distance bound `δ_{x,z}`, not `δ_{x,z}/2`, in `lem:qld-4-10`.
- [ ] F2 (changes) `docs/paper-gaps/qpbt_linearity-theorem-quotation.tex:106` — The living gap note still says the theorem preserves `δ` and remains unproved, contradicting this PR’s proved `2δ` statement.

## Review

F1 must be fixed before merge. Lines 877–887 define `δ_{x,z}` as the average squared operator defect and derive correlation `1 - δ_{x,z}/2`. Applying `thm:linearity` with parameter `δ := δ_{x,z}/2` now gives
\[
2(\delta_{x,z}/2)=\delta_{x,z}
\]
for `stateDepDistSq`, whereas line 895 claims `δ_{x,z}/2`. Either change the displayed operator-distance bound to `δ_{x,z}`, or explicitly use the binary-measurement distance, for which the half-sized bound is valid.

F2 requires changes under the paper-gap policy. Lines 111–115 of the older note say `exists_exactly_linear_observables` returns the same defect and “its proof remains open.” The declaration now proves an operator-distance bound of `2δ`, while `exists_exactly_linear_observables_binaryObservableDistSq` proves the measurement-distance bound `δ`. The note should be revised or explicitly superseded while preserving its still-relevant common-ancilla discussion. This follows `docs/paper-gaps/policy.tex:198-202`, which requires notes to remain coherent accounts of the present mathematical status.

The normalization correction itself is mathematically supported. The printed conclusion of Theorem 10 at `references/nv-paper/fullpaper.tex:1074-1088` uses `δ`, but the binary-measurement computation at lines 900–912 differs from squared operator distance by a factor of two. The new counterexample note and the Fourier–Naimark proof consistently establish `2δ` for `stateDepDistSq`, equivalently `δ` for `binaryObservableDistSq`.

Statement-integrity audit: the Lean hypotheses match the provider theorem, apart from ordinary finite-dimensional and decidability boundary data. No bridge, residual, witness package, or other proof-debt hypothesis was added. Exact linearity and averaging quantifiers are preserved. The changed Lean route contains no `sorry`, `axiom`, or prohibited kernel bypass; the final declarations’ axiom dependencies are only standard logical axioms (`propext`, `Classical.choice`, and `Quot.sound`).

VERDICT: CHANGES_REQUESTED