## Findings
- [ ] F1 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:1311` — The source sub-line lemma still links to a construction using a different distribution.
- [ ] F2 (changes) `MIPStarRE/QPBT/Games/RestrictedAverage.lean:60` — The new conditional-average identities and restoration bound lack blueprint entries.
- [ ] F3 (changes) `MIPStarRE/QPBT/Combining/ExtendedLineGame/PassingValue.lean:89` — The new passing-value bounds and their error function lack blueprint entries.
- [ ] F4 (changes) `MIPStarRE/QPBT/Combining/DirectLowDegree/ResampledCoefficientConsistency.lean:192` — The new coefficient-resampling results lack blueprint counterparts.
- [ ] F5 (changes) `MIPStarRE/QPBT/Combining/ExtendedLineGame/DiagonalParameterDefectTransport.lean:11` — The module description uses implementation terminology instead of describing the mathematical identity.
## Review
**F1: Mathematical mismatch.** The blueprint requires the seed-indexed extended-line marginal, whereas `exists_subLineWitness` at `Combining/Lines.lean:329` constructs the directly indexed marginal. Its docstring explicitly records that transport remains open. Move the existence link to a separate lemma following `def:direct-subline-law`, with the appropriate completion markers. Retain the source statement without certification; its `\notready` tag does not make the mismatched link accurate.
**F2: Missing synchronization.** Neither `avgOver_restrict_mul_mass` nor `avgOver_le_restrict_add_discarded_mass` appears in the blueprint. Add subordinate support entries stating the normalization identity and
\(\mathbb E_\mu f\le p\,\mathbb E_{\mu\mid G}f+\mu(G^c)\),
with \(p=\mu(G)>0\) and \(f\le1\), together with declaration links, completion markers, and actual dependencies.
**F3: Missing synchronization.** The original and Naimark-dilated strategy bounds, and `directPassingErrorEnvelope`, have no blueprint entries. Record the error function and supplied-measurement hypotheses explicitly in separate auxiliary entries. These results must not certify the source theorem `lem:qld-4-7`.
**F4: Missing synchronization.** The axis and diagonal coefficient-consistency bounds at lines 192 and 215, and the joint evaluation defect they use, are absent from the blueprint. Add support entries preserving the unit-state hypothesis, the `degree/q` term, and the full parameter average for zero directions.
**F5: Prose.** Replace “identifies the two coefficient readers definitionally” with “the two rules for extracting coefficients coincide.” Replace “a formalization-only bridge” at line 13 with “an auxiliary identity.” These replacements describe the mathematics without implementation vocabulary.
Counts: **1 mathematical mismatch, 3 missing-entry groups, 1 prose issue.** The mismatch is the principal concern.
I used the authoritative on-disk patch because the attachment was truncated. No files were modified or builds rerun. Source-distribution transport and the complex-modulus obligations remain open; the separate real-part entries appropriately avoid certifying them.
VERDICT: CHANGES_REQUESTED
