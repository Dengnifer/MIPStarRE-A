## Findings
- [ ] F1 (changes) `docs/DEVIATIONS.md:19` — The register already indexes all 21 `qpbt_*.tex` notes, so the new “19 of 21” and “two unindexed notes” claims are false.
- [ ] F2 (changes) `docs/DEVIATIONS.md:25` — c10 is assigned to a gap note that contains no combining-map analysis and whose register row covers only Claims 17-1–17-3, leaving this new open deviation unregistered.

## Review
The singleton calculation agrees with the cited Lean definitions: the three compatible affine choices produce `0`, `1`, and `T`, and `T` lacks a value under the zero-direction evaluation relation.

The documentary records do not yet satisfy their contracts. `docs/paper-gaps/qpbt-gap-register.md` currently has 21 rows, including both notes called “unindexed” here. More importantly, `qpbt_subline-claims-line-marginal.tex` does not discuss `eq:combine-lines`, the affine-choice example, or the combining-map carrier. Its register row is explicitly scoped to the three subline claims.

The paper-gap policy requires the mathematical discrepancy to live in a standalone LaTeX note, and the register is the C3 record. Update that note and its register row to cover `def:combine-map`, or add a dedicated note and register entry; then correct the counts in `docs/DEVIATIONS.md`. The Markdown audit can remain supporting evidence, but it is not the required paper-gap record.

VERDICT: CHANGES_REQUESTED