## What changed

Committed `2c7a4b5` (`fix(review): consolidate Magic Square swap helpers`).

- Moved shared binary-field and Pauli laws into [Pauli.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-104-ms-swap-isometry/MIPStarRE/QPBT/Algebra/Pauli.lean).
- Updated [Observables.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-104-ms-swap-isometry/MIPStarRE/QPBT/Test/MagicSquareTheorems/PerfectStrategy/Observables.lean) and [Swap.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-104-ms-swap-isometry/MIPStarRE/QPBT/Test/MagicSquareTheorems/Rigidity/Swap.lean) to use canonical `tauShift`/`tauPhase`.
- Removed duplicate swap Pauli definitions, stale `isReflection_*` names, and the `Rigidity.Transfer` import.
- Marked public swap support declarations as formalization-only and replaced stale blueprint line citations with labels.
- Added YAML metadata and held-status guidance to the [orientation audit](/home/drx/MIPStarRE-qpbt/.worktrees/issue-104-ms-swap-isometry/audits/2026-09-04_magic-square-rigidity-orientation-obstruction.md).
- No compatibility aliases were needed: issues #105 and #115 have no external source use of the removed names.
- Worktree is clean; nothing was pushed.

## Evidence

- `lake env lean` passes for all three changed Lean files.
- `rg -n "sorry|axiom"` reports no matches in those files.
- `lake build MIPStarRE.QPBT.Test.MagicSquareTheorems` succeeds: 8766 jobs.
- Lock-aware full `lake build` succeeds: 9082 jobs.
- `python3 scripts/blueprint_lean_sync.py` passes.
- Git hooks installed and verified; pre-commit integrity checks passed.
- The direct consumer checks with only its pre-existing `exists_ms_rigidity` `sorry` warning.

## Statement integrity

Paper assumptions: one universal constant, nonnegative `ε`, and a Magic Square strategy of value at least `1 - ε`.

Lean assumptions: unchanged.

Paper conclusion: local isometries extracting two EPR pairs, four distinguished effect estimates, and the associated anticommutation estimates at `O(sqrt ε)`.

Lean conclusion: unchanged in `exists_ms_rigidity`.

Verdict: exact for this repair; no paper-labelled theorem signature changed. The held symmetric-game orientation concern remains unresolved.

## Open obligations

No `sorry` remains in the changed Lean files.

The pre-existing `exists_ms_rigidity` proof remains open at `MagicSquareTheorems.lean:134`. Its joint extraction cannot be discharged until owner decision B5 on #26 resolves the orientation obstruction recorded in the held audit.

## Handoff

Review the canonical `tauShift`/`tauPhase` orientation and the local replacement for the removed `Transfer` dependency first. No `MIPStarRE/QPBT.lean` re-export is needed. Exact-head CI and an independent reviewer session remain the operator’s next steps.