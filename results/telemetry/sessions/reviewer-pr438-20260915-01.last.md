## Findings

- [ ] F6 (blocker) `blueprint/src/chapter/ch13_qpbt_test.tex:209` — The `\leanok` node additionally asserts the unformalized fixed-index probability and averaged \(2/(mq)\) estimate.
- [ ] F7 (changes) `blueprint/src/chapter/ch13_qpbt_test.tex:209` — “asserted by the linked declarations” is Lean-facing status prose and should be dropped.

## Review

F6 is an equivalence/status mismatch. `aLinePointDist_zero_direction_mass` states only that the axis expectation is zero (`ZeroDirectionMass.lean:24`), while `dLinePointDist_zero_direction_mass_le` states only the diagonal bound \(1/|\F_q|\) (`DiagonalZeroDirectionMass.lean:30`). Neither linked signature states the conditional probability \(q^{-(m-i+1)}\) or its average bounded by \(2/(mq)\). Move that separate calculation to an untagged remark, or add and link a fully proved declaration with that exact statement; otherwise the statement- and proof-level `\leanok` markers overclaim the node’s formalized content.

For F7, drop “These are the bounds asserted by the linked declarations.” The preceding mathematical sentences already state the two bounds.

Prior findings are resolved:

- F1: the \(2/(mq)\) estimate is now correctly attributed to averaging over the prefix index.
- F2: both missing dependencies were added.
- F3: “for the Lean proof” was removed.
- F4: “raw seed” and “raw coordinate” were replaced.
- F5: the implementation-history wording was removed from the Lean docstring.

Summary: 1 blueprint/Lean equivalence-status mismatch and 1 prose issue. The equivalence/status mismatch is the blocking concern.

VERDICT: CHANGES_REQUESTED