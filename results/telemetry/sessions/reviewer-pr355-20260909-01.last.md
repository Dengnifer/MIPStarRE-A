## Findings
- [ ] F1 (changes) `local/bin/ci.sh:868` — Preserve the bibliography copy performed by `leanblueprint pdf`.
## Review
The installed `leanblueprint.client.mk_pdf()` copies `blueprint/print/print.bbl` to `blueprint/src/web.bbl` after compilation succeeds. Calling `latexmk` directly drops this step.
When `texra-blueprint` is unavailable, the existing fallback skips bibliography generation and runs `leanblueprint web`. That renderer reads `web.bbl`, which is untracked. Consequently, a clean worktree loses its web bibliography, while a reused worktree can retain stale citations. Missing bibliography files produce warnings, so this regression can still report success.
Restore the copy after the compiler and PDF checks succeed. Extend the success test so the fake compiler creates `print.bbl` and the web renderer verifies that `web.bbl` was refreshed.
All four new regression tests, `bash -n local/bin/ci.sh`, and `git diff --check` passed at `66deaef7`. The current fake web renderer does not check bibliography inputs. No real TeX render was run; the worktree remains unchanged. The prior findings ledger was empty.
VERDICT: CHANGES_REQUESTED
