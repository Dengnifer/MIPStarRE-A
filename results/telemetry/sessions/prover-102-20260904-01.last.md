## What changed

Added required `## References` sections to both rigidity modules. Centralized `heteroKron_neg_left` and `heteroKron_neg_right` in `Games/Defs.lean`, removing four duplicated local definitions. No public theorem signatures changed.

Committed as `b55d974` (`fix(review): centralize tensor-negation lemmas`).

## Evidence

All five changed files pass `lake env lean`. The changed-file `sorry|axiom` scan is empty. `lake build MIPStarRE.QPBT.Test.MagicSquareTheorems` completed successfully (8731 jobs). Repository hooks passed, and the worktree is clean.

A bare whole-repository build was not run; the operator-prescribed direct-consumer target build passed. Its warnings were pre-existing and outside the changed files.

## Statement integrity

No paper-labelled theorem changed. The shared helper statements are identical to the removed local statements.

## Open obligations

None introduced or present in the changed files.

## Handoff

Review the shared tensor-negation API and unchanged call sites first. `MIPStarRE/QPBT.lean` and blueprint files were not modified.