## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/ExtendedLines/Estimates.lean:32` — Sixty-six newly public auxiliary declarations lack blueprint traceability entries and corresponding `\uses` coverage.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/Lines/Construction.lean:388` — “Lean-only consistency theorem” uses Lean-specific jargon in reader-facing prose.
- [ ] F3 (changes) `MIPStarRE/QPBT/Combining/Lines/DiscardedMass.lean:16` — “the last raw direction coordinate” uses the explicitly banned term “raw.”

## Review

**F1:** The unlinked declarations span `ExtendedLines/{Estimates,Measurement,Overlap}.lean`, `Lines/{Conditioning,Construction,DiscardedMass,PointComparison}.lean`, `OrderedPoints.lean`, `SubLineXDeficit.lean`, and `XEvalDeficit.lean`. Examples include `subline_remove_X_factor_at`, the `SubLineWitness` conditional-measurement chain, the line-conditioning and restoration results, and the point-comparison bounds.

These are newly public mathematical declarations but occur nowhere under `blueprint/src`. Consequently, related proof dependencies are also absent from `\uses`; for example, the direct-line overlap proof invokes unlinked intermediate estimates. Add formalization-only blueprint entries, grouping related declarations where appropriate, with accurate `\lean`, status, and `\uses` tags. Alternatively, make genuinely implementation-local helpers private. In particular, `CombinedPointsWitness.orderedXZ_dist_le` should be recorded alongside its already-linked `orderedZX_dist_le` counterpart.

**F2:** Replace “Lean-only consistency theorem” with “Formalization-only consistency theorem.”

**F3:** Replace “the last raw direction coordinate” with “the final unprojected direction coordinate.”

I found no mathematical-signature mismatch for `exists_globalPairWitness`: its universal constants, parameter quantification, and `GlobalPairWitness` conclusion agree with `lem:qld-4-7`. Its new `\leanok` is therefore accurate. The remaining open source declarations continue to be marked `\notready`.

Summary: 1 blueprint status/traceability issue and 2 prose issues. The category-A issue is the primary merge blocker.

VERDICT: CHANGES_REQUESTED