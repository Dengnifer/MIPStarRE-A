## Findings
- [ ] F1 (blocker) `local/bin/lake-root.sh:49` — Detached registered worktrees are omitted from target ownership checks, allowing cleanup to delete their live `.lake` target
## Review
F1 is reproducible. After preparing a branch worktree, switching it to detached `HEAD`, and invoking cleanup for the original branch, `git worktree list --porcelain` emits `detached` rather than a `branch` record. `validate_repo_paths` therefore never compares that worktree with the target. `pr_merge.py:433` also treats the worktree as absent and proceeds with cleanup. The external directory is deleted while the registered worktree’s `.lake` symlink still points to it, contradicting the ownership guarantee in `local/protocols/build-cache.md:52`.
Inspect each registered worktree’s actual canonical `.lake` symlink destination during cleanup, including detached worktrees. During preparation, exempt only the current worktree so idempotent setup remains valid. Add a detached-worktree regression test.
The concrete findings from rounds 1 and 2 are otherwise addressed: canonical root and escape guards, bidirectional `hot-main` protection, checkout overlap checks, branch-alias collision detection, sandbox `--add-dir`, and missing-worktree cleanup warnings are present.
Verification passed for 17 focused Lake/dispatch tests, 13 merge-gate tests, shell syntax checks, and `git diff --check`. The detached-worktree reproduction failed as described above.
VERDICT: CHANGES_REQUESTED
