## Findings

- [ ] F2 (changes) `local/bin/dispatch.sh:203` — `mathfix` accepts any model or effort, so the forbidden pre-astra Codex lane remains dispatchable.
- [ ] F3 (changes) `local/protocols/issues-prs.md:171` — The activation protocol depends on `results/telemetry/owner-tools/astra-poll.sh`, which is absent from the head tree.
- [ ] F4 (changes) `local/protocols/meta.md:62` — `owner-sessions.jsonl` is listed under schemas without a record schema or writer contract.

## Review

Prior F1 is resolved: `local/personas/main.md:16-22` now routes current mathematical gaps to the owner-launched Fable 5.1 lane through #27 and reserves Codex dispatch for future astra availability.

For F2, the normative protocol permits Codex `mathfix` only with astra/ultra after #26 reports availability. The dispatcher merely checks role membership and effort syntax, then optionally forwards any model and effort. The positive test at `scripts/tests/test_dispatch.py:97` proves the intended invocation works but does not verify that missing or incorrect model/effort is rejected. Add a role-specific preflight or keep the role unavailable until activation, with negative tests.

For F3, the named poller does not exist at head, although multiple new documents treat it as the authoritative activation signal. Add the tool or reference an existing, auditable mechanism.

For F4, Fable attempts bypass `telemetry.py`, while the shared ten-session/time budget depends on reconstructing them across both lanes. Define the owner-session fields and append ownership, including how attempts are associated with a mathematical gap.

`git diff --check` and `bash -n local/bin/dispatch.sh` pass. The supplied exact-head CI is green. I could not rerun the unit suite because the read-only review sandbox provides no writable temporary directory; the errors occurred during `tempfile.TemporaryDirectory()`, before test logic ran.

VERDICT: CHANGES_REQUESTED