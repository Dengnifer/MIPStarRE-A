## Findings

- [ ] F1 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:37` — The marginal’s projectivity theorem is incorrectly included in a definition block.
- [ ] F2 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:51` — Projectivity and completeness of `tauDotProj` are theorem statements, not part of its definition.
- [ ] F3 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:64` — Projectivity and completeness of `tildeM` require separate lemma blocks.
- [ ] F4 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:79` — The blueprint uses one-based basis indices while the linked Lean declarations use zero-based `Fin`.
- [ ] F5 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:15` — The new full-field decoder statements lack blueprint traceability, and the conjugation dependency prose is inaccurate.
- [ ] F6 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:131` — `deltaConstructPaulis` is a definition linked from inside a lemma rather than from its own definition entry.
- [ ] F7 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:197` — The non-encoding entry omits `nonencodingMarginalMass` and has no `\uses`.
- [ ] F8 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:200` — `\notready` is stale because `nonencodingMarginalMass_le` now has the matching Lean statement.
- [ ] F9 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:300` — The displayed extraction scale lacks a traceability entry for `deltaExtract`.
- [ ] F10 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:383` — `deltaExtract_le_deltaQld` has no separate, non-`\leanok` blueprint entry.
- [ ] F11 (advisory) `blueprint/src/chapter/ch16_qpbt_extraction.tex:15` — “raw global-witness error” uses process-shaped terminology.
- [ ] F12 (advisory) `MIPStarRE/QPBT/Algebra/Decoding.lean:8` — Decoder documentation repeatedly uses Lean and software terminology and misattributes the left-inverse identity.
- [ ] F13 (advisory) `MIPStarRE/QPBT/Algebra/LowDegreeCodeTheorems.lean:7` — “raw polynomial specialization” and “Poly API” are implementation terminology.
- [ ] F14 (advisory) `MIPStarRE/QPBT/Extraction/Defs.lean:6` — Register documentation uses “bookkeeping,” “landed,” “Lean realization,” “Lean-only support,” and “API.”
- [ ] F15 (advisory) `MIPStarRE/QPBT/Extraction/Consistency.lean:42` — “raw” and “The Lean conclusion” should be replaced by mathematical descriptions.

## Review

F1-F3: the blueprint definitions assert separate facts. In Lean, these are respectively `marginalPoly_isProjective` at `Observables.lean:64`, `tauDotProj_isProj` and `sum_tauDotProj_eq_one` at `Defs.lean:211,223`, and `tildeM_isProj` and `sum_tildeM_eq_one` at `Observables.lean:91,105`. Split those facts into lemma blocks and give them independent status instead of allowing each definition’s `\leanok` to cover both kinds of declaration.

F4: lines 79, 95, 100, 150, and 268 use \(j,j'\in\{1,\ldots,t\}\), whereas the linked signatures use `Fin P.model.basisDim`, for example `Observables.lean:118,134,184,244` and `Consistency.lean:218`. Use \(\{0,\ldots,t-1\}\) or state an explicit reindexing. Until then, the affected `\leanok` markers are not exact.

F5: `decodeFq`, `IsEncoding`, `decodeFq_add`, `decodeFq_smul`, `decodeFq_lowDegreeEncoding`, and `decodeFq_dotProduct_indicatorVec` at `Decoding.lean:61,98,112,123,136,151` match the corrected decoder assertions but are unlinked. Add definition entries for the completed definitions and separate trace-only entries for the open obligations. Also correct line 28: the calculation at blueprint line 292 cancels the decoder term directly, so decoder linearity and the left-inverse identity are not used in that conjugation step.

F6-F8: make the displayed definition of \(\delta_P\) its own definition node linked to `deltaConstructPaulis`. Likewise, give `nonencodingMarginalMass` at `Consistency.lean:107` its own definition entry and add the dependencies for the bound. The bound’s statement matches `nonencodingMarginalMass_le`; remove `\notready`, but retain its open-obligation link without `\leanok`.

F9-F10: add a definition entry for `deltaExtract` at `Defs.lean:257`. Add `deltaExtract_le_deltaQld` at `Unitary.lean:115` as a separate formalization-only lemma without `\leanok`; its exact statement should not be presented as formalizing the entire square-root discussion in the robustness remark.

F11-F15: replace “raw” with “before enlargement” or “unadjusted.” In `Decoding.lean`, replace “Poly-facing,” “Lean-only helper,” “function-level implementation,” “Package,” and “subtype wrapper” with direct descriptions of polynomial evaluation and bounded representatives. The citation at line 129 should identify `04_preliminaries.tex:917-922` as the source of \(\mathrm{Dec}(g_h)=h\). In `Defs.lean`, describe register-order equivalences and operator placements directly. In `Consistency.lean:152`, use “The conclusion keeps the global-witness error \(\delta_G\) separate.”

Prior-round triage: the missing unitarity content is fixed by `ExtractionWitness.swap_right_unitary` and `swap_left_unitary`; the earlier `Unitary.lean` packaging phrase and “definitional bridge” wording are gone. The stale `\notready`, register jargon, and decoder packaging terminology remain. The projectivity links now resolve, but F1-F3 remain declaration-kind mismatches.

All added tags resolve, the Chapter 11 decoder block remains statement-accurate, and `GlobalPairWitness` represents precisely the measurement and consistency data supplied by `lem:qld-4-7`, not an additional proof-debt hypothesis. The category-A findings are the merge-blocking issues: 4 equivalence/kind mismatches, 5 missing or misplaced traceability entries, and 1 stale status marker. There are also 5 prose clusters.

VERDICT: CHANGES_REQUESTED