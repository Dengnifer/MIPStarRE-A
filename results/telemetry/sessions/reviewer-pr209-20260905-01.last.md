## Findings
- [ ] F1 (changes) `local/personas/main.md:13` — The operator is told to dispatch `mathfix` through a Codex-only path, but the protocol requires the current Fable model.

## Review
`local/protocols/issues-prs.md:164` requires `mathfix` to use Fable 5.1 until Astra is available, and `local/DESIGN.md:140` confirms that Fable is a Claude-side lane while `dispatch.sh` only admits Astra in the future. However, the trusted main persona lists `mathfix` among `dispatch.sh` roles and says all sub-sessions use that dispatcher. `dispatch.sh` always executes `codex`; `agent.sh` provides only the generic optional Codex model override.

Consequently, an operator following the committed instructions cannot launch the currently mandated Fable session through the prescribed path and may silently run an ordinary Codex session instead. Define the sanctioned Fable launch and telemetry/budget-accounting path, or align the model policy with an executable dispatcher route. The regression test should verify the selected model, effort, and persona; it currently forces `test-model` and checks only parser acceptance and sandbox placement.

`git diff --check`, shell syntax, Python AST parsing, and telemetry help passed. The unit suite could not be rerun because the read-only review sandbox has no writable temporary directory; exact-head CI was supplied as green. `shellcheck` is unavailable.

VERDICT: CHANGES_REQUESTED