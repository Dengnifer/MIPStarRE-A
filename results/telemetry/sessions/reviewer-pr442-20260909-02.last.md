## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Points/Commutation.lean:58` — Correct the Parseval docstring: the theorem requires column orthogonality, not row orthogonality.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/Points/Closeness.lean:57` — Reuse the pointwise distance-by-four estimate instead of maintaining a second proof.
- [ ] F3 (changes) `MIPStarRE/QPBT/Combining/Points/Sandwich.lean:44` — Remove the duplicate operator-action multiplication lemma and use the existing public API.

## Review
**F1:** `sum_norm_sum_smul_sq_of_orthogonal` assumes orthogonality of the columns `j ↦ c j i`, indexed by `i`. Its docstring instead asserts the result from orthogonal rows `i ↦ c j i`. Those assumptions are not interchangeable for arbitrary `I` and `J`: one row `(1, 0)` has squared norm one, but with `v = (0, 1)` the advertised identity reads `0 = 1`. The Lean statement and proof are correct; change the mathematical description to column orthogonality.

**F2:** The pointwise argument inside `opFamilyDistSq_uniform_le_four` duplicates `DistanceCalculus.sum_norm_sub_apply_sq_le_four` in `MIPStarRE/QPBT/Observables/LineMeasurement/SquareRootError.lean:60`, with identical square-summability and normalization hypotheses. Keep one shared pointwise theorem and apply it under `avgOver_mono`. Place it in common distance infrastructure if needed to preserve the module hierarchy. This avoids maintaining two substantial proofs of the same bound.

**F3:** `applyOperatorToState_mul'` has exactly the statement of `DistanceCalculus.applyOperatorToState_mul` in `MIPStarRE/QPBT/Games/DistanceTheorems/Support.lean:64`. That declaration is already available through the existing imports; I verified its direct application in Lean. Remove the new declaration and update its callers.

**Statement Audit:** No source-statement drift found in the paper-facing results. Against `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:523` (`lem:qld-comm-line-cons`) and `:689` (`lem:qld-4-10`), the Lean statements retain the winning projective-strategy assumptions, measurement conclusions, all directed opposite placements, and universally quantified polynomial-error bounds. The intermediate estimates are discharged internally. The existing completed-evaluation convention is preserved.

**Verification:** Direct Lean checks passed for `Points.lean`, `LineMeasurement.lean`, and `SamePlacementDistance.lean`. The four audited result closures contain only `propext`, `Classical.choice`, and `Quot.sound`. No new proof holes or prohibited bypasses were found. The authoritative on-disk patch was used despite attachment truncation.

Residual risk: full project and blueprint builds were not rerun; the supplied exact-head CI reports success.

VERDICT: CHANGES_REQUESTED