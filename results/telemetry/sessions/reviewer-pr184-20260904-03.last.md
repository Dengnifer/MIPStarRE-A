## Findings
- [ ] F1 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:1830` — The downstream proof still depends on the source-shaped combined-lines lemma instead of the new established-bound support node.
- [ ] F2 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:252` — The linearity-import discussion neither cites the registered quotation-gap note nor includes its factor-two discrepancy.
## Review
F1: `lem:qld-4-13-established` is introduced at line 1757, but its label appears nowhere else. The proof of `lem:qld-4-7` still lists `lem:qld-4-13` in `\uses` and later claims to use the weaker established error. This leaves the formalization-support node disconnected and makes the register’s “used in the sequel” claim inaccurate. Connect the proof to `lem:qld-4-13-established` and distinguish any remaining source-lemma references. This is required by the auxiliary-node dependency convention in `docs/blueprint_style_guide.md`.
F2: The new `gap:qpbt_linearity-theorem-quotation` bibliography entry has no citation anywhere in the blueprint. `rem:linearity-import` is the corresponding discussion, but it says the quotation differs in three respects while the updated note also identifies the lost factor of two. Cite the note there and reconcile the enumeration with the corrected normalization.
Prior-round F1/F5/F6 are resolved by restoring the source-shaped symmetrization node and separately certifying the proved given-strategy helper. F3/F8, F4, and F9 are also resolved. The combined-lines separation requested by prior F2/F7 now exists; F1 above concerns its missing downstream dependency edge.
The complete 1,168-line patch was reviewed without truncation, and `git diff --check` is clean. The exact-head local CI was reported green; no additional build was run.
VERDICT: CHANGES_REQUESTED
