## Findings

- [ ] F1 (changes) `audits/2026-09-12-issue509-extended-line-point-error.md:1` — The new audit lacks mandatory YAML front matter and uses the wrong dated filename format.

## Review

F1 must be fixed before merge. `audits/2026-04-29_audit-document-format.md:17-24` requires every audit to begin with YAML front matter, while lines 61-66 require dated files to use `YYYY-MM-DD_`. Add at least `title`, `date`, and `purpose` metadata, include `issue: "#509"` and `pr: "#531"`, and rename this file to `2026-09-12_issue509-extended-line-point-error.md`. No repository references currently use the old filename.

The Lean changes are otherwise sound. `CombinedPointsWitness.exists_error_of_projective` correctly bounds the finitely many witness comparisons, and `linear_quadratic_agreement_le` correctly specializes `directCoefficientCollision_avg_le`. The new file has no proof holes or integrity bypasses, and focused Lean checks passed.

The source comparison is accurate: `lem:qld-4-10` at `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:689-709` constructs a polynomially controlled point family, while `lem:qld-4-13` at lines 1020-1034 uses that family rather than quantifying over arbitrary supplied witnesses. The new `**Unfaithful:**` marker and paper-gap revision correctly distinguish the false unrestricted auxiliary from the paper theorem. The quadratic-answer lower-bound calculation, including the X-axis branch mass `1/8`, is mathematically consistent.

The five `sorry` declarations in `Apply.lean` are pre-existing and clearly documented as open obligations. The complete strategy-level counterexample remains informal, but the note explicitly limits the Lean claim to its two verified supporting lemmas.

VERDICT: CHANGES_REQUESTED