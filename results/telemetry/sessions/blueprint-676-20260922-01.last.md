## In sync

The three canonical-complement entries now explicitly document rows, columns, pivots, prefix lengths, and sum indexing.

## Drifted

F1 addressed; independent confirmation remains with main.

## Mathematically suspect

None found in scope. The existing algorithm obligation remains unchanged.

## Changed

Committed `3249d644`. No Lean or `\leanok` changes. PR body updated; [exact receipt written](/tmp/main-fullspeed-fix691-indexing-20260922-result.md).

## Evidence

Web build, lint, sync, all 2,133 declaration checks, normal hooks, and whitespace checks passed. Six pre-existing sync warnings remain. Worktree clean.

Per-file Lean checks were not rerun. Main handles publication, full CI, and independent review.