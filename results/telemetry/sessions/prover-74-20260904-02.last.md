## What changed

Committed `d32e6d4` (`fix(review): share direct rejection probability API`).

- Moved generic Born-weight nonnegativity and POVM normalization into [Defs.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-74-direct-ld-game-value/MIPStarRE/QPBT/Games/Defs.lean:121).
- Updated [GameValue.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-74-direct-ld-game-value/MIPStarRE/QPBT/Combining/DirectLowDegree/GameValue.lean:1) to use that shared API and corrected its source citations and formalization-only classification.
- Re-exported `GameValue` through [DirectLowDegree.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-74-direct-ld-game-value/MIPStarRE/QPBT/Combining/DirectLowDegree.lean:2). `MIPStarRE/QPBT.lean` remains untouched.
- All seven requested declarations remain proved with unchanged signatures.

## Evidence

- `lake env lean` passed for all three touched Lean files.
- `lake env lean MIPStarRE/QPBT.lean` passed.
- Lock-aware full `lake build` passed.
- `rg -n "sorry|axiom"` returned no matches in touched files.
- `git diff --check` and hook checks passed.
- Worktree is clean and unpushed.

## Statement integrity

Paper assumptions: uniform ordered QPBT type pairs, the stated verifier, and the mature LDT’s equal outer and role weights.

Lean assumptions: `DirectLdParams`, an arbitrary game strategy, and only `ε` plus the acceptance bound for the final error theorem. No projectivity or bridge hypothesis.

Paper conclusion: no named source theorem; the calculus is a direct consequence of the two probability weightings.

Lean conclusion: exact rejection/value identity and factor-three comparison.

Verdict: exact formalization-only auxiliary calculus.

## Open obligations

None in the changed files.

## Handoff

Review the shared `outcomeWeight` normalization API first, then the facade import. No additional `MIPStarRE/QPBT.lean` re-export edit is needed. The branch is currently 12 commits behind `github/main`, so integration may require the operator’s normal update procedure.