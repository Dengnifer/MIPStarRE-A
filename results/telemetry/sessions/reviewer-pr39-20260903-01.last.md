## Findings

- none

## Review

The argv fix matches the installed CLI grammar: the new ordering is accepted, while the previous ordering fails on `-C`. Fresh and resumed dry-run assembly is covered, with a hermetic fake `codex` preflight; all three focused tests pass.

The protocol amendment cites the correct incident, the committed smoke records share the expected thread ID, and the pre-commit budget now counts the regression test. The guarded workflow diff is 145 lines. Shell syntax checks and `git diff --check` pass, and exact-head CI is green. No material residual risk found.

VERDICT: APPROVED