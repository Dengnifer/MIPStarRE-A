## Findings

- [ ] F1 (blocker) `local/bin/checked-push.sh:108` — Exact-tuple validation is bypassed whenever the target worktree's native pre-push hook is stale or not selected.
- [ ] F2 (changes) `.githooks/pre-push:7` — An already-current checked push advertises no tuple, so the confirmation hook rejects a valid no-op publication.
- [ ] F3 (changes) `.githooks/pre-commit:40` — The workflow budget omits `scripts/tests/test_pre_push_hook.py`, causing the newly added regression test to fail.
- [ ] F4 (changes) `scripts/install_git_hooks.sh:97` — Hook installation still directs full checks through plain `git push`, recreating the idle-transport failure.

## Review

F1 remains from the prior ledger. The helper directly runs the target worktree's `.githooks/pre-push`, then assumes ordinary `git push` will invoke that same hook. It neither pins nor validates `core.hooksPath`. In an isolated reproduction with native hook selection disabled, the remote moved after preflight, yet the command returned success and published the target commit. This contradicts the fail-closed contract in `local/protocols/issues-prs.md`.

F2 also remains. Repeating a successful checked push returned 1 because Git supplied no tuple for the already-current ref. This breaks `pr_open.py`'s create-or-adopt behavior and retry paths in `github-sync.sh` and `autofix.sh`.

The full helper suite ran 464 tests and failed exactly `test_standalone_workflow_test_growth_is_budgeted`: the pre-commit budget list does not include the newly tested path. The eight dedicated pre-push tests, shell syntax checks, and `git diff --check` passed.

The earlier mutable-local-ref and emergency-bypass findings are resolved: the captured SHA is published and explicit `MIPSTARRE_SKIP_HOOKS=1` is preserved.

VERDICT: CHANGES_REQUESTED