## Findings
- [ ] F5 (changes) `MIPStarRE/QPBT/Combining/Points/Commutation.lean:58` — Correct the Parseval docstring: the hypothesis requires orthogonal columns, not rows.
- [ ] F6 (changes) `MIPStarRE/QPBT/Combining/Points/Closeness.lean:92` — Reuse the imported projective square-summability theorem instead of repeating its proof.
## Review
F5: `sum_norm_sum_smul_sq_of_orthogonal` assumes orthogonality of the vectors `j ↦ c j i`, indexed by `i`. Its docstring instead describes `i ↦ c j i`, indexed by `j`. The documented assertion is false for rectangular coefficient matrices: take one row `(1,0)`, `N = 1`, and `v = (0,1)`. The documented hypothesis holds, but the asserted equality becomes `0 = 1`. Correct the docstring to describe column orthogonality. The Lean statement and proof are correct.
F6: `sum_place_effect_conjTranspose_mul_self_le_one` repeats the argument already provided by `sum_effect_conjTranspose_mul_self_le_one_of_projective` in `Points/Consistency.lean:98`, which is already imported. Apply that theorem to `S.placedMeasurement p M` with `S.placedMeasurement_isProjective p M hM`. I type-checked this replacement successfully. This must be addressed under the review’s duplication criterion.
All four prior findings are addressed:
- F1: `Points.lean:2` imports `WitnessMarginals`, making it reachable through the root import.
- F2: `Placement.lean:290` correctly distinguishes `eprCrossSwap` from the blueprint permutation product.
- F3: The paper-gap note now explains Parseval transfer, sandwich consistency, rounding, and the final error bound.
- F4: The duplicated operator-product application helper is removed; callers reuse `DistanceCalculus.applyOperatorToState_mul`.
The statement-integrity audit found no theorem-level drift. `exists_combinedPointsWitness` retains the admissible projective-strategy premises and produces projective measurements on the specified expanded local spaces, both ordered-product comparisons, and all directed opposite placements. Its universal `K ε^(1/8)` bound realizes the polynomial-error conclusion of `lem:qld-4-10` in `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:689–709`. The marginal bounds retain the supplied witness error as `4 * δQ`, consistent with the calculations following `eq:qqm` at paper lines 912–935.
Validation: the authoritative patch matches the checkout diff. `Points.lean` and `WitnessMarginals.lean` type-check without warnings. The changed files contain no proof holes or forbidden proof mechanisms; the construction and both marginal theorems depend only on `propext`, `Classical.choice`, and `Quot.sound`.
I did not rerun the full build; its success is supplied by the session context. The referenced `lean-conventions` skill was unavailable locally, so I applied the repository’s supplied conventions.
VERDICT: CHANGES_REQUESTED
