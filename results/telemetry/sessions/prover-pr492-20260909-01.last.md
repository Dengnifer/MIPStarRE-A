Addressed both findings:

- F1: Removed four duplicate lemmas and reused `reindexOp_nonneg`, `place_finsetSum`, and `place_comm` from the shared placement module.
- F2: Corrected the truncation description: preceding blocks vanish; later blocks remain untruncated, matching paper Equation `eq:cl-dlnf`.

Both files type-check. Full `lake build` passed (9,191 jobs), as did `git diff --check`. No new proof holes; existing repository warnings remain.

Changes are uncommitted in the two reviewed files. The lifecycle scripts must commit them and update PR #492’s findings.