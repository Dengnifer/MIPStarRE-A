## Findings

- [ ] F1 (changes) `docs/paper-gaps/references.bib:43` — `Natarajan2017Linearity` now has two `booktitle` fields; replace the existing value instead of adding a duplicate.

## Review

F1 must be fixed before merge. Duplicate BibTeX fields produce ambiguous metadata and are commonly ignored with a warning. Keep one brace-protected `booktitle` value.

The mathematical review otherwise passes. The factor-of-two correction agrees with the calculation at `references/nv-paper/fullpaper.tex:903-911`, and the paper-gap note’s four-point counterexample correctly rules out the printed operator-distance bound even with an ancillary extension. `exists_exactly_linear_observables` has no proof-debt hypotheses: its assumptions match the finite-dimensional encoding of Theorem 10 at lines 1074-1088, while its corrected `2δ` operator bound and equivalent `δ` measurement bound are explicitly documented.

The Fourier, BLR, Naimark, and stability proofs are grounded in proved local/Mathlib results, with no `sorry`, axioms, forbidden tactics, default witnesses, or conclusion-shaped inputs. All six changed Lean files type-check cleanly; the paper-gap structural check, proof-evasion audit, and `git diff --check` also pass.

VERDICT: CHANGES_REQUESTED