## Findings
- [ ] F1 (blocker) `blueprint/src/chapter/ch04_projective.tex:1492` — `lem:left-marginal-state-evaluation` asserts full complex trace equality, but its linked Lean theorem proves only equality of real parts.
## Review
F1 must be fixed before merge. `leftMarginal_ev_eq` at `LocalityPreservingRepair.lean:170` states equality using `ev`, which is defined at `MIPStarRE/LDT/Basic/QuantumState.lean:173` as the real part of the normalized trace. Thus it proves only
\[
\operatorname{Re}\tau(\rho(X\otimes I))
=
\operatorname{Re}\tau(\rho_A X),
\]
whereas the new blueprint node displays equality in \(\mathbb C\). Since \(X\) is arbitrary rather than self-adjoint, those traces need not be real, so the Lean conclusion is genuinely weaker.
The private lemma `normalizedTrace_leftMarginalDensity_mul_eq` at `LocalityPreservingRepair.lean:128` already proves the stronger complex identity. Either expose a public version stated through `(leftMarginalState ψ).density` and link the blueprint node to it, or change the blueprint display to equality of real parts so it matches `leftMarginal_ev_eq`.
All five affected Lean files type-check. `checkdecls` resolved all 976 blueprint declarations, and `git diff --check` passed. The only `sorry` encountered is the pre-existing, explicitly marked `**Unfaithful:**` attainment obligation in `StrategyClasses.lean:771`, tracked by issue `#98`.
VERDICT: CHANGES_REQUESTED
