Addressed F1–F3; changes are uncommitted for `autofix.sh`.

- **F1:** Corrected prefix truncation prose: earlier blocks vanish; later blocks remain untruncated, matching paper `eq:cl-dlnf`, lines 230–237.
- **F2:** Extracted `avgOver_lineRepMap_resample_parameter` into shared support, updated both callers, and added its blueprint entry.
- **F3:** Reused `Quantum.reindex_nonneg`.

Validation passed: `lake build` (9,173 jobs), all five edited Lean files, blueprint rendering, and all 1,647 declaration links. Five axiom checks showed only standard Lean axioms.

Existing theorem statements and proof debt are unchanged. Existing style warnings remain.

Mathlib audit: reused the project’s positivity API, backed by `Matrix.posSemidef_submatrix_equiv` in `Mathlib/LinearAlgebra/Matrix/PosDef.lean`; no new Mathlib dependencies.