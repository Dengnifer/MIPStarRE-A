## Findings

- [ ] F1 (changes) `.githooks/pre-commit:31` — The `MERGE_HEAD` path still trusts non-main merge heads, allowing a side branch to import more than 400 workflow lines without the owner override.
- [ ] F2 (changes) `.githooks/pre-commit:37` — The new merge-budget behavior has no regression test distinguishing inherited main changes from oversized merge content.

## Review

F1 is only partially resolved. Comparing the index with `MERGE_HEAD` correctly handles the intended fresh-base merge and includes conflict-resolution edits. However, the condition accepts every merge head. If a side branch contributes 401 workflow lines and the current branch contributes none, the merged index equals `MERGE_HEAD`, so this diff reports zero and bypasses the owner-only budget. Use the alternate base only after validating that `MERGE_HEAD` belongs to the trusted main lineage; otherwise retain the ordinary `HEAD` comparison or reject the merge.

F2 remains unresolved. The authoritative patch changes only the hook and evolution ledger. The existing test merely checks that a path literal occurs in the hook; it never executes merge behavior. Add temporary-repository tests covering a permitted main refresh, rejected oversized merge-time or non-main content, and unchanged ordinary-commit enforcement.

The hook passes `sh -n`, and the patch passes `git diff --check`.

VERDICT: CHANGES_REQUESTED