## Findings

- [ ] F1 (changes) `local/bin/codex-policy-shim.sh:21` — Attached short model options bypass the Astra-only validation.

## Review

F1: The matcher handles `-m MODEL`, `--model MODEL`, and `--model=MODEL`, but forwards `-mMODEL` unchanged. A read-only reproduction using an intercepted `exec` confirmed that `exec -mgpt-5.6-sol -- "review fixture"` exits successfully and forwards the non-Astra model option to the `exec` subcommand. The injected top-level model option does not validate that subcommand override. Normalize or reject attached short model options, and add regression coverage.

I reviewed the complete authoritative patch, including the portion omitted from the attachment. Four read-only-compatible tests passed, along with in-memory continuation checks covering resumed charges, legacy metadata, status deduplication, and the cumulative-time boundary. All four changed shell scripts and three Python files passed syntax checks; `git diff --check` passed.

The full fixture suite was not rerun because it requires filesystem writes. No real Codex invocation or provider verification was performed. The checkout remains unchanged.

VERDICT: CHANGES_REQUESTED