## Findings

- [ ] F1 (changes) `scripts/tests/test_dispatch.py:140` — No rejecting test exercises oversized workflow edits during a qualifying main merge, so restoring a zero-budget bypass would still pass these assertions.

## Review

Both prior F1 variants are resolved. The ancestry check limits the exemption to merge heads contained in `github/main`, while non-main merges retain the `HEAD` comparison; the side-branch test covers that rejection path. This is consistent with Git’s documented [`MERGE_HEAD` behavior](https://git-scm.com/docs/git-merge).

Both prior F2 variants remain represented by F1 above. The main-merge test only verifies that inherited content passes, while the rejection test never enters the qualified `BUDGET_BASE=MERGE_HEAD` branch. Setting the qualifying branch back to `INFRA_CHANGED=0` would satisfy both tests. Add a case that performs the qualifying main merge, stages more than 400 workflow lines afterward, and expects exit 1 and the budget diagnostic.

The authoritative 144-line patch was complete. `git diff --check`, Dash syntax validation, and Python AST parsing passed. The focused unit suite could not run in this read-only sandbox because `tempfile` found no writable temporary directory; the supplied exact-head local CI status is green.

VERDICT: CHANGES_REQUESTED