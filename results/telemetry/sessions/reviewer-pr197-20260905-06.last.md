## Findings

- [ ] F1 (blocker) `local/bin/checked-push.sh:146` — `push.followTags=true` lets the final command publish tag refs omitted from preflight.

## Review

The prior findings are addressed for the explicit branch update: no-op pushes succeed, the branch lease survives stale or missing native hooks, the test budget and guidance are updated, and validation uses the branch’s registered clean worktree.

F1 remains because Git honors `push.followTags` despite the explicit branch refspec. I reproduced this through the full helper with an annotated tag and a missing native hook: preflight received only the branch tuple, but the remote received both the branch and tag. With the current hook selected, the unexpected second tuple instead rejects the push. The lease protects only `REMOTE_REF`, so it cannot constrain the tag when the native hook is stale or absent. Disable implicit tag publication in the final command and add a regression covering this configuration.

The 14 changed hook/budget tests pass; shell syntax and `git diff --check` also pass. At the round cap, F1 must be fixed or deferred to a tracked issue through the §12 adjudication record; do not start another full review round.

VERDICT: CHANGES_REQUESTED