## Findings
- [ ] F5 (changes) `MIPStarRE/QPBT/Games/DistanceTheorems.lean:688` — `avg_closeness` duplicates the imported weighted Cauchy–Schwarz/Jensen estimate.
- [ ] F6 (changes) `MIPStarRE/QPBT/Games/DistanceTheorems/Support.lean:95` — `finset_sum_norm_mul_apply_le` repeats the preceding contraction proof almost verbatim.
- [ ] F4 (advisory) `MIPStarRE/QPBT/Games/DistanceTheorems.lean:103` — `fact:agreement` item 3 remains implemented only for left-projective measurements.
## Review
F5 must be fixed before merge. The `hcs` and `hnorm_sqrt` block specializes the already imported `MIPStarRE.LDT.Preliminaries.avgOver_abs_le_sqrt_of_pointwise` to `f x := ‖v x‖` and `g x := ‖v x‖ ^ 2`. Apply that lemma using `sq_nonneg`, the probability-mass bound, and `le_abs_self`, retaining only the vector triangle estimate specific to `avg_closeness`.
F6 must also be fixed. Lines 95–121 repeat lines 68–93 with a `Finset` replacing the full finite type. State the more general finite-set contraction first, then derive `sum_norm_mul_apply_le` by taking `s := Finset.univ`.
For F4, the paper says “either” family is projective in `references/qpbt-paper/06_nonlocal_games_and_mipstar.tex:306-309`, label `fact:agreement`, as does `blueprint/src/chapter/ch12_qpbt_games.tex:252`. The `_left` theorem is correct and honestly named, and removing the aggregate `\leanok` correctly acknowledges incomplete coverage. A right-projective or disjunctive wrapper is still needed before restoring that tag.
The remaining source-facing statements and constants match the cited results in `06_nonlocal_games_and_mipstar.tex:295-461` and `14_analysis_of_the_pauli_basis_test.tex:100-129`. In particular, the commutator proof’s explicit constant `16` is a valid realization of the paper’s asymptotic bound. I found no added bridge assumptions, changed quantifier order, or weakened conclusions beyond the candidly restricted agreement theorem.
Prior F1 and F2 are resolved by reuse of `avgOver_abs_le_sqrt_of_pointwise` and `qMatchMass_leftRight_postprocess_ge`. Prior F3 is also resolved: the duplicate named adapters were removed, and the remaining projective wrapper minimally composes the canonical measurement conversion with projectivity while preserving empty-space support.
The exact-head local CI is green, the changed files type-check, and `git diff --check` passes. No new `sorry`, axiom, or prohibited proof mechanism was introduced. The unchanged sorries at lines 639 and 806 are recorded as deferred imported results in `local/briefs/42-residual-brief.md:173` and `local/briefs/42-ch14-brief.md:127`. The external `lean-conventions` skill was unavailable, so I applied the checked-in `AGENTS.md`, project conventions, anti-pattern catalog, and review checklist.
VERDICT: CHANGES_REQUESTED
