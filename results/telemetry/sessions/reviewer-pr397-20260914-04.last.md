## Findings

- [ ] F1 (blocker) - — Retarget this PR from the already-merged stack base to `main`; the current target expands the review and merge to 859 unrelated files.

## Review

The base commit `8c401506` from `issue-388-combined-point-producer` was merged into `main` by PR #392 at commit `d7cfa505` on September 9, 2026. Because PR #397 still targets that obsolete branch while its head merges newer `main`, its authoritative diff contains 859 files, 80,704 additions, and 10,478 deletions. The attached review diff was consequently truncated, and merging the PR would update the obsolete stack branch rather than `main`.

Retarget PR #397 to `main`, refresh it from current `github/main`, and rerun CI and review on a new head SHA. Against `main`, the intended change is only three files with 86 additions and one deletion.

The intended mathematical delta itself looks correct. `consistencyDefect_opposite_symm` matches blueprint `lem:opposite-placement-consistency-symmetry`: swapping the two finite outcome sums, applying `ProjectiveSetting.place_comm` for opposite placements, and using symmetry of inequality of outcomes proves the stated equality. It is explicitly presented as formalization-only support rather than as the full symmetric-equivalents assertion from `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:882-894`.

The Lean file type-checks, contains no proof holes or forbidden tactics, and its axiom closure is limited to `propext`, `Classical.choice`, and `Quot.sound`. No source-statement drift was found in this three-file delta.

VERDICT: CHANGES_REQUESTED