Addressed F1–F11: separated `extendedQ_spec` into conditional blueprint support, corrected the combining-map status, added the missing support entries and dependencies, and revised the flagged prose.

Statement audit: the paper asserts existence; `extendedQ_spec` assumes supplied joint measurements and proves their coarse-graining guarantees. That condition is now explicit, and the source node remains `\notready`. No Lean statements or proofs changed.

Validation passed:

- `lake build`: 9,158 jobs.
- All five edited Lean files type-check.
- Blueprint web build and all 1,656 declaration checks.
- All 12 support declarations have no `sorryAx` dependency.
- `git diff --check`.

Six modified files remain uncommitted for `autofix.sh`. Existing proof debt and repository warnings remain unchanged.