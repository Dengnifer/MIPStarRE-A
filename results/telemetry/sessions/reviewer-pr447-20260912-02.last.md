## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Lines/AxisLineResampling.lean:45` — Extract the generic joint-resampling lemma shared with the existing diagonal proof.
## Review
F1: The local `hresample` duplicates the mathematical statement and proof in `MIPStarRE/QPBT/Combining/Lines/DiagonalResampling.lean:52`. It contains no axis-specific reasoning, so the same canonical-representative argument is now maintained independently in two files. Extract one named lemma into a shared dependency and use it in both proofs. This requires changes under the review’s modularity and duplication criterion.
The statement of `avgOver_aLinePointDist_resample_parameter` is mathematically faithful. The axis distribution is defined at `references/qpbt-paper/08_classical_and_quantum_low_degree_tests.tex:278`, label `def:line-point-dist`; the conditional sampling supports the pasting application at `14_analysis_of_the_pauli_basis_test.tex:955`. Lean uses the existing `LdParams` domain and an arbitrary real-valued test function, proving equality of joint expectations without additional proof assumptions or error terms. Retaining the seed agrees with the refinement documented in `blueprint/src/chapter/ch13_qpbt_test.tex:177`.
Both changed files type-check. The new theorem depends only on `propext`, `Classical.choice`, and `Quot.sound`. The importing file’s sole `sorry` warning belongs to the unchanged `combined_line_measurement_consistency`, explicitly tracked under #18 and #414.
Validation limits: no full build was rerun; the supplied context reports successful CI for this head. The external `lean-conventions` skill was unavailable locally, so review used the checked-in proof-integrity and convention documents.
VERDICT: CHANGES_REQUESTED
