## Findings
- [ ] F1 (changes) `local/bin/dispatch.sh:218` — `mathfix` can run before the required #26 activation report because the guard checks only a model-name substring and effort.
- [ ] F2 (changes) `results/telemetry/owner-tools/astra-poll.sh:11` — The hourly poller invokes `codex exec` outside `dispatch.sh`, losing the required session capture and telemetry.
- [ ] F3 (changes) `results/telemetry/owner-tools/astra-poll.sh:21` — The poller posts through raw, hard-coded `gh api` instead of the mandatory `gh_common.py` GitHub layer.
- [ ] F4 (changes) `local/README.md:65` — The operator guide still says every session uses `dispatch.sh`, contradicting the newly documented owner-launched Fable lane.
- [ ] F5 (changes) `local/DESIGN.md:113` — The new eight-role vocabulary was not propagated to `agent.sh` and the prose-reviewer persona, which still enumerate only seven roles.
## Review
F1: `issues-prs.md:171-177` makes the successful #26 report the activation gate, but `dispatch.sh` never reads the poller’s `astra-announced` or `astra.txt` state. The new success test likewise launches `model="astra"` without any activation record. Require verified activation and bind the dispatch to the reported model identifier; test rejection before activation and acceptance afterward.
F2: `sessions.md:24-30` and `meta.md:76-78` define every `codex exec` as a dispatched, metered session. Running this command hourly creates systematic telemetry loss. Since the task is configuration inspection, a deterministic parser would avoid a model session entirely; otherwise it must use the dispatcher and normal accounting.
F3: This bypasses `AGENTS.md:16-18` and `issues-prs.md:35-43`. It also loses repository resolution, API headers, bounded retry, and ambiguous-write handling. A POST accepted by GitHub followed by client failure can be repeated because the sentinel is not created. Add the needed idempotent issue-comment operation to `gh_common.py`.
F4: The patch correctly qualifies the rule as “Every Codex session” in `meta.md` and `sessions.md`, but not in the operator guide. `DESIGN.md:126` also still calls `sessions.jsonl` the record for every agent session while omitting `owner-sessions.jsonl`. These surfaces should describe the Fable exception consistently.
F5: `agent.sh:193-194` rejects `mathfix` as unknown and falsely reports the dispatcher’s accepted role set; `local/personas/prose-reviewer.md:16-18` also claims the fixed vocabulary has seven roles. Either support the future route, including its effort and activation requirements, or document an explicit wrapper exclusion and correct both enumerations.
Prior-ledger triage: the main-persona routing issue is resolved; the non-astra/non-ultra checks are resolved, though activation remains F1; the missing poller is resolved as an existence issue; and `owner-sessions.jsonl` now has a schema and writer contract.
Verification: `python3 -B -m unittest scripts.tests.test_dispatch` passed all 10 tests; both changed shell scripts passed `bash -n`; `git diff --check` passed. A broad repository grep was truncated, so no conclusion relies on it; scoped searches over the affected workflow files were completed. The worktree remained unchanged.
VERDICT: CHANGES_REQUESTED
