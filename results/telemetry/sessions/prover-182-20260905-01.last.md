## What changed

Added the public complex identity `normalizedTrace_leftMarginalState_density_mul_eq` in [LocalityPreservingRepair.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-182-projective-rounding-support-and-dependen/MIPStarRE/LDT/MakingMeasurementsProjective/LocalityPreservingRepair.lean:152). Marginal normalization and real-valued evaluation now derive from it. Public support docstrings identify their formalization-only role and supported theorems.

Updated Chapters 4 and 14 to:

- Link the full complex normalized-trace equality.
- Move marginal evaluation to proof-level normalization dependencies.
- Connect all marginal/rank support nodes and document the zero-rank branch.
- Restore `lem:Q-completeness` and add `lem:P-projectivity`.
- Use normalized trace and `ρ_{\mathsf A}` consistently.
- Preserve the Chapter 14 theorem range at lines 425-470.

Committed as `679e1a5 fix(review): align rounding support dependencies`.

## Evidence

All focused Lean checks passed:

- `LocalityPreservingRepair.lean`
- `DistanceTheorems.lean`
- `DistanceTheorems/Calculus.lean`
- `DistanceTheorems/ProjectiveRounding.lean`
- `StrategyClasses.lean`

`rg -n "sorry|axiom"` is empty for `LocalityPreservingRepair.lean`. The changed-file scan reports only the pre-existing tracked `StrategyClasses.lean:771` obligation.

Additional results:

- Full lock-managed `lake build`: passed, 9,073 jobs.
- `lake build MIPStarRE.LDT.Test.AxiomAudit`: passed.
- `leanblueprint web`: passed.
- Blueprint sync and reverse-coverage checks: passed, 977 declarations.
- `lake exe checkdecls blueprint/lean_decls`: all 977 resolved.
- Blueprint axiom audit: 827 passed, 0 failed; no proof-level `\leanok` declaration depends on `sorryAx`.
- Comparator, paper-gap, statement-origin, proof-integrity, unit-test, and hook checks passed.
- Worktree is clean.

Exact-head `local/bin/ci.sh` was not published because the commit is intentionally unpushed and the protocol rejects a local head differing from the remote PR head. The PDF gate remains baseline-blocked by the unrelated malformed `\Id_\hilb` at `ch15_qpbt_combining.tex:760`; the requested web renderer passes.

## Statement integrity

No paper-labelled Lean signature changed.

Paper assumptions/conclusion: consistent POVMs on a normalized bipartite state admit an Alice-side projective measurement with \(O(\delta^{1/4})\) distance.

Lean assumptions/conclusion: unchanged; the formalization-only auxiliary gives the explicit constant \(220\). The new marginal theorem assumes finite coordinate spaces and a nonempty right factor and proves the full complex normalized-trace equality.

Verdict: exact paper-facing statement; faithful boundary hypotheses for the formalization-only marginal identity.

## Open obligations

The only changed-file `sorry` remains `exists_symmetric_projective_strategy` in `StrategyClasses.lean`. It requires an independent finite-dimensional attainment theorem for `Game.value`; it is documented in `docs/paper-gaps/qpbt_symmetrization-attainment.tex` and issue `#98`. No new obligation was introduced.

## Handoff

Review the complex identity and its Chapter 4 link first, then the Chapter 14 nonzero-rank/projectivity explanation. No `MIPStarRE/QPBT.lean` change or new re-export is needed because `Orthonormalization.lean` already imports the locality module. Push `679e1a5`, then run exact-head local CI and review.