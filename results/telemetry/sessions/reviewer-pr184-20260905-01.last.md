## Findings
- [ ] F1 (blocker) `blueprint/src/chapter/ch16_qpbt_extraction.tex:366` — The source-labelled extraction lemma still links a conditional `GlobalPairWitness` helper.
- [ ] F2 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:252` — The provider's factor-two normalization defect is incorrectly counted as a QPBT quotation discrepancy.
- [ ] F3 (changes) `docs/paper-gaps/qpbt_anticommuting-probability.tex:62` — The `d = 0` counterexample is outside the paper's positive-integer parameter domain and does not show a missing source hypothesis.
## Review
F1: `exists_extractionWitness` takes `w : GlobalPairWitness S deltaG` as a public hypothesis (`MIPStarRE/QPBT/Extraction/Unitary.lean:89-99`). The revised note correctly identifies this as proof debt, but the source-labelled node still links that declaration. This conflicts with `docs/paper-gaps/proof-gap-protocol.tex:247-257` and `docs/project_conventions.md:53-57`, which require conditional helpers to be conditionally named and kept outside source theorem blocks. Its Lean docstring also still calls it source-facing and claims it introduces no extra premise. Move it to an explicit formalization-support node and link a paper-facing construction theorem from `lem:qld-unitary`, even if that theorem retains a tracked `sorry`.
F2: The adjacent normalization remark at lines 291-304 correctly observes that the QPBT quotation uses `\approx_\delta`, which only asserts an `O(\delta)` bound and therefore absorbs the factor two. The exact erroneous `≤ δ` operator bound occurs in the cited provider theorem itself. The chapter, quotation note, and register should retain the three actual quotation differences and describe normalization as an independent provider correction.
F3: The source declares `\N` to be the positive integers at `references/qpbt-paper/04_preliminaries.tex:6` and places `q,m,d` in `\N` at `08_classical_and_quantum_low_degree_tests.tex:903-904`. Chapter 13 already records that `d ≥ 1` is implicit in the source domain. The note should retain the valid exact calculation and identify the false assertions in the printed proof, but present positivity as an explicit faithful boundary condition rather than a missing source hypothesis.
The prior symmetrization, combined-lines routing, decoder, characteristic-scope, extraction-status, and proof-level `\leanok` findings are otherwise resolved. The authoritative patch was reviewed and `git diff --check` passes; the supplied local-CI status is green.
VERDICT: CHANGES_REQUESTED
