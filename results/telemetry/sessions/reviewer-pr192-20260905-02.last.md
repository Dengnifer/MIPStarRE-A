## Findings

- [ ] F1 (blocker) `blueprint/src/chapter/ch13_qpbt_test.tex:279` — The displayed binary effects omit Lean’s wrong-form-answer postprocessing, so the statement `\leanok` is not mathematically equivalent.
- [ ] F2 (blocker) `blueprint/src/chapter/ch13_qpbt_test.tex:298` — The anticommutator distance is evaluated on the original state, whereas Lean evaluates it on the extracted ideal state.
- [ ] F3 (changes) `MIPStarRE/QPBT/Test/MagicSquareTheorems/Rigidity/Consistency.lean:69` — The new public consistency-distance and transfer declarations have no blueprint definition or formalization-support nodes.
- [ ] F4 (changes) `MIPStarRE/QPBT/Test/MagicSquareTheorems.lean:187` — The symmetric-consistent specialization lacks a blueprint corollary, and its source-class attribution silently omits projectivity.
- [ ] F5 (changes) `MIPStarRE/QPBT/Test/MagicSquareTheorems/Rigidity/Consistency.lean:14` — “Consistency defect” reuses the source term for a different functional, namely squared state-dependent distance.
- [ ] F6 (changes) `MIPStarRE/QPBT/Test/MagicSquareTheorems/Rigidity/Consistency.lean:99` — “Formalization-only bridge” uses banned process terminology.
- [ ] F7 (changes) `blueprint/src/chapter/ch13_qpbt_test.tex:307` — The proof ends with Lean-status and issue-tracking prose instead of mathematical exposition.
- [ ] F8 (changes) `MIPStarRE/QPBT/Test/MagicSquareTheorems.lean:152` — “The `sorry` is the assembly” exposes Lean and process jargon in a mathematical docstring.

## Review

The added consistency hypothesis is a documented mathematical correction to a false source statement, not a proof-obligation bundle. The paper-gap note and the blueprint both make that restriction explicit. Two remaining mismatches nevertheless invalidate the statement-level `\leanok` as written.

For F1, Lean defines the defect using `(S.A (.var j)).postprocess msBitOrZero` and similarly for Bob in [Consistency.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-172-ms-rigidity-gap/MIPStarRE/QPBT/Test/MagicSquareTheorems/Rigidity/Consistency.lean:69). The same postprocessing occurs in the four operator distances. The map sends every triple answer to bit `0`, as documented in [MagicSquare.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-172-ms-rigidity-gap/MIPStarRE/QPBT/Test/MagicSquare.lean:73). By contrast, the blueprint displays the binary effects \(A_b^{\mathsf{Variable}_j}\) and \(B_b^{\mathsf{Variable}_j}\) without defining this coarse-graining. Define that binary relabeling mathematically and use the resulting effects throughout the hypothesis and conclusion.

For F2, [the blueprint](/home/drx/MIPStarRE-qpbt/.worktrees/issue-172-ms-rigidity-gap/blueprint/src/chapter/ch13_qpbt_test.tex:298) writes
\(\langle\psi|(M-N)^\dagger(M-N)|\psi\rangle\), where \(\psi\) is the original state. Both Lean anticommutator distances instead use `idealMsState w.aux` in [MagicSquareTheorems.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-172-ms-rigidity-gap/MIPStarRE/QPBT/Test/MagicSquareTheorems.lean:105). The transported operators do not even act on the original state’s space. Introduce \(\ket{\Theta}=\ket{\mathrm{EPR}_2}^{\otimes2}\otimes\ket{\mathrm{aux}}\) and use \(\bra{\Theta}\cdots\ket{\Theta}\).

For F3–F4, add blueprint nodes for the new mathematical interface: the variable-measurement distance, its vanishing for consistent symmetric strategies, the dilated-reflection agreement, and the symmetric specialization. Purely file-local auxiliary results may instead be made private. The specialization’s node must also resolve the projectivity discrepancy: blueprint `def:consistent-strategy` defines a consistent strategy to be projective, while `S.IsConsistent` contains only the state-consistency equations. State the broader Lean hypothesis explicitly or restore projectivity. Because the specialization invokes the open rigidity theorem, it must not receive a proof-level `\leanok`.

For F5, the established `consistencyDefect` is the off-diagonal outcome weight in `def:consistency`; the new quantity is `opFamilyDistSq` from `def:povm-distance`. Prefer a name such as `msVariableStateDistanceSq` and prose such as “squared state-dependent distance.”

For F6, replace “Formalization-only bridge” with “Formalization-only: equality of the matrix-vector products implies equality of the corresponding operator actions on the state.”

For F7, drop phrases such as “implemented in issue,” “the formal proof follows … route,” “linked declaration,” “completion mark,” and “assembly tracked.” Retain the mathematical argument and place status information in a LaTeX comment; the tags already express formalization status.

For F8, use: “**Remaining proof.** It remains to combine the one-way self-test, approximate anticommutation, cross-player agreement, and transfer estimates; issue #105 tracks this derivation.”

Statement-integrity audit: the paper has no consistency hypothesis and concludes bounds at scale \(\sqrt\varepsilon\); Lean adds the two distance bounds and uses \(\sqrt\varepsilon+\sqrt\delta\). This is a transparent, documented correction, with the source conclusion recovered at \(\delta=0\). The existing `\leanok` is statement-level, so the open proof alone is not an error; no proof-level marker is present. No stale `\notready` was introduced.

Summary: 2 equivalence mismatches invalidating 1 statement-level `\leanok`, 2 blueprint coverage/status issues, and 4 prose or terminology issues.

VERDICT: CHANGES_REQUESTED