## Findings
- [ ] F1 (blocker) `blueprint/src/chapter/ch15_qpbt_combining.tex:2106` — The displayed variables are one-indexed, while the linked Lean declarations use zero-indexed `Fin` coordinates, invalidating the statement `\leanok`.
- [ ] F2 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:2100` — Schwartz--Zippel is listed as a statement dependency although it is used only in the proof.
- [ ] F3 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:2195` — The new dependency claims the global-pair Lean proof invokes block specialization, but that proof remains entirely open.
- [ ] F4 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:2092` — The unnamed auxiliary from the paper proof lacks visible formalization-support provenance and a bibliographic citation.
- [ ] F5 (changes) `MIPStarRE/QPBT/Combining/BlockSpecialization.lean:47` — The docstring uses the Lean identifier `totalDegree_combinedCoef_le` as mathematical prose.
- [ ] F6 (changes) `MIPStarRE/QPBT/Combining/BlockSpecialization.lean:66` — The docstring uses the Lean identifier `totalDegree_combinedRestrict_le` as mathematical prose.

## Review

**F1:** The blueprint writes
`z_1,\dots,z_m,x_1,\dots,x_k`, whereas the principal Lean signatures at `BlockSpecialization.lean:113` and `BlockSpecialization.lean:162` use `Fin m` and `Fin k`, hence indices beginning at zero. Replace this with `z_0,\dots,z_{m-1},x_0,\dots,x_{k-1}`, or preferably use the coordinate-free formulation “a polynomial on \(\F_q^m\times\F_q^k\), written \(p(z,x)\).” Until then, the statement-level `\leanok` at line 2099 is inaccurate.

**F2:** `lem:schwartz-zippel-total-degree` is not needed to state the specialization lemma. It is invoked in the Lean proof at lines 131 and 145 and is already correctly listed in the blueprint proof at line 2128. Remove the statement-level `\uses` at line 2100.

**F3:** The linked global-pair theorem `exists_globalPairWitness` is still `by sorry` at `MIPStarRE/QPBT/Combining/Apply.lean:190`, so it does not invoke `block_specialization_weighted_avg_le`. Remove the newly added edge until that result is actually integrated; this does not require changing the existing `\notready` status.

**F4:** The source passage contains this calculation but does not name it as a lemma. The hidden `%` comment does not provide reader-facing provenance. A suitable replacement title is “Formalization support for uniform specialization in a block of variables,” followed by: “This is not a named statement of \(\cite{Ji2020MIPStar}\); it isolates the specialization estimate between equations `eq:qld-g-2` and `eq:qld-g-prime-xpt-bound`.”

**F5:** Replace “the total-degree counterpart of `totalDegree_combinedCoef_le`” with “the corresponding total-degree estimate; the related bound obtained from an individual-degree hypothesis is \(md\).”

**F6:** Replace “the total-degree counterpart of `totalDegree_combinedRestrict_le`” with “the corresponding total-degree estimate; the related bound obtained from an individual-degree hypothesis is \(kd\).”

Apart from F1, the displayed hypotheses, quantifier order, exceptional-set conclusion, and weighted inequality agree with the Lean signatures. The proof-level `\leanok` has no unresolved axiom dependency, and the existing `\notready` on `lem:qld-4-7` remains appropriate.

Summary: 1 equivalence/indexing mismatch invalidating a statement `\leanok`, 2 dependency-metadata issues, and 3 prose issues.

VERDICT: CHANGES_REQUESTED