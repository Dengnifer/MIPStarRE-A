## What changed

Created:

- `MIPStarRE/QPBT/Combining/Witnesses.lean`
- `MIPStarRE/QPBT/Combining/Points.lean`

Added real definitions for scalar transport, direct-line finiteness, subline marginal distributions, and `CombinedPointsWitness.extendedQ`.

Public structure fields:

- `CombinedPointsWitness`: `Q`, `projective`, `self_consistent`, `consistent_XZ`, `consistent_ZX`
- `CombinedLinesWitness`: `T`, `projective`, `axis_degree_X`, `axis_degree_Z`, `consistent`
- `SubLineWitness`: `D`, `isProbability`, `extended_marginal`, `incidence`, `compatibility`, `source_mixture`, `axis_closure`
- `ExtendedLinesWitness`: `Qline`, `axis_degree`, `consistent_alice`, `consistent_bob`
- `GlobalPairWitness`: `Smeas`, `projective`, `point_consistent_alice`, `point_consistent_bob`

## Evidence

Passed:

- `lake env lean MIPStarRE/QPBT/Combining/Witnesses.lean`
- `lake env lean MIPStarRE/QPBT/Combining/Points.lean`
- Locked full `lake build`
- `git diff --cached --check`
- 100-character line-length check
- Git-hook installation check
- Forbidden-token scan

The only proof holes are:

```text
Points.lean:55: sorry
Points.lean:95: sorry
```

No `axiom` or forbidden proof-integrity token was found.

## Statement Integrity

`exists_combinedPointsWitness` faithfully uses `ProjectiveSetting P ε` and produces the side-indexed projective joint measurements, self-consistency, and both ordered `XZ`/`ZX` guarantees from `lem:qld-4-10`. Verdict: exact.

`extendedQ_spec` uses the real postprocessing definition and states projectivity, self-consistency, and both ordered coarse-grained guarantees from `lem:qld-4-12`. Verdict: exact.

No symmetry, divisibility, `[FieldModel]`, bridge, or withheld joint subline law was introduced.

## Open Obligations

- `exists_combinedPointsWitness`: construction required by `lem:qld-4-10`, paper lines 689–709.
- `extendedQ_spec`: postprocessing proof required by `lem:qld-4-12`, paper lines 993–1011.

These are direct formalization obligations, not paper-gap deviations.

## Handoff

Both files are staged with no unstaged changes. The commit could not be created because the approval service rejected three attempts with an account concurrency-limit error before Git started. A fresh approval is required to retry:

```text
feat(QPBT/Combining): add combining witness interfaces

Addresses #18
```