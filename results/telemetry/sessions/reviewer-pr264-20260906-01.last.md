## Findings

- [ ] F1 (changes) `local/bin/account_router.py:143` — Resume preflight crashes on unnamed affinity records accepted by existing routing.
- [ ] F2 (changes) `local/protocols/sessions.md:154` — The new fail-closed policy leaves `agent.sh`’s direct-launch fallback enabled.

## Review

F1: Every ordinary resume now calls `resume_continuation`, which unconditionally reads `row['name']`. A record such as `{"thread_id": T, "account": "primary"}` causes `KeyError('name')`, even alongside a valid named session and without any continuation metadata. I reproduced this in memory: account-affinity lookup succeeds, but the new preflight fails. Skip unnamed records without continuation metadata while retaining rejection of malformed continuation data, and add a regression covering this previously accepted history shape.

F2: The new protocol states that missing dispatchers fail closed, but `local/bin/agent.sh:420` still falls back to direct `codex exec`. That branch bypasses capacity admission and session telemetry. Remove this fallback consistently with `review.sh` and `autofix.sh`, and verify that all three entry points refuse an unavailable dispatcher.

No mathematical findings. Both supported-reference estimates correctly use completeness, support, and positivity. Their auxiliary status matches blueprint `eq:qld-nonencoding-mass` and `lem:qld-nonencoding-mass-bound` in `blueprint/src/chapter/ch16_qpbt_extraction.tex:237`: the reference construction and quantitative bound remain open, without adding assumptions to a paper-facing theorem.

`SupportMass.lean` and the QPBT aggregate type-check. Source-based axiom checks report only `propext`, `Classical.choice`, and `Quot.sound`; the additions contain no proof holes. Shell syntax checks and focused in-memory continuation/occupancy checks passed. The full build and filesystem-writing integration suite were not rerun in this read-only session.

VERDICT: CHANGES_REQUESTED