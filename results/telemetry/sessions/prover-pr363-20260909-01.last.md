Addressed both findings. Changes remain uncommitted.

- **F1:** Moved the shared coefficient definition and degree/evaluation lemmas into [Defs.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-362-native-point-consistency/MIPStarRE/QPBT/Combining/Defs.lean:199), preserving public names and removing duplicate proofs.
- **F2:** Added the averaged affine contraction [blueprint node](/home/drx/MIPStarRE-qpbt/.worktrees/issue-362-native-point-consistency/blueprint/src/chapter/ch15_qpbt_combining.tex:1109). Updated `lem:qld-4-12` to use the `1/q` collision calculation and equal-total condition. Its incomplete status remains unchanged.

**Validation**
`lake build` passed all 9,175 jobs. Blueprint web rendering passed; all 1,647 declaration links resolved. Six targeted axiom checks contain only `propext`, `Classical.choice`, and `Quot.sound`. Statement-header and whitespace checks passed.

**Statement Integrity**
For `eq:combine-lines`, the paper’s bounded inputs and compatible affine parametrizations remain faithfully encoded; the evaluation formula and degree bound `c + 1` are unchanged, specializing to `c = md`. No new assumptions or proof holes were introduced. The three existing construction holes remain.

**Mathlib Audit**
The moved proofs retain:
- `Polynomial.eval_eq_sum_range'`, from `Mathlib.Algebra.Polynomial.Eval.Degree`, for coefficient evaluation.
- `Polynomial.natDegree_C_mul_le`, from `Mathlib.Algebra.Polynomial.Degree.Lemmas`, and `natDegree_pow_le`/`natDegree_X_le`, from `Mathlib.Algebra.Polynomial.Degree.Defs`, for the coefficient degree bound.

No commits, pushes, PR comments, or repository telemetry changes were made.