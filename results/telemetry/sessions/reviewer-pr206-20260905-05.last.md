## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Witnesses.lean:274` — The corrected `ExtendedLinesWitness` description is still contradicted by the module header’s claim that it formalizes `lem:qld-4-13`.
- [ ] F2 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:407` — The renamed conditional extraction theorem still lacks a subordinate `\lean{}` link, so machine-readable traceability remains lost.
- [ ] F3 (changes) `docs/paper-gaps/qpbt-gap-register.md:22` — The register incorrectly says the directly indexed extended-line space appears in no blueprint node.

## Review

F1: Paper `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:1020-1034`, `lem:qld-4-13`, uses lines in `F_q^(2m+2)`, its seed-indexed line-point distribution, and an answer sum over `a ∈ F_q`. `ExtendedLinesWitness` instead uses `DirectLineDesc`, `directLinePointDist`, and `Option`-completed answers. Its changed docstring now states this accurately, but the module header at lines 18-22 still advertises all witnesses as formalizations of `lem:qld-4-13`. The header must distinguish this directly indexed auxiliary from the source theorem.

F2: Replacing the former support node with `\texttt{MIPStarRE.QPBT.exists_extractionWitness_ofGlobalPairWitness}` does not restore declaration tracking. The reverse-coverage check reports this theorem as missing from the blueprint. Restore a separate, explicitly conditional formalization-support theorem carrying `\lean{MIPStarRE.QPBT.exists_extractionWitness_ofGlobalPairWitness}` and displaying the `GlobalPairWitness` assumption. It must remain separate from source `lem:qld-unitary`, with no proof-level `\leanok` while its proof contains `sorry`.

F3: The register claims the directly indexed space “appears in no blueprint node,” but `ch15_qpbt_combining.tex:1774-1804`, `lem:qld-4-13-established`, explicitly describes that construction, and the source-labelled node at lines 1160-1161 also links `ExtendedLinesWitness`. The register should instead say that the replacement is not adopted as the source-shaped statement.

The other prior findings are addressed: the conditional extraction theorem is correctly renamed and marked unfaithful, the combined-line declarations no longer claim source-facing status locally, and the `lem:qld-4-7` proof now exposes the carrier, distribution, and completed-answer transports.

The new `\leanok` on `lem:qld-4-10` is statement-faithful to paper lines 689-709. No new proof holes were introduced; the existing `sorry` sites are documented obligations. Name-level blueprint synchronization, LaTeX convention checking, and `git diff --check` pass. The referenced external `lean-conventions` skill was unavailable, so the checked-in convention and proof-integrity documents were used.

VERDICT: CHANGES_REQUESTED