## What changed

Proved `MIPStarRE.QPBT.exists_qubitIsometry` in [PauliTheorems.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-69-qubit-isometry/MIPStarRE/QPBT/Algebra/PauliTheorems.lean:538).

The proof constructs the binary-coordinate isometry and establishes EPR transport, conjugation as matrix reindexing, phase compatibility, both `X` and `Z` observable cases, and Fourier transport of the projectors. The public signature, `qubitPauliProj`, and documented local index correction are unchanged.

Committed as `574149f` (`feat(QPBT/Algebra): prove qudit-to-qubit isometry`).

## Evidence

- `lake env lean MIPStarRE/QPBT/Algebra/PauliTheorems.lean`: passed without diagnostics.
- Proof-integrity scan for `sorry`, `axiom`, bypasses, and debugging tactics: no matches.
- Target build: passed.
- Lock-aware full `lake build`: passed.
- `leanblueprint web`: passed with existing bibliography warnings.
- Blueprint sync after official declaration-list generation: passed.
- `lake exe checkdecls blueprint/lean_decls`: all 822 declarations resolved.
- Pre-commit statement-integrity checks: passed.
- Worktree is clean.

## Statement integrity

- Paper assumptions: binary field size with a fixed self-dual basis and nonnegative tensor exponent.
- Lean assumptions: `q : ℕ`, `F : FixedFieldModel q`, and `L : ℕ`.
- Paper conclusion: an isometry preserving the EPR state and intertwining both `X` and `Z` Pauli projectors.
- Lean conclusion: exactly those two identities, including the `L = 0` case.
- Verdict: **faithful boundary hypotheses**, with the existing documented correction from the erroneous paper range to `Fin F.basisDim`.

## Open obligations

No proof holes remain in the changed file.

The current stacked branch still reports `sorryAx` for the target because its pre-#64 copy of `pauliProj_eq_avg_tauObservable` is sorry-backed. The trace-coordinate dependency is axiom-clean. Merged #64 already supplies the missing proof, so this is an integration/restack obligation rather than new proof debt; no paper-gap note is needed.

## Handoff

Review the `phi`/`phi.symm` orientation and the separate `X`/`Z` transport cases first. After PR #93 merges, restack onto current `main` to inherit #64 and rerun `#print axioms MIPStarRE.QPBT.exists_qubitIsometry`. No re-export change is required.