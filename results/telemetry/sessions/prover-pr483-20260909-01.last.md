Addressed both findings:

- F1: Corrected the overview: preceding blocks vanish; later blocks remain untruncated, matching paper `eq:cl-dlnf`.
- F2: Removed `avgOver_prod` and updated all five call sites to `SandwichProduct.avgOver_distribution_prod`.

Validation passed: four targeted Lean checks, locked `lake build` (9196 jobs), and `git diff --check`. Existing warnings remain unchanged; no new proof holes or paper-facing statement changes.

Changes are uncommitted for the lifecycle script.