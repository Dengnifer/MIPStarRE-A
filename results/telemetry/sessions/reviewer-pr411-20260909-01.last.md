## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT/Extraction/Consistency.lean:101` — Reuse the shared register-placement API instead of duplicating measurement constructors and their proofs.

## Review

F1 requires changes before approval. The private `placedMeasurement`, effect identity, projectivity theorem, and finite-sum lemma duplicate `ProjectiveSetting.placedMeasurement`, `placedMeasurement_effect`, `placedMeasurement_isProjective`, and `place_finsetSum` in `MIPStarRE/QPBT/Combining/Points/Placement.lean:139,256,264,270`. These declarations already arrive through the existing imports. The new `PointPointRejection.lean:94` repeats the construction again for two placements. Replace these private implementations with the shared API; maintaining separate coordinate decompositions and projectivity proofs creates unnecessary duplication.

The mathematical checks found no introduced statement drift. `exists_combinedPointsWitness` retains the hypotheses and conclusions of `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:689–709`, label `lem:qld-4-10`: projective joint measurements on the original expanded spaces, both ordered-product comparisons, and all directed opposite placements. Its universal \(K\varepsilon^{1/8}\) bound supplies the required polynomial error. The paper-gap note explains the alternative Parseval/sandwich proof and its distinction from the unresolved linearity extension.

The line-game estimates retain both failed-evaluation marginals and the independent affine parameter, including zero directions. The changed extraction proofs preserve their statements.

Validation: `Points.lean` and `Extraction/Consistency.lean` type-check. Nineteen selected axiom checks report only `propext`, `Classical.choice`, and `Quot.sound`; the added Lean code contains no proof holes or prohibited bypasses. Extraction still reports five existing `sorry` declarations, each explicitly tracked under issue #47. I did not rerun the full build.

VERDICT: CHANGES_REQUESTED