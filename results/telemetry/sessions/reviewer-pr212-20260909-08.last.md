## Findings

- [ ] F1 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:344` — Replace the common-ancilla proof’s `lem:naimark-helper` dependency with `lem:naimark-rounding`, which contains the results directly invoked by Lean.

## Review

The common-ancilla proof directly uses `norm_naimarkAncilla`, `roundedObservable_isBinaryObservable`, and `roundedObservable_mul` in `MIPStarRE/QPBT/Combining/Linearity.lean:209`. These are linked under `lem:naimark-rounding` at blueprint lines 641–658. Its `\uses` instead names the underlying Naimark helper. Replace that dependency and retain the stability and BLR dependencies. This is dependency metadata, not a mathematical equivalence failure.

All 11 prior prose-lane findings are resolved, including the existential extended-point statement, pointwise bounds, finite-dimensional quantifiers, adjoint products, Fourier-square correction, indexing, equation references, and coarse-graining argument. Code-lane findings remain outside this review’s scope.

All 136 chapter declaration links resolve. I checked the authoritative patch and checkout beyond the truncated attachment; builds were not rerun.

Counts: 0 equivalence mismatches, 0 newly invalid status tags, 1 dependency issue, 0 prose issues.

VERDICT: CHANGES_REQUESTED