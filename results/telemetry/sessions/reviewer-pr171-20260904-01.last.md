## Findings

- [ ] F1 (changes) `local/protocols/issues-prs.md:22` — The authoritative prerequisite record has no adoption-safe write path in the documented issue lifecycle
- [ ] F2 (changes) `local/protocols/issues-prs.md:23` — Omitting already-closed prerequisite edges loses dependencies when an issue is reopened
- [ ] F3 (changes) `local/protocols/EVOLUTION.md:519` — Root-only traversal cannot reveal the unparented packets the amendment says it exposes
- [ ] F4 (changes) `scripts/tests/test_ready_packets.py:1` — The new workflow regression test is excluded from the enforced infrastructure line budget
- [ ] F5 (advisory) `local/protocols/EVOLUTION.md:505` — The ledger says 35 packets but the stated issue range contains 43

## Review

F1: Prerequisite edges become the sole record, but `issue_new.py` exposes only `--parent`, while `gh_common.py` has no dependency mutation helper or CLI command. A generic POST also lacks the layer’s required ambiguity recovery. Following the documented lifecycle can therefore create a packet whose prose names prerequisites but whose missing edges make it immediately READY. Add an adoption-safe helper and a repeatable `--blocked-by` option or documented edit command, with tests. GitHub supports dependencies during issue creation and editing in its [issue dependency workflow](https://docs.github.com/en/issues/tracking-your-work-with-issues/using-issues/creating-issue-dependencies).

F2: Record every prerequisite edge, including prerequisites already closed when the packet is created or migrated. GitHub retains both open and closed dependency relationships, as reflected by its separate active and total dependency counts in the [Issues schema](https://docs.github.com/en/graphql/reference/issues). Without the edge, reopening a prerequisite cannot re-block its dependents. The implementation and test already handle retained closed edges.

F3: `collect()` only visits descendants returned by `sub_issues(root)`. An orphan is therefore absent rather than reported as a gap, and `issue_new.py --parent` remains optional. Since prior orphaned packets motivate this amendment, add an orphan/integrity audit or enforce packet parenting at creation; otherwise revise the claimed invariant.

F4: `.githooks/pre-commit` budgets `local/`, `.githooks/`, and two named workflow tests only. Consequently, all 137 lines of `test_ready_packets.py` are omitted from `INFRA_CHANGED`. This PR remains below 400 lines even when counted, but the guard must include this test or use a durable workflow-test classification.

F5: `#97` through `#135` names 39 issues; with `#63`, `#77`, `#146`, and `#156`, the written set contains 43. If only 35 members were reparented, enumerate the exclusions or exact set so the evolution ledger remains auditable.

The traversal’s tested happy path, pagination calls, output modes, and REST payload assumptions are otherwise coherent. The six focused tests and `git diff --check` passed. Full test discovery could not be meaningfully rerun in this read-only sandbox because tests requiring temporary directories failed environmentally; the review trigger records green exact-head local CI.

VERDICT: CHANGES_REQUESTED