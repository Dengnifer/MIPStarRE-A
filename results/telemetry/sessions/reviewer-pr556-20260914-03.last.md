## Findings
- [ ] F12 (blocker) `local/bin/telemetry.py:1403` — Telemetry still rejects every account except `primary` and `second`, so a third-account worker runs and then exits with an unrecordable session.
- [ ] F13 (blocker) `local/bin/accounts_file.py:90` — The schema permits the reserved name `auto`, allowing resume affinity to be interpreted as automatic routing and sent through another key.
- [ ] F14 (changes) `local/bin/dispatch.sh:593` — Checkpoint continuations are forcibly routed to `primary`, so valid account sets without that ordinary name cannot continue.
- [ ] F15 (changes) `local/bin/dispatch.sh:1106` — Comparing only `git status --porcelain` before and after execution misses edits to paths that were already dirty and can launch a second worker over partial changes.
- [ ] F16 (changes) `local/bin/run_mode.py:1561` — Resume rejects cap zero for every enabled account even though `external_reserved == ceiling` is explicitly a valid live state.
- [ ] F17 (changes) `local/bin/capacity_controller.py:646` — Probe recovery does not consume the triggering failure observations, so the next tick can immediately retrip the recovered key.
- [ ] F18 (changes) `local/protocols/full-speed-mode.md:44` — Normative protocols still prescribe count-based account mode and fixed two-account routing despite the new live N-account contract.

## Review
**F12.** `dispatch.sh` and `account_router.py` accept live arbitrary names, but `session-summarize --account` retains fixed argparse choices. An end-to-end dispatch through `third` ran Codex once, then exited 6 because both the initial telemetry append and documented replay command reject `third`. Validate against the account-name syntax instead.

**F13.** With configured accounts `primary, auto`, `resume_account` correctly returned `auto`, but `reserve(..., "auto", ...)` treated that affinity as the selector sentinel and chose `primary`. Reject `auto` as an account name everywhere, including fallback cap-file discovery, or represent automatic selection separately.

**F14.** A valid configuration containing only `third` accepted the continuation metadata, then failed with exit 2 because line 593 replaced the selected account with absent `primary`. Continuations need an explicit or configured live account while retaining their fresh-thread and shared-budget rules.

**F15.** `git status --porcelain` records path state, not content. With an already-untracked file, the worker overwrote that file while both snapshots remained identical; the dispatcher consequently executed the prompt twice. Require a clean pre-run tree for automatic retry or compare a content-sensitive snapshot.

**F16.** After setting an enabled account's reserved slots equal to its ceiling, the controller correctly produced cap 0. Pause succeeded, but resume returned 3 and re-paused. `owner-resume.sh:230-243` repeats this assumption and also treats intentionally retained zero-valued historical cap files for absent accounts as enabled because `enabled.<name>` returns `unknown`. Resume checks must enumerate configured accounts and distinguish zero share from disabled/down state.

**F17.** Using the shipped `probes_to_recover = 2`, the health machine produced `probe-ok`, then `recovered`, then `trip` from the same recent `auth` row. Recovery needs a failure cursor or recovery timestamp so observations already discharged by successful probes cannot disable the key again.

**F18.** The protocol says `both` only when more than one account is enabled, while the corrected implementation uses `both` whenever any enabled account has a non-default home. The same file still says names outside `{primary, second}` are invalid and only two cap files are derived; `sessions.md:268-313` and `meta.md:78-87` retain the fixed-pair schema. These documents are normative and must be synchronized with the implementation.

Round-one F1-F11 are resolved: invalid files now fail routing closed; account mode follows actual homes; directive mutation is transactional; inbox persistence and trusted markers are ordered correctly; resume remains paused on controller failure; reports use the last-good snapshot; attribution, live labels, policy ordering, and probe locking are repaired.

Validation completed with `git diff --check`, shell syntax checks, Python AST parsing, and the affected 271-test suite under a sanitized CI-like environment. That suite passes, but it lacks the failing reproductions above.

VERDICT: CHANGES_REQUESTED