Addressed both review findings:

- **F1:** Routed workflow-budget overrides, large rewrites, and consecutive workflow episodes to recorded main decisions while preserving the 1,000-line pre-commit guard.
- **F2:** Changed the owner reply format to `DECISION B<n>: <letter>` and listed valid letters separately.
- Updated the existing September 9 protocol evolution entry.

Validation passed:

- `lake build` — 9,197 jobs
- `python3 -m unittest scripts.tests.test_dispatch.PreCommitBudgetTests`
- `sh -n .githooks/pre-commit`
- `git diff --check`

Existing Lean warnings and tracked `sorry` reports remain unrelated to this documentation/hook-only diff. Changes are uncommitted as required by the local autofix contract.

PR comment text:

> Addressed F1 by keeping the infrastructure budget guard enforced while assigning override and consecutive workflow-episode decisions to main rather than the owner. Addressed F2 by using `DECISION B<n>: <letter>` and documenting the allowed letters separately. Full `lake build` and focused pre-commit budget tests pass.