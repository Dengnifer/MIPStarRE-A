## Findings

- [ ] F1 (changes) `docs/paper-gaps/qpbt_linearity-distance-normalization.tex:10` — The note coins `d^{raw}` despite the source and project terminology requiring “state-dependent operator distance.”

## Review

F1 must be fixed before merge. `docs/mathematical_language.md:75-79` explicitly prohibits presenting `raw` as mathematical terminology unless the cited source does so. Natarajan–Vidick instead calls \(d_\rho(S,T)\) the “state-dependent distance” at `references/nv-paper/fullpaper.tex:868-875`. The note defines \(d^{\mathrm{raw}}\) and repeats “raw distance/convention/error” throughout its theorem forms, counterexample, comparison, and conclusion. Use \(d^{\mathrm{op}}\), \(d^{\mathrm{obs}}\), or the source’s \(d_\rho\), and consistently say “state-dependent operator distance.”

The mathematics otherwise checks out. Equation (3) at `references/nv-paper/fullpaper.tex:903-911` loses the factor \(1/2\), while the Fourier–Naimark calculation at lines 1095-1112 yields measurement-distance error \(\delta\), equivalently operator-distance error \(2\delta\). The four-point counterexample remains valid after ancillary extension by simultaneous spectral decomposition into Boolean characters.

All prior-ledger issues are otherwise addressed. The module is re-exported, the duplicated \(\rho\) is discussed explicitly, the correction has its `**Local fix:**` marker, the formal theorem is named, and the principal declarations have subordinate blueprint coverage.

Verification passed for the new Lean file and `MIPStarRE/QPBT.lean`; all 866 blueprint declarations resolve. The linked proofs depend only on `propext`, `Classical.choice`, and `Quot.sound`, with no `sorryAx`.

VERDICT: CHANGES_REQUESTED