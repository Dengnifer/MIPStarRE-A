## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Lines/PointSelfConsistencyCompleted.lean:71` — Add separate Lean-only blueprint nodes for the completed product-law and conditioned inverse-mass self-consistency estimates.

## Review

F1 remains unresolved. The paper’s `eq:qld-q-self-cons` states self-consistency under uniform \(x,z\) sampling (`references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:693-697`), and lines 955-961 use it as `eq:pasting-2`. The conditioned Lean theorem instead uses `nondegenerateLinePastingDist` and changes the bound to `δQ / nondegenerateLinePastingMass`. This is a valid formalization-only consequence, but not literally the source equation.

The blueprint currently contains only the source form at `blueprint/src/chapter/ch12_qpbt_games.tex:1024-1026` and no links for either new public theorem. Add subordinate formalization-support nodes for:

- `self_consistency_completed_linePoint_defect_le`
- `self_consistency_conditioned_completed_defect_le`

The nodes should explicitly display their respective distributions and error bounds rather than linking the conditioned theorem directly to source-labelled `eq:pasting-2`.

Prior-ledger disposition:

- The `Extraction/Consistency.lean` placement duplication is identical to current `main`; it is an existing mainline cleanup and is out of scope for PR 487.
- `avgOver_mix_noFinite` is unchanged relative to the current PR base branch. Its duplication belongs to that parent branch and should be tracked separately rather than carried as a PR 487 finding.
- The missing blueprint coverage remains branch-specific and is retained above.

The mathematical proofs are otherwise coherent: injective completion by `some × some` preserves the defect, line-point sampling transports uniform point marginals, and restriction inflates the bound by the inverse retained mass. No extra proof-debt hypothesis was introduced.

Both focused checks passed:

- `lake env lean MIPStarRE/QPBT/Combining/Lines/PointSelfConsistency.lean`
- `lake env lean MIPStarRE/QPBT/Combining/Lines/PointSelfConsistencyCompleted.lean`

The six `sorry` occurrences found elsewhere in the authoritative changed-file set are explicitly documented construction obligations with issue and paper-gap references, so none is an unmarked hole. The attached sanitized diff was truncated at 4,001 lines from a 70,202-line merge-heavy patch; review used the full patch and checkout by relevant path.

The four-round cap has already been exceeded. The operator should either fix F1 on the final head or defer it to a numbered issue in an `ADJUDICATION` comment for head `5c3296da220928601b46c9e634273052a8874c29`; do not start another full review round.

VERDICT: CHANGES_REQUESTED