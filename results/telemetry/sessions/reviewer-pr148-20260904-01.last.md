## Findings
- [ ] F1 (changes) `docs/paper-gaps/qpbt_linearity-distance-normalization.tex:188` — The formalization comparison omits `exists_exact_boolean_representation` and its essential reparameterization `η = 2δ`.
- [ ] F2 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:196` — The remark attributes an exact raw-distance bound by `δ` to the QPBT quotation, although that paper defines `≈_δ` only up to `O(δ)`.
- [ ] F3 (advisory) `MIPStarRE/QPBT/Combining/Linearity/Stability.lean:255` — The new public stability theorem has no blueprint declaration link or dedicated corrected-theorem entry.
## Review
F1 must be fixed before merge. The paper-gap policy requires the note to state what the formal development proves instead. The note currently says only that the Fourier foundation adds normalization identities and that theorem repair remains future work. This PR also proves `avg_stateDepDistSq_roundedObservable_eq_avg_multiplicativeDefect` and `exists_exact_boolean_representation`. Their relationship to the source parameter is material: the identity
\[
\mathbb E_{a,b}d_\rho(A(a)A(b),A(a+b))^2=2-2C
\]
turns the source hypothesis \(C\geq1-\delta\) into the new theorem’s hypothesis with \(\eta=2\delta\), yielding raw output error at most \(2\delta\). The note, and the theorem docstring at `Stability.lean:245`, should identify this as the corrected raw-distance reformulation rather than simply calling it Theorem 10.
F2 must also be fixed. The QPBT source defines `≈_δ` at `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:95-99` as an expectation bounded by `O(δ)` and uses that notation in `thm:linearity` at lines 713–725. A factor of two is absorbed there. The false exact constant belongs to the printed Natarajan–Vidick statement at `references/nv-paper/fullpaper.tex:1079-1085` when its distance is read as the raw operator distance, and to the blueprint/Lean expansion at `ch15_qpbt_combining.tex:124-132`. The remark should distinguish those claims.
F3 is advisory because the supplied PR context does not establish the `enforce-blueprint-coverage` label. A separate blueprint item for the corrected raw-defect theorem would document the new public result without marking the still-false source-labelled `thm:linearity` as proved.
The Lean implementation otherwise checks out mathematically: the noncommutative Fourier ordering is preserved, the Naimark residual outcome is folded without losing compression, and the final distance/defect equality is sound. All four new Lean files type-check; `checkdecls` resolved all 866 declarations; the changed files contain no proof holes or prohibited proof bypasses. The new stability theorem’s axiom closure is only `propext`, `Classical.choice`, and `Quot.sound`. The pre-existing `sorry` in `exists_exactly_linear_observables` remains explicitly tracked by `\notready` and issue #129 and is not used by the new proof.
VERDICT: CHANGES_REQUESTED
