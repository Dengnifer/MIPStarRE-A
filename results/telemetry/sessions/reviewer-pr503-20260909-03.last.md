## Findings
- [ ] F1 (changes) `local/protocols/issues-prs.md:262` — The mutable status marker cannot be closed through the marker-keyed comment command without duplicating the blocker.

## Review
The two prior findings are resolved. Infrastructure overrides now consistently require a recorded main decision, and the reply template now requires one offered letter.

F1 remains because all GitHub traffic must use `gh_common.py`, whose `ensure-pr-comment` command locates comments using the complete marker. Replacing `status=open` with `status=closed` therefore misses the original comment and creates another; retaining the old lookup marker leaves the blocker marked open. Use an immutable identity marker with separately mutable status, or add a tested helper that updates the existing blocker by ID.

The remaining policy changes are consistent. `git diff --check` and `sh -n .githooks/pre-commit` passed; CI was already green for this head.

VERDICT: CHANGES_REQUESTED