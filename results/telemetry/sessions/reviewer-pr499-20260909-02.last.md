## Findings

- [ ] F3 (changes) `local/bin/pr_merge.py:164` — Inherited submodule-ignore configuration can hide nontelemetry changes and incorrectly pass gate 2b.

## Review

The raw diff inherits `diff.ignoreSubmodules=all`. Git then omits gitlink changes entirely, so the empty-output branch returns `True` before checking their paths or modes.

Reproduced in a disposable repository: advance `main` by adding a `160000` gitlink at `vendor/tool`. With default configuration, `--check-only` rejects at gate 2b; with `diff.ignoreSubmodules=all`, it exits successfully and reports the entire gate passed. This violates the requirement that nontelemetry paths and submodules remain freshness-relevant. Add `--ignore-submodules=none` to the diff invocation and a regression covering inherited ignore settings.

Prior findings:

- **F1:** The blanket exclusion is fixed by the explicit suffix, boundary, and mode checks. F3 identifies a separate omission before classification.
- **F2:** The [scope amendment](/home/drx/MIPStarRE-qpbt/.worktrees/issue-498-telemetry-tolerant-freshness/local/protocols/EVOLUTION.md:1085) explicitly defers daemon integration. I am not retaining the retired-service finding; the active daemon rollout remains unverified here.

All 13 `MergeGateTests` passed, and `git diff --check` passed. The additional reproduction exposed the uncovered failure. The checkout remains clean at `95b20e9f5a3e50112ff176e3053b0e25c86109dc`; no GitHub writes occurred.

VERDICT: CHANGES_REQUESTED