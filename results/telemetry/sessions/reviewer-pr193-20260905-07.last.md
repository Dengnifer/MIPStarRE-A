## Findings

- [ ] F1 (blocker) `MIPStarRE/QPBT/Test/PauliBasisTest.lean:51` — `AdmissibleParams.one_le_m` remains an untracked `sorry` and introduces `sorryAx` into every new Pauli typed-CL law.

## Review

F1 must be fixed before merge. `AdmissibleParams.toLdParams` uses `P.one_le_m` for its `hm` field, and the point, axis-line, and diagonal branches of `pauliCL` use that parameter conversion. Consequently, `#print axioms MIPStarRE.QPBT.pauliCL` reports `sorryAx`, independently of the explicitly documented holes in `isCondLinear_pauliCL` and `pauliQuestionDistribution_eq_typedCL`.

The new `**Unfaithful:**` markers only track the two issue-#180 obligations, so they do not disclose this additional dependency. This is not a genuine source gap: `references/qpbt-paper/08_classical_and_quantum_low_degree_tests.tex:958-961` assumes that `q` is admissible and `m ∣ q`, while `blueprint/src/chapter/ch13_qpbt_test.tex:383` correctly observes that admissibility gives `q ≥ 2`, hence `m ≥ 1`. I verified that the following direct proof type-checks:

```lean
  obtain ⟨k, -, hk⟩ := P.hq
  have hq : 0 < P.q := by
    rw [hk]
    positivity
  exact Nat.pos_of_dvd_of_pos P.hdvd hq
```

The statement-integrity audit otherwise found no drift. `IsCondLinearOn.mono_level` faithfully formalizes the containment observation after the CL definition; the low-degree family and distribution conjunction matches the source with only Lean boundary data; and the Pauli conjunction has no extra bridge hypotheses. Its remaining component and distribution proofs are explicitly marked unfaithful, tied to issue #180, and are not marked `\leanok`.

`git diff --check` and targeted Lean checks for the changed modules passed. The blocker is the hidden axiom dependency above, not a type-checking failure.

VERDICT: CHANGES_REQUESTED