## Findings
- [ ] F1 (changes) `docs/QPBT-theorem-index.md:200` — The index misattributes ε-monotonicity to `deltaQld_mono` and omits the actual `deltaQld_mono_epsilon` dependency.

## Review
`deltaQld_mono` keeps `ε` fixed while adjusting the universal constants `a` and `b`. The newly documented `deltaQld_mono_epsilon` is the theorem that varies `ε`; it is also directly listed in the raw prescribed-answer proof’s `\uses` at `ch16_qpbt_extraction.tex:1695` and has its own blueprint node at line 1845. Update the existing row’s description and add the new declaration to the index.

All prior-ledger issues are otherwise resolved: the comparator record exists, snapshot counts are reproducible, the authoritative proof-debt scanner is documented, `thm:pauli` has its proof-level `\leanok`, and the raw-transfer proof dependencies now match Lean. `git diff --check` and the blueprint convention check pass; completion-gate C1 also passes.

VERDICT: CHANGES_REQUESTED