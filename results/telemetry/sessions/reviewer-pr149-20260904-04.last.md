## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Test/MagicSquareTheorems/Rigidity/Dilation.lean:85` — Remove the unused `[DecidableEq α]` assumptions from both family-projection theorem signatures.
- [ ] F2 (changes) `MIPStarRE/QPBT/Test/MagicSquareTheorems/Rigidity/Dilation.lean:106` — Replace the goal-changing `show` tactics at lines 106, 114, and 502 with `change` or direct goal statements.
- [ ] F3 (changes) `MIPStarRE/QPBT/Test/MagicSquareTheorems/Rigidity/GroundSlice.lean:46` — Reuse or promote the existing `applyOperatorToState_mul` helper instead of adding an exact duplicate proof.

## Review

The dilation and transfer layer is mathematically coherent, and I found no source-statement drift. The linter issues must nevertheless be fixed before approval. `unusedDecidableInType` is reported for `mul_eq_zero_of_isProj_family` and `isProj_sum_of_isProj_of_sum_le_one` (warnings anchored at lines 80 and 97; signatures at lines 85 and 101). The instances are needed only while proving finite-sum facts, not in the public proposition; remove them and use a local classical instance if necessary. `style.show` is reported at lines 106, 114, and 502. This follows the repository requirement in `docs/project_conventions.md:20-27` to fix warnings at the source.

The duplicate-helper audit identifies `GroundSlice.applyOperatorToState_mul` as an exact duplicate of the pre-existing private theorem at `MIPStarRE/QPBT/Games/DistanceTheorems/Support.lean:60`. Promote the shared theorem or move it to a common support module, then have both call sites use that declaration.

I compared the changed declarations with `thm:ms-rigidity` in `references/qpbt-paper/08_classical_and_quantum_low_degree_tests.tex:620-649` and `blueprint/src/chapter/ch13_qpbt_test.tex:228-250`, where the theorem quantifies over an arbitrary strategy of value at least `1 - ε` and produces local isometries and the stated state/operator estimates. `exists_ms_rigidity` remains unchanged and source-shaped; the new declarations are explicitly auxiliary dilation and transfer lemmas and do not add bridge or proof-debt hypotheses to the paper-facing theorem.

Verification succeeded with `lake env lean` on the new files, `lake env lean MIPStarRE/QPBT.lean`, `lake build` (9066 jobs), the duplicate-helper audit, and `git diff --check`. The changed-file proof-hole scan found no new `sorry`, `axiom`, or kernel-bypass construct. The existing `exists_ms_rigidity` `sorry` at `MIPStarRE/QPBT/Test/MagicSquareTheorems.lean:132` is unchanged and remains the documented proof frontier.

VERDICT: CHANGES_REQUESTED