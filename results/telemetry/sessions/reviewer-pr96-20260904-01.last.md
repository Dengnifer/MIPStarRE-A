## Findings

- none

## Review

The proofs are mathematically sound and source-faithful.

`basisCoord_apply_eq_trace`, `trace_mul_eq_dotProduct_basisCoord`, and `chi_mulVec_basisCoord` match `lem:downsize_field` in `references/qpbt-paper/04_preliminaries.tex:509-550` and the blueprint at `ch11_qpbt_algebra.tex:259-287`. The finite and decidable instances are faithful boundary data; no bridge assumptions were added.

`trace_basis_eq_one_of_selfDual_normal` and `basisCoord_one_of_selfDual_normal` match `lem:one` at paper lines 730-765 and blueprint lines 290-310. The Frobenius trace invariance, common-trace argument, contradiction from self-duality, and `ZMod 2` conclusion are correct.

The block indexing and multiplication order in `chi_mulVec_basisCoord` agree with `chiOfBasis`, `basisCoordVec`, and paper item 3. All public signatures are unchanged from the merge base.

The declaration-local linter suppressions are documented and preserve the established paper-facing interfaces, so they satisfy the project’s narrow-exception policy.

Verification completed:

- Focused Lean check passed.
- `git diff --check` passed.
- No proof holes or prohibited proof mechanisms occur in the changed file.
- Every completed target and `chi_mulVec_kappa` has axiom closure limited to `propext`, `Classical.choice`, and `Quot.sound`; none depends on `sorryAx`.
- The worktree remained unchanged.

The external `lean-conventions` skill was unavailable in this runtime; the checked-in `docs/PROOF_INTEGRITY.md`, project addenda, anti-pattern checklist, and Mathlib linter sources were reviewed instead.

VERDICT: APPROVED