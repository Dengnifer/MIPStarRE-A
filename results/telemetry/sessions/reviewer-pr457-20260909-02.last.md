## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Points/Sandwich.lean:44` — Reuse the existing operator-calculus lemmas instead of introducing duplicate public helpers.

## Review

F1: `applyOperatorToState_mul'` repeats the exact statement of `DistanceCalculus.applyOperatorToState_mul` in `MIPStarRE/QPBT/Games/DistanceTheorems/Support.lean:64`, already available through this file’s imports. Replace its downstream uses with that declaration and remove the duplicate.

Similarly, `sum_effect_conjTranspose_mul_self_le_one_of_projective` in `Combining/Points/Consistency.lean:98` reproves a specialization of `measurement_sum_adjoint_mul_le_one` in `Games/DistanceTheorems.lean:336`. The existing result requires no projectivity assumption. Direct applications of both existing lemmas type-check under the current imports. These duplicate proofs and public names require correction under the review’s modularity rule.

No mathematical blocker was found:
- `exists_combinedPointsWitness` preserves `lem:qld-4-10`, paper `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:689`: projective joint measurements, both ordered-product comparisons, self-consistency, all opposite placements, and universal polynomial error.
- The expanded-line declarations preserve `lem:qld-comm-line-cons` at paper line 523, using the existing blueprint’s completed evaluation convention.
- The new marginal bounds implement `eq:pasting-q1` at paper lines 936–941 with independent line-point sampling and explicit error `8 * δQ + C * (ε + deltaLine ε)`. Projectivity and line consistency are derived, not added as proof-debt assumptions.

Validation: direct Lean checks passed for `Points.lean`, `LineMeasurement.lean`, and `CombinedPointLineMarginalDefect.lean`. Four principal axiom closures contain only `propext`, `Classical.choice`, and `Quot.sound`. No introduced proof holes or prohibited bypasses were found; `git diff --check` passed.

The truncated attachment was supplemented from disk and the checkout. No full rebuild or blueprint build was rerun. The external `lean-conventions` reference was unavailable locally; checked-in integrity and review rules were applied.

VERDICT: CHANGES_REQUESTED