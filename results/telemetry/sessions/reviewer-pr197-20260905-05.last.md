## Findings

- [ ] F1 (blocker) `local/bin/checked-push.sh:99` — The preflight can inspect a checkout other than `LOCAL_REF`, approving bytes different from those pushed.

## Review

The helper resolves `LOCAL_SHA` from an arbitrary branch, but runs the hook in `REPO_ROOT`. The hook uses that SHA only to derive changed filenames; its Lean checks and audits read files from the current checkout. `github-sync.sh` always passes the primary checkout for every named ref, while `pr_open.py` defaults there. Publishing a feature branch in another worktree can therefore check `main`; a feature-only file is skipped entirely by `.githooks/pre-push:80`.

Require `LOCAL_REF` to be checked out at `REPO_ROOT`, or resolve its linked worktree before preflight. Cover this with a two-worktree regression test.

All prior ledger findings are resolved: no-op publication succeeds, the exact lease protects against stale or absent native hooks, the regression module is budgeted, and full-mode guidance uses the helper.

Exact-head CI is reported green. `git diff --check`, `dash -n`, and `bash -n` passed. Temp-backed tests could not be rerun in the read-only reviewer sandbox because no writable temporary directory was available. At the round cap, F1 must be fixed or explicitly deferred to a tracked issue through operator adjudication, without another full review round.

VERDICT: CHANGES_REQUESTED