## Findings
- [ ] F1 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:1105` — `def:combine-map` is now source-faithful and formalized, so `\notready` is stale and should be `\leanok`.
- [ ] F2 (blocker) `blueprint/src/chapter/ch15_qpbt_combining.tex:1254` — The restricted-line indices are silently one-based while the linked Lean declarations use zero-based `Fin m`.
- [ ] F3 (blocker) `blueprint/src/chapter/ch15_qpbt_combining.tex:1271` — The source block links a conditional theorem requiring supplied point and line witnesses absent from the paper statement.
- [ ] F4 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:970` — “agree at at most” contains a duplicated word.
- [ ] F5 (changes) `MIPStarRE/QPBT/Combining/Lines/SubLineConstruct.lean:53` — The public `SubLineRaw` family uses the prohibited process-shaped term “raw”.
- [ ] F6 (changes) `MIPStarRE/QPBT/Algebra/Coefficients.lean:32` — The coefficient documentation describes Mathlib implementation choices rather than the mathematical construction.
- [ ] F7 (changes) `MIPStarRE/QPBT/Combining/DirectLowDegree/CoefficientCollision.lean:28` — New docstrings use the Lean-specific label “Lean-only” instead of “formalization-only”.
- [ ] F8 (changes) `MIPStarRE/QPBT/Combining/ExtendedLineGame/DiagonalParameterDefectTransport.lean:10` — The module prose uses “postprocessings”, “definitionally”, and “bridge” instead of describing the mathematical identity.
- [ ] F9 (changes) `MIPStarRE/QPBT/Combining/DirectLowDegree/RejectionBounds.lean:5` — “when a caller caps” is software-interface language.
- [ ] F10 (changes) `MIPStarRE/QPBT/Extraction/Consistency.lean:524` — The comments about keeping “instance search local” are Lean implementation narration and should be dropped.

## Review
**F1:** The linked combining declarations now match the two source constructions: `combinePoly_mem_polyFunc` uses the admissibility boundary condition \(1\le d\), while `IsCombineLineCompatible` expresses the projected-line hypothesis printed before Equation `eq:combine-lines`. The formerly open degree and specification results are closed. Replace the stale `\notready` with `\leanok`.

**F2:** The blueprint uses \(i\in\{1,\ldots,m\}\) and coordinates \(1,\ldots,i-1\), including again at lines 1279 and 1292. Lean uses `i : Fin L.m`, `chiIndex : ... → Fin L.m`, and the prefix condition `j.val < i.val`. Introduce an explicit shift from the paper index or rewrite using a zero-based \(\chi_0\). Once aligned, line 1252 should become `\leanok`, since all four linked declarations are complete.

**F3:** `restricted_lines_consistency_bound` at `MIPStarRE/QPBT/Combining/Lines.lean:208` consumes `CombinedPointsWitness` and `CombinedLinesWitness`. The latter is still not produced by the source-facing theorem. Move this tag to a separate formalization-only entry that displays those supplied-witness hypotheses and may carry `\leanok`; keep the source consequence `\notready`.

**F4:** Replace “agree at at most \(c\)” with “agree at most \(c\)” or “agree at no more than \(c\)”.

**F5:** Rename `SubLineRaw`, `subLineRawDist`, `subLineRawDist_isProbability`, `exists_raw_of_mem_subLineDist_support`, and the `subLineBranchRaw_map_*` declarations. For example, use `SubLineSeededSample`, `subLineSeededSampleDist`, and `exists_seeded_sample_of_mem_subLineDist_support`.

**F6:** Replace references to “Mathlib's actual polynomial composition operation”, “Mathlib's coefficient-vector construction”, and “Mathlib polynomial evaluation” with direct mathematical descriptions. The same issue occurs in `Combining/Lines/PolynomialCollision.lean:8` and `DirectLowDegree/CoefficientCollision.lean:8`.

**F7:** Use “formalization-only auxiliary” at this line, `ExtendedCoefficientLoss.lean:21`, and `Games/Sandwich/Pasting/Heterogeneous.lean:223`.

**F8:** A suitable replacement is: “The diagonal coefficient defect equals the diagonal parameter-evaluation defect because both average the same evaluated coefficient measurements over the same line and parameter law.”

**F9:** Replace the phrase with “needed to cap the assembled direct-game rejection estimate by one.”

**F10:** Drop this comment and its duplicate at line 599; neither describes mathematical content.

The new Chapter 12 pasting entries and the coefficient-polynomial lemma otherwise have matching signatures and accurate `\leanok` tags. There are 3 category-A findings, including 2 mathematical mismatches, and 7 prose findings. The category-A mismatches are the more serious issues. At the review-round cap, each remaining item must be fixed or converted to a tracked issue before merge.

VERDICT: CHANGES_REQUESTED