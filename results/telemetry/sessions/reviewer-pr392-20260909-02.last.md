## Findings
- [ ] F4 (changes) `MIPStarRE/QPBT/Combining/Points/Sandwich.lean:44` — `applyOperatorToState_mul'` duplicates the already imported `DistanceCalculus.applyOperatorToState_mul`.
## Review
**F4 requires changes.** The existing theorem in `MIPStarRE/QPBT/Games/DistanceTheorems/Support.lean:64` has identical hypotheses and conclusion. The new declaration adds a second public name and independent proof without specialization. Remove it and update its uses in `Sandwich.lean` and `Consistency.lean` to the existing theorem. I verified that the existing theorem proves this exact statement under the current import.
Prior findings F1–F3 are addressed: the ancillary involution is distinguished from the blueprint permutation, the paper-gap note explains the replacement construction, and the previously identified square-summability duplicates use the imported general estimate.
**Statement integrity:** `exists_combinedPointsWitness` preserves `lem:qld-4-10` at `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:689` and `blueprint/src/chapter/ch15_qpbt_combining.tex:802`. It assumes a projective strategy passing with probability at least `1 - ε` and produces projective measurements on the existing expanded spaces, with both ordered-product comparisons and all symmetric equivalents. The universal `K ε^{1/8}` bound introduces no proof-debt hypotheses. I found no mathematical discrepancy in the documented alternative proof.
All seven changed Lean files type-check without warnings. Proof-hole scans and `git diff --check` pass. The main theorem’s axiom closure contains only `propext`, `Classical.choice`, and `Quot.sound`.
Limitations: I did not rerun the full build or TeX rendering; the external `lean-conventions` reference was unavailable locally.
VERDICT: CHANGES_REQUESTED
