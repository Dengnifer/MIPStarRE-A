## Findings

- [ ] F1 (changes) `blueprint/src/chapter/ch12_qpbt_games.tex:539` — Proof-only dependencies are attached to statement blocks throughout the new formalization-support nodes.
- [ ] F2 (changes) `blueprint/src/chapter/ch12_qpbt_games.tex:558` — Several new support nodes list dependencies that do not match the corresponding Lean proofs.
- [ ] F3 (changes) `blueprint/src/chapter/ch12_qpbt_games.tex:784` — The overlap-gap proof omits the existing theorem that its Lean proof invokes.
- [ ] F4 (changes) `blueprint/src/chapter/ch14_qpbt_observables.tex:381` — The POVM corollary’s proof metadata and prose describe the auxiliary proof rather than its actual specialization proof.
- [ ] F5 (changes) `MIPStarRE/QPBT/Games/DistanceTheorems.lean:151` — Several blueprint line ranges in docstrings now point to preceding results rather than their stated counterparts.

## Review

**F1:** Statement `\uses` must contain only objects needed to formulate the statement. The theorem dependencies at lines 539, 558, 593–594, 702, 721, 754, 794, 886, 905, 924, and 942–943 belong in the corresponding proof blocks when genuinely used. For example, `sum_norm_mul_apply_le` has no quadratic-form hypothesis in its signature at `Support.lean:98`; `quadratic_form_mono` enters the proof at line 91. Retain statement-level definition dependencies, but move proof dependencies.

**F2:** Moving those edges is insufficient without correcting their sets:

- Line 558 names `lem:distance-sum-contraction`, but `Support.lean:132` applies the private finite-subset estimate instead.
- Line 594 names projective-measurement orthogonality, while `Support.lean:206` assumes orthogonality explicitly and the proof calls only the quadratic-form estimate at line 230.
- Lines 701–702 omit `lem:distance-quadratic-form-mono`, directly invoked at `Support.lean:409–412`.
- Lines 849–850 name the placed-measurement definitions, although `TensorSupport.lean:57–64` uses neither definition.
- The reverse marginal statement at lines 938–947 requires `def:povm-conventions`; its two theorem dependencies belong in the proof block.

**F3:** `Support.lean:662–663` directly invokes `MIPStarRE.LDT.Preliminaries.easyApproxFromApproxDelta`, whose blueprint node is `prop:easy-approx-from-approx-delta`. Add that label to the overlap-gap proof’s `\uses`.

**F4:** `povm_to_obs_of_measurements` is proved solely by applying `povm_to_obs` to the two effect families at `DistanceTheorems.lean:791–792`. Its blueprint proof should therefore say this and list only `lem:povm-to-obs-operator-families`. The expansion at lines 386–397 already belongs to the auxiliary theorem’s proof; the two definitions remain statement dependencies rather than proof dependencies.

**F5:** The inserted blueprint metadata shifted the referenced statements. Examples include `ch12_qpbt_games.tex:316-321` at `DistanceTheorems.lean:259`, while the triangle lemma is now at lines 326–331, and `ch14_qpbt_observables.tex:356-383` at line 797, while orthonormalization is now at lines 404–424. The ranges at lines 151, 193, 222, 259, 307, 378, 443, 634, 647, and 797 should be updated or replaced by stable labels.

I found no quantifier, hypothesis, conclusion, indexing, or notation mismatch in the newly linked statements. In particular, `povm_to_obs_of_measurements` now gives the source-faithful POVM specialization, and omitting aggregate `\leanok` from `fact:agreement` correctly reflects the missing right-projective branch. The new proof-level markers have no `sorryAx`, and the remaining `\notready` is still appropriate. I found no category-B language issue.

Summary: 0 mathematical-equivalence mismatches, 0 status-tag errors, 4 dependency/proof-description issues, 1 stale cross-reference issue, and 0 prose-language issues.

VERDICT: CHANGES_REQUESTED