## Findings

- [ ] F1 (changes) `local/bin/gh_common.py:251` — Update the newest matching review so retries replace the operative ledger

## Review

F1: The merge gate (`pr_merge.py:169-171`) and auto-fix reader (`autofix.sh:511-514`) explicitly make the last matching review authoritative when duplicate same-head reviews exist. The new loop instead updates the first match and returns. After an ambiguous historical retry, a re-review therefore edits an older record while the gate and fixer continue reading the stale newer ledger. Select the last matching row and add a two-match regression test.

The `PUT` endpoint and body payload otherwise match [GitHub’s review-update API](https://docs.github.com/en/rest/pulls/reviews#update-a-review-for-a-pull-request), and `git diff --check` passes. Exact-head CI is reported green. An independent test rerun could not start because the read-only review sandbox provides no writable temporary directory; it failed in `TemporaryDirectory` before executing test bodies.

VERDICT: CHANGES_REQUESTED