## Findings
- [ ] F1 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:167` — The pulled-apart measurement proof has an inaccurate statement/proof `\uses` split and omits invoked tensor lemmas.
- [ ] F2 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:400` — The swap-conjugation proof’s `\uses` list does not reflect several mathematical ingredients called by the Lean proofs.
- [ ] F3 (changes) `MIPStarRE/QPBT/Extraction/Observables.lean:565` — The revised mathematical docstring names a Lean declaration instead of stating its identity.

## Review
**F1:** The statement-level list at lines 158–159 includes the two projectivity lemmas, although only `def:tilde-m-measurement` is needed to state the result. Those lemmas belong in the proof list. Conversely, the Lean proofs invoke the tensor-projector result at `Observables.lean:103` and the two tensor/sum identities at lines 142 and 151. Add `lem:sandwich-tensor-projector`, `lem:distance-tensor-sum-right`, and `lem:distance-tensor-sum-left` to the proof-level `\uses`.

**F2:** The Lean proof uses inverse Fourier expansion at `Observables.lean:430`, the controlled-sum identities at lines 487 and 520, unfolds the polynomial marginal at line 579, and unfolds bracket notation at line 599. The proof dependencies should therefore include `lem:pauli-observable-expansion`, `def:s-w-marginals`, `def:bracket`, and `lem:distance-tensor-sum-left`. The controlled-sum results need a subordinate formalization-support node that this proof can cite. Also replace the generic `def:decoding-map` dependency with `def:qld-full-field-decoder` if the decoder is retained in this list, since the proof uses `decodeFq`.

**F3:** Replace “The final identification uses `lowDegreeEnc_eq_dotProduct`” with mathematical prose, for example: “The final identification is \(h\cdot\mathrm{ind}_m(u)=g_h(u)\).”

There is no blueprint–Lean equivalence mismatch: the quantifiers, player sides, \(0,\ldots,t-1\) indexing, conjugation orientation, and encoded-value fiber agree. The added proof-status markers correspond to closed proof bodies. Counts: 0 equivalence mismatches, 2 dependency-tag issues, 1 prose issue.

VERDICT: CHANGES_REQUESTED