## What changed

The failure was a prepared merge whose index became identical to the first-parent tree before `git commit`. Both bad commits have the same tree as their first parent, and reflogs record `commit (merge)`, not an `ort` merge. The exact shell command was not retained, but normal conflict resolution is ruled out.

Added:

- A NUL-safe merge-loss checker supporting pending and committed merges.
- `pre-commit` protection for prepared merges.
- A `reference-transaction` guard that rejects lossy automatic merges before the branch ref moves, including `--no-verify`.
- Handling for branch-owned deletions, recorded conflicts, and multiple merge bases.
- Installer, protocol, telemetry, and regression-test updates.

Committed as `2952317` and `1fa1fc7`, with fresh-base merge `96898f0`. No push was performed.

## Evidence

- Focused guard suite: 10 tests passed.
- Full workflow suite: 481 tests passed manually and again during commit.
- `35bdc2a` and `8ad1de8` were rejected with the same two reverted and five deleted paths.
- Latest merges on stack branches 108–118, 156, and 204 all passed.
- Hook installation check, Python compilation, shell syntax, and `git diff --check` passed.
- Worktree is clean and based on current `github/main`.
- Lean checks and `lake build` were not applicable because no Lean or blueprint declaration files changed.

## Statement integrity

Not applicable: no paper-labelled theorem, Lean signature, or blueprint declaration changed.

## Open obligations

None.

## Handoff

Review [merge_loss_guard.py](/home/drx/MIPStarRE-qpbt/.worktrees/issue-222-post-merge-silent-loss-guard/local/bin/merge_loss_guard.py:1) and the [reference-transaction hook](/home/drx/MIPStarRE-qpbt/.worktrees/issue-222-post-merge-silent-loss-guard/.githooks/reference-transaction:1) first. The live lane v17 already contains the immediate post-merge deletion check; these commits provide the durable repository-side guard.