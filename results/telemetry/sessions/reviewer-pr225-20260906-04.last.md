## Findings
- [ ] F1 (changes) `blueprint/src/chapter/ch13_qpbt_test.tex:716` — The recovered point relation uses the original point-answer effects, whereas the linked Lean theorem uses effects after relabeling every wrong-form answer to the zero tuple.

## Review

**One category-A mathematical-equivalence mismatch remains; no additional category-B findings.**

### F1 — Specify the point-answer relabeling in the consistency conclusions

The displayed recovery relation uses \(A^{\mathsf{Point},u}_{a_0,\ldots,a_{k-1}}\). However, `MIPStarRE/QPBT/Combining/DirectLowDegree/Transport/Combining/RecoveryTransport.lean:169` compares the polynomial measurement with the original measurement **post-processed by** `directLdPointValuesOrZero`. The general-simultaneity theorem and `exists_direct_ld_soundness` use the same post-processing.

That map, defined in `MIPStarRE/QPBT/Combining/DirectLowDegree/Game.lean:194`, sends both kinds of line answer to the zero tuple. Consequently, its zero-tuple effect is the original zero-tuple point effect **plus all wrong-form answer effects**. Projectivity does not force those additional effects to vanish, and success at least \(1-\varepsilon\) does not require perfect answer-form validity.

The convention at blueprint lines 474–478 defines the **combined strategy’s** answer map; it does not define a corresponding relabeling of the original point measurements in the recovery conclusions. Thus the displayed relation, and the subsequent reference to the same three conclusions, do not yet identify the mathematical objects in the linked signatures.

**Requested correction:** Define the relabeled point measurements explicitly in the combining subsection, specifying that wrong-form answers are assigned to the zero tuple. Use those measurements in the recovery equation and the general-simultaneity conclusions. This is a notation/statement correction, not a request for additional theorem hypotheses.

### Prior findings and remaining scope

The prior prose-lane findings are resolved: the recovery loss is retained, the pre-absorption error is identified as `mainFormalError`, the constants are existentially quantified in the proposition, the direct parameter domain omits divisibility, projectivity has its own theorem, and the cited terminology and indexing issues are corrected. The source-game correspondence and tensor-code parameter obligations remain explicitly open.

The inline attachment was truncated; I consulted the on-disk patch and checkout for the relevant comparisons. No files were modified or GitHub actions performed. Builds and proof-integrity review were outside this review’s scope.

VERDICT: CHANGES_REQUESTED