## What changed

Committed `d12c49d` (`feat(QPBT/Algebra): prove encoding and restricted decoding`).

- Proved `lowDegreeEnc_eq_dotProduct` in [LowDegreeCode.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-62-encoding-restricted-decoding/MIPStarRE/QPBT/Algebra/LowDegreeCode.lean:78).
- Proved the private indicator and cube-interpolation lemmas, then `decodeAt_lowDegreeEnc`, in [LowDegreeCodeTheorems.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-62-encoding-restricted-decoding/MIPStarRE/QPBT/Algebra/LowDegreeCodeTheorems.lean:29).
- No public signatures or re-exports changed.

## Evidence

- Both direct `lake env lean` checks pass.
- Owned-file proof-debt scan returns no matches.
- Full build passes: 9,040 jobs.
- Both targets’ axiom closures exclude `sorryAx`.
- `checkdecls`: all 822 declarations resolved.
- Blueprint axiom audit: 646 pass, 0 fail.
- Pre-commit integrity suite passes.
- Worktree has no uncommitted tracked changes.

## Statement integrity

`lowDegreeEnc_eq_dotProduct`: paper assumes `F_q`; Lean allows any commutative ring. The conclusion is unchanged. Verdict: faithful generalization.

`decodeAt_lowDegreeEnc`: `Finset K`, pointwise membership, and `DecidableEq K` faithfully represent `H ⊆ F_q` and `a ∈ H^M`. The conclusion is exactly `Dec_H(g_a) = a`. Verdict: faithful boundary hypotheses.

## Open obligations

None in the owned files.

## Handoff

PR #46 is still open, so its `decodeFqRep` and `IsEncodingRep` declarations are not yet present on this branch. Merge #46 first, then rebase or replay `d12c49d`; the relevant Lean changes auto-combine in the merge-tree check. Exact-head local CI and independent review remain for the operator after that integration.