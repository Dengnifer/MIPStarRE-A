## Findings
- none
## Review
All prior ledger findings are resolved. The placement proofs now reuse the shared API and `Commute.mul_nonneg`; the duplicate sandwich helper is gone; and polynomial collision uses `evalCoefficient_collision_card_le`.
The conditional-collision construction matches the pasting hypothesis at `references/qpbt-paper/06_nonlocal_games_and_mipstar.tex:504-525`. Its `G₁ = Z`, `G₂ = X` orientation correctly produces the paper’s X-Z-X sandwich, as documented in `docs/paper-gaps/qpbt_pasting-product-error.tex:430-441`. The nonzero-direction restriction is confined to clearly identified Lean-only auxiliaries and does not change the source-facing `lem:qld-xz-lines` statement. The restoration estimate explicitly retains the discarded mass.
Focused Lean checking succeeded. All changed key theorems depend only on `propext`, `Classical.choice`, and `Quot.sound`; no new `sorry`, axiom, unsafe mechanism, or proof-evasion pattern was found. The existing `sorry` in `combined_line_measurement_consistency` remains clearly marked as the known obligation tracked by issues #18 and #414 and its paper-gap note.
Residual risk is limited to that already tracked unconditioned consistency proof; this PR correctly supplies collision infrastructure without claiming to discharge it.
VERDICT: APPROVED
