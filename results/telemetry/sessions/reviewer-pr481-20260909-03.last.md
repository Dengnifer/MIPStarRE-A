## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/UniformLinePoint.lean:84` — Consolidate the duplicated uniform-point proof and its supporting `_current` lemmas with the public subline APIs.

## Review
**F1 requires changes.** `uniformDistribution_map_lineRepMap_add_smul_current` repeats the statement and full fiber-counting proof of `uniformDistribution_map_lineRepMap_add_smul` in `Lines/SubLineUniform.lean:60`. This creates two implementations of the same substantial argument in this PR.

The surrounding copies are also unnecessary:
- `Distribution.bind_const_current` duplicates `Lines/SubLineBind.lean:81`.
- `lineRepMap_zero_apply_current` duplicates `Lines/SubLineUniform.lean:43`.
- The projected-point uniformity proof at line 253 follows from `Lines/SubLineTransport.lean:106` and `uniformDistribution_prod`.

Reuse those public declarations and remove the duplicate proofs. I verified replacements for all four statements in a Lean stdin harness; the supporting imports introduce no cycle.

**Prior feedback.** The earlier source-certification and placement-collision findings are addressed. The specifically cited duplicates in `Claims.lean` are removed; F1 above concerns remaining copies elsewhere. The prior prose findings concerning claim scope, the gap note, point-constructor proof, prefix truncation, Parseval columns, uniform fibers, Z-only scope, and completion markers are addressed in the current checkout.

**Mathematical assessment.** The concrete sandwich matches `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:942-949`; its X marginal is proved rather than assumed. Claim 17-2 retains the source error order from `claim:17-2`, lines 1168–1201. The weaker real-part conclusions and directly indexed distribution are explicitly separated from the pending source claims. The polynomial point-error quantifier repair does not add a hypothesis to the source-facing existence theorem.

**Verification.** `Claims.lean`, `Lines.lean`, and `Observables/LineMeasurement.lean` type-check. Axiom inspection confirms standard axioms only for the three scalar claims, subline constructor, expanded-line consistency results, and restricted consistency bound. The sole scanned `sorry` is the documented `combined_line_measurement_consistency` obligation.

The combined-line construction, source-distribution transport, and complex-overlap correspondence remain unfinished as documented. Full build and blueprint rendering were not rerun. The inline attachment was truncated; scope was checked against the authoritative on-disk patch and checkout.

VERDICT: CHANGES_REQUESTED