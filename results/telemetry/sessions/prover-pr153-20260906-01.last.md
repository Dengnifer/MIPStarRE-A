Addressed F1 in `blueprint/src/chapter/ch13_qpbt_test.tex:890`:
- Explicitly distinguish base-two `\log` from natural `\ln`.
- Replace all exponential-conversion factors `\log 2` with `\ln 2`, including the constant, envelope, and Taylor substitutions.
- Preserve all parameter expressions involving `\log R`, Lean declarations, and paper-labelled theorem statements.

Validation: `leanblueprint web`, the single-file Lean check for `MIPStarRE/LDT/Basic/RpowBounds.lean`, and `git diff --check` passed. The corrected support statement matches the Lean denominator `(t * Real.log 2)^⌈s⌉₊`.

Only the blueprint file changed. Changes remain uncommitted; no PR comment was posted, per the local contract. Full-project build was not rerun.