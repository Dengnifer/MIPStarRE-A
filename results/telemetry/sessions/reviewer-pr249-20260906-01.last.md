## Findings
- [ ] F1 (changes) `audits/2026-09-06_issue-241_extraction-error-absorption.md:1` — Add the prescribed YAML front matter to this new audit document.

## Review

**F1 — Audit format requires changes.** The audit starts directly with its heading. `docs/CONTRIBUTING.md:388` directs new audits to follow `audits/2026-04-29_audit-document-format.md:18`, which prescribes YAML front matter. Add `title`, `date`, and `purpose`, together with quoted `issue: "#241"` and `pr: "#249"` metadata. This documentation-convention violation must be fixed before approval; no mathematical revision is needed.

**Statement-integrity audit.** `MIPStarRE.QPBT.deltaExtract_le_deltaQld` at `MIPStarRE/QPBT/Extraction/Unitary.lean:122` retains its public signature exactly:
- The assumptions match blueprint `lem:qld-extraction-error-form` at `blueprint/src/chapter/ch16_qpbt_extraction.tex:437`: `C ≥ 1`, `a > 1`, `0 < b < 1`, admissible parameters, and `0 ≤ epsilon ≤ 1`. Positivity of the integer parameters is faithful: the source explicitly defines its natural numbers as positive integers at `references/qpbt-paper/04_preliminaries.tex:6`.
- The existential constants precede universal quantification over parameters and error, preserving uniformity. No construction witness, bridge, or additional estimate enters the statement.
- The conclusion matches the blueprint’s scalar inequality and faithfully makes explicit the extraction bound and robustness-family comparison at `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:1857` and `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:1876`.

**Proof and validation.** The witnesses `a' = 4 * C² * a` and `b' = b / 8` give the claimed bounds. Fourth-root subadditivity and exponent monotonicity cover `epsilon = 0`; absorption does not require `md ≤ q`.

I checked the current file through `lake env lean --stdin`, appending an axiom inspection. The theorem depends only on `[propext, Classical.choice, Quot.sound]`, with no `sorryAx`. The signature comparison, `git diff --check`, and 100-character Lean line-width check also pass.

**Residual scope.** The sole remaining file hole is the unchanged, explicitly tracked `sorry` in `exists_extractionWitness_ofGlobalPairWitness` at `MIPStarRE/QPBT/Extraction/Unitary.lean:105`. It is not a dependency of this scalar proof. The completion tag therefore establishes only the scalar lemma, not extraction or full soundness. No full project or blueprint build was rerun in this read-only session.

VERDICT: CHANGES_REQUESTED