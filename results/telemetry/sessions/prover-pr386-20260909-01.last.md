Addressed both findings:
- F1: Removed `avgOver_prod`; both uses now call `SandwichProduct.avgOver_distribution_prod`.
- F2: Replaced the positivity proof with `exact reindex_nonneg e.symm hM`.

Both files type-check without warnings. Full `lake build` passed (9,171 jobs), as did `git diff --check`.

Surviving statements, blueprint links, and paper-gap records are unchanged. No new proof holes; existing repository `sorry` warnings remain.

Changes are uncommitted for `autofix.sh`. No push or GitHub mutation performed.