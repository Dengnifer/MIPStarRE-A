## Findings
- [ ] F1 (changes) `docs/paper-gaps/qpbt_linearity-theorem-quotation.tex:222` — Correct the false assertion that projective Fourier squares require an exactly linear original family.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/Points.lean:291` — Remove unused duplicate proof blocks that repeat existing implementations.

## Review

**F1: The claimed necessity is false.** Take \(t=1\) and \(O^u=Z=\operatorname{diag}(1,-1)\) for every \(u\). Then \(\widehat O^0=Z\), all other Fourier coefficients vanish, and the Fourier-square POVM is \(\{I,0\}\), hence projective. Nevertheless, \(O^uO^v=I\ne O^{u+v}\). With \(\rho=\operatorname{diag}(1-\eta,\eta)\), the correlation is \(1-2\eta\), so this counterexample permits arbitrarily small positive error.

The provider’s `thm:qblr`, at `references/nv-paper/fullpaper.tex:1095`, asserts that the Fourier squares form a POVM, not the claimed necessity. Replace “projective only when” with the justified statement that compression **need not preserve projectivity**, and qualify the following “no choice … and no compression” assertion. The same false claim appears in blueprint `rem:linearity-import`, at `blueprint/src/chapter/ch15_qpbt_combining.tex:310`. This correction does not invalidate the direct Lean construction.

**F2: The additional coarse-graining chain has no consumer.** The five private lemmas beginning at `Points.lean:55` culminate in `opFamilyDistSq_linearCoarseGrain_le`, which is never used. `extendedQ_spec` still uses the imported `opFamilyDistSq_uniform_affine_postprocess_le` from `MIPStarRE/QPBT/Combining/PointsDataProcessing.lean:275`. Remove this roughly 280-line duplicate chain.

Likewise, the new private degree lemmas at `MIPStarRE/QPBT/Combining/Defs.lean:219` and `:239` are unused and repeat the existing local `coefficientDegree` and `affineDegree` proofs. Delete them or consolidate their consumers. These duplicates add proof maintenance without supporting another result.

**Statement Audit**
The joint-measurement construction preserves the conclusions of QPBT `lem:qld-4-10` at `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:689`: projectivity on the original expanded spaces, both ordered products, all directed opposite placements, and a universal polynomial error. No additional load-bearing hypothesis was introduced. The common-ancilla quantifiers and corrected \(2\delta\) normalization are consistent with the documented Fourier–Naimark argument.

**Verification**
Reviewed the code and mathematical documentation beyond the truncated attachment using the on-disk patch and checkout. All nine changed Lean files type-checked without warnings. Proof-hole scans were clean; the four new/discharged public theorem axiom audits contained only standard axioms. A read-only Lean check confirmed F1’s counterexample mechanism.

The worktree remains unchanged. Full project and blueprint builds were not rerun; the shared `lean-conventions` skill was unavailable locally.

VERDICT: CHANGES_REQUESTED