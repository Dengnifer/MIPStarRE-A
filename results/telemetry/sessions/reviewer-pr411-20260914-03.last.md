## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Extraction/Consistency.lean:101` — The prior register-placement duplication remains; replace the private constructor and proofs with the shared `ProjectiveSetting.placedMeasurement` API.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/Points.lean:52` — Generalize the product-marginal effect identities and reuse them instead of adding witness-specific copies of existing local calculations.
- [ ] F3 (changes) `MIPStarRE/QPBT/Combining/Points.lean:65` — Correct the source attribution: `eq:qld-qxz-close-to-point-2` is an approximate consistency equation, not this exact fiber-sum identity.

## Review
**F1:** The private `placementEquiv`, `placedMeasurement`, effect, Hermitian, and projectivity infrastructure remains in `Extraction/Consistency.lean:40-159`. The shared API at `Combining/Points/PlacementSupport.lean:255-283` now provides the same measurement, effect, and projectivity facts directly. Continuing both implementations creates parallel placement APIs and duplicates substantial proofs. This prior finding remains unresolved and must be fixed before merge.

**F2:** Neither new theorem uses any property of `CombinedPointsWitness`; both are facts about every `Measurement (α × β) ι`. The repository already defines generic `marginalLeft` and `marginalRight` in `Games/Defs.lean:53-74`, while `Games/Sandwich/Pasting/PinchedReduction.lean:196-211` repeats the same two fiber-sum calculations locally. Add generic effect lemmas alongside the marginal definitions or `Measurement.postprocess`, then use them here and in `PinchedReduction`. Specialized corollaries may remain if useful.

**F3:** Paper `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:928-930`, equation `eq:qld-qxz-close-to-point-2`, states that the second marginal is approximately consistent with the expanded Z-point measurement. Lines 933-935, currently cited by the docstring, instead state line-versus-point consistency. The exact identities proved here appear explicitly in `blueprint/src/chapter/ch15_qpbt_combining.tex:949-952`. The docstrings must identify these declarations as definitional marginal auxiliaries rather than as the paper’s approximate equations.

The previous quadratic-form duplication is resolved by `Combining/QuadraticForm.lean`. Both focused Lean checks passed, `git diff --check` passed, and the new marginal proofs contain no proof-integrity bypasses. The added `combined_line_measurement_consistency` hole elsewhere in the merge-base patch is explicitly marked as the known issues #18/#414 proof obligation.

VERDICT: CHANGES_REQUESTED