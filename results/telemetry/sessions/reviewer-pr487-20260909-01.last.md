## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/ExtendedLineGame/PointPointRejection.lean:94` — Replace the duplicated measurement-placement constructions and projectivity proofs with the existing `ProjectiveSetting` API.

## Review

**F1: Modularity requires changes.** `aaPlacedMeasurement`, `baPlacedMeasurement`, and their six supporting lemmas reimplement the existing `placedMeasurement`, `placedMeasurement_effect`, and `placedMeasurement_isProjective` API in [Points/Placement.lean](/tmp/qpbt-point-self-consistency-completed-20260909/MIPStarRE/QPBT/Combining/Points/Placement.lean:258). This introduces approximately 90 lines of parallel register-placement infrastructure without additional mathematical content.

Use `setting.placedMeasurement .AA'` and `.BA''` in `point_point_rejection_le`, together with the existing effect and projectivity lemmas, and remove the redundant helpers. I verified that these replacements type-check through the module’s current imports. This must be addressed before approval under the duplication criterion.

**Mathematical audit.** No proof-integrity or source-statement blocker found. The three completed polynomial proofs preserve their public statements and the degree bounds in `def:combine-map` and `eq:combine-lines`. The completed-point lemmas preserve consistency under `some` and explicitly retain the inverse-mass loss after conditioning. The new line-game estimates remain auxiliary consequences of supplied witnesses, not replacements for the paper’s existence theorems. Failed completed evaluations are accounted for separately from coefficient disagreement.

**Verification.** Four files type-checked: `Defs.lean`, `PointSelfConsistencyCompleted.lean`, `PointPointRejection.lean`, and `AxisParameterDefectTransport.lean`. Ten principal results passed axiom inspection with only standard Lean axioms. The proof-hole scan, shell syntax check, and whitespace check passed.

The prior ledger contained no findings. I used the full patch to recover the truncated attachment and did not inspect telemetry records. Full-project builds and workflow runtime tests were not rerun; the checkout remains unchanged.

VERDICT: CHANGES_REQUESTED