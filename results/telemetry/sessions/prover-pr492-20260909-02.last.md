Addressed F1: removed `applyOperatorToState_mul'` and updated all callers in `Sandwich.lean` and `Consistency.lean` to use `DistanceCalculus.applyOperatorToState_mul`.
Both files type-check without warnings. `git diff --check` passes; no stale references or proof holes remain in either file. Theorem assumptions and conclusions are unchanged.
Changes are uncommitted. Full `lake build` was not rerun; validation used the permitted single-file checks.
