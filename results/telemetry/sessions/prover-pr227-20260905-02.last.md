Addressed F1 in `scripts/tests/test_github_workflow.py:451`: explicitly set `LOCAL_REVIEW_ENABLED="true"` in the regression subprocess environment, isolating it from the parent’s review kill switch.

Validation:
- Reproduced the original failure with `LOCAL_REVIEW_ENABLED=false`.
- All 14 workflow tests pass with the variable both `false` and unset.
- `git diff --check` passes.
- `lake env lean MIPStarRE/Quantum/FiniteMatrix.lean` passes.

Only the test environment changed; production behavior is untouched. Changes remain uncommitted for the lifecycle script. No known residual issue with F1.