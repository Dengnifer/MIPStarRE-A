## Findings

- [ ] F1 (changes) `local/bin/useful_queue.py:212` — Previously observed logs can disappear without triggering HOLD, allowing further admissions after evidence loss.

## Review

`refresh()` scans only currently existing files. When an observed launch log or session capture disappears, its saved cursor is never checked again. A live launcher therefore remains `running`, and subsequent packets can launch despite the unreadable-evidence rule in `local/protocols/useful-queue.md:16`. An in-memory reproduction returned `status=admitted`, one new launch, and zero HOLD calls after removing an observed active log. Recheck previously observed paths and add a regression requiring HOLD when active evidence disappears.

The prior findings are resolved: F1 through canonical paths in `account_router.py:273` and `dispatch.sh:287`; F2 through diagnostic filtering in `useful_queue.py:175`; F3 through Lake-root preservation in `useful_queue.py:362`.

The authoritative patch matches the checkout diff. Python parsing, Bash syntax, and whitespace checks passed; in-memory checks confirmed the prior F2/F3 repairs. Filesystem integration suites were not rerun in this read-only sandbox. No files or GitHub records were changed.

VERDICT: CHANGES_REQUESTED