## Findings

- [ ] F1 (changes) `blueprint/src/chapter/ch16_qpbt_extraction.tex:406` — The renamed conditional extraction obligation is mentioned only in a LaTeX comment, so the blueprint has lost machine-readable traceability to the Lean declaration.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/Witnesses.lean:274` — `ExtendedLinesWitness` still calls its directly indexed, completed-answer relations “the two source consistency displays,” contradicting the corrected provenance elsewhere in this PR.
- [ ] F3 (changes) `MIPStarRE/QPBT/Combining/Apply.lean:12` — “proposition-valued declarations,” “skeleton,” “statement links,” and “proof closure” are formalization jargon rather than mathematical prose.

## Review

F1: `exists_extractionWitness_ofGlobalPairWitness` is a renamed, open conditional declaration, and the new remark states its mathematical content in detail. Replacing the old `\lean{...}` with `% MIPStarRE...` removes it from declaration tracking. Keep it off the source-labelled `lem:qld-unitary` block, but restore a separate support entry containing `\lean{MIPStarRE.QPBT.exists_extractionWitness_ofGlobalPairWitness}` without `\leanok`.

F2: The witness fields use `DirectLineDesc`, `directLinePointDist`, and an `Option`-completed answer alphabet, while the paper statement uses lines in \(\mathbb F_q^{2m+2}\), its seed-induced law, and sums over \(a\in\mathbb F_q\). The revised `Apply.lean` documentation and chapter 15 now disclose these differences correctly, but the defining docstring still claims source equivalence. Replace it with wording such as “Directly indexed extended-line POVMs and two completed-answer consistency relations supporting `lem:qld-4-13`,” with the two gap citations.

F3: Replace the sentence with the direct status statement: “The existence statements below remain unproved.”

The seven prior-ledger findings are resolved. In particular, the extraction helper is now conditionally named and marked unfaithful, the combined-line text no longer claims a source-facing construction, and the transport and completed-answer obligations are identified. The new statement-level `\leanok` for `lem:qld-4-10` is accurate: its Lean signature matches the blueprint, while its still-open proof correctly has no proof-level mark. The added decoding dependency also reflects the Lean proof.

Summary: 1 blueprint-status issue and 2 prose/provenance issues. I found no remaining equivalence mismatch on a `\leanok`-marked source theorem.

VERDICT: CHANGES_REQUESTED