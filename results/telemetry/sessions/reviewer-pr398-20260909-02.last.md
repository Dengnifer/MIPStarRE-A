## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Points/Orthonormalization.lean:38` — The cited paper-gap note does not document the replacement combined-point proof.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/Points/Closeness.lean:57` — Reuse the shared distance-calculus lemmas instead of duplicating their proofs.

## Review

**F1: Document the replacement argument.** The new point modules repeatedly claim that `docs/paper-gaps/qpbt_linearity-theorem-quotation.tex` explains their direct field-valued sandwich construction. That note contains neither the Parseval transfer nor the direct projective-rounding argument. It discusses the separate quantum-linearity route and its common-extension obligation.

In `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:831`, the proof of `lem:qld-4-10` applies quantum linearity and assumes sufficient zero ancillas. The new proof instead rounds the field-valued sandwich on the original expanded spaces. Under `docs/paper-gaps/policy.tex`, this mathematical replacement requires an account explaining the dimension-independent Parseval estimate, sandwich consistency, rounding and averaging, and the resulting `K ε^{1/8}` error. Add that account to the cited note or a separate note, distinguishing the completed replacement from the still-open common-extension argument. This is a missing mathematical justification record, not a prose-style finding.

**F2: Consolidate the duplicated estimates.** The pointwise calculation inside `opFamilyDistSq_uniform_le_four` repeats `DistanceCalculus.sum_norm_sub_apply_sq_le_four` from `Observables/LineMeasurement/SquareRootError.lean:60`, with exactly the needed hypotheses. The uniform theorem needs only averaging and the constant-average identity. Also, `Points/Sandwich.lean:44` re-proves `DistanceCalculus.applyOperatorToState_mul`, already available from `Games/DistanceTheorems/Support.lean:64`. Reuse these results and update callers rather than maintaining independent proofs. I verified both replacements in a read-only Lean harness.

**Statement Audit**
- `exists_combinedPointsWitness` retains the paper’s projective measurements, both ordered-product comparisons, all directed opposite placements, and universal polynomial error without extra assumptions.
- The expanded-line declarations retain their existing statements and completed-evaluation convention. Their new proofs introduce no bridge hypotheses.
- The paired-line constructor matches the X–Z–X definition at paper lines 943–949 and proves both axis-degree support properties. It does not claim the unfinished consistency estimate.
- The conditional line-helper repair correctly places polynomial control of `deltaQ` before choosing `deltaP`; the source-facing theorem remains unchanged.

**Verification**
Direct checks passed for `Combining/Points.lean`, `Observables/LineMeasurement.lean`, and `Combining/Lines/CombinedMeasurement.lean`. Axiom closures for the main completed constructions contain only `propext`, `Classical.choice`, and `Quot.sound`. `git diff --check` passed; the checkout remains unchanged.

No proof-integrity blocker was found. The combined-line consistency proofs remain documented existing obligations. Full project and blueprint builds were not rerun; focused checks used existing dependency artifacts. The external `lean-conventions` reference was unavailable in the installed skill roots.

VERDICT: CHANGES_REQUESTED