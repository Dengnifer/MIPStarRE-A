## Findings
- [ ] F3 (changes) `MIPStarRE/QPBT/Combining/Claims.lean:567` — The deprecated alias still marks the now-proved complex estimate as open and unfaithful.
## Review
F3 must be fixed before merge. `subline_remove_X_factor_direct` now proves the complex Cauchy--Schwarz estimate at lines 401–458, but the alias documentation still says that estimate remains open and prescribes precisely the proof added by this PR. Since the alias has the same explicitly restricted direct-law statement, source-law transport is not an unresolved dependency of its proof. Remove the obsolete `**Unfaithful:**` paragraph while retaining the scope restriction and noting that paper Claim `claim:17-2` over the source distribution remains open.
Prior findings F1 and F2 are resolved: the restricted theorem and proof now carry completion marks, and both auxiliary theorems have blueprint support nodes.
The mathematical proof is otherwise sound. The general overlap estimate correctly requires each X/Z projection to commute with the line effect, without assuming X and Z commute with one another. The specialization uses the concrete X-Z-X measurement and the existing direct-law X deficit. The public target signature remains unchanged and preserves the complex modulus and \(C m\sqrt{\delta_{\mathrm{Line}}}\) bound.
The source distinction is correctly maintained: paper Claim `claim:17-2` at `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:1168-1201` uses the source law \(D\), while blueprint `lem:claim-17-2-direct` explicitly records the directly indexed, zero-completed restriction. The source entry remains unfinished.
No proof holes or forbidden tactics occur in the changed Lean files. Axiom checks for both helpers, the deficit theorem, the target, and its alias report only `propext`, `Classical.choice`, and `Quot.sound`. The authoritative patch was complete, and `git diff --check` passed.
VERDICT: CHANGES_REQUESTED
