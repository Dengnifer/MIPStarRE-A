## What changed

Committed `c964b18` (`feat(QPBT/Algebra): construct self-dual field models`).

[FieldBasis.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-65-self-dual-field-models/MIPStarRE/QPBT/Algebra/FieldBasis.lean:369) now proves `exists_selfDualNormalBasis` using the trace-dual normal basis and a symmetric square root in the binary Galois group algebra. It also proves `exists_fixed_field_model` using `GaloisField 2 k` and an exact little-endian coordinate equivalence.

The basis declarations were moved upstream from [SelfDualBasis.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-65-self-dual-field-models/MIPStarRE/QPBT/Algebra/SelfDualBasis.lean:20) without changing their public signatures. No re-export change is needed.

## Evidence

- Guarded full `lake build`: passed.
- Focused Lean checks passed for `FieldBasis`, `SelfDualBasis`, `SelfDualBasisTheorems`, and `PauliTheorems`.
- `FieldBasis.lean` `sorry|axiom` scan: no findings.
- `SelfDualBasis.lean` scan: only pre-existing `binaryCoordinates_mul` at line 34.
- Axiom closures for both targets and `fixedFieldModel`: `[propext, Classical.choice, Quot.sound]`; no `sorryAx`.
- `leanblueprint web`: passed with existing bibliography warnings.
- `checkdecls`: all 1,053 declarations resolved.
- `git diff --check`, line-length, prohibited-token, hook, and pre-commit integrity checks: passed.
- Worktree is clean, ahead 1 and behind `github/main` by 16 commits.

## Statement integrity

Paper assumptions: binary extension of odd positive degree. / Lean assumptions: finite field `K`, `Algebra (ZMod 2) K`, odd `k`, and `card K = 2^k`. / Paper conclusion: a simultaneously self-dual normal basis. / Lean conclusion: exactly such a `Fin k`-indexed basis with the stated trace pairing and Frobenius orbit. / Verdict: **faithful boundary hypotheses**.

`exists_fixed_field_model` is Lean-only and derives every stored field from admissibility. The mathematical existence assertion is proved; the paper’s deterministic polynomial-time complexity claim is not formalized.

## Open obligations

The only remaining hole in the owned downstream file is the pre-existing `binaryCoordinates_mul`, citing `eq:eq-mult` and paper lines 684–700. It belongs to the separate coordinate-identity packet; no dedicated paper-gap note is currently present in that file.

## Handoff

Review the group-algebra square-root argument and the little-endian `representation_natural` proof first. Exact-head GitHub CI and independent review remain for the operator after push; this session neither pushed nor reviewed its own commit.