## Findings
- [ ] F1 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:1823` — The combining narrative still unconditionally claims the line measurements constitute an LDT strategy, despite the unresolved carrier, distribution, and answer-alphabet transports documented immediately afterward.
- [ ] F2 (changes) `docs/paper-gaps/qpbt-gap-register.md:25` — The gap register retains duplicated stale rows, including the false claim that the directly indexed extended-line space appears in no blueprint node and the removed `exists_extractionWitness` name.

## Review
F1 is a category-A status and mathematical-description mismatch. The proof discussion at lines 1874–1902 correctly says that `lem:qld-4-13-established` uses a directly indexed carrier and completed answer alphabet, and therefore is not yet a strategy for the seed-indexed classical low-degree game. However, the preceding transition at line 1823 still states that the measurements “constitute a strategy,” and the later Naimark/soundness transition continues as though that strategy already exists. Rewrite this narrative so the soundness application is explicitly conditional on the two transport obligations, consistent with the source entry remaining `\notready`.

F2 is a stale cross-file status record rather than a prose-language issue. The newly added register rows are followed by older duplicate rows. Remove the duplicates or update them to the current declaration names and support-node location; in particular, the assertion that the directly indexed construction appears in no blueprint node is now false because `lem:qld-4-13-established` records it.

The prior findings concerning the witness provenance, conditional extraction traceability, Apply prose, and `ExtendedLinesWitness` wording appear resolved. The new statement-level `\leanok` for `lem:qld-4-10` matches the source-shaped declaration; the conditional extraction helper is linked only from the formalization-support example without `\leanok`. I found no additional category-B prose issues in the changed blueprint or Lean-facing text.

VERDICT: CHANGES_REQUESTED