## Findings
- [ ] F1 (blocker) `blueprint/src/chapter/ch15_qpbt_combining.tex:1354` — `\leanok` certifies a source consequence whose Lean theorem assumes the line witness it should obtain
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/Lines/ZeroDirectionMass.lean:24` — the public zero-direction lemma lacks a blueprint entry and corresponding `\uses` dependency
- [ ] F3 (changes) `MIPStarRE/QPBT/Combining/Lines/ConsistencyPositivity.lean:16` — “shared API” is software terminology rather than mathematical prose
- [ ] F4 (changes) `MIPStarRE/QPBT/Combining/Lines/FiberCollision.lean:28` — “normalization adapter” is software terminology rather than mathematical prose
- [ ] F5 (changes) `MIPStarRE/QPBT/Games/SupportedCompletion.lean:321` — “finite-measurement adapter” is software terminology rather than mathematical prose

## Review
**F1:** Blueprint item 3 at lines 1372–1386 claims Equation `eq:qld-xz-lines-restricted` as a consequence of `lem:qld-xz-lines`, and its proof says to apply the preceding inflation estimate to that lemma. The linked Lean theorem at `MIPStarRE/QPBT/Combining/Lines.lean:290` instead assumes `points : CombinedPointsWitness` and `lines : CombinedLinesWitness`, then uses `lines.consistent` at line 316. The construction of that consistency field remains open at lines 68–85, while the source lemma correctly remains `\notready`. Thus both the statement `\leanok` at line 1354 and proof `\leanok` at line 1391 are inaccurate. Keep items 1–2 certified, but separate item 3 into an explicitly conditional, non-source-facing entry or leave it uncertified until issue #414 is discharged.

**F2:** `nondegenerateLinePastingMass_bounds` invokes `aLinePointDist_zero_direction_mass` at `NondegeneratePastingMass.lean:52`, but that new public theorem has no blueprint node and the certified proof at blueprint line 935 cannot name it in `\uses`. Add a formalization-support lemma for the zero mass of axis-line samples with zero direction, then cite it from the retained-mass proof.

**F3:** Replace “is the shared API of” with, for example, “is supplied by the placement identities proved in”.

**F4:** Replace “formalization-only normalization adapter” with “formalization-only normalization lemma”.

**F5:** Replace “finite-measurement adapter” with “finite-measurement estimate used in the completion step”.

The four new retained-mass and conditioned-distribution statements themselves match their Lean signatures. The attached prior ledger was empty after truncation, so earlier-review duplicate triage was unavailable.

Summary: 1 blueprint/Lean status mismatch, 1 missing blueprint dependency entry, and 3 prose issues. The status mismatch is the principal merge blocker.

VERDICT: CHANGES_REQUESTED