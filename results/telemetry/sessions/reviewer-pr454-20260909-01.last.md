## Findings
- [ ] F7 (changes) `MIPStarRE/QPBT/Combining/Claims.lean:246` — Replace duplicated completed-outcome and averaging proofs with the already-imported lemmas.
- [ ] F8 (changes) `MIPStarRE/QPBT/Combining/UniformLinePoint.lean:80` — Consolidate the duplicated uniform-point fiber-counting proof with `Lines/SubLineUniform.lean`.
## Review
F7: `completed_pair_norm_sq_sum` duplicates `ProjectiveSetting.completedPair_norm_sq_sum_ZX` from `ZEvalDeficit.lean:165`. I verified that its entire proof can be replaced by `S.completedPair_norm_sq_sum_ZX points .BA'' x z`. Likewise, `avgOver_prod_current` duplicates `RestrictedAverage.lean:89`, and the three completed-point `_current` lemmas duplicate `LinePointOverlap.lean:142`, `:152`, and `:165`. These declarations are already imported. Remove the redundant proofs and use that API; this requires changes under the mandatory duplication criterion.
F8: `uniformDistribution_map_lineRepMap_add_smul_current` repeats the statement and roughly sixty-line proof of `uniformDistribution_map_lineRepMap_add_smul` in `Lines/SubLineUniform.lean:61`, including both zero-direction and nonzero-direction cases. Its private zero-direction helper is also duplicated. Retain one implementation and import it into both consumers. I verified that the existing theorem directly proves the repeated statement.
The previous-round findings F1–F6 are addressed: Claims 17-1 and 17-3 now have separate auxiliary blueprint entries; the placement compatibility module re-exports shared declarations; the point-construction and restricted-distribution completion markers are present; and the prefix-truncation description is corrected. The combined placement imports compile.
The Claim 17-2 domain repair correctly selects the X-Z-X measurement defined in `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:942–949`, whose X marginal is used at lines 1187–1193. It preserves the universal constant and \(m\sqrt{\delta_{\mathrm{Line}}}\) error without introducing a marginal or bridge assumption. The direct-distribution and real-part limitations remain documented.
Validation: `Claims.lean` and `Lines.lean` type-check. The changed Lean files contain exactly two explicitly tracked `sorry` sites, for Claim 17-2 and `combined_line_measurement_consistency`. Axiom checks for the six completed principal results contain only `propext`, `Classical.choice`, and `Quot.sound`; the line-witness constructor retains the documented `sorryAx` dependency.
I used the on-disk patch and checkout beyond the truncated attachment. Full builds and blueprint builds were not rerun. The external `lean-conventions` reference was unavailable locally; repository review and integrity rules were inspected.
VERDICT: CHANGES_REQUESTED
