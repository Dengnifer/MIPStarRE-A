## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Games/Sandwich/Defs.lean:106` — The new measurement-placement constructors still duplicate the already imported `DistanceCalculus.leftPlacedMeasurement` and `rightPlacedMeasurement`; consolidate the implementations.
- [ ] F2 (changes) `docs/paper-gaps/qpbt_pasting-product-error.tex:531` — The mathematical note includes extensive session-budget accounting and superseded validation handoffs; move the operational history out and retain one current mathematical status.
## Review
**Request changes.** The previous source-statement blocker is addressed by the one-sided Schmidt-mirror construction, but the placement-API duplication remains unresolved, and the expanded paper-gap note needs a focused documentation repair.
### F1 — Consolidate measurement placements
`Measurement.leftPlacement` and `Measurement.rightPlacement` reconstruct the same measurements, with the same index types and hypotheses, as the existing constructors in `MIPStarRE/QPBT/Games/DistanceTheorems/TensorSupport.lean:27` and `MIPStarRE/QPBT/Games/DistanceTheorems/TensorSupport.lean:42`. That module is already imported.
This is not merely similar notation: both implementations separately establish positivity and completeness for identical tensor-placed effects. The new pasting development uses the new constructors while `consistencyDefect_placed_le_one` still uses the old constructors, maintaining two interfaces within the same argument.
This is the preceding code review’s unresolved placement-duplication finding. Reuse the existing constructors and attach the required projectivity and postprocessing lemmas to that implementation. If namespace-compatible names are needed, make them thin aliases rather than independently reconstructed measurements. This must be fixed before approval under the modularity requirement.
### F2 — Separate the mathematical note from operational history
The statement-integrity account is followed by session numbers, charged worker hours, operator deadlines, preserved briefs, cache details, and three successive validation records. For example, `docs/paper-gaps/qpbt_pasting-product-error.tex:531` begins the session-budget history; the continuation record at line 597 describes an intermediate state with the source theorem still unfinished, before the later assembly record supersedes it.
The historical qualifiers avoid a straightforward logical contradiction, but readers must reconstruct several handoffs to determine the current formal status. This conflicts with `docs/paper-gaps/policy.tex:27`, which requires a mathematical note readable without the issue discussion, and its “Concision and Readability” section.
Preserve the counterexample, source-domain comparison, Schmidt-mirror argument, conditional theorem’s precise restriction, and final statement-integrity verdict. Replace the operational history with one concise current validation paragraph; move session accounting and intermediate build reports to the workflow records. This finding concerns paper-gap exposition, which is explicitly within this review’s scope.
### Statement-integrity audit
For `exists_pasting_error`, the relevant source is `references/qpbt-paper/06_nonlocal_games_and_mipstar.tex:504`, `lem:pasting`, rather than the inherited LDT theorem track.
- **Paper assumptions:** projectivity of \(G_2\) and \(A\), conditional collision control, the two forward marginal comparisons `eq:pasting-1`, and joint self-consistency `eq:pasting-2`.
- **Lean assumptions:** these comparisons remain, together with explicit finite-dimensional, probability, normalization, nonnegative-error, and decidability conditions. The register-exchanged fourth comparison is absent.
- **Paper conclusion:** consistency with the evaluated sandwich \(G_2G_1G_2\), with universal polynomial error.
- **Lean conclusion:** the same evaluated sandwich, using the explicitly documented additive error contract.
- **Verdict:** faithful finite-dimensional encoding with a documented scalar correction, not literal equivalence to the source’s product shorthand.
The mirror is constructed from the state rather than supplied as proof data. The separately named `exists_pasting_error_of_register_exchange` is explicitly conditional and is not called by the source-facing proof. The two-dimensional counterexample in the note supports rejecting the literal product-error interpretation.
### Prior findings and validation limits
The earlier code findings concerning stale commutator documentation, the gap-register status, downstream error-contract documentation, private tensor-identity copies, and the assembly module description are addressed. The right-placement projectivity lemma now has a consumer; the unused postprocessing helpers remain an advisory consideration. Prior prose-lane findings remain for the dedicated blueprint review and are not duplicated here.
No proof-hole or bypass declaration was found by the scan of the pasting files. I did not rerun Lean, the full build, or kernel dependency checks; the supplied exact-head CI success is not an independent validation performed in this session. The attachment was truncated, so I consulted the on-disk patch and checkout; this assessment does not claim exhaustive verification of every new proof term.
VERDICT: CHANGES_REQUESTED
