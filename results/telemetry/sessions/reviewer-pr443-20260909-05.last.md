## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Extraction/Consistency.lean:101` — Reuse the existing measurement-placement API instead of duplicating its construction and projectivity proof.

## Review
This round is limited to the §12 adjudication audit rather than a fifth full review.

The prior workflow finding is fixed. `local/protocols/useful-queue.md:53-63` now requires fresh root-issued reviewer follow-ups and explicitly disables coordinator-issued follow-ups until both validators support authenticated delegation.

F1 remains. `ProjectiveSetting.placedMeasurement`, `placedMeasurement_effect`, and `placedMeasurement_isProjective` already provide the required API in `MIPStarRE/QPBT/Combining/Points/Placement.lean:254-282`. The private block in `Extraction/Consistency.lean:35-159` independently rebuilds the same placement, effect identity, and projectivity result. Replace its uses with the shared API and retain only any genuinely extraction-specific Hermitian helper. Maintaining two constructions for the same mathematical placement risks divergence and violates the required modularity policy.

Because the review cap has been reached, the operator must either fix F1 or defer it to a tracked issue, then record the exact-head `ADJUDICATION` disposition required by `local/protocols/review.md` §12. The supplied context reports green exact-head local CI; I did not rerun the build in this read-only review.

VERDICT: CHANGES_REQUESTED