## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT/Extraction/SuppliedPointConsistency.lean:105` — The three proved `_ofGlobalPairWitness` theorems duplicate unused public `sorry` declarations in `Consistency.lean`, leaving obsolete source-looking proof-debt APIs.
- [ ] F2 (changes) `MIPStarRE/QPBT/Extraction/Construction.lean:37` — The zero-error global-witness composition is attributed to the decoder gap and issue `#519`, contrary to the extraction-transfer record assigning it to issue `#123`.

## Review

**F1:** The new declarations at `SuppliedPointConsistency.lean:105,223` and `ObservableConsistency.lean:48` have the same statements as `Consistency.lean:823,850,877`. The old declarations have no consumers, retain `sorryAx`, and their names do not expose the supplied-witness restriction. They meet the retirement criteria in `docs/formalization-patterns.md`. Remove them and update the old module documentation, retaining the explicitly conditional proved versions.

**F2:** The decoder note at `docs/paper-gaps/qpbt_decoding-identity.tex:87-100,131-145` documents the encoding-support correction and assigns that work to issue `#47`; it does not document the missing zero-error construction. Conversely, `docs/paper-gaps/qpbt_extraction-transfer.tex:160-170` explicitly assigns composition through `exists_globalPairWitness` to issue `#123`. Correct this citation in `Construction.lean`, the supplied-witness helper markers, and blueprint `lem:qld-construct-the-paulis` at line 255. The decoder note remains appropriate for the decoder correction itself.

All prior ledger findings are otherwise resolved. The restored `exists_pulled_apart_consistency` matches paper lines 1267-1274 and 1463-1480: constants precede strategy quantification, the global witness is an output shared by all three estimates, and zero error is included. Its sole `sorry` is clearly marked as a paper-realignment obligation. The conditional extraction theorem remains correctly named, marked `**Unfaithful:**`, and linked only from the explicit supplied-witness blueprint node.

The exact-head CI was reported green. Focused axiom checks found only standard axioms for the completed extraction declarations; only the documented construction obligation contains `sorryAx`. `git diff --check` also passed.

VERDICT: CHANGES_REQUESTED