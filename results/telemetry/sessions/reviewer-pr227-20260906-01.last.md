## Findings
- none

## Review
The change correctly excludes carried publications before both head-SHA deduplication and prior-ledger selection, consistent with `local/protocols/review.md` §13. Fresh-review deduplication remains unchanged.

Prior finding F1 is resolved at `scripts/tests/test_github_workflow.py:451`: the regression subprocess explicitly sets `LOCAL_REVIEW_ENABLED="true"`, isolating the offline fixture from an inherited kill switch without changing production behavior.

Validation passed: shell syntax, diff whitespace, Python test syntax, and in-memory execution of the actual counter for mixed, carried-only, and empty histories. The mixed-history check produces round 8 and only the last three fresh ledgers.

The full subprocess regression was not rerun because it requires filesystem writes outside this read-only review. Some combined reading output was truncated; targeted reads covered the changed logic and relevant contract. No remaining correctness issue identified.

VERDICT: APPROVED