## What changed

All ten issue #106 targets are proved with unchanged public signatures. Generic distribution lemmas now live in [DistributionAux.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-106-ld-geometry-sampling/MIPStarRE/QPBT/Games/DistributionAux.lean), while direct-sum support is isolated in [DirectSumSupport.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-106-ld-geometry-sampling/MIPStarRE/QPBT/Games/CondLinearTheorems/DirectSumSupport.lean).

All F1-F11 review findings were addressed, including typed-distribution blueprint alignment, separate conditional-linearity nodes, representative support nodes, dependencies, terminology, and current source spans.

Committed as `79fdfdf` (`fix(review): refresh QPBT blueprint citations`). Worktree is clean and unpushed.

## Evidence

- Direct `lake env lean` checks passed for all nine changed Lean files.
- `rg` finds no proof holes in the target declarations. The sole match is the pre-existing [exists_ld_soundness](/home/drx/MIPStarRE-qpbt/.worktrees/issue-106-ld-geometry-sampling/MIPStarRE/QPBT/Test/LowDegreeGameTheorems.lean:474).
- `lake build MIPStarRE.QPBT`: passed, 8,765 jobs.
- `lake build`: passed, 9,063 jobs.
- `lake exe checkdecls blueprint/lean_decls`: all 909 declarations resolved.
- `leanblueprint web`: passed.
- Hook installation and all pre-commit integrity checks passed.

`lake build MIPStarRE.QPBT.Test` cannot run because `MIPStarRE/QPBT/Test.lean` does not exist; the QPBT aggregate and full repository builds passed instead.

## Statement integrity

Paper assumptions: finite field of admissible size, positive `m`, `d`, and `k`, with `m ∣ q`; line membership where applicable.

Lean assumptions: `LdParams` encodes those conditions directly; `linePoints_eq_of_mem` is valid more generally over any field.

Paper and Lean conclusions agree on line equivalence, level-2/level-3 conditional linearity, sampler probability, uniform marginals, line incidence, diagonal prefix-zero geometry, and the typed low-degree sampler identity.

Verdict: **exact**, with faithful boundary encoding through `LdParams`. The generic `typedCLDistribution` remains a strict generalization and therefore its generic blueprint node intentionally has no `\leanok`; the concrete low-degree specialization has its own proved node.

## Open obligations

[exists_ld_soundness](/home/drx/MIPStarRE-qpbt/.worktrees/issue-106-ld-geometry-sampling/MIPStarRE/QPBT/Test/LowDegreeGameTheorems.lean:447) remains assigned to the later soundness packet. Its discharge requires the game-correspondence and auxiliary parameter-bound results documented in `docs/paper-gaps/qpbt_ld-dimension-divisibility.tex` and tracked by issue #16.

## Handoff

Independent review and exact-head CI remain operator steps after pushing `79fdfdf`. If the exact test aggregate command is required, the missing re-export module is `MIPStarRE/QPBT/Test.lean`; it was not added in this packet.