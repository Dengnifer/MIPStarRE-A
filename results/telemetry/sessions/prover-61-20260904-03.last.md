## What changed

Moved `weight_sum_eq_one_of_subset` into the shared [Distribution.IsProbability API](/home/drx/MIPStarRE-qpbt/.worktrees/issue-61-finite-distribution-normalization/MIPStarRE/LDT/Basic/Distribution.lean:128).

Updated the `univ` specialization, [total-variation proof](/home/drx/MIPStarRE-qpbt/.worktrees/issue-61-finite-distribution-normalization/MIPStarRE/LDT/Basic/DistributionAvg.lean:271), and all QPBT normalization uses. Removed the private duplicate.

Committed as `991d7c7` (`fix(review): share the support-normalization lemma`). Not pushed.

## Evidence

- All three changed files pass `lake env lean`.
- Locked `lake build` passed.
- Forbidden-token scans returned no matches.
- Shared lemma and all three QPBT targets have no `sorryAx`.
- `git diff --check` and pre-commit integrity gates passed.
- Worktree is clean.

## Statement integrity

No paper-labelled theorem statements changed. Every existing public signature remains unchanged; the new shared theorem is formalization infrastructure.

## Open obligations

None.

## Handoff

Review finding F1 is addressed. No re-export change is needed, and `MIPStarRE/QPBT.lean` was not modified.