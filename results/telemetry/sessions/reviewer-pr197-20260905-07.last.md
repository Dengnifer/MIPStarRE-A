## Findings

- [ ] F1 (blocker) `local/bin/checked-push.sh:80` — the emergency bypass still lets `push.followTags` publish refs outside the explicit branch mapping

## Review

The prior finding is resolved for validated publication: line 147 disables follow-tag expansion, and the regression covers operation without a native hook.

The bypass path remains exposed. With `MIPSTARRE_SKIP_HOOKS=1`, line 80 executes an unrestricted `git push`; when `push.followTags=true`, a reproduced bypass push returned success and published an annotated tag alongside the branch. The bypass is documented as skipping tooling checks, while the helper and protocol still promise one-ref publication. Apply `-c push.followTags=false` and `--no-follow-tags` to this path too, and extend the bypass regression to assert that the tag remains absent.

All 15 relevant unit tests passed, as did shell syntax and `git diff --check`; the current bypass test does not exercise `push.followTags=true`. Under the round cap, this finding must be fixed or deferred to a tracked issue through an exact-head §12 adjudication, without another full review round.

VERDICT: CHANGES_REQUESTED