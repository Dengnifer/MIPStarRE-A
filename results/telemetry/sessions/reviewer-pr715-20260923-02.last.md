## Findings

- [ ] F1 (changes) `docs/paper-gaps/qpbt-gap-register.md:38` — The new terminal status is unsupported by the completion protocol and gate.

## Review

The register explicitly defines its `Terminal status` column through C3, but `local/protocols/completion.md:48` permits only `corrected` and `no-difference`. `scripts/completion_gate.py:386` enforces those values. Running `criterion_paper_gaps` on this head reports line 38’s `documented-deviation` as nonterminal, contradicting the newly documented terminal classification.

Land the corresponding protocol and gate amendment, with tests and the required evolution entry, before this transition; alternatively, retain `pending` in the C3 column and record documentary closure separately until that amendment lands. This requires no additional mathematical proof.

The mathematical disclosures otherwise agree with the inspected paper passages and Lean declarations: the square-root obstruction, independent constants, zero boundary, and separate downstream limitations are accurately distinguished. No Lean statements or proofs change.

Paper-gap style, blueprint label resolution, and `git diff --check` passed. Builds were not rerun in this read-only review; the supplied context reports successful CI for `4ac910dfb08185deedc13eb9ee27c5776687d3e3`. The checkout remains unchanged.

VERDICT: CHANGES_REQUESTED