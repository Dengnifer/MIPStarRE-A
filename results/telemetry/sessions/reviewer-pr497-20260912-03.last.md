## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Lines/AxisLineResampling.lean:45` — Extract the duplicated generic affine-resampling lemma and reuse it in both sampler proofs.
## Review
F1: The local `hresample` statement and proof duplicate `hresample` in `MIPStarRE/QPBT/Combining/Lines/DiagonalResampling.lean:52`. Both establish the same identity for an arbitrary direction and function; neither uses axis-specific or diagonal-specific hypotheses. Extract this identity into their shared supporting module, such as `SubLineUniform.lean`, and invoke it from both proofs. This requires changes under the review’s modularity and duplication criterion.
I found no mathematical correctness or source-statement fidelity defects. The conditional collision theorem retains conditioning on the complete first-question fiber required by `lem:pasting` (`references/qpbt-paper/06_nonlocal_games_and_mipstar.tex:504-525`). Nondegeneracy is confined to the auxiliary distribution, and the restoration estimate includes discarded mass. The two consistency-transport identities preserve the intended opposite placements without additional consistency assumptions.
Single-file Lean checks passed for pair-state transport, conditional collision, axis resampling, and restricted averages. Axiom checks for eight relevant declarations returned only `propext`, `Classical.choice`, and `Quot.sound`; the patch adds no proof holes or forbidden tactics. A full build was not repeated.
The existing `combined_line_measurement_consistency` proof obligation at `Lines.lean:84` remains explicitly tracked under issues #18 and #414. This PR supplies supporting lemmas without claiming to discharge it. The checkout remains unchanged.
VERDICT: CHANGES_REQUESTED
