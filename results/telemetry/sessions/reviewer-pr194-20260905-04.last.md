## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Games/DistributionMarginals.lean:48` — `distribution_map_map_of_isProbability` retains an unused normalization hypothesis and duplicates `Distribution.map_map`.
## Review
F1 remains unresolved from the prior review. Renaming the argument to `_hmu` makes the unused hypothesis explicit but does not remove it. The generalized `Distribution.map_map` theorem proves the same identity for every finite distribution, so this wrapper needlessly requires six callers to construct probability proofs. This conflicts with the project rule to remove or narrow unused arguments. Migrate the callers in `DirectLowDegree/Geometry.lean` and `Transport/Correspondence.lean` to `Distribution.map_map`, then remove the wrapper.
The other prior findings are addressed: the support lemmas are classified as formalization-only, the prefix-projection label is corrected, the blueprint distinguishes `chiIndex = χ - 1`, finite/nonempty hypotheses are displayed, the seed-fiber dependency is recorded, and `def:line-point-dist` no longer carries an invalid `\leanok`.
The source-statement audit otherwise passes. The seed decomposition matches `eq:chi-func` at `references/qpbt-paper/08_classical_and_quantum_low_degree_tests.tex:215-221`, with the zero-based shift stated explicitly. Uniformity matches the argument at lines 253-255. The `lem:alnf` and `lem:dlnf` conclusions at lines 243-272 are represented without extra proof-debt assumptions.
Verified the critical changed Lean files individually, including `DistributionAux.lean`, `DistributionMarginals.lean`, `LowDegreeGame.lean`, `LowDegreeGameTheorems.lean`, and `Correspondence.lean`. All type-check; the sole warning is the pre-existing, explicitly tracked `exists_ld_soundness` `sorry`. `lake exe checkdecls blueprint/lean_decls` resolved all 1000 declarations.
VERDICT: CHANGES_REQUESTED
