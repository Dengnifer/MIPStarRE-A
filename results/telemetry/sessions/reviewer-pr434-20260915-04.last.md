## Findings
- [ ] F1 (changes) `blueprint/src/chapter/ch14_qpbt_observables.tex:1715` — The postprocessing identity omits `def:bracket` from its statement and proof dependencies.
- [ ] F2 (changes) `blueprint/src/chapter/ch12_qpbt_games.tex:2146` — The conjugation auxiliary does not identify the paper or blueprint result it supports.
- [ ] F3 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:1565` — The zero-direction auxiliary does not identify its role in the paired-line pasting argument.
- [ ] F4 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:1582` — The pointwise-defect auxiliary does not identify the paper-facing construction for which it is needed.

## Review
**F1.** The Lean proof at `MIPStarRE/QPBT/Observables/LineMeasurement/Expanded.lean:224` explicitly invokes `Measurement.postprocess_effect`. Its blueprint counterpart is `def:bracket`, which defines precisely the displayed fibre sum. Add `def:bracket` to both `\uses` lists at lines 1715 and 1727. `fact:data-processing` correctly remains only in the later consistency proof.

**F2.** “The placed-sandwich estimates” is not a named paper equation, theorem, or construction. The corresponding Lean docstring at `Support.lean:318` has the same omission. Identify the exact role on both surfaces, for example: “This formalization-only auxiliary supports the exact sandwich-overlap calculation for Lemma~`\ref{lem:qld-4-10}`, corresponding to the paper displays `eq:qld-rw-self-cons-1` through `eq:qld-rw-self-cons-4`.”

**F3.** Add that this is not a named paper statement and that it supplies the retained-mass estimate for the nonzero-direction conditioning used in the pasting argument for Lemma~`\ref{lem:qld-xz-lines}`.

**F4.** Likewise, explain that this is not a named paper statement and is the pointwise bound needed when restoring the mass omitted by that conditioning in the proof of Lemma~`\ref{lem:qld-xz-lines}`.

The mathematical statements otherwise agree with their Lean signatures: the quadratic-form identity, product measurement and addition postprocessing, zero-direction expectation, and pointwise defect bound all match. Their new `\leanok` markers are accurate, and the existing open direct-law obligation remains correctly without `\leanok`.

All nine prior-ledger issues are substantively repaired: the duplicate declaration and incorrect outcome-space link are gone; the missing entries and partial-evaluation terminology are corrected; the conjugation and nonnegativity dependencies were added; and the coarse-graining theorem now has its own lemma and proof status with `fact:data-processing` and `lem:distance-point-defect` placed at the correct proof level.

Counts: 0 equivalence mismatches, 0 invalid or stale status tags, 1 `\uses` mismatch, and 3 prose-attribution issues.

VERDICT: CHANGES_REQUESTED