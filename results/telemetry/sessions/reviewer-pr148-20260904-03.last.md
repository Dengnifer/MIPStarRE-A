## Findings
- [ ] F1 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:321` — The `\leanok` Fourier proof still never justifies Hermiticity of the Fourier coefficients.
- [ ] F2 (changes) `docs/paper-gaps/qpbt_linearity-distance-normalization.tex:212` — “No restriction on δ” contradicts the nonnegativity boundary in the Lean and blueprint statements.
- [ ] F3 (advisory) `MIPStarRE/QPBT/Combining/Linearity/NaimarkRounding.lean:69` — Reverse blueprint coverage reports all 16 public Naimark-rounding declarations as unlinked.
## Review
F1 remains from prior F7. `lem:boolean-fourier-transform` links and asserts `operatorFourier_isHermitian` at lines 296–308, but its proof only discusses inversion, Parseval, and completeness. Since the node is marked `\leanok`, the proof must also explain that the Boolean characters and normalization are real, so conjugate transpose passes through the normalized Fourier sum and preserves Hermiticity of each `O(a)`. The missing character dependency was fixed at line 320; the Hermiticity argument was not.
F2 is an inaccurate formalization comparison in the paper-gap note. The corrected construction theorem requires `_hη : 0 ≤ η` in `Stability.lean:269`, and substitution `η = 2δ` therefore uses `δ ≥ 0`, matching `thm:linearity` at `ch15_qpbt_combining.tex:104` and the QPBT quotation at `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:715`. Replace “no restriction on δ” with “no upper restriction on δ beyond δ ≥ 0,” or retain the cited source domain explicitly.
F3 is advisory because the supplied PR context does not indicate the `enforce-blueprint-coverage` label. The prescribed reverse-coverage check reports every public declaration in `NaimarkRounding.lean`, from `naimarkAncilla` through `trace_heteroKron_mul_roundedObservable`, as missing a blueprint `\lean{}` link. A formalization-support node or an explicit explanation of why these declarations remain unlinked would resolve this. With the enforcement label, this becomes requires-changes.
Prior-round triage:
- Prior F1 is resolved by the formal comparison and `η = 2δ` discussion at `qpbt_linearity-distance-normalization.tex:190`.
- Prior F2 is resolved at `ch15_qpbt_combining.tex:204`, which now correctly distinguishes the QPBT `O(δ)` quotation.
- Prior F3 is resolved by the dedicated `lem:boolean-representation-stability` blueprint entry.
- Prior F4 is resolved: `exists_exact_boolean_representation` is separately identified as a corrected Lean-only theorem, while source-facing `thm:linearity` remains `\notready`.
- Prior F5 is resolved by the explicit “mean squared state-dependent operator distance” wording.
- Prior F6 is resolved by `\uses{def:povm-conventions}` at line 302.
- Prior F7 is partially resolved and carried forward as current F1.
- Prior F8, F9, and F10 are resolved by the revised mathematical descriptions in the three new Lean module docstrings.
The factor-of-two calculation, Fourier identities, residual-outcome folding, compression identities, and distance/defect equality are mathematically consistent with `references/nv-paper/fullpaper.tex:866-912,1074-1113`. No commutation hypothesis is smuggled in, and no bridge or proof-obligation package enters the public stability theorem. The paper-gap note’s main `false-source` verdict and ancillary-stable counterexample are sound apart from F2.
All four new Lean files type-check individually. The new declarations have only the standard axioms `propext`, `Classical.choice`, and `Quot.sound`; no new `sorry`, `axiom`, or prohibited proof mechanism was found. `git diff --check` also passes. Exact-head local CI was already green; I did not rerun the full build.
VERDICT: CHANGES_REQUESTED
