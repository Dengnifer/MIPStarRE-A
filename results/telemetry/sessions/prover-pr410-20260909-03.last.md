Both findings are already fixed in current HEAD `b7b2f600`, which follows the reviewed `b58aa308`:

- F1: Parseval’s docstring correctly describes orthogonal columns.
- F2: The placement lemma reuses `sum_effect_conjTranspose_mul_self_le_one_of_projective`.

Both files type-check without warnings. Integrity and whitespace scans pass. No theorem statements changed.

No edits were needed; the working tree remains clean. Full build was not rerun. The operator should reconcile the stale findings against `b7b2f600`.