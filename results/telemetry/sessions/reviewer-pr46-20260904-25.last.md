## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT/Extraction/Defs.lean:18` — The new extraction modules retain obsolete Chapter 16 line ranges, so their citations no longer locate the declarations they claim to formalize.
- [ ] F2 (changes) `MIPStarRE/QPBT/Algebra/Decoding.lean:128` — `decodeFq_lowDegreeEncoding` is attributed to the paper’s invalid lines 1805–1822, contradicting this PR’s corrected decoder analysis.

## Review

F1 is systematic. For example, `Defs.lean` cites `def:tau-dot-product-projector` at blueprint lines 46–53, but it now starts at line 85; `Observables.lean` cites `lem:tildew-product-form` at 75–88, but it starts at 138; `Consistency.lean` cites the helper at 213–244, but it starts at 266; and `Unitary.lean` cites `lem:qld-unitary` at 295–314, but it starts at 348. Update the repeated ranges or rely on the stable labels.

For F2, the actual source for
\(\operatorname{Dec}_{\mathbb F_q}(g_h)=h\) is `references/qpbt-paper/04_preliminaries.tex:917-922`. Lines 1805–1822 of the extraction paper instead use the invalid reverse identification \(g_{\operatorname{Dec}(g)}=g\). The corrected calculation uses neither decoder linearity nor decoder inversion, exactly as `docs/paper-gaps/qpbt_decoding-identity.tex:102-111` now explains. The conflicting claim also remains in `blueprint/src/chapter/ch16_qpbt_extraction.tex:58`; similarly, `decodeFq_add` cites lines 1442–1450 even though decoder linearity is used in the symmetry argument near lines 1548–1550, not that Fourier regrouping.

The mathematical statements otherwise pass the source audit against `14_analysis_of_the_pauli_basis_test.tex:1415-1860`. The encoding restriction and non-encoding support obligation repair the false unrestricted decoder identity; the cross-basis phase is correct; the consistency estimates preserve all player placements; and `ExtractionWitness` packages conclusions rather than introducing a bridge assumption. The explicit error enlargement is reconciled by `deltaExtract_le_deltaQld`.

All 26 new `sorry` sites are explicitly marked as proof obligations owned by issue #47. I found no unmarked proof hole, axiom, kernel bypass, or conditional proof-debt input. The earlier naming, import-cycle, basis-index, constant-quantifier, blueprint-link, and paper-gap comparison findings are resolved.

`git diff --check` is clean at head `2198c0b218cc31c8b3c6715e3fd046b79911b6d6`; the supplied exact-head local CI is green. At the round cap, F1 and F2 should either be corrected directly or converted into explicit tracked follow-up issues before merge.

VERDICT: CHANGES_REQUESTED