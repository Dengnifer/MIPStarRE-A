## What changed
No source changes or new commits; HEAD remains `cb6d86d`, with a clean worktree. Refreshed 19 inherited dependency artifacts using individual Lean invocations, without a full build.
**The quantitative bounds remain unproved.** I identified the precise prerequisite rather than adding assumptions or unrelated lemmas.
## Evidence
- `Apply.lean` type-checks with its three retained `sorry` warnings. This is still a **partial-cache check**, not source-aligned validation of the entire dependency chain.
- Refresh stopped at a genuine duplicate declaration:
  - `MIPStarRE/QPBT/Algebra/Pauli.lean:110`
  - `MIPStarRE/QPBT/Observables/ExpandedCommutation.lean:35`

  Both declare `MIPStarRE.QPBT.phaseSign_mul_self` with identical statements. Neither file is in the current write scope.
- Imported PR153 declarations `exists_directSimultaneousPolynomialMeasurements_of_k_eq_one` and `exists_directLdTransportConstants` have only standard kernel axiom dependencies.
- `Claims.lean` remains unchanged and contains no `sorry|axiom` tokens.
## Statement integrity
No theorem statement, hypothesis, error expression, or conclusion changed. No global-measurement consistency assumption was introduced to bypass the missing construction.
## Open obligations
The first missing source-derived input is **`exists_extendedLinesWitness_established_ofPointsWitness`**, at `MIPStarRE/QPBT/Combining/Apply.lean:304`: the #118 extended-line measurements with error
`C * m * deltaCombine ε (md/q)`.
The subsequent application must construct a projective direct-game strategy and prove its value bound. PR153’s `k = 1` theorem requires those inputs; it does not construct them from `ProjectiveSetting`.
Only then does the proof obtain the extended-polynomial measurement whose consistency with `extendedQ` supplies the **first approximation in `eq:qld-g-42`**. That relation is needed for `eq:qld-g-non-separable` and the later consistency transfer. The completion identities from `cb6d86d` alone do not bound the missing state mass.
All three existing holes remain:
- Printed-error extended-line construction.
- Established-error extended-line construction.
- `exists_globalPairWitness`.
Their existing gap references remain `qpbt_combined-lines-error-term.tex` and `qpbt_ld-dimension-divisibility.tex`.
## Handoff
1. Resolve the duplicate by retaining main’s algebra lemma and removing the inherited duplicate, under appropriate scope authorization.
2. Complete #118’s extended-line witness, then construct the passing extended-game strategy.
3. Resume the noncombined-mass proof at `eq:qld-g-42`.
No #118 claims or #201 files were edited. No push, merge, full build, or subagent was used.
