**Result**

Fixed F1 in [issues-prs.md](/home/drx/MIPStarRE-qpbt/.worktrees/issue-501-owner-inbox-500/local/protocols/issues-prs.md:252):

- Blocker identity is now immutable: `<!-- owner-inbox id=B<n> -->`
- Status is a separate mutable body field.
- Both creation and resolution use the unchanged marker with `ensure-pr-comment`, ensuring closure PATCHes the original comment.

Synchronized `DESIGN.md`, the main persona, paper-gap policy, and evolution ledger. Changes remain uncommitted as required.

**Validation**

- `lake build`: passed, 9199 jobs
- GitHub comment idempotency regression: passed
- `pdflatex` policy build: passed
- `git diff --check`: passed
- `sh -n .githooks/pre-commit`: passed

Existing unrelated `sorry` and linter warnings remain unchanged. No live GitHub mutation was performed under the local execution contract.

**PR Comment**

> Addressed F1. Owner-inbox blockers now use an immutable `<!-- owner-inbox id=B<n> -->` lookup marker and a separate mutable open/closed status field. The protocol documents using the unchanged marker for both `ensure-pr-comment` calls, so resolution updates the original blocker instead of creating a duplicate. The architecture, persona, paper-gap policy, and evolution records are synchronized. Full `lake build`, the marker-keyed PATCH regression, LaTeX compilation, and formatting checks passed.