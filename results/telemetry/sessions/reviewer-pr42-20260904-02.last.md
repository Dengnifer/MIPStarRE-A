## Findings

- [ ] F1 (changes) `local/bin/gh_common.py:251` — Update the newest matching review so retries replace the operative ledger

## Review

F1 remains unresolved. `post_review` iterates reviews in creation order and updates the first match, but `pr_merge.py:169-171` explicitly treats the last matching review as operative. With duplicate same-head reviews, this updates the oldest body while the merge gate continues reading the stale newest body, potentially disagreeing with the newly posted summary status.

Select the last match and add a regression test containing two matching reviews that asserts the newer review is updated. The current test covers only one match.

The focused test could not run in the read-only reviewer sandbox because no writable temporary directory was available. A write-free synthetic call reproduced the issue: reviews 5 and 9 caused review 5 to be updated. Exact-head CI was reported green.

VERDICT: CHANGES_REQUESTED