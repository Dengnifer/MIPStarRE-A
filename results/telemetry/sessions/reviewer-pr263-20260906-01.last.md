## Findings

- [ ] F1 (changes) `local/bin/codex-policy-shim.sh:29` — The `-c=KEY=VALUE` form corrupts configuration arguments and loses an explicit `xhigh` request.

## Review

F1 requires changes before approval. For `-c=model_reasoning_effort=xhigh`, the second assignment retains the leading `=`, so policy matching misses the requested effort. Intercepting `exec` reproduced forwarding both `model_reasoning_effort="max"` and the malformed `=model_reasoning_effort=xhigh`. Strip the optional `=` after `-c` and add this case to `test_runtime_shim_preserves_selected_effort_and_prompt`.

No Lean findings. `encodingPauliMeas` constructs the tensor POVM and postprocesses outcomes by their sum’s encoding. Its support and convolution identities agree with `references/qpbt-paper/04_preliminaries.tex:882` (`eq:low-degree-encoding-definition`) and `blueprint/src/chapter/ch16_qpbt_extraction.tex:229`. They introduce no proof-debt assumptions or claim to discharge `nonencodingMarginalMass_le`.

Both changed Lean modules and `MIPStarRE/QPBT.lean` type-check. All eleven new or newly public declarations depend only on `propext`, `Classical.choice`, and `Quot.sound`. Shell syntax, focused accounting checks, two existing read-only tests, and `git diff --check` passed.

I read the complete authoritative patch despite attachment truncation. The full build and filesystem-writing dispatcher suite were not rerun. The external `lean-conventions` skill was unavailable; repository integrity and review rules were applied. No files were modified.

VERDICT: CHANGES_REQUESTED