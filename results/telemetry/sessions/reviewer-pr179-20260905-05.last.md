## Findings

- [ ] F1 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:1943` — The support lemma does not state the domain of “directly indexed parameters,” so its `\leanok` scope is ambiguous about the deliberate absence of \(m\mid q\).
- [ ] F2 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:1935` — The statement-level `\uses` edge and “obtained from” prose assert a `mainFormal` dependency that the linked Lean conversions neither invoke nor provide.
- [ ] F3 (changes) `MIPStarRE/QPBT/Combining/DirectLowDegree/Transport/Consistency/State.lean:410` — The public `mature...` pass-through aliases are retained for an internal branch, exposing implementation-history terminology in the mathematical namespace.

## Review

F1 is a blueprint-to-Lean domain mismatch. `DirectLdParams` in `Geometry.lean:36-45` requires an admissible `q` and positive `m`, `d`, and `k`, while deliberately omitting `m ∣ q`. The source `LdParams` used by `def:ld-game` does include that divisibility field. The preceding gap note explains that the direct repair is intended to remove this condition, but the support statement only gives a bare tuple and then cites `def:ld-game`. State the direct domain explicitly, including “no divisibility hypothesis,” and either add a direct-game formalization node or remove the source-game dependency from this node.

F2 is a dependency and provenance mismatch. The linked declarations in `Transport/Consistency/Defect.lean:83-114`, `:168-215`, and `:259-284` take an arbitrary `ConsRel` as a premise and apply the generic state/defect conversion. They do not invoke `mainFormal` or construct its existential polynomial measurements. `mainFormal` itself has a pass hypothesis and returns those measurements (`MainFormal.lean:300-325`), and its auxiliary `k` is distinct from the direct tuple coordinate `D.k`. Describe these as coordinate-change lemmas applicable to `mainFormal`’s conclusions, remove `thm:main-formal` from the statement-level `\uses`, and revise the corresponding Lean docstrings at `Defect.lean:80`, `:165`, and `:257`.

F3 is a prose and naming violation. `State.lean:406-461` adds empty public aliases such as `matureCoordinatePointMeasurementA` and explicitly justifies them by a “stacked branch”; `Defect.lean:286-308` adds the analogous deprecated theorem. These names describe assembly history rather than mathematical objects, and the cited branch is not a paper- or blueprint-facing compatibility requirement. Remove or update the downstream branch, or isolate temporary compatibility names outside the mathematical namespace with an explicit migration/removal record.

Summary: one Category-A domain issue, one Category-A dependency/provenance issue, and one Category-B naming/prose issue. The corrected `ldGame` tag, projectivity and zero-based indexing scopes, mixed compression claims, proof markers, and `\notready` statuses are otherwise accurate.

VERDICT: CHANGES_REQUESTED