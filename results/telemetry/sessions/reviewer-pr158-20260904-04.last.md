## Findings

- [ ] F1 (blocker) `blueprint/src/chapter/ch14_qpbt_observables.tex:438` — The explicit-constant lemma uses asymptotic `\simeq_\delta` instead of Lean’s exact defect bound, invalidating its statement `\leanok`.
- [ ] F2 (changes) `blueprint/src/chapter/ch14_qpbt_observables.tex:448` — The proof’s `\uses` omits the completeness-of-`√Q` and triangle estimates directly used by the Lean proof.
- [ ] F3 (changes) `blueprint/src/chapter/ch14_qpbt_observables.tex:450` — The proof treats Alice’s generally mixed reduced state as a ket rather than evaluating against its density operator.
- [ ] F4 (changes) `MIPStarRE/LDT/MakingMeasurementsProjective/LocalityPreservingRepair.lean:55` — The ten declarations changed from private to public have no corresponding formalization-support entries in the blueprint.
- [ ] F5 (changes) `MIPStarRE/QPBT/Games/StrategyClasses.lean:775` — The given-strategy theorem cites the unproved source block rather than its new matching support entry.
- [ ] F6 (changes) `MIPStarRE/QPBT/Games/DistanceTheorems/Calculus.lean:18` — The inserted ch12 support node left blueprint line-range citations stale across the affected QPBT modules.

## Review

F1 is the principal mathematical-equivalence issue. Definition `def:consistency` at `ch12_qpbt_games.tex:210` defines `\simeq_\delta` through an `O(\delta)` bound and explicitly says it is not a pointwise inequality. In contrast, `projective_rounding_with_explicit_constant` at `ProjectiveRounding.lean:502` assumes the exact inequality `consistencyDefect ≤ δ` and concludes the exact distance bound at line 507. A hidden constant in `O(\delta)` would alter the displayed constant `220`. State the premise and conclusion as their explicit sums, for example `consistencyDefect ≤ δ` and `Σ_a ‖((P_a-Q_a)⊗I)ψ‖² ≤ 220 δ^(1/4)`. The undefined `d_\psi` notation at line 442 should likewise be replaced by that sum.

For F2, the proof invokes `sqrtQCompleteness` at `ProjectiveRounding.lean:346` and `questionSDD_triangle` at lines 445 and 487. Add the corresponding blueprint dependencies, including `lem:sqrt-Q-completeness` and the exact triangle result represented by `prop:triangle-inequality-for-approx_delta`.

For F3, Lean defines `φ` as `leftMarginalState Ψ` at line 231 and uses `ev φ` at line 259. Since this marginal need not be pure, replace the ket expression with either
`\operatorname{Tr}(\rho_A(Q_a-Q_a^2))`
or the equivalent bipartite expectation
`\langle\psi|((Q_a-Q_a^2)\otimes I_B)|\psi\rangle`.

For F4, `diagBlock` through `matrix_eq_zero_of_rank_eq_zero` are newly public names at lines 55–328. Under the blueprint coverage rule, they need appropriately typed formalization-support entries and status tags.

For F5, `exists_symmetric_projective_strategy_of_strategy` now matches the support lemma at `ch12_qpbt_games.tex:140-154`; its docstring still points to the source theorem at lines 116–135, whose attainment step remains open.

For F6, examples include `Calculus.lean:18` citing lines 244–416 although the relevant entries now start at 260, and `DistanceTheorems.lean:537` citing 569–577 although the close-values lemma is now at 584–597. The post-insertion ranges in `StrategyClasses.lean` are similarly stale and should be updated together.

The new proof-level status tags otherwise check out: the closed close-values, orthonormalization, explicit-rounding, and given-strategy symmetrization declarations have no `sorryAx` dependency. The source attainment theorem correctly remains without `\leanok`, and no stale `\notready` was introduced.

Counts: 1 equivalence/status blocker, 2 blueprint coverage/dependency issues, 1 mathematical-prose issue, and 2 stale-reference issues.

VERDICT: CHANGES_REQUESTED