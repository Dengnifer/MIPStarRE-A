## Findings

- [ ] F7 (changes) `MIPStarRE/QPBT/Combining/Lines/PointSelfConsistency.lean:31` — Remove `avgOver_mix_noFinite`, which duplicates the existing `MIPStarRE.QPBT.avgOver_mix` statement and proof.

## Review

F7 requires changes before approval. The private helper copies the proof in [RestrictedAverage.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-445-combined-point-line-marginal/MIPStarRE/QPBT/Combining/Lines/RestrictedAverage.lean:60), whose statement already omits a `Finite` assumption. Maintaining both copies duplicates the distribution API. Import that module and use `MIPStarRE.QPBT.avgOver_mix`, or move the existing theorem into a shared distribution module. I verified that `avgOver_linePointDist_point` type-checks with the existing theorem.

All supplied prior findings are addressed: the operator-action, square-summability, and placement proofs reuse shared APIs; overlap formulas include real parts; the Parseval description specifies columns; and the blueprint now records the combined-point construction, expanded sandwich notation, and supplied-marginal theorem with exact hypotheses and a proof.

The statement audit found no new mathematical blocker. The relevant source is `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex`: `lem:qld-comm-line-cons` at lines 523–678, `lem:qld-4-10` at 689–709, and the marginal comparisons through `eq:pasting-q1` at 917–941. The Lean strategy assumptions, universal-constant quantifiers, four directed placements, and conclusions agree with the source under the blueprint’s explicit completed-outcome convention. The supplied-witness result is separately identified as auxiliary, and its `8δ_Q + C(ε + √ε)` bound preserves both errors. No unproved bridge was added.

Validation passed for direct Lean checks of `CombinedPointLineMarginalDistance.lean` and `LineMeasurement.lean`. The six checked axiom closures, including both construction theorems, `extendedQ_spec`, and the marginal theorem, contain only `propext`, `Classical.choice`, and `Quot.sound`. No proof holes or prohibited bypasses were found in the new Lean modules.

The truncated attachment was recovered from the full patch and ledger; the patch matches the specified commits. I did not rerun the full build or blueprint build. The `lean-conventions` skill was unavailable in the installed skill locations; repository conventions and the supplied review checklist were applied. The worktree remains unchanged.

VERDICT: CHANGES_REQUESTED