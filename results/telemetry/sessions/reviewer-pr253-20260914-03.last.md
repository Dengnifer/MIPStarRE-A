## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT/Extraction/RegisterTransport.lean:2` — Remove the unused `ExpandedPlacement` import and unused `BigOperators` scope.
- [ ] F2 (changes) `MIPStarRE/QPBT/Extraction/RegisterTransport.lean:144` — Narrow the file-wide classical scope that triggers the `style.openClassical` linter.
- [ ] F3 (changes) `audits/2026-09-06_extraction-register-transport-245.md:15` — Refresh the audit, which now misstates the final scope, declaration count, warnings, build, and re-export status.

## Review

**F1:** The complete source elaborates successfully after deleting the `ExpandedPlacement` import and changing line 28 to `open scoped Matrix`. Conversely, deleting both `ExpandedPlacement` and `GroundSlice` fails exactly at the three reused `MagicSquareRigidity` declarations. Keep the direct `GroundSlice` dependency and remove the stale transitive import and unused scope.

**F2:** The exact-head `lake build` succeeds but reports:

```text
RegisterTransport.lean:144:12: please avoid 'open (scoped) Classical'
```

Project policy requires fixing new warnings rather than suppressing them. Use declaration-scoped `open Classical in`, proof-local `classical`, or appropriately explicit decidability instances.

**F3:** The PR-bound audit still describes the initial packet. Lines 15-17 say no blueprint or shared import changed, although this diff changes `ch16_qpbt_extraction.tex` and re-exports the module from `MIPStarRE/QPBT.lean`. It also claims 28 declarations although 26 remain after removing `psiHat_norm` and `stateQForm_reindexState`; it claims warning-free checking despite F2; and lines 161-163 claim no full build and no shared re-export. Add an exact-head amendment or update these statements.

The prior duplicate-`psiHat_norm` finding is resolved. `ProjectiveSetting.psiHat_norm` now exists only in `ExpandedPlacement.lean:233`, while this module defines the distinct `extractionAuxReference_norm`.

Mathematically, the new declarations are faithful formalization support rather than a restatement of paper Lemma `lem:qld-unitary`. The permutation and placement identities match `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:1669-1685,1769-1783,1812-1840`, and the blueprint clearly separates them at `ch16_qpbt_extraction.tex:628-677`. No bridge input or extraction witness is smuggled into a paper-facing theorem.

All 23 new theorem proofs have only `propext`, `Classical.choice`, and `Quot.sound` in their axiom closures. The changed Lean file contains no `sorry`, `admit`, `axiom`, or prohibited proof bypass.

VERDICT: CHANGES_REQUESTED