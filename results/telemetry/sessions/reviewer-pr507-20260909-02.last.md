## Findings
- [ ] F4 (changes) `local/bin/pr_train.py:243` — Successful trains leave external Lake build directories behind when `MIPSTARRE_LAKE_ROOT` is configured.
## Review
With an external Lake root, bootstrap makes the train’s `.lake` a symlink to `<root>/<train-branch>`. Removing the worktree deletes that symlink, but retains its populated target. The success path then deletes the branch and returns zero without invoking Lake cleanup. Each timestamped train therefore leaves another unused build directory, causing storage to accumulate.
Use the existing guarded `lake-root.sh cleanup` path after successfully removing the train worktree, preserving artifacts for failed or unknown publications. Add a successful external-root fixture that verifies the train target is removed while developer targets remain. The current external-root test exercises only failure and retention.
The prior findings are resolved:
- **F1:** `local/bin/ci.sh:869` builds the complete library and axiom audit together; the added fixtures cover cold publication and downstream failures.
- **F2:** `local/bin/pr_train.py:147` distinguishes published, refused, and unknown outcomes, including remote descendants and failed reconciliation.
- **F3:** `local/bin/pr_train.py:206` generates single-component branch names accepted by the external-root validator.
Shell syntax, Python parsing, patch whitespace, and 18 mocked reconciliation/manifest checks passed. A mocked successful train confirmed the missing cleanup call. The filesystem-writing fixture suite and Lean builds were not rerun under this read-only review contract. The checkout remains unchanged.
VERDICT: CHANGES_REQUESTED
