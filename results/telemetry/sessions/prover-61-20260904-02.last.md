## What changed

Proved all three normalization lemmas in [DistributionAux.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-61-finite-distribution-normalization/MIPStarRE/QPBT/Games/DistributionAux.lean:47):

- `Distribution.prod_isProbability`
- `Distribution.mix_isProbability`
- `Distribution.bind_isProbability`

Added one private support-sum helper. Public signatures and `restrict_isProbability` are unchanged.

Committed as `ed8d17a` (`feat(QPBT/Games): prove distribution normalization`). Not pushed.

## Evidence

- Target file type-checks without diagnostics.
- Locked `lake build` passed.
- Direct consumers type-check; `LineDefs.lean` retains two unrelated pre-existing `sorry` warnings.
- Target forbidden-token scan returned no matches.
- All three axiom closures contain only `propext`, `Classical.choice`, and `Quot.sound`, with no `sorryAx`.
- `git diff --check` and Git hooks passed.
- Worktree is clean.

## Statement integrity

Not applicable: these are formalization-only infrastructure lemmas, not paper-labelled theorems. All public signatures remain unchanged.

## Open obligations

None in the target file.

## Handoff

Review the private support-enlargement helper and the sum commutation in `bind_isProbability` first. No re-export change is needed; `MIPStarRE/QPBT.lean` was not modified.