## Findings
- [ ] F1 (changes) `blueprint/src/chapter/ch12_qpbt_games.tex:1054` — The complete, source-faithful CL predicates are linked without the required definition-level `\leanok`.
- [ ] F2 (changes) `blueprint/src/chapter/ch12_qpbt_games.tex:1068` — The complete shared-seed distribution definition is linked without the required definition-level `\leanok`.
## Review
For F1, `IsCondLinearOn` and its full-register specialization `IsCondLinear` at `CondLinear.lean:91-103` encode the paper’s inductive definition without additional mathematical hypotheses. The documented representation by supported terms does not change the predicate. Split the macros at line 1054 and place `\leanok` immediately after `\lean{...}`.
For F2, `clDistribution` at `CondLinear.lean:657-660` is exactly the pushforward of the uniform seed by \(x\mapsto(L(x),R(x))\). Its harmless extension to arbitrary maps agrees with the source definition on CL maps. Add definition-level `\leanok` after the link.
The statements for `lem:cl-kth`, `lem:cl-concat`, `lem:cl-func-prod`, and `lem:cl-dist-prod` otherwise match their paper counterparts, including ranges, partition hypotheses, levels, and conclusions. No bridge or proof-obligation inputs were introduced. The pointwise weight identity faithfully expresses equality of the finite product distributions.
All newly added support nodes match their Lean declarations. Their proof-level markers are valid: the checked axiom closures contain no `sorryAx`. The prior ledger’s citation, auxiliary-node, graph-normalization, and terminology findings are resolved. Count: 2 missing status markers, 0 equivalence mismatches, 0 prose issues.
VERDICT: CHANGES_REQUESTED
