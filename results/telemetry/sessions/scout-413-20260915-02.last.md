<!-- scout: scout-413-20260915-02 2026-09-15 -->
## Mathlib scouting report — 2026-09-15

### Mathematical source
- `def:line-point-dist`, `references/qpbt-paper/08_classical_and_quantum_low_degree_tests.tex:274-287`.
- The source defines the axis and diagonal line-point laws and defines the full law as their equal mixture. Issue #413 asks for axis mass `0`, diagonal mass at most `1/q`, and consequently mixed mass at most `1/(2q)`, without changing the sampler.
- The downstream use is `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:943-963`. These mass estimates are formalization-support results, not named paper statements.

### Relevant Mathlib definitions
- `mul_le_mul_of_nonneg_left` — scales the diagonal bound by the nonnegative factor `1/2` (`Mathlib/Algebra/Order/GroupWithZero/Defs.lean:226`).
- `one_div_mul_one_div` — rewrites `(1/2) * (1/q)` as `1/(2q)` (`Mathlib/Algebra/Group/Basic.lean:533`).
- No Mathlib probability object directly replaces the project’s explicit-support `Distribution` and `avgOver` API.

### Relevant MIPStarRE declarations
- `MIPStarRE.QPBT.LdParams` — general low-degree parameters; the target theorems do not require `AdmissibleParams` (`MIPStarRE/QPBT/Test/LowDegreeGame.lean:40`).
- `MIPStarRE.LDT.FieldModel.card` — proves `Fintype.card (ScalarQ L) = L.q`, so the displayed constants are exactly `1/q` and `1/(2q)` (`MIPStarRE/LDT/Basic/ParametersBase.lean:226`).
- `MIPStarRE.QPBT.aLinePointDist`, `dLinePointDist`, `linePointDist` — the unchanged component laws and their explicit `1/2` mixture (`MIPStarRE/QPBT/Observables/LineDefs.lean:165,172,180`).
- `MIPStarRE.QPBT.avgOver_mix` — linearity of `avgOver` over a convex mixture (`MIPStarRE/QPBT/Combining/Lines/RestrictedAverage.lean:60`).
- `MIPStarRE.QPBT.aLinePointDist_zero_direction_mass` — exact axis expectation `= 0` (`MIPStarRE/QPBT/Combining/Lines/ZeroDirectionMass.lean:24`).
- `MIPStarRE.QPBT.dLinePointDist_zero_direction_mass_le` — diagonal expectation `≤ 1 / Fintype.card (ScalarQ L)` (`MIPStarRE/QPBT/Combining/Lines/DiscardedMass.lean:20`).
- `MIPStarRE.QPBT.linePointDist_zero_direction_mass_le` — mixed expectation `≤ 1 / (2 * Fintype.card (ScalarQ L))` (`MIPStarRE/QPBT/Combining/Lines/DiscardedMass.lean:65`).
- `MIPStarRE.QPBT.prod_linePointDist_zero_X_direction_mass_le` — downstream product-law consequence already on main (`MIPStarRE/QPBT/Combining/Lines/DiscardedMass.lean:83`).
- The diagonal, mixed, and product bounds are linked with `\leanok` at `blueprint/src/chapter/ch15_qpbt_combining.tex:1101`; the axis result is linked separately at line 1783.

### Published-main compiled evidence
- A read-only `gh_common.py` API query verified live GitHub `main` as `bf864c016b3426a690c02f93bea9d40986604195`, the merge commit of PR #535 on September 15, 2026.
- The warmer snapshot `snap-20260915T080141Z-bf864c016b34` is stamped `status=complete`. Its recomputed project keyhash is exactly `ce2a00c4ed264b375191755c0044ef2d07f11c728408560ec2424e49bb328e18`; its package key is exactly `185353eebe93a5ab`; its toolchain is Lean `v4.32.0`.
- Direct `lean --stdin` inspection used only that snapshot’s ordinary `.olean` directory, its verified package store, and the matching Lean toolchain. All three compiled signatures have the sole explicit parameter `L : LdParams` and the laws and constants stated above.
- `#print axioms` reports `[propext, Classical.choice, Quot.sound]` for each of the axis, diagonal, and mixed theorems. No `sorryAx` occurs.
- Source scans of the corresponding published-main files found no `sorry`, `axiom`, `admit`, or forbidden proof bypass.

### Live issue and PR state
- Issues #413, #436, and #446 remain open. Issues #436 and #446 are exactly the two open children of #413.
- Issue #446 is blocked by exactly #436, and #436 blocks #446.
- PR #438 remains open at `766b2ab5320a81e7516cac4c374d5678dd3cdd0c`. Exact-head CI is green; `local-review/summary` is failure because two blueprint-prose findings remain unresolved.
- PR #535 is merged as the current GitHub-main commit `bf864c01`.
- No open PR exists for branch `issue-446-mixed-zero-direction-mass`. All three issues currently have zero comments.

### Candidate comparison
- PR #438’s diagonal theorem statement is textually identical to the compiled theorem on main. Its private resampling helper is unnecessary because main uses the existing `dLineDescOf_ldDLineCL` result. No unique required proof remains.
- The local #446 theorem at commit `01513e10a03fb909c6f517caf9763e409bc8fbfb` is textually identical to the mixed theorem on main. Its private `avgOver_mix_support` is superseded by the public `avgOver_mix`.
- Main is more complete than either candidate because `DiscardedMass.lean` also contains the product-law consequence used downstream.
- PR #438’s published Chapter 13 node still overclaims an unformalized fixed-index calculation according to its exact-head review. Local commit `5cfb37e9` separates that calculation into an untagged remark; this is optional documentation history, not required to satisfy #413/#436/#446, because main already has accurate `\leanok` support nodes in Chapter 15.
- Merging either candidate module unchanged would create duplicate namespace declarations. Current `Lines.lean` imports `Construction → Conditioning → DiscardedMass`; adding `DiagonalZeroDirectionMass` or `MixedZeroDirectionMass` would import second declarations of the same diagonal or mixed theorem.

### Suggested approach
The full mathematical objectives of #413, #436, and #446 are satisfied on verified published main, both structurally and through compiled axiom-closure checks.

1. Adopt `ZeroDirectionMass.lean` and `DiscardedMass.lean` on `bf864c01` as canonical.
2. Record #436 as superseded by the exact main theorem, then complete #436 through the normal issue workflow.
3. Once its recorded blocker is closed, record #446 as superseded by the exact main theorem and complete it.
4. Complete #413 only after both children are recorded complete.
5. Supersede PR #438 rather than merge its duplicate module. Preserve PR #438, commits `5cfb37e9` and `766b2ab5`, local commit `01513e10`, and both worktrees as historical evidence.
6. Any desired Chapter 13 prose improvement should be extracted later as a documentation-only change against the canonical declarations, without adding either duplicate Lean module.

No issue, PR, dependency, comment, branch, worktree, status, or file was mutated during this audit.

### Gaps to fill
- No mathematical or Lean proof gap remains for the axis/diagonal/mixed scope.
- Workflow records lag published code: the three issues and duplicate PR remain open.
- Optional documentation polish remains available from local PR #438 history, but it is not closure-critical.

### Searched
Read the full issue bodies and live parent/dependency state through `local/bin/gh_common.py`; queried the live main ref, PR #438, PR #535, exact-head statuses and reviews; inspected the paper passages, blueprint nodes, relevant audits, source declarations, import graph, candidate commits and worktrees; compared theorem statements mechanically; scanned for proof holes and bypasses; verified snapshot/toolchain/package hashes; and ran read-only compiled signature and axiom inspection against the complete `bf864c01` snapshot.