## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT/Extraction/Consistency.lean:101` — Reuse the existing measurement-placement API instead of introducing duplicate constructions and projectivity proofs.

## Review

F1 requires changes before approval. The new private `placedMeasurement`, its effect and projectivity lemmas, and `place_finset_sum` duplicate `ProjectiveSetting.placedMeasurement`, `placedMeasurement_effect`, `placedMeasurement_isProjective`, and `place_finsetSum` in [Placement.lean](/tmp/qpbt-uniform-affine-collision-20260909/MIPStarRE/QPBT/Combining/Points/Placement.lean:256). These declarations are already imported. [PointPointRejection.lean](/tmp/qpbt-uniform-affine-collision-20260909/MIPStarRE/QPBT/Combining/ExtendedLineGame/PointPointRejection.lean:94) introduces another copy for two placements. Consolidate these uses onto the existing API and remove the redundant coordinate constructions. I type-checked representative replacements using the shared lemmas.

The prior workflow finding is resolved: [useful-queue.md](/tmp/qpbt-uniform-affine-collision-20260909/local/protocols/useful-queue.md:53) requires fresh root-issued reviewer assignments and explicitly disables coordinator-issued follow-ups.

The statement comparison found no mathematical blocker in the reviewed changes:

- Combining-polynomial proofs retain the individual-degree and affine-evaluation statements. The existing `1 ≤ d` condition accounts for the fresh scalar variables.
- The pulled-apart projectivity, completeness, and swap-unitarity proofs match the cited paper calculations.
- `sum_marginalPoly_pointMeas_approx_id` retains its hypotheses, all four directed placements, and the documented error enlargement.
- Collision bounds retain `degree / q`; resampling includes zero directions, and subsequent parameter comparisons explicitly charge failed completed evaluations.
- The new paper-gap note explains the direct combined-point construction and distinguishes it from the unresolved common-extension argument.

Validation: six targeted Lean checks passed, including the QPBT root import. Extraction reported only five unchanged, explicitly tracked `sorry` warnings. Six principal theorem endpoints have only `propext`, `Classical.choice`, and `Quot.sound` dependencies. No new proof holes or forbidden proof primitives were found in the Lean additions; shell syntax validation passed.

The attachment was truncated, so I used the authoritative on-disk patch. Full `lake build` and workflow runtime tests were not rerun. The checkout remains unchanged.

VERDICT: CHANGES_REQUESTED