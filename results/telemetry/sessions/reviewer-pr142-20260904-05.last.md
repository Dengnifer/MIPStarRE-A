## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Games/CondLinear.lean:667` — `condLinearConcat` cites the direct-sum lemma rather than its concatenation source
- [ ] F2 (advisory) `blueprint/src/chapter/ch12_qpbt_games.tex:1053` — Paper-facing CL definitions still lack direct links to their Lean declarations
## Review
F1 requires correction before merge. `condLinearConcat` realizes \(T(x)=L(x^U)+R_{L(x^U)}(x^V)\), which is `lem:cl-concat` in `references/qpbt-paper/05_conditionally_linear_functions.tex:282-314`. Its docstring instead cites lines 315-364, which contain `lem:cl-func-prod`. The adjacent theorem has the correct citation; use the same range here.
F2 is advisory under warning-mode coverage. The reverse-coverage check reports nine declarations, but the clearly paper-facing omissions are `IsCondLinearOn`, `IsCondLinear`, and `clDistribution` from blueprint definitions `def:cl-func` and `def:cl-dist`. The remaining reported declarations are documented formalization helpers.
The statement-integrity audit otherwise passes. `isCondLinear_iff_nonempty_clData`, `IsCondLinearOn.concat`, `IsCondLinear.directSum`, and `clDistribution_directSum_eq_prod` match the hypotheses, conclusions, level indices, and quantifier structure of `lem:cl-kth`, `lem:cl-concat`, `lem:cl-func-prod`, and `lem:cl-dist-prod`. The field, finiteness, decidability, partition, positivity, and graph-nonemptiness assumptions are faithful boundary encodings; no bridge or proof-debt inputs were introduced.
Both changed Lean files type-check without warnings. The changed files contain no `sorry`, `admit`, axioms, unsafe bypasses, or placeholder tactics; axiom inspection found only standard `propext`, `Classical.choice`, and `Quot.sound` dependencies. The earlier ledger findings concerning auxiliary blueprint nodes, graph normalization, stale ranges, and implementation-oriented prose are addressed. The worktree remains clean.
VERDICT: CHANGES_REQUESTED
