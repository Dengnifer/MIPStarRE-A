## Findings

- [ ] F1 (blocker) `MIPStarRE/QPBT/Algebra/SelfDualBasis.lean:34` — `binaryCoordinates_mul` retains an untracked `sorry`, leaving paper Equation `eq:eq-mult` axiom-dependent.
- [ ] F5 (changes) `MIPStarRE/QPBT/Algebra/SelfDualBasis.lean:5` — The edited module docstring lacks the required `## References` section.
- [ ] F6 (changes) `MIPStarRE/QPBT/Algebra/SelfDualBasis.lean:2` — Relocating the basis construction leaves both direct Mathlib imports and `open MIPStarRE.LDT` unused.
- [ ] F7 (changes) `MIPStarRE/QPBT/Algebra/FieldBasis.lean:353` — The final `apply congrArg` / `change` / `rfl` sequence is definitionally a single `rfl`.

## Review

F1 remains a proof-integrity blocker. `binaryCoordinates_mul` formalizes Equation `eq:eq-mult` from `references/qpbt-paper/04_preliminaries.tex:684-697`, namely the basis-coordinate expansion of `ab`, and is linked from `def:binary-representation` at `blueprint/src/chapter/ch11_qpbt_algebra.tex:313-334`. `#print axioms` confirms a `sorryAx` dependency. The proof can be completed directly from `F.basis.sum_repr a`, multiplication over finite sums, and linearity of `F.binaryCoordinates`.

F5-F7 must also be fixed under the repository’s documentation and Mathlib-style rules. Move the existing source citations under `## References`; remove imports at lines 2-3 and the unused open at line 19, which I verified can all be deleted while the file still type-checks; and replace lines 353-355 with `rfl`, which also type-checks.

The new group-algebra construction itself is correct. Squaring over `ZMod 2`, the trace-dual transition, inversion-invariant square root, pairing calculation, and Frobenius reindexing all have the correct orientations. `exists_selfDualNormalBasis` faithfully represents the mathematical existence consequence at paper lines 494-502: its field, algebra, cardinality, and oddness assumptions are boundary data, not proof-debt hypotheses. The algorithmic complexity claim of `lem:efficient_basis` is explicitly excluded.

Prior F2-F4 are resolved: the duplicate trace lemma was replaced by `Algebra.trace_eq_of_algEquiv`, and docstrings were added to `squareMulEquiv` and `exists_self_dual_normal_basis_gal`. Both changed files type-check; the only warning is F1. The two newly completed public existence theorems use only `propext`, `Classical.choice`, and `Quot.sound`.

VERDICT: CHANGES_REQUESTED