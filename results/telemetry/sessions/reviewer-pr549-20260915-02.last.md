## Findings

- [ ] F1 (blocker) `blueprint/src/chapter/ch15_qpbt_combining.tex:2093` — The certified conditional node is not signature-equivalent to `exists_combinedLinesWitness_ofPointsWitness`.
- [ ] F2 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:1070` — The proof derives a `1/(2q)` restoration cost from only a stated `1/q` event bound.
- [ ] F3 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:2118` — The new `\uses` edges cite `\notready` source nodes and omit the directly indexed declarations actually invoked.
- [ ] F4 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:2116` — The certified support blocks lack blueprint entries for the new overlap and extended-measurement definitions they state.
- [ ] F5 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:2099` — Status prose uses private workflow language such as “quarantined,” “formalization-support node,” and “formalization mark.”
- [ ] F6 (changes) `MIPStarRE/QPBT/Combining/Lines/Conditioning.lean:119` — The comment describes a Lean “normalization goal” rather than the mathematical normalization.
- [ ] F7 (changes) `MIPStarRE/QPBT/Combining/ExtendedLines/Measurement.lean:108` — The docstrings use control-flow language including “zero-mass branch” and “field-valued fallback.”
- [ ] F8 (changes) `MIPStarRE/QPBT/Combining/ExtendedLines/Estimates.lean:30` — `BBprime` and `ABdoubleprime` expose private identifier spellings instead of the paper’s register notation.
- [ ] F9 (changes) `MIPStarRE/QPBT/Combining/ExtendedLines/Estimates.lean:8` — Repeated module prose says conventions “retain their existing meanings” instead of stating those conventions mathematically.
- [ ] F10 (changes) `MIPStarRE/QPBT/Combining/Lines/Sampling.lean:33` — Several new docstrings use process-shaped “proof-only” terminology instead of “formalization-only auxiliary.”

## Review

**F1:** The blueprint assumes only “a joint point family with consistency error,” while the Lean theorem requires the complete `CombinedPointsWitness` contract from `Witnesses.lean:59`, including projectivity and all three consistency estimates. More importantly, the blueprint asserts that the concrete POVM has the properties, whereas `Lines.lean:85` returns only `Nonempty (CombinedLinesWitness ...)`; its public signature does not constrain the witness field `T` to equal `combinedLineMeasurement`. Restate the block existentially, display the full completed-answer and opposite-placement contract, and reserve identification with the concrete POVM for the proof.

**F2:** `Lines/Sampling.lean:132` proves the discarded event itself has mass at most `1/(2q)`. The blueprint’s weaker `1/q` statement does not imply the claimed `1/(2q)` cost. Change the probability bound to `1/(2q)` and mention the equal axis/diagonal mixture.

**F3:** Lines 2118, 2138, 2151, 2165, and 2235 point certified proofs through `lem:qld-sublines` and Claims 17-1 through 17-3, although those source nodes remain `\notready`. The Lean proofs instead use the directly indexed law and helpers such as `subline_remove_X_factor_at`, `subline_Z_term_near_one_at`, `paired_ordered_overlap_gap_le`, and `exists_subLineWitness`. Add certified support entries for the direct declarations and make the `\uses` graph reflect those dependencies.

**F4:** `pairedSublineOverlap` at `ExtendedLines/Overlap.lean:252` and `SubLineWitness.extendedMeasurement` at `ExtendedLines/Measurement.lean:173` are new public mathematical definitions explicitly described by the support blocks, but neither has a blueprint `\lean{...}` entry. State and link these definitions before certifying the dependent results.

**F5:** Replace the process paragraph with direct status prose, for example: “Because the joint point family is an explicit hypothesis, this auxiliary is separate from Lemma qld-xz-lines and does not prove that source lemma.” Likewise replace “uncertified,” “source-shaped replacement,” and “certification” at lines 2133, 2210, and 2244 with direct mathematical assertions.

**F6:** Drop the comment or say, “Expanding the conditioned product law gives the corresponding finite normalization identity.”

**F7:** Replace “There is no zero-mass branch” with “Every extended line has positive marginal mass.” At line 254, replace “assigning a field-valued fallback” with “assigning an arbitrary field value.”

**F8:** Use the paper notation, such as “Bob’s \(BB'\) registers and Alice’s \(AB''\) registers.”

**F9:** The same vague historical sentence occurs in five new modules. State concretely that the construction uses the directly indexed line space, the completed answer alphabet, and the corrected sum-form polynomial error convention.

**F10:** Replace the new “proof-only” occurrences in `Sampling.lean`, `Conditioning.lean`, and `Construction.lean` with “formalization-only auxiliary,” followed by the paper equation or construction each result supports.

The prior completed-answer and newly closed-proof status issues are otherwise resolved. The source `\notready` tags for `lem:qld-xz-lines`, `lem:qld-sublines`, and `lem:qld-4-13` remain appropriate. Counts: 1 signature-equivalence blocker, 3 additional synchronization/status findings, and 6 prose findings.

VERDICT: CHANGES_REQUESTED