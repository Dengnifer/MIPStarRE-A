## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Points/Placement.lean:289` — `eprCrossSwap` is incorrectly identified with the blueprint’s `U_σ U_θ`.
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/Points/Closeness.lean:21` — The cited paper-gap note does not document the replacement combined-point argument.
- [ ] F3 (changes) `MIPStarRE/QPBT/Combining/Points/Consistency.lean:98` — The new square-summability helpers duplicate an already imported, more general theorem.

## Review

**F1: Incorrect mathematical identification.** In `blueprint/src/chapter/ch14_qpbt_observables.tex:1413`, `lem:symmetric-equivalents-transfer` defines σ by exchanging corresponding Alice/Bob registers and θ by exchanging A/B and A′/A″. Consequently, σθ acts on the ancillary registers as the four-cycle `(A′ B″ A″ B′)`. The new `eprCrossSwap` instead implements `(A′ B″)(A″ B′)`, an involution. These are different permutations, not different notation for the same operator. The new invariance and transport proofs remain valid independently. Correct the identification here and in the related module/theorem docstrings, describing the independently proved ancillary symmetry without claiming it is that blueprint element.

**F2: Missing replacement-argument documentation.** The cited `docs/paper-gaps/qpbt_linearity-theorem-quotation.tex:118` discusses fiberwise linearity and a common ancillary extension; it contains no account of the field-valued sandwich, Parseval commutation transfer, exact overlap identity, or direct rounding used here. The paper’s `lem:qld-4-10`, at `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:711`, explicitly proceeds through binary refinements and quantum linearity. Under `docs/paper-gaps/policy.tex:31`, this substantive replacement requires a mathematical account. Extend the note or cite a dedicated note explaining the replacement and the dimension-independent error bound, while distinguishing it from the still-open common-extension construction.

**F3: Duplicate square-summability API.** `measurement_sum_adjoint_mul_le_one` in `MIPStarRE/QPBT/Games/DistanceTheorems.lean:336` already proves this inequality for every POVM, without projectivity. It also handles the new placed variants in `Sandwich.lean:182` and `Closeness.lean:87` by application to `S.placedMeasurement p M`. I type-checked those replacements using only the existing `Points.Commutation` import. Reuse that theorem instead of maintaining three specialized proofs of its consequence.

**Statement integrity.** `exists_combinedPointsWitness` preserves the hypotheses and conclusions of `lem:qld-4-10` at paper lines 689–709: admissible parameters, the projective winning setting, projective joint measurements on the original expanded spaces, both ordered products, and all directed opposite placements. The universal `K ε^(1/8)` bound satisfies the existing polynomial-error encoding. No extra proof-obligation assumption or weakened conclusion was found.

**Verification and limits.** All seven changed Lean files passed individual type-checking. The changed files contain no proof holes or forbidden bypasses; the theorem’s transitive axioms are exactly `propext`, `Classical.choice`, and `Quot.sound`. The checkout diff matches the authoritative patch, and the worktree remains unchanged. I did not rerun the full build. The standalone `lean-conventions` skill was unavailable locally; I applied the checked-in review and integrity rules.

VERDICT: CHANGES_REQUESTED