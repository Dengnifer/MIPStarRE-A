## Findings
- [ ] F1 (changes) `blueprint/src/chapter/ch13_qpbt_test.tex:721` — The recovered point relations are claimed with error \(O(\delta+1/q)\), dropping the parameter-dependent recovery loss retained by the linked Lean statements.

## Review

**Category A: one unresolved mathematical mismatch.** In `lem:ld-combining-recovery`, \(\delta\) denotes the original combined-strategy consistency error. The proof at `blueprint/src/chapter/ch13_qpbt_test.tex:700` explicitly increases that error by \(O(\delta+(m+k)d/q)\), but its final sentence reports only \(O(\delta+1/q)\), without redefining \(\delta\).

The linked theorem `directCombinedRecovery_relation_one` at `MIPStarRE/QPBT/Combining/DirectLowDegree/Transport/Combining/RecoveryTransport.lean:169` bounds the recovered defect by the original combined defect **plus \((m+k)d/q\)**; the second relation retains the same loss. Since \(m,k,d\) vary, that loss cannot be absorbed into a universal \(O(1/q)\) term. Replace the final error with \(O(\delta+(m+k)d/q)\), consistent with the lemma statement and Lean. No Lean signature change or additional hypothesis is needed.

**Prior-feedback triage.** The earlier prose-lane findings are addressed: the directly indexed parameter domain excludes divisibility, wrong-form answers have an explicit convention, projectivity has a separate theorem, recovery uses zero-based indices, universal constants are existentially quantified, and the direct single-component theorem is distinguished from the source soundness import. The previously flagged implementation-oriented phrases are removed. Earlier code-lane findings remain outside this review’s remit.

**Category B:** no additional actionable prose finding.

**Scope and residual risk.** This is a read-only mathematical-equivalence and prose review, not build or proof-integrity certification. The attached excerpt was truncated; the on-disk patch and checkout were used for inspection. The source-game correspondence and tensor-code parameter obligations remain explicitly open; the direct-game result must not be treated as closing them.

VERDICT: CHANGES_REQUESTED