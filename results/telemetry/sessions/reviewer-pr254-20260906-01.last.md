## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/ExtendedLineGame.lean:1` — Add the new module to the project’s import graph so the default build and CI compile it.

## Review

**F1 — Build integration requires changes.** No existing module imports `ExtendedLineGame.lean`. Traversing the source imports from `MIPStarRE.lean` confirms that this is the only unreachable QPBT module. The configuration at `lakefile.toml:4` selects the `MIPStarRE` library, whose default module glob builds its root and transitive imports—not every file in its directory. Consequently, the normal CI build at `local/bin/ci.sh:842` does not compile these new declarations, and importing the public QPBT module does not expose them.

Add `import MIPStarRE.QPBT.Combining.ExtendedLineGame` alongside the combining imports at `MIPStarRE/QPBT.lean:50`, then rerun the normal build. The successful single-file check establishes current type-correctness but does not replace ongoing build coverage. This must be fixed before approval.

**Mathematical review.** The construction respects its explicitly limited scope. In `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:1020`, `lem:qld-4-13` supplies degree-`md+1` line POVMs, degree-`d` axis support, and two oriented consistency relations. The opening proof of `lem:qld-4-7` at `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:1278` asserts a passing strategy and then applies Naimark dilation and soundness. This PR supplies construction increments from existing witnesses; it neither replaces that existence theorem with a conditional statement nor marks the source theorem complete.

I found no mathematical blocker in those increments:
- Axis truncation uses the witness’s degree-support field; diagonal zero-padding preserves evaluation.
- The verifier equivalences retain universal evaluation for zero directions and do not replace `none` by zero.
- `strategy_value_eq` retains all seven potentially rejecting branches, including both coefficient-equality branches.
- `completedLinePointDefect_sums_le` remains a statement about `psiHat`; `projectiveStrategy_value` proves exact value preservation without asserting a passing bound.

**Validation and residual risk.** The checkout matches the authoritative patch. The new file type-checks without warnings. An independent source-based `#print axioms` check of all 31 declarations reports only subsets of `propext`, `Classical.choice`, and `Quot.sound`, with no `sorryAx`. Forbidden-token, line-length, and whitespace checks pass. I did not run a full build.

Register-correlation transport, quantitative branch bounds, and witness existence remain open as documented at `audits/2026-09-06_extended-line-game-244.md:111`. Approval of these construction lemmas would not discharge those obligations or establish `lem:qld-4-7`.

VERDICT: CHANGES_REQUESTED