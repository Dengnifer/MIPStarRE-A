## Findings

- [ ] F3 (changes) `MIPStarRE/QPBT/Extraction/Consistency.lean:101` — Replace the duplicated measurement-placement constructions in extraction and point/point rejection with the existing placement API.

## Review

F3 requires changes before approval. `Extraction/Consistency.lean` introduces another placement equivalence, measurement constructor, effect identity, projectivity proof, and finite-sum proof. These duplicate `ProjectiveSetting.placedMeasurement`, `placedMeasurement_effect`, `placedMeasurement_isProjective`, and `place_finsetSum` in `MIPStarRE/QPBT/Combining/Points/Placement.lean:136,256,264,270`. `ExtendedLineGame/PointPointRejection.lean:94` repeats the same construction for two placements. The shared API already arrives through the imports; I type-checked its use with the required dependent types. Reuse it and remove the redundant coordinate decompositions and proofs. Maintaining these independent implementations unnecessarily multiplies the register-placement code that must remain consistent.

Prior F1 and F2 are resolved: consistency positivity uses the existing placement calculus, and product averages use `SandwichProduct.avgOver_distribution_prod`.

I found no new source-statement drift in the inspected mathematical changes. The restriction estimates preserve the `2m` and `4m²` factors in `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:1049–1061`. The marginal identity estimate retains all four opposite placements and explicitly accounts for the agreement constant from `eq:qld-sg-cons`, lines 1609–1635. The restoration theorem keeps both the retained probability factor and discarded zero-direction mass; it does not claim to complete `lem:qld-xz-lines`.

Validation: `PastingRestoration.lean` and `Extraction/Consistency.lean` type-check. Seven representative completed declarations have only `propext`, `Classical.choice`, and `Quot.sound` dependencies. No added proof holes were found; the five extraction warnings are existing tracked obligations.

I used the full on-disk code patch beyond the truncated attachment. Full builds and workflow tests were not rerun. The referenced `lean-conventions` installation was unavailable; repository integrity rules were applied. The checkout remains unchanged.

VERDICT: CHANGES_REQUESTED