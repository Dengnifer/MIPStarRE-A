## What changed

Qualified all five stale `applyOperatorToState_mul` uses in [Swap.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-183-magic-square-dilation-proof-support/MIPStarRE/QPBT/Test/MagicSquareTheorems/Rigidity/Swap.lean:203) with the canonical `MIPStarRE.QPBT.DistanceCalculus` namespace. Lean exposed two additional stale uses beyond the three reported.

Committed as `8c1619e` (`fix(ci): qualify shared helper in Magic Square swap`).

## Evidence

- `lake env lean .../Rigidity/Swap.lean`: passed.
- `rg -n "sorry|axiom" .../Rigidity/Swap.lean`: no matches.
- Duplicate-helper audit: exactly one definition remains, in `DistanceTheorems/Support.lean`.
- Pre-commit integrity gates: passed.
- Git status: clean, with no untracked files.
- `lake build MIPStarRE.QPBT.Test.MagicSquareTheorems` was not run because the machine-wide build lock remained held by `agent116 lake build MIPStarRE.QPBT`. The protocol forbids a competing build.

## Statement integrity

Paper assumptions / Lean assumptions / paper conclusion / Lean conclusion: unchanged. Verdict: exact.

## Open obligations

None introduced. The existing `exists_ms_rigidity` obligation remains outside issue #183 and is tracked in `local/briefs/42-residual-brief.md:186`.

## Handoff

Review the five qualified calls, particularly the two additional occurrences around lines 293–296 that were masked by earlier elaboration failures. Rerun the requested aggregate target after the active build lock clears. No `MIPStarRE/QPBT.lean` re-export is needed.