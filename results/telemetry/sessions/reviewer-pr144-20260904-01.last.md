## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Linearity/NaimarkRounding.lean:142` — `roundedFourierPVM` exposes a bare operator family instead of a bundled projective measurement
## Review
F1: The cited source calls `{C^u}` a projective measurement (`references/nv-paper/fullpaper.tex:1099-1100`), but `roundedFourierPVM` returns only `u → Op`; projectivity and completeness are deferred to `roundedFourierPVM_isProj` and `roundedFourierPVM_sum_eq_one`. This violates the required invariant-bundling check in `docs/CONTRIBUTING.md:458-476` and bypasses the existing `ProjMeas` abstraction in `MIPStarRE/LDT/Basic/SubMeasurementCore.lean:73`. Keep a raw effect function private if useful, but expose the construction as a `ProjMeas` or equivalent bundled measurement and make downstream observables use its effects. This must be fixed before merge.
The Fourier, BLR, compression, and exact-linearity calculations otherwise agree with `references/nv-paper/fullpaper.tex:1074-1112`; operator order is preserved and no unjustified commutation is introduced. The paper-gap note correctly identifies the factor-of-two error at source lines 903-911 and supplies a valid ancillary-stable counterexample.
The new Lean files contain no proof holes or prohibited proof devices. Axiom checks on the principal declarations report only `propext`, `Classical.choice`, and `Quot.sound`. The existing `sorry` in `exists_exactly_linear_observables` remains a clearly tracked `\notready` normalization-realignment task rather than new unmarked debt.
VERDICT: CHANGES_REQUESTED
