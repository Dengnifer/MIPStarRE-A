## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT/Test/PauliBasisTest.lean:260` — Reconcile the hard-coded blueprint line references after the chapter expansion.

## Review

F1 must be fixed before merge. The changed docstring points `def:cl-func` to `blueprint/src/chapter/ch12_qpbt_games.tex:453-463`, where line 454 now begins `lem:ld-sandwich`; `def:cl-func` actually begins at line 1168. The same systematic drift appears in `CondLinear.lean:16` (`1053-1257`, now `1168-1387`) and `CanonicalParams.lean:13` (`471-492`, now `531-552`). These references are part of the repository’s source-traceability contract and currently direct reviewers to unrelated mathematics. Audit all hard-coded blueprint locators in the touched modules against the final chapter layout, or cite stable labels without brittle ranges.

I found no source-statement or proof-integrity blocker in the changed declarations. The line representative results, conditionally linear concatenation and direct sums, distribution factorization, low-degree samplers, Pauli sampler, and anticommuting probability bounds preserve the cited hypotheses and conclusions. Lean-only assumptions are ordinary boundary conditions such as finiteness, decidable equality, field structure, and positivity; no bridge or proof-obligation bundle was introduced.

No new `sorry`, `axiom`, admission, or kernel-bypass construct appears in the patch. Existing proof holes in the affected dependency surface are explicitly tracked as known obligations. The authoritative patch passes whitespace checking, the worktree is clean, and the dispatched exact-head CI was green. I did not rerun Lean in this read-only reviewer session.

VERDICT: CHANGES_REQUESTED