## Findings

- [ ] F1 (changes) `blueprint/src/chapter/ch14_qpbt_observables.tex:453` — The proof dependencies omit completeness of \(\sqrt Q\) and the squared-distance triangle estimates used by the Lean proof.
- [ ] F2 (changes) `MIPStarRE/LDT/MakingMeasurementsProjective/LocalityPreservingRepair.lean:55` — Ten declarations changed from private to public still have no formalization-support blueprint entries.
- [ ] F3 (changes) `blueprint/src/chapter/ch14_qpbt_observables.tex:438` — The consistency hypothesis uses Lean identifiers and function-call syntax instead of mathematical notation.
- [ ] F4 (changes) `MIPStarRE/QPBT/Games/DistanceTheorems/Calculus.lean:18` — Support-node insertions left blueprint line-range citations stale across the changed QPBT modules.

## Review

F1: The proof calls `sqrtQCompleteness` at `ProjectiveRounding.lean:346`, `sddOpRel_triangle` at line 309, and `questionSDD_triangle` at lines 445 and 487. The `\uses` list should include `lem:sqrt-Q-completeness` and the appropriate triangle dependency, such as `prop:triangle-inequality-for-approx_delta` or a subordinate entry for the binary forms actually used.

F2: No blueprint reference exists for `diagBlock`, `leftMarginalDensity`, `leftMarginalDensity_nonneg`, `leftMarginalState`, the five associated marginal/trace lemmas, or `matrix_eq_zero_of_rank_eq_zero`. Keep implementation-only declarations private, or add clearly identified formalization-support entries for those retained as public.

F3: Replace `\operatorname{consistencyDefect}(\operatorname{uniformDistribution}(\mathrm{Unit}),…)` with the equivalent mathematical condition
\[
\sum_{a\ne b}\langle\psi|Q_a\otimes R_b|\psi\rangle\le\delta.
\]
This preserves the corrected exact statement without exposing Lean representation details.

F4: The principal corrections are `Calculus.lean:18` to blueprint lines 260–460, `DistanceTheorems.lean:20` to 260–597, its close-values citation at line 537 to 584–597, and `StrategyClasses.lean:11` to 89–198, together with their declaration-level references. `ProjectiveRounding.lean:30` should now cite lines 425–470 rather than 425–464.

The earlier mathematical blocker is resolved: the explicit rounding lemma now states the exact defect hypothesis matching the Lean signature, and the reduced-state prose uses density-operator expectations correctly. The source symmetrization theorem remains uncertified while its source-faithful given-strategy form has a separate matching `\leanok` entry. The newly completed close-values and rounding results also have accurate statement and proof status. No remaining statement-equivalence mismatch or stale `\notready` was found.

Summary: 0 equivalence mismatches, 2 category-A dependency/traceability issues, and 2 category-B prose/citation issues.

VERDICT: CHANGES_REQUESTED