## What changed

Committed `01b9713` (`feat(QPBT/Test): add binary swap-isometry kernel`).

[Swap.lean](/home/drx/MIPStarRE-qpbt/.worktrees/issue-104-ms-swap-isometry/MIPStarRE/QPBT/Test/MagicSquareTheorems/Rigidity/Swap.lean:53) now proves:

- The norm-preserving binary controlled-swap map and isometry.
- Exact transport of the controlling `Z` reflection.
- Quantitative `X` transport from the anticommutator defect.
- Concrete binary Pauli reflection identities.
- Named Alice and Bob specializations on the projective dilation.

The requested two-EPR extraction cannot be completed faithfully. The zero-error obstruction is documented in [the audit](/home/drx/MIPStarRE-qpbt/.worktrees/issue-104-ms-swap-isometry/audits/2026-09-04_magic-square-rigidity-orientation-obstruction.md:46): the symmetric game permits a perfect two-copy strategy where the two edge orientations use independent EPR systems, contradicting simultaneous Alice/Bob variable transport.

## Evidence

- `lake env lean .../Rigidity/Swap.lean`: passed without warnings.
- Explicit Lake build of the `Swap` module: passed, 8726 jobs.
- `lake env lean .../Rigidity/Anticommutation.lean`: passed.
- `rg -n "sorry|axiom" Swap.lean`: no matches.
- Git hooks installed and pre-commit checks passed.
- Worktree is clean.

The protocol-managed full build reached 9060/9064, then failed in the inherited facade: `GroundSlice` redeclares `norm_applyOperatorToState_sq` already imported from `Reflections`. This failure predates and does not involve `Swap.lean`.

## Statement integrity

Paper assumptions: arbitrary strategy winning the symmetric Magic Square game with probability at least `1 - ε`.

Lean assumptions: the same, as `Strategy msGame`, `0 ≤ ε`, and `1 - ε ≤ S.value`.

Paper and Lean conclusions: two local isometries, two EPR pairs with auxiliary state, and four distinguished variable-effect transports.

Verdict: exact statement correspondence, but the shared statement is not implied by the cited one-way LCS self-test for the current symmetric-game interface. The two-copy strategy refutes it at `ε = 0`.

## Open obligations

No proof holes remain in the changed Lean file.

Auxiliary normalization, joint two-EPR extraction, four `idealMagicBitProj` bounds, and trace-to-Euclidean conversion remain blocked until the game/theorem boundary is repaired. Adding a variable-agreement hypothesis would be unfaithful.

## Handoff

Review the counterexample audit first. A source/blueprint decision is needed between an oriented LCS game, consistency self-loops, or an explicitly restricted theorem. Also repair the existing `Reflections`/`GroundSlice` duplicate declarations before expecting a full build.