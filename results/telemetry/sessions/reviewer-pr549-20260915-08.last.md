## Findings

- [ ] F1 (blocker) `blueprint/src/chapter/ch15_qpbt_combining.tex:2689` — The certified statement gives the wrong point-measurement outcome space for `extended_consistencyDefect_le`.
- [ ] F2 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:1412` — The source Lemma 4.13 dependency was incorrectly changed from Lemma 4.12 to Lemma 4.10.
- [ ] F3 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:1285` — The consistency proof omits two declarations directly invoked by its Lean proof from `\uses`.
- [ ] F4 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:1893` — The sub-line witness proof cites only the abstract law and omits every concrete construction result it invokes.
- [ ] F5 (changes) `MIPStarRE/QPBT/Combining/Lines/Sampling.lean:97` — Two new public sampling theorems have no blueprint entries.
- [ ] F6 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:2795` — “support field” is Lean structure jargon rather than a mathematical conclusion.
- [ ] F7 (changes) `MIPStarRE/QPBT/Combining/Apply.lean:117` — “Retained conditional statement” describes formalization history rather than the mathematical assertion.

## Review

**F1:** The blueprint says the comparison is with a “zero-extended joint point family,” suggesting outcomes in \((\mathbb F_q\cup\{\bot\})^2\). The Lean theorem instead postprocesses the joint outcome \((a,b)\) to the single value \(\alpha a+\beta b\), with outcome type `Option F_q`, at `ExtendedLines/Overlap.lean:358-362`. Thus the statement-level `\leanok` is invalid. Say “the combined-value point measurement, extended by a zero effect at \(\bot\),” or remove this duplicate node; `lem:qld-extended-line-overlap-comparison` already states the result correctly.

**F2:** Lemma 4.13 compares against \(\hat Q^{x,z,\alpha,\beta}\), introduced by Lemma 4.12. The paper explicitly identifies that dependency at `14_analysis_of_the_pauli_basis_test.tex:1126`. Lemma 4.10 supplies the underlying pair-valued measurements, not the object appearing in this source statement. Restore `lem:qld-4-12`.

**F3:** `combined_line_measurement_consistency` directly invokes `consistencyDefect_opposite_symm` at `Lines/Construction.lean:428` and `consistencyDefect_integrand_le_one` at line 458. Add `lem:qld-opposite-bipartition-transport` and `lem:consistency-defect-integrand-le-one` to the proof’s `\uses`.

**F4:** The Lean proof at `Combining/Lines.lean:313-331` invokes the concrete sub-line distribution, its probability and marginal theorems, the support reconstruction, and the incidence, compatibility, and axis-closure results. A subordinate blueprint node should record those construction declarations and be cited here; `def:direct-subline-law` alone does not describe the proof dependencies.

**F5:** `avgOver_lineRepMap_resample_parameter` and `linePointDist_nondegenerate_mass_ge` are new public formalization-only theorems, but neither has a `\lean{...}` entry. Record them in appropriate subordinate support nodes with accurate status and dependencies, or cease exposing them as public declarations.

**F6:** Replace “gives the support field” with the mathematical conclusion, for example: “proves that the extended-line measurement has no outcomes above degree \(d\) on axis-parallel lines.”

**F7:** Replace the opening with “Conditional existence of extended-line measurements with the first-route estimate …”. The later `**Unfaithful:**` paragraph already records the necessary status and deletion plan.

The earlier ledger issues concerning the \(1/(2q)\) restoration cost, source sub-line link, point-witness quantifiers, completed-answer caveat, register notation, and quarantine marker are resolved. F3 is the remaining prior dependency issue. Summary: **1 equivalence mismatch, 4 dependency/tag-coverage issues, and 2 prose issues**. At the round cap, F1 must be corrected before its `\leanok` can remain; the remaining items require a fix or explicit operator-tracked issue.

VERDICT: CHANGES_REQUESTED