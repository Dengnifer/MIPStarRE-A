## Findings
- [ ] F2 (changes) `MIPStarRE/QPBT/Combining/DirectLowDegree/Transport/Consistency/State.lean:211` — The nonemptiness docstring still exposes typeclass inference and reducibility instead of mathematical prose.
- [ ] F3 (changes) `MIPStarRE/QPBT/Combining/DirectLowDegree/Transport/Questions.lean:554` — The deprecated-alias section retains workflow history and the rejected `mature` vocabulary in public names.
## Review
F2: “The usual instance cannot be inferred here because `DirectLdParams.toLDTParameters` is reducible” is an implementation explanation, not mathematics. Replace the docstring with, for example, “The zero vector witnesses that the LDT point space associated with directly indexed parameters is nonempty.”
F3: “required by the open `issue-134` branch,” “cleanup,” and “branches stacked on this module” encode workflow history. More importantly, the aliases retain `matureDiagonalLineOf` and `directDiagonalQuestionOf_matureDiagonalLineOf`, although neither is paper- or blueprint-facing terminology and neither has an in-tree consumer. Drop the deprecated section and both aliases; migration coordination belongs outside mathematical documentation.
Prior F1 is resolved: the revised `ldDLineCL` docstring now correctly assigns the canonical representative map to the point block and the prefix projection to the direction block.
I found no blueprint/Lean equivalence or status defect. All five linked declarations match their blueprint statements, the parameter-domain clarifications agree with `LdParams` and `DirectLdParams`, the revised `\uses` edges are accurate, and no stale blueprint reference remains after the renames. Summary: 0 equivalence mismatches, 0 status issues, 2 prose issues.
VERDICT: CHANGES_REQUESTED
