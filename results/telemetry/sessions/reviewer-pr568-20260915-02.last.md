## Findings
- [ ] F1 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:907` — The notation does not identify the seed-bearing sample space used by Lean, and `\operatorname{dir}` is undefined
- [ ] F2 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:936` — The `\uses` annotations omit direct retained-mass dependencies and place proof dependencies in statement blocks
- [ ] F3 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:981` — The claim that transfer to the unconditioned law remains uncertified is stale and uses progress-report prose

## Review
**F1:** Definition `def:line-point-dist` names the source law \(D_{\mathsf{Line}}\) and separately describes its seed-bearing refinement (`ch13_qpbt_test.tex:177`). Lean’s declarations instead operate on `LineDesc L`, whose axis case retains the seed and whose direction is `LineDesc.direction` (`LineDefs.lean:55-86`). The conditioned law also retains those descriptions in its codomain (`NondegeneratePastingDistribution.lean:35-42`). Explicitly introduce a symbol for the seed-bearing refinement, state that each \(\ell\) is such a description, and define \(\operatorname{dir}(\ell)\) before using it.

**F2:** The bounds proof unfolds `nondegenerateLinePastingMass` (`NondegeneratePastingMass.lean:38-44`), so its proof-level `\uses` must include `def:nondegenerate-line-pasting-mass`. Conversely, `thm:nondegenerate-line-pasting-mass-bounds` at lines 947-948 is needed by the positivity proof, not its statement. The new support nodes also need direct downstream edges: the conditioning/restoration proofs use the mass, conditioned law, and positivity theorem; the paired-line construction invokes `nondegenerateLinePastingDist_isProbability` (`Construction.lean:251`); and the final consistency proof invokes the retained-mass bounds (`Construction.lean:464-465`).

**F3:** Transfer back to the unconditioned product law is already proved by `combinedLineMeasurement_consistency_le_conditioned` (`Conditioning.lean:174-197`) and used in the completed proofs at `Construction.lean:292-357` and `Construction.lean:402-465`. The remaining mismatch with Lemma `lem:qld-xz-lines` is instead the completed answer alphabet in `CombinedLinesWitness` (`Witnesses.lean:118-130`). Replace the sentence with mathematical status prose such as: “The resulting estimate is transferred back to the unconditioned product law. The current formal statement uses the completed answer alphabet \((\mathbb F_q\cup\{\bot\})^2\), so it does not yet certify Lemma~\ref{lem:qld-xz-lines}.”

All five new declarations resolve, and the three theorem proofs have no `sorryAx`. The existing `\notready` on `lem:qld-xz-lines` remains appropriate because of the answer-alphabet mismatch.

Summary: 1 equivalence/notation mismatch, 1 inaccurate `\uses` graph, and 1 status/prose issue. The equivalence mismatch is the most serious.

VERDICT: CHANGES_REQUESTED