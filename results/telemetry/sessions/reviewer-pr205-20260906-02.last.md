## Findings

- [ ] F14 (changes) `blueprint/src/chapter/ch12_qpbt_games.tex:1329` — The commutator expansion omits the real part required by its Lean counterpart; the cross-move statement has the same mismatch.
- [ ] F15 (changes) `blueprint/src/chapter/ch12_qpbt_games.tex:1433` — The fine-commutator estimate omits the restrictions that the second codeword measurement depends only on \(x\) and the first family only on \((x,y_1)\).
- [ ] F16 (changes) `blueprint/src/chapter/ch12_qpbt_games.tex:1012` — The pasting proof omits blueprint entries and dependency links for its scalar error comparison and universal defect bound.

## Review

**Three category-A findings remain: two mathematical-equivalence mismatches and one declaration/dependency-coverage issue. No additional category-B finding is requested.**

### F14 — Real versus complex expectations

`commutator_mass_eq_expansion` in `MIPStarRE/QPBT/Games/Sandwich/Pasting/CrossMove.lean:191` uses `stateQForm`, hence the **real part** of each expectation. The blueprint instead subtracts the potentially complex expectation of \(G_gP_aG_gP_a\). This product need not be self-adjoint, and summing over the outcomes does not generally make its expectation real. The displayed equality therefore does not match the real-valued Lean equality.

The same discrepancy occurs at `blueprint/src/chapter/ch12_qpbt_games.tex:1295`: the blueprint bounds the complex modulus of the cross-term difference, whereas `abs_cross_move_gap_le_sqrt` at `MIPStarRE/QPBT/Games/Sandwich/Pasting/CrossMove.lean:35` bounds the absolute value of its real part.

Insert explicit \(\operatorname{Re}\) in both statements and align the accompanying explanations. Their existing statement `\leanok` tags do not certify the current formulas.

### F15 — Dependence on the sampled questions

The Lean signature at `MIPStarRE/QPBT/Games/Sandwich/Pasting/Assembly.lean:390` specifies
\(G_2:\mathcal X\to\mathrm{Measurement}\) and
\(P:\mathcal X\times\mathcal Y_1\to\mathrm{Measurement}\).
The blueprint instead says that \(G\) depends “on a question” and leaves \(P\)'s dependence unspecified.

These restrictions matter: the conditional collision estimate averages over \(y_2\) with \((x,y_1)\) fixed. Its operator weights must be independent of \(y_2\); arbitrary question-dependent measurements are not covered by the Lean theorem.

State the probability distribution on \((x,y_1,y_2)\), write \(G^x\) and \(P^{x,y_1}\), and explicitly identify \(Q^{x,y_2}\) as the evaluation coarse-graining. This restores the hypotheses needed for the statement `\leanok`.

### F16 — Missing final-estimate entries

The proof of `exists_pasting_error` uses both:

- `consistencyDefect_placed_le_one`, at `MIPStarRE/QPBT/Games/Sandwich/Pasting/Assembly.lean:657`;
- `pasting_error_sqrt_le_rpow`, at `MIPStarRE/QPBT/Games/Sandwich/Pasting/Assembly.lean:752`.

Neither has a blueprint declaration link. The proof’s new `\uses` lists the measurement construction, coarse commutator estimate, and explicit error estimate, but omits the results that turn that estimate into the globally defined polynomial error function.

Add mathematical support entries with matching hypotheses and conclusions, appropriate statement/proof status tags, and dependency links from the pasting proof.

### Prior findings and scope

The prior prose findings F8–F13 are addressed by the dependency correction, downstream register-exchange explanation, explicit sum-error convention, renamed files, complete error assertion, and bipartite clarification. F7’s originally absent `\uses` is repaired; F16 records the remaining coverage gap. Prior code-lane findings F1–F6 are left to that review lane.

The attachment was truncated; I consulted the on-disk patch and checkout. No files were modified or reviews published. Residual risk is limited by this review’s scope: proof correctness and build validation are not assessed here.

VERDICT: CHANGES_REQUESTED