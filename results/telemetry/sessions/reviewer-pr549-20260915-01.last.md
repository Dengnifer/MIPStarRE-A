## Findings
- [ ] F1 (blocker) `MIPStarRE/QPBT/Combining/Apply.lean:136` — `exists_extendedLinesWitness_established_ofPointsWitness` retains an unmarked `sorry` for a known-false arbitrary-`δQ` statement.
- [ ] F2 (changes) `blueprint/src/chapter/ch15_qpbt_combining.tex:1070` — The restored-defect proof derives a `1/(2q)` cost from only a stated `1/q` discarded probability.
- [ ] F3 (changes) `docs/paper-gaps/qpbt_subline-claims-line-marginal.tex:173` — The note incorrectly calls `CombinedLinesWitness` source-faithful despite its unresolved completed-`Option` answer comparison.

## Review
F1 must be fixed before merge. The statement of `exists_extendedLinesWitness_established_ofPointsWitness` accepts an arbitrary scalar `δQ`, while its output error does not depend on that scalar. `docs/paper-gaps/qpbt_combined-lines-error-term.tex:217-289` documents this obstruction, including issue #511 and the failure of the unrestricted supplied-point auxiliary. Nevertheless, the theorem’s docstring presents it as a construction obligation and gives neither the required `**Unfaithful:**` marker nor a discharge or deletion plan. The module summary also says only the printed-error and global-pair assertions remain open. Add the prescribed marker, cite the gap and issue, and state that the helper must be restricted to polynomially controlled point errors or removed.

F2 requires a mathematical correction. The blueprint says the discarded event has probability at most `1/q`, then concludes that restoring it costs at most `1/(2q)`. That conclusion does not follow from the stated premise. The Lean proof in `MIPStarRE/QPBT/Combining/Lines/Sampling.lean:79-90` obtains the sharper factor from the equal axis/diagonal mixture: the axis component contributes zero and only half of the diagonal zero-direction mass remains. The blueprint proof should state that argument or directly state the `1/(2q)` bound.

F3 requires a more precise verdict in the changed paper-gap note. Paper `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:882-893` formulates `lem:qld-xz-lines` with field-valued evaluations, whereas `CombinedLinesWitness.consistent` in `MIPStarRE/QPBT/Combining/Witnesses.lean:118-131` compares completed `Option` answers. The corresponding blueprint node `lem:qld-xz-lines` remains not ready because this transport is unresolved. The note should describe the record as capturing the concrete measurement and degree data while retaining a completed-answer deviation, not as faithfully expressing the source conclusion.

The new proof of `exists_extendedLinesWitness_established` otherwise has a coherent dependency chain: it constructs the point and line witnesses, uses the genuine conditional extended-line measurement, proves both placement estimates, and applies the scalar absorption lemma. Its direct carrier, completed answer alphabet, and weaker established error are all displayed explicitly by blueprint support node `lem:qld-4-13-established`; it is not being advertised as the printed source theorem.

Targeted Lean checks of `Apply.lean`, `Lines/Construction.lean`, and `ExtendedLines/Estimates.lean` passed. The target theorem and its new dependencies report only `propext`, `Classical.choice`, and `Quot.sound` under `#print axioms`. Prior-round findings F1-F13 were addressed; the remaining findings above concern the current documentation and proof-status contracts.

VERDICT: CHANGES_REQUESTED