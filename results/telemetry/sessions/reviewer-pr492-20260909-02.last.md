## Findings

- [ ] F1 (changes) `MIPStarRE/QPBT/Combining/Lines/ConsistencyPositivity.lean:114` — Reuse shared placement positivity and `Commute.mul_nonneg`; remove the two redundant bipartition-specific positivity proofs.

## Review

F1 is partially addressed: finite sums and commutation now use the shared placement calculus. However, `place_mul_place_nonneg` still depends on two separate tensor-product calculations that duplicate an existing API consequence. This replacement type-checks with the current imports:

```lean
exact Commute.mul_nonneg (S.place_nonneg p₁ hX) (S.place_nonneg p₂ hY)
  (S.place_comm p₁ p₂ hopp X Y)
```

The supporting declarations are in `Combining/Points/Placement.lean:171` and `:216`, and Mathlib’s `Analysis/CStarAlgebra/ContinuousFunctionalCalculus/Instances.lean:292`. The two specialized helpers have no other callers. Remove their duplicated calculations before approval.

The other prior findings are resolved: `SubLinePrefix.lean:11` correctly describes which blocks vanish, and `Points/Sandwich.lean` uses `DistanceCalculus.applyOperatorToState_mul`.

The statement audit found no additional mathematical issue:

- The mixture identities and inflation estimates preserve the factors `2m` and `4m²` from `references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:1049–1061`, including the universal constant’s quantifier order.
- `nondegenerateLinePastingDist_collision_bound` proves the conditional hypothesis of `lem:pasting` (`06_nonlocal_games_and_mipstar.tex:504–525`) for its explicitly restricted auxiliary law.
- `exists_subLineWitness` retains its documented status as a directly indexed auxiliary construction; transport to the source carrier remains open.

Targeted checks passed for `ConditionalCollision.lean`, `Lines.lean`, and `Games/RestrictedAverage.lean`. The six principal completed results use only standard axioms, with no `sorryAx`. `Lines.lean` retains its two existing joint-line construction TODOs. No full build was rerun; the supplied CI status is successful for this head.

The truncated attachment was supplemented by the authoritative patch and complete prior ledger. The shared `lean-conventions` skill was unavailable; repository proof-integrity and review documents were used.

VERDICT: CHANGES_REQUESTED