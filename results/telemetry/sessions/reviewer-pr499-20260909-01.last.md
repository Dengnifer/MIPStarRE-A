## Findings
- [ ] F1 (changes) `local/bin/pr_merge.py:126` — The blanket telemetry exclusion also exempts executable workflow code from freshness checks.
- [ ] F2 (changes) `local/protocols/issues-prs.md:188` — The checked-in merge daemon still requires strict ancestry and does not use the new predicate.
## Review
**F1:** `results/telemetry/` contains executable modules, including `owner-tools/merge-service-space-cap5.py` and `model-comparison/compare.py`, which repository tests import. Real-Git fixture checks confirmed that base changes to either module return `fresh=True`. A PR changing their consumers could therefore merge against changed executable dependencies without validating that combination. Restrict the exception to passive telemetry outputs, or relocate executable code outside the excluded directory, and add rejection coverage.
**F2:** The daemon referenced by `scripts/tests/test_merge_service_space_cap5.py` still runs `merge-base --is-ancestor` directly in `eligible_prs`; it never calls `head_is_fresh`. Using the existing telemetry-only advancement to `9dced45f`, I reproduced `merge_gate_fresh=True` but `daemon_candidate_fresh=False`. Its subsequent filtering holds the candidate before the updated gate runs, preserving the refresh requirement this amendment intends to remove. Update that enforcement point and add an integration test exercising actual candidate classification; existing daemon tests mock `eligible_prs`.
Verification: all five `MergeGateTests` passed, additional Git boundary checks completed, and `git diff --check` passed. The full workflow suite was not run. The checkout remains unchanged.
VERDICT: CHANGES_REQUESTED
