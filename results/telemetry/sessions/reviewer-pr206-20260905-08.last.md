## Findings
- [ ] F1 (blocker) `MIPStarRE/QPBT/Combining/Witnesses.lean:18` — The new “source-shaped” list misclassifies the directly indexed `SubLineWitness` as the paper’s `lem:qld-sublines`.
## Review
F1 blocks merge on source-statement fidelity. The paper’s `lem:qld-sublines` requires the extended-line marginal to equal the seed-indexed line-point distribution over `F_q^{2m+2}` (`references/qpbt-paper/14_analysis_of_the_pauli_basis_test.tex:1063-1067`). The blueprint preserves that statement at `blueprint/src/chapter/ch15_qpbt_combining.tex:1285-1302`.
In Lean, however, `SubLineTriple` uses `DirectLineDesc P.extendedDirectLd`, and `SubLineWitness.extended_marginal` is explicitly `directLinePointDist` (`Witnesses.lean:172-175,246-248`). The gap register already identifies these direct carriers as different from the source’s seed-indexed objects. This is an unresolved transport obligation, not a faithful boundary encoding. Remove `lem:qld-sublines` from the “source-shaped” claim and describe `SubLineWitness` and its constructor as directly indexed auxiliaries, unless the source carrier and transport are actually supplied.
The previous-round issues concerning `ExtendedLinesWitness`, the conditional extraction theorem, machine-readable extraction traceability, and the chapter 15 transport claims are otherwise addressed. The changed `lem:qld-4-10` link is statement-faithful and does not claim proof completion. No new proof holes or integrity bypasses were introduced; the existing `sorry` sites remain documented open obligations. Exact head `10be99d09cd78387d0690c57203040af086a0aa8` was reviewed, with a clean worktree and successful exact-head CI reported by the dispatcher.
VERDICT: CHANGES_REQUESTED
