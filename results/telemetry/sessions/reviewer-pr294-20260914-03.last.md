## Findings

- none

## Review

The four public swap declarations preserve their existing headers and match paper `14_analysis_of_the_pauli_basis_test.tex:1687-1713,1805-1822` and blueprint `lem:v-swap-conjugation`. `GlobalPairWitness` represents the projective measurement already fixed by `lem:qld-4-7`; the proofs use only its measurement and projectivity fields, so no additional proof-debt hypothesis is introduced.

The crossed Pauli selection and phases are correct. Fourier inversion gives the label translation `h ↦ h + Dec(g_W)`, and the dot-product fiber reindexing has the corresponding outcome shift. In `swapUnitary_conj_tildeM`, the two decoded dot products cancel in characteristic two before `lowDegreeEnc_eq_dotProduct` identifies the final fiber, avoiding the known false unrestricted decoder interpolation identity.

The new helpers are grounded in existing Pauli and controlled-unitary lemmas. The changed Lean code contains no `sorry`, axioms, bypasses, placeholder tactics, or debug commands; line lengths and `git diff --check` pass. The still-open global-witness construction and downstream extraction obligations are pre-existing, explicitly tracked, and not hidden by these exact algebraic proofs.

VERDICT: APPROVED