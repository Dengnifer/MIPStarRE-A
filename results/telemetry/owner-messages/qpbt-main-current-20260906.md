# QPBT main: current owner instructions and integration handoff

This replaces the active instructions in older /tmp handoffs; those files remain
historical evidence. Read the checkpoint
`/tmp/qpbt-meta-migration-checkpoint-20260906.md`, then current AGENTS.md,
local/README.md, local/protocols/meta.md and the relevant committed protocols.

## Authority and scope

You own the Track A project loop in `/home/drx/MIPStarRE-qpbt`, GitHub
`Dengnifer/MIPStarRE-A`. The meta session guides deviations; it owns only the
explicitly assigned conservative migration under #291. Main chooses proof
packets and worker assignments. Do not duplicate the meta-owned migration edits.

Current owner policy: **Space key only, gpt-6-astra, literal CLI Ultra for main
and every worker; five total sessions including main, at most four useful
native descendants shared by the root; external admissions zero.** The scoped
home `~/.cache/mipstarre-dev/codex-home-qpbt-relay1` has a historical name but
contains the selected Space route. Do not change global homes or credentials.
Do not fall back to relay, Sol, Claude, Max or Xhigh based on old examples.

The owner authorized full-access mode for main and assigned Track A workers.
Keep `on-request` and `auto_review`, all proof-integrity/review/merge rules,
credential confidentiality and project boundaries. Never suppress a safety stop
or use a bypass. Never touch Track B, MIPStarRE-auto, tmux auto or other projects.
No owner-granted infrastructure override is in force. The workflow episode limit
remains 1000 changed lines and two hours unless the owner grants an extension.

Main adjudicates mathematical/workflow questions with evidence and independent
review. #26 is for owner-only access, permissions, credentials or scope/resource
grants. Existing #26 items wait for the owner unless explicitly returned to main.
All gap attempts and working-time budgets survive restarts. In particular B8/#118
was checkpointed at 13 attempts and 26509 working seconds; inspect its shared
budget before any continuation, and never reset it.

## Completed repair and preserved state

The owner required the current integration job before migration. That job is
complete: PR254 merged through the normal gate at 17:27:11Z as `ad5adbed`;
local and GitHub main aligned cleanly at `e1fba87`. The corrected periodic
service runs `merge-service-space-cap5.py --merge --interval 300`, PID1664171
at checkpoint, with successful scheduled checks at 17:32:59Z and 17:37:58Z.
Verify current process identity rather than relying on a historical PID.
The old merge daemon and stack watcher remain stopped. Do not resume them.

The checkpoint records preserved dirty worktrees for #118, #168, #261 and the
old core lane, plus stashes and transaction markers. Do not delete/reset them.
Confirm writer liveness before transferring a worktree or clearing a stale lock.
Keep both sides of telemetry merges, including duplicate raw-row multiplicities.

## Migration integration and normal work

PR290/#287 merged through the normal service at 18:18:08Z as `b430f683`.
Its refreshed head `60a6335` passed full CI and the canonical complete-patch
review carry from approved `a3eb4d57` (review 5126194906, zero unresolved).
The committed canonical lease tool released dead PID1064752 and reserved the
resumed main at PID1792844/start174304094 with four Space descendant slots.

Meta implemented PR292 / #291, branch
`codex/issue-291-astra-prompts-labels`, worktree
`.worktrees/codex-issue-291-astra-prompts-labels`. It changes active prompt wording,
descriptive PR-label inheritance/preflight, the documented owner-inbox boundary,
and telemetry; five focused tests and all 571 workflow regressions pass.
Meta owns author edits. Normal independent review, CI and service integration
remain required; use trusted main review prompts. After PR290 merges, propagate
the parent, retarget the child PR to main, and run fresh required gates.

Use the existing owner-authorized native runtime until the reviewed transport
lands; preserve its lease/accounting rather than admitting a second pool.
Record each root and native child with identity, role, worktree, timestamps,
effective metadata and observed usage. Unknown usage is null, not zero.

Every cycle and worker completion should produce an action: fill available
capacity with useful independent mathematics, bounded integration repair or a
required review. Main chooses disjoint work and successor chains. Maintain one
writer per worktree and one primary telemetry owner. Count actual active work,
not idle threads or reserved slots; record vacancies and why they occur.

Read one bounded census and reuse it until new evidence arrives. The old full
status-snapshot --prs is a diagnostic, not a dispatch prerequisite. Approved
stale PRs require action; the main session must keep the periodic merge service
live and provide fresh eligible work to it. Only the daemon/service calls the
merge gate. Coordinate telemetry before final gates, keep main stable through
merge, then restore/publish every new row. Report #27 once per stage boundary or
merge and continue without awaiting another owner reminder. Completion estimates
remain six-hourly; meta's optional two-hour heartbeat remains paused.

After the reviewed migration is integrated, use the committed main persona and
continue the formalization. Do not initiate another workflow redesign. Resume a
useful mathematics assignment immediately after this authorized workflow work.
