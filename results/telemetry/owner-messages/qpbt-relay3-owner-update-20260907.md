# Owner update: resume main on relay-3

The owner explicitly instructed: from now on use the relay-3 key currently
in auth.json, resume the main session, and rename it to main.

This is the same native coordinator, 01a076bc-f4ad-7813-805b-c8b4dac71a14.
Its Codex title is now main. The tmux name remains qpbt. Its historical scoped
CODEX_HOME is retained so all history, child identities, and cumulative proof
budgets survive. Do not resume the legacy coordinator 01a0760d.

Relay-3 was identified by verifying that ghz's canonical auth.json matches the
owner's saved auth-3.json without exposing either key. The scoped auth now uses
that key, and the scoped provider uses the canonical ghz relay endpoint. This
owner update supersedes the older Space-only credential instructions. Main and
all descendants must continue to inherit the scoped home and this credential.
Never print keys or revert the route from an old handoff.

Astra Ultra remains selected. External admissions remain zero and the total
allocation remains five. The existing canonical router counts a live VS Code
app-server as one additional primary process, so this resumed main has three
shared native descendant slots. The VS Code process was left running. Restore
four only through the existing capacity checks and a suitable checkpoint.

The canonical router schema still calls its existing native allocation bucket
space; the actual credential is now relay-3. Preserve the capacity guard and
record the actual key label in owner telemetry. Reconcile this metadata through
the normal reviewed workflow if necessary; do not treat the historical label as
an instruction to change credentials or create another pool.

Resume the previously authorized continuous QPBT project cycle after verifying
current state. Check surviving native worker state before retrying failed work.
Preserve all mathematical gap budgets, worktrees, review rules, permission
settings, and merge gates. This request does not authorize changes to unrelated
projects or any new publication beyond previously authorized project work.

Incorporate /tmp/qpbt-relay3-resume-receipt-20260907.json into the normal owner
telemetry along with this note. The launcher is
/tmp/qpbt-resume-relay3-20260907.sh. It selects the scoped home, the exact root
UUID, Astra Ultra, and the current shared descendant cap explicitly.
