OWNER MESSAGE — main has not moved for 8 hours. Binding, effective now.

1. STOP all work on PR #51 immediately. Do not commit, push, or edit anything in .worktrees/issue-50-shared-package-store. The owner is resetting that PR to its original 107-line head plus a 10-line fix for the two real round-1 findings, rebasing it on main and re-running CI and review. When it is green, merge it; if the reviewer churns again, adjudicate at that head (owner pre-authorized). Issues #54–#58 are closed by the owner as not planned: they are hardening of the hardening.
2. MERGE NOW: PR #39 and PR #44 are CI-green and review-green on their exact heads. Commit the primary checkout's telemetry dirt first (gate 2), then pr_merge.py 39, pr_merge.py 44, github-sync.sh. Do this before anything else.
3. THEN PR #46 (Lean, issue #19): it has had two review rounds — repair only findings inside its diff, otherwise adjudicate at the current head. Then #18. Mathematics is the queue; workflow PRs #41/#42/#43 get one CI+review pass each and merge or adjudication, no rework loops.
4. Rules from now on (the reason main stalled): (a) at the start of EVERY loop iteration, merge every PR that is green on its exact head before touching anything else; (b) a workflow PR gets at most two review rounds, then adjudication at its current head — never a third round; (c) never grow a PR to satisfy findings — the 400-line budget is a ceiling, not a target; a PR that has grown past twice its original size is reset to its original head; (d) reviewer findings that ask for new mechanisms are dispositioned "out of scope" in the review, not turned into issues.
5. The primary checkout has an uncommitted edit to local/personas/main.md. If it is the #26/#27 inbox wording, make it a separate 5-line PR; otherwise discard it (git checkout -- local/personas/main.md). Never edit workflow files in the primary checkout.
6. An owner-side watchdog now runs hourly: when main is stale, a green PR sits unmerged, or a PR passes two review rounds, it will nudge you here and report to #26. Treat its message as an owner instruction.
Post a #27 report after step 2.
