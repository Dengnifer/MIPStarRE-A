# Review — PR 137 @ e19f699d6f9d08cd43c0e0600d2f8607d3d5a440
reviewer: opus read-only pass   ci_status: success (`local-ci/summary` green on e19f699)   base: main
issue: #101 (stage 4.3, child of #77)   files changed: 2 (+758 new, +5/-3 facade)
CHANGES_REQUESTED
## Findings
- [ ] F1 (changes) `MIPStarRE/QPBT/Test/MagicSquareTheorems/Rigidity/Relations.lean:114-413` — roughly 300 lines of game-generic Born/event/rejection-mass API are hard-specialized to `msGame`, re-declare the private `outcomeWeight`, and are exported through the stable public facade, contradicting the issue's scope note.
  Lines 114-413 (`outcomeMass`, `aliceOutcomeMass`, `bobOutcomeMass`, `eventMass`, `aliceEventMass`, `bobEventMass`, `eventMass_nonneg/_left_eq/_right_eq/_mono`, `acceptanceMass`, `rejectionMass`, `acceptanceMass_add_rejectionMass`, `totalRejectionMass_eq_one_sub_value`, `eventMass_le_rejectionMass`) use nothing about the Magic Square except the literal type `msGame`; only `ms_support_card_le:64` and `ms_question_weight_ge:93` are genuinely MS-specific. `outcomeMass:114-118` is a verbatim redefinition of `private noncomputable def outcomeWeight` at `MIPStarRE/QPBT/Games/Defs.lean:124-129`, which is private and therefore could not be reused. Three sibling soundness targets take the identical `1 - ε ≤ S.value` hypothesis and will need the same machinery — `MIPStarRE/QPBT/Test/LowDegreeGameTheorems.lean:171`, `MIPStarRE/QPBT/Test/QubitForm.lean:143`, `MIPStarRE/QPBT/Test/Soundness.lean:41` — so this guarantees a second copy. Fix: de-privatize the Born weight in `Games/Defs.lean` and lift the generic block to `MIPStarRE/QPBT/Games/` over `{G : Game}`, leaving only the 36-incidence facts here.
- [ ] F2 (changes) `MIPStarRE/QPBT/Test/MagicSquareTheorems.lean:4` — the helper layer is wired into the stable public facade and advertised in its module docstring, whereas issue #101's scope note requires "Keep the helper surface private to the rigidity subtree".
  The new import at line 4 and the docstring edit at lines 9-11 ("re-exports the elementary symmetry facts, the value-to-parity relation layer, and the perfect strategy construction") publish all 63 public declarations of `Relations.lean` to every downstream importer; only the eight `*_rejects` helpers (`:466`, `:471`, `:476`, `:481`, `:558`, `:575`, `:692`, `:710`) are `private`. The `MagicSquareRigidity` sub-namespace segregates the names but does not restrict the surface. Fix: either mark the internal helpers `private`/`scoped`, or drop the facade import and let the next rigidity packet import `Rigidity.Relations` directly.
- [ ] F3 (changes) `MIPStarRE/QPBT/Test/MagicSquareTheorems/Rigidity/Relations.lean:20-27` — none of the 63 new public declarations is marked as a formalization-only auxiliary lemma, contrary to `docs/CONTRIBUTING.md` §5 item 9 and to the convention the sibling Magic Square files already follow.
  §5.9 requires that a public auxiliary lemma which is not a named statement in the paper be recorded explicitly: "the Lean docstring should call it a formalization-only auxiliary lemma and the blueprint should either omit it as private/internal infrastructure or include it as a clearly subordinate support node". The paper states no value-to-parity relations — `references/qpbt-paper/08_classical_and_quantum_low_degree_tests.tex:620-653` imports `thm:ms-rigidity` wholesale from Coladangelo–Stark Theorem 6.9 — so every declaration here is Lean-only. `MIPStarRE/QPBT/Test/MagicSquare.lean:73-75` and `:80-84` show the house style ("A formalization-only total relabeling…", "Lean-only carrier infrastructure for `def:ms-game`"); the new file carries source anchors only in the module header at lines 20-27 and none per declaration. Fix: add the formalization-only marker plus a `def:ms-game` / `thm:ms-rigidity` anchor to each public docstring, and note in the PR body that the blueprint deliberately omits them as internal infrastructure.
- [ ] F4 (changes) `MIPStarRE/QPBT/Test/MagicSquareTheorems/Rigidity/Relations.lean:466-484` — the four forward/reverse `*_rejects` pairs are proved twice instead of being derived from `msWinPredicate_symm`, which this file already imports.
  `msWinPredicate_symm (x y a b) : msWinPredicate x y a b = msWinPredicate y x b a` is proved at `MIPStarRE/QPBT/Test/MagicSquareTheorems/Basic.lean:34-39`, and `Relations.lean:3` imports that file. `forward_wrongVariable_rejects:466-469` and `reverse_wrongVariable_rejects:471-474` have byte-identical proof bodies (`cases a <;> cases b <;> simp_all [wrongVariableAnswer, msWinPredicate]`), as do `forward_wrongConstraint_rejects:476-479` / `reverse_wrongConstraint_rejects:481-484`; `forward_cellMismatch_rejects:558-573` / `reverse_cellMismatch_rejects:575-591` and `forward_parityFailure_rejects:692-708` / `reverse_parityFailure_rejects:710-727` differ only in constructor order and one `.symm`. `scripts/check_duplicate_private_helpers.py` exists precisely to flag duplicate private helper bodies. Fix: keep the four forward lemmas and define each reverse one as `(msWinPredicate_symm _ _ _ _).trans (forward_… i k b a h)`.
- [ ] F5 (changes) `MIPStarRE/QPBT/Test/MagicSquareTheorems/Rigidity/Relations.lean:621-636` — `bitSign`, `bitSign_add` and `bitSign_sum_fin_three` re-derive a binary additive character the QPBT tree already has, in a less general form and without the Mathlib link.
  `MIPStarRE/QPBT/Algebra/Pauli.lean:45-47` defines `phaseSign : ZMod 2 → ℂ`, `:48-64` proves `phaseSign_eq_ffChar` tying it to Mathlib's `ZMod.stdAddChar`, `:66-69` proves `phaseSign_add`, and `:74-80` proves `phaseSign_sum` for an arbitrary `Fintype` index — of which `bitSign_sum_fin_three:633-636` is the `Fin 3` special case. Those lemmas are `private`, so they are unusable from here, and `Relations.lean` does not import `Algebra/Pauli.lean` at all; the result is two independent binary characters in one chapter. AGENTS.md ("Do not re-declare standard Mathlib lemmas") and §5 item 7 (modularity) both apply. Fix: promote one shared binary character with `_add`/`_sum` lemmas into the algebra layer (real- or `RingHom`-valued) and derive `phaseSign` and `bitSign` from it.
- [ ] F6 (advisory) `MIPStarRE/QPBT/Test/MagicSquareTheorems/Rigidity/Relations.lean:327-336` — `strategy_value_eq_acceptanceMass` is proved and then immediately restated inline rather than used, and the `IsProbability` side condition three lines later re-proves `msGame.μ_prob`.
  Line 327-331 proves `S.value = avgOver … acceptanceMass` by `rfl`; lines 334-336 open `totalRejectionMass_eq_one_sub_value` with `rw [show S.value = avgOver … acceptanceMass … by rfl]`, so the named lemma has no use site in the file. Lines 351-356 discharge `IsProbability` with the same four tactic lines that already appear as the `μ_prob` field of `msGame` at `MIPStarRE/QPBT/Test/MagicSquare.lean:139-142`. The `show P by exact h` idiom also appears at `:313-315` and `:345-346` where the hypothesis term would do. Fix: `rw [strategy_value_eq_acceptanceMass]`, pass `msGame.μ_prob`, and drop the redundant `show … by exact` wrappers.
- [ ] F7 (advisory) `MIPStarRE/QPBT/Test/MagicSquareTheorems/Rigidity/Relations.lean:327-331` — the only link between this layer and `Strategy.value` is a bare `rfl` that silently depends on two definitional coincidences outside this file.
  The `rfl` closes only because `outcomeMass:114-118` is definitionally the private `outcomeWeight` of `MIPStarRE/QPBT/Games/Defs.lean:124-129` and because the `DecidableRel` synthesized for `fun a b => msWinPredicate x y a b = true` in `acceptanceMass:304-306` is defeq to the `instDecidableEqBool` that `Strategy.value:137-140` gets from its `if G.decide … then` coercion. This is legitimate bookkeeping rather than the A2 anti-pattern (the real content is in `acceptanceMass_add_rejectionMass:313` and `totalRejectionMass_eq_one_sub_value:333`), but any refactor of `Games/Defs.lean` breaks it with no explanatory error. Fix: resolving F1 removes the coincidence; failing that, prove it by `unfold`/`simp only` on named equations.
- [ ] F8 (advisory) `MIPStarRE/QPBT/Test/MagicSquareTheorems/Rigidity/Relations.lean:654-656` — an unnamed global `noncomputable instance : DecidablePred (constraintParityFailure i)` backed by `Classical.propDecidable` is exported through the public facade.
  `constraintParityFailure i a` (`:649-651`) is a real-number disequality `constraintParityProduct a ≠ bitSign (msParity i)`, so classical decidability is forced by the choice to phrase the predicate over `ℝ`; in substance it is decidable, being `(∑ k, β k) ≠ msParity i` on `.triple β` and unconditionally true on `.bit _`. Because `eventMass`/`aliceEventMass` take `[DecidableRel]`/`[DecidablePred]` as statement-level arguments, a downstream file that supplies a different instance gets a propositionally-but-not-definitionally equal mass, and there is no `eventMass_congr` to bridge it. Fix: make the predicate `Bool`-valued (or state it as the `ZMod 2` parity disequality) and drop the instance, or scope it `local`.
- [ ] F9 (advisory) `MIPStarRE/QPBT/Test/MagicSquareTheorems/Rigidity/Relations.lean:451-464` — the wrong-form and mismatch masses are never related to `Measurement.postprocess msBitOrZero`, which is the form the layer's only consumer uses.
  `msOperatorDistanceA`/`msOperatorDistanceB` at `MIPStarRE/QPBT/Test/MagicSquareTheorems.lean:67-86` are stated against `((S.A (.var j)).postprocess msBitOrZero).effect b`, and `Measurement.postprocess` (`MIPStarRE/Quantum/Measurement.lean:127-134`, via `Submeasurement.postprocess:67-70`) is the fiberwise sum `∑ a ∈ filter (f a = b), M.effect a`. `grep -n postprocess` over the new file returns nothing, so the one-line lemma `aliceOutcomeMass` of a postprocessed measurement `= ∑ a ∈ filter (msBitOrZero a = b), aliceOutcomeMass S x a` is absent and the next packet will have to add it before any of these bounds can be applied. This is within the issue's stated scope ("bounding wrong-form answers after `msBitOrZero`"). Fix: add the postprocess-marginal lemma here.
- [ ] F10 (advisory) `MIPStarRE/QPBT/Test/MagicSquareTheorems/Rigidity/Relations.lean:26` — the module docstring anchors `def:ms-game` at `blueprint/src/chapter/ch13_qpbt_test.tex:201-222`, one line range wider than the repository's canonical anchor.
  `ch13_qpbt_test.tex:201` is `\section{The Magic Square game}`; the definition environment runs `:207-222`, which is the range cited by `MIPStarRE/QPBT/Test/MagicSquare.lean:15`, `MagicSquareTheorems/Basic.lean:14-15` and every docstring in `MagicSquare.lean`. The two other citations in the same header are correct: the paper's Magic Square subsection opens at `08_classical_and_quantum_low_degree_tests.tex:512` and the rigidity theorem runs `:620-649` inside the cited `612-652`. Fix: cite `:207-222`.
- [ ] F11 (advisory) `-` — the PR body's "Description" section is a raw commit dump of about 25 entries, most of them unrelated merges and telemetry commits inherited from PRs 42, 94 and 0008.
  `docs/CONTRIBUTING.md` §1 prescribes a Motivation / Description / Testing body; the three headings are present but the Description conveys nothing about the change under review. `local/personas/reviewer.md` §8 makes commit and PR conventions part of the review. Fix: replace it with a short prose description of the four relation families.
## Verified (no finding)
- No `sorry`, `admit`, `axiom`, `native_decide` or `set_option` bypass in the diff; `local-ci/build`, `proof-debt`, `proof-evasion`, `statement-origin`, `blueprint-sync`, `file-length` and `summary` are all `success` on e19f699.
- No paper-labelled declaration is touched and no `\leanok` is added, as issue #101 requires. `thm:ms-rigidity` keeps its statement-level `\leanok` at `ch13_qpbt_test.tex:228-230` against the still-`sorry` `exists_ms_rigidity` (`MagicSquareTheorems.lean:117-129`); that is the leanblueprint statement tag, and the proof environment `:251-255` carries none, so it is not stale.
- Mathematics checked by hand: the support is exactly the 36 directed incidences of the 18-edge type graph, so `ms_question_weight_ge:93-109` (weight `≥ 1/36`) and hence the `36 * ε` constant are tight, not slack. `eventMass_left_eq:258` / `eventMass_right_eq:269` are genuine no-signalling facts derived from `Measurement.sum_eq_one`, not definitional. `forward_cellMismatch_rejects:558` correctly uses `constraintVars_injective:433` to pin the witness index `l = k`. Wrong-form answers are preserved in every event mass as the issue demanded — `constraintParityProduct (.bit _) = 0 ≠ ±1` keeps malformed constraint answers inside the parity-failure event (`wrongConstraintAnswer_implies_parityFailure:658-665`). No projectivity, consistency or commutation hypothesis is assumed anywhere.
- Not verified: I did not run `lake`, so I rely on the green `local-ci/build` status for compilation and for the two `rfl` proofs at `:327-331` and `:329-331`.
## Review
The mathematics in this packet is correct and the four relation families the issue asks for are all present, proved for arbitrary finite POVM strategies with the explicit constant `36 * ε` and in both directed orientations. I found no faithfulness defect: nothing here is advertised as a paper theorem, no hypothesis was added to a source-labelled statement, no `\leanok` was moved, and none of the A1–A6 anti-patterns applies — the one `rfl` theorem is bookkeeping over a definition that predates the PR, with the real content proved separately. What blocks merge is the shape of the API rather than its content. About three hundred of the seven hundred and fifty-eight new lines are game-generic probability infrastructure that has been welded to `msGame`, re-declares a private definition from the games layer, and is then re-exported through the stable public facade in direct contradiction of the issue's own scope note; three sibling soundness theorems take the same `1 - ε ≤ S.value` hypothesis and will each need a copy. Around that sit four smaller reuse and traceability defects: a second binary character alongside `phaseSign`, four duplicated forward/reverse helper proofs that the already-imported `msWinPredicate_symm` collapses, and the missing formalization-only marking that `docs/CONTRIBUTING.md` §5.9 requires of exactly this kind of non-paper public lemma. Since this layer is the foundation the remaining children of #77 will be written against, the placement decision is worth making once, now.
Confidence: high (4/5) on F1–F5 and F10–F11, which rest on line-level reading of the diff, the two facade files, `Games/Defs.lean`, `Algebra/Pauli.lean` and the cited protocol documents; moderate (3/5) on F7–F9, which are judgements about how the layer will behave for its future consumers rather than defects in what is written. I did not type-check anything myself and took CI's word for compilation.
